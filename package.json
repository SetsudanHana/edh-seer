{
  "name": "edh-seer",
  "private": true,
  "workspaces": [
    "packages/*",
    "research"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "oxlint --deny-warnings packages research scripts",
    "lint:bins": "node scripts/check_bin_placement.mjs --self-test && node scripts/check_bin_placement.mjs",
    "tokens:build": "node scripts/build_tokens.mjs --out tokens/theme.css && cat tokens/app-surface.css >> tokens/theme.css",
    "tokens:check": "node scripts/validate_tokens.mjs && node scripts/validate_contrast.mjs && node scripts/validate_theme_refs.mjs tokens/theme.css packages/web/client/src"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.13.6",
    "oxlint": "^1.85.0"
  }
}
