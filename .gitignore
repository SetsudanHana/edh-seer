node_modules/
dist/
*.log
.DS_Store

# secrets — never commit any of these
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json
.npmrc
.claude/settings.local.json

# generated / downloaded caches, all re-fetchable on demand
.cs-cache/
.edhrec-cache/
packages/data/.mtgjson-cache/

# scratch output
packages/tagger/score-debug*.json
packages/tagger/_*.mts
persona-shots/

# static build artifacts (build-static.ts) — one JSON file per card name, tens of thousands of them
static-out/

# test + browser automation output
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# IDE
.idea/

# saved web pages and stray root screenshots are always accidents
*_files/
/*.png
/*.jpg

# Wrangler local dev state (miniflare caches, local DO/KV/D1 sqlite).
.wrangler/
.edhrec-cache/

# The Comprehensive Rules, fetched by research/rules/fetch-comprehensive-rules.ts.
# Wizards’ text, ~1MB, and it changes with every set -- a local copy, never a committed one.
/rules/

# Third-party SEO report drops (screenshots, exports). Local review material, never shipped.
/seo-review/
