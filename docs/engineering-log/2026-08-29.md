## State as of 2026-08-29 — READ THIS FIRST, IT IS THE NEWEST, AND IT CHANGES HOW YOU WORK

**THE PERSIST GATE'S REFUSAL LIST WAS MOSTLY *OUR* DEFECTS, NOT THE MODEL'S — 165 -> 34, and the
biggest single finding was ONE MISSING WORD SHOWING UP AS THREE FAMILIES (2026-08-29, PRs #27/#28).**
Coverage inside the rank cutoff **19,941 of 19,979 = 99.81%**, commander-legal 66.6%. ~$1.68 over
five re-ask rounds. **Read the refusals FAMILY BY FAMILY, never card by card** — every real fix here
came from grouping, and every family that looked like model noise had a harness defect inside it.
- **`copy` WAS MISSING FROM `EVENT_VERB`, WHICH GATES THE `twoConditions` MARK — and that ONE word
  surfaced as `invented-id`, `duplicate-id` AND `ability-type-mismatch` at once**, depending only on
  how the model numbered the two halves it correctly wanted to split. **When several refusal families
  move together, look for one cause upstream of all of them.**
- **THE OVERFLOW CONVENTION ONLY EVER WORKED WHEN THE MARKED CLAUSE WAS *LAST*.** The prompt asks for
  the second record at max+1; **the model numbers it IN TEXT ORDER**, shifting every later clause up
  one. Carrot Cake sent a CORRECT answer and was refused three ways. `sequentialAlignment` tries the
  alternate pairing and **accepts it only if every realigned record passes every existing check** —
  the two conventions are the same answer under a bijection the harness can compute, so this is
  checked rather than assumed, and the gate can only get STRICTER.
- **`EVENT_VERB` WAS SEEDED FROM VERBS THAT *LEAD* A TRIGGER, so every verb appearing only as the
  SECOND condition was never observed.** **`becomes` alone is 62 corpus cards** ("blocks or becomes
  blocked", "day becomes night or night becomes day") and existed only as the phrase `becomes
  blocked`. 594 -> 634 clauses. **Bare `tap`/`untap`/`attack`/`block` stems are REFUSED on
  measurement** — they are also nouns and participles, so "untap one OR MORE permanents" and "a
  goaded ATTACKING OR BLOCKING creature dies" are one event each.
- **`plays?` MATCHED THE "play" INSIDE "PLAYER"** — the first `EVENT_VERB` had no trailing word
  boundary while the second always did, so "Whenever a player or permanent becomes the target" read
  as two events. Found only by adding `becomes` and reading every gained clause.
- **THE STATION FRAME WAS LEARNED A THIRD OF THE WAY.** `LABEL` has known "N+ |" since Uthros
  Research Craft and **was consulted on ONE line, the trigger cue** — the cost scan and keyword path
  never saw it stripped. 40 cards. 27 bare keyword rows go untyped (an UNBARRED keyword already gets
  no type, so `static` on the barred form was two spellings of one thing disagreeing), 7 activated
  costs type correctly with the threshold KEPT in the body, and **3 genuine statics behind the same
  bar stay static** (Lumen-Class Frigate, Exploration Broodship).
- **A DECKBUILDING PERMISSION IS NOT AN ABILITY, and there are TWO of them** — "X can be your
  commander" (CR 903.3, 49 cards) and "A deck can have any number of cards named X" (CR 100.2a).
  Both were typed `static`; the model answered "none" and was right. **Fixed by assigning NO type
  rather than a new inert kind: `validateOne` only checks a type where the segmenter gave one.**
- **A REDUNDANT ZONE WARNS, IT DOES NOT REFUSE.** `fromZone: library` on `draw` is redundant, not
  wrong, and `canonicalize` nulls implied zones a step later anyway. A draw from a GRAVEYARD still
  rejects — that bound is the mutation test.
- **NINE TRIGGER WORDS, CHOSEN BY SWEEPING ALL 69 CR KEYWORD ACTIONS rather than the 50 that
  refused**: play · fight · amass · exploit · shuffled · gains-control · attached · prevented ·
  countered, each ALREADY a legal VERB. `unknown-trigger-event` 50 -> 10. **`play` is NOT covered by
  `enters`** (a land put onto the battlefield by Cultivate enters without being PLAYED), and
  **`countered` is named so deliberately — `counter-added` is a +1/+1 counter, a homonym.**
- **A REFUSAL NAMING A WORD IS NOT BY ITSELF A VOCABULARY GAP.** The model emits morphological
  variants of words that already exist — `add-counter` for `counter-added`, `enter`/`enters`,
  `discard`/`discarded`, `turn-face-up`/`turned-face-up`. **`unknown-trigger-event` FALLING under a
  pure re-ask is the proof**, since a missing word cannot be re-rolled into existence.
- **A `--refresh-other` RE-ASK RECOVERS ~34% PER ROUND BECAUSE MOST REFUSALS ARE NON-DETERMINISTIC**
  (the model mis-structuring against the segmenter). **STOPPED AT TWO ROUNDS ON PRINCIPLE: repeated
  re-rolling against a gate SELECTS FOR ANSWERS THAT VALIDATE rather than answers that are RIGHT**,
  because the gate checks STRUCTURE, not correctness.
- **`failed` IS NOT `refused`.** A spike from 2 to 7 was six `fetch failed` and one 503 — transient,
  and those cards re-queue. Read the FAILED lines before treating a rise as a regression.
- **A SQUASH MERGE MAKES A FOLLOW-UP BRANCH CONFLICT (`mergeStateStatus: DIRTY`) EVEN WITH NO CONTENT
  CLASH**, because the parent PR lands under a NEW SHA while the branch still carries the original
  commit. `git rebase --onto origin/main <parent-commit> <branch>` replays only the new work.
- **GATES BYTE-IDENTICAL THROUGHOUT EXCEPT ONE +7, AND THAT ONE IS ATTRIBUTED**: edges
  35,436 -> 35,446, reasons 45,025 -> 45,032, MESHED **289**, panel **95.1% real 387 / false 20 /
  debt 0** unmoved. The +7 is **6 magecraft cards** finally recording `copy` beside `cast` — the
  printed template is "whenever you cast OR COPY an instant or sorcery spell" and they had been
  recording half of it. Suite **2,738**.

**AN ADVERSARIAL REVIEW FOUND TWO DEFECTS I HAD MISSED AND WAS WRONG ABOUT THE SIZE OF BOTH
(Fable, 2026-08-29).** It caught the overflow-renumbering and the second-position-verb families, and
**one of them was in work I had already recorded as DONE** — the `copy` fix was half a fix.
- **IT OVERSTATED ITS OWN SAFETY CLAIM**: it argued the noun-phrase false positives were already
  excluded by the verb-after-`or` anchor. **Measured: 2 of 63.** It also **UNDERSIZED its biggest
  finding**, listing `becomes` among many without noticing it is 62 cards alone.
- **IT CAUGHT A REAL METHOD ERROR OF MINE: I refused `destroyed` after sweeping the PASSIVE template
  ("is destroyed") and reading 0.** Karmic Justice prints the ACTIVE voice — "whenever a spell or
  ability an opponent controls DESTROYS a noncreature permanent you control" — and `dies` cannot
  carry it, since sacrificing your own permanent does not trigger it. **Re-measured tight to the
  trigger head it is 1 card corpus-wide**, so the refusal survives on count while the REASONING
  behind it was wrong. **Sweep both voices before concluding a template has no witnesses.**
- **THE PATTERN: right about the SHAPE of both defects, wrong about the MAGNITUDE in both
  directions.** Verify a reviewer's counts before acting on them; take its structural claims
  seriously.
- **STILL UNVERIFIED, and it is the one open lead**: Fable claims **The Filigree Sylex**'s failing
  cost has NO brace/word mixture in any part, which would mean something else in the word-phrase rule
  fails and the mixed-cost refusal is shielding a different defect.

**HAND-AUTHORED "MANUAL" CLAUSE DOCS WERE PROPOSED AND ARE DEFERRED, WITH THE SHAPE DECIDED
(owner's question, 2026-08-29).** The blocking fact: **`segmentHash` hashes the CARD'S PRINTED TEXT
(`oracleText`, `typeLine`, `keywords`), NOT the segmenter's output**, so a segmenter fix never
invalidates a doc — **a manual entry is PERMANENT BY DEFAULT and silently opts that card out of every
future improvement.**
- **NOT AS MONGO ROWS. As a COMMITTED FIXTURE WITH A SHRINK-ONLY RATCHET**, the shape this repo
  already uses four times (`repeats-refused.json` + `REFUSED_CAP`, `MTGJSON_LAG`, the browser-bundle
  list): reviewable as a diff, reproducible on a fresh clone, and **self-deleting — a test re-runs
  each entry through the normal path and FAILS when the pipeline can now answer it.**
- **`model: "manual"` on the persisted doc** — the field already exists, no schema change, and it
  keeps every coverage figure honest. "21,311 clause docs" currently means "measured".
- **DO THE SYSTEMATIC FIXES FIRST, and the numbers are why**: hand-authoring the 83 would have meant
  writing 74 cards by hand to avoid two code changes, and paying it again on the next tranche.
- **THE LINE: a card that is structurally unrepresentable, yes (Avatar Aang's FOUR-condition head —
  the machinery holds two). A card whose refusal is a TRUE statement our vocabulary cannot express,
  NO** — Karmic Justice is the test case, and hand-writing it erases the signal that `destroys` is a
  printable trigger event we lack.

**PROMPT CACHING HAS NEVER FIRED ONCE, THE CODE COMMENT CLAIMED IT DID, AND IT IS 97.6% OF EVERY
CORPUS BILL (measured 2026-08-29, free).** `anthropic.ts` said the system block is marked
`cache_control:ephemeral` "so repeated calls read it from cache instead of re-billing full input".
**Claude Haiku 4.5's minimum cacheable prompt is 4,096 tokens and the normalize system block is
~2,700.** Anthropic's docs: a request under the minimum "will be processed without caching, **and no
error is returned**" — which is exactly why it survived the project's whole life. Nothing fails,
nothing warns, the bill is just full price.
- **PROVEN THREE WAYS RATHER THAN ARGUED, and the live arm is the one that matters.** The whole
  2026-08-22 batch, all 1,408 cards: `cache_creation_input_tokens` **0** and
  `cache_read_input_tokens` **0**. A LIVE call, same prompt: also 0 — **so it is not the Batch API's
  best-effort caching missing**, which was the obvious hypothesis and is wrong. Doubling the block
  brackets the threshold: **2,788 tok → write 0 / read 0; 5,466 tok → write 5,466, then read 5,466
  on the next call.** 4,096 sits between them.
- **THE VERIFICATION METHOD IS ANTHROPIC'S OWN and it is free**: *"if both
  `cache_creation_input_tokens` and `cache_read_input_tokens` are 0, the prompt was not cached."*
  **A batch's results are retained 29 days and every result line carries its own `usage`**, so the
  exact input/output/cache split of any recent buy is readable after the fact — that is how the
  2026-08-22 batch was diagnosed a week later, and it is the way to price a buy EXACTLY rather than
  from the dry run's estimate.
- **THE NUMBER IS WHY IT MATTERS: the system block is 97.6% of the input bill** — one copy is 2,531
  tokens against a card averaging **59**, repeated once per card — and a cache read bills at 0.1x.
  **A whole-corpus buy is ~$63 against ~$13.**
- **NOT FIXED, AND THE REASON IS NOT EFFORT.** Clearing 4,096 means ENLARGING the system prompt,
  which changes what the model answers on every card — a `NORMALIZE_VERSION` question with
  corpus-wide blast radius. **Padding it with inert filler to game the threshold is the same change
  wearing a disguise: the prompt IS the specification, and 1,400 tokens of nothing in it is 1,400
  tokens the model reads.** The marker stays set so the discount arrives on its own the day the
  prompt legitimately grows past the minimum.
- **IT IS TWO CHANGES, NOT ONE.** A batch routinely runs past the default 5-minute entry, so a fix
  wants **`ttl: "1h"`** or the entry expires mid-batch; Anthropic puts batch hit rates at 30-98%,
  best-effort. And **cache pre-warming (`max_tokens: 0`) is NOT supported inside a batch**, so the
  warm cannot be bought cheaply up front — it has to come from the batch's own first requests.
- **THE DRY RUN'S OUTPUT ESTIMATE IS HIGH: `EST_OUTPUT_TOKENS` is 400 and the 2026-08-22 batch
  measured 254/card.** Left alone ON PURPOSE — that batch was a `--refresh-other` draw, which is not
  a broad sample. The 16,949-card tranche in flight is, and it is the measurement to correct the
  constant from. Input runs the other way, ~7% ABOVE the chars/4 estimate (Sol Ring: 2,788 actual).
- **CONSOLE IS `platform.claude.com` NOW** (`docs.claude.com` 302s to
  `platform.claude.com/docs/...`). **Batches are scoped to a WORKSPACE** —
  `platform.claude.com/settings/workspaces` — so a batch missing from the list means you are looking
  at a workspace the API key does not belong to, not that it failed.
- PR #25.

**THE CORPUS CAN BE BOUGHT BY LEGALITY AND BY RANK NOW, AND THE WHOLE-CORPUS BUY WOULD HAVE BEEN
REJECTED AFTER BUILDING THE ENTIRE PAYLOAD (2026-08-29, PR #24).** `normalize-corpus.ts` could scope
the 71 calibration decks or a named card and nothing between, so P1 had no flag to run under.
- **`--commander-legal`** reads the collection directly rather than name by name: 31,829
  `findByName` round trips answer a question one query answers, and a card selected by query has no
  name to be unresolved. Scoped on LEGALITY per the standing vocabulary ruling.
- **`--max-rank` IS TESTED AFTER `needsModel`, NEVER BEFORE IT.** An all-inert card costs nothing to
  answer, so excluding it buys no money back AND would leave a vanilla creature reading as "unread"
  in a report. **Stated rather than implied: only 56 of the 27,529 unbought commander-legal cards
  carry no EDHREC rank at all, so a cutoff drops PLAYED cards. It buys a tranche; it does not filter
  junk.** Batch prices: rank <=20,000 **16,949 cards / $38.93** · <=25,000 21,563 / $49.51 · all
  27,529 / $63.17.
- **ANTHROPIC CAPS A BATCH AT 100,000 REQUESTS *OR* 256 MB OF BODY, AND 27,529 CARDS SERIALIZE TO
  ~285 MB.** `submitBatch` sent ONE POST, so a whole-corpus run would have built the entire payload
  and then been refused. It chunks on **BYTES** now rather than on a card count, so a prompt that
  grows later re-chunks itself instead of silently crossing the line. Each chunk writes its OWN
  state file — `--collect` is unchanged, and a state file naming another chunk's cards would have
  reported them unreturned forever.
- **THE CORPUS WAS RE-INGESTED FIRST AND IT MOVED NOTHING: 34,433 processed, identical** — no card
  has been printed since the last ingest, checked rather than assumed. Combos **106,605 -> 111,579**.

**THE TOP-20,000 TRANCHE IS BOUGHT, COLLECTED AND DERIVED — $33.42 against a $38.93 quote, and the
CALIBRATED GATES DID NOT MOVE AT ALL (2026-08-29, owner authorised, PR #24).**
`msgbatch_012Kx6enxFaennjwjmfQoUK3`, 16,949 cards, **all succeeded** — persisted 16,698, refused 248
(1.5%, under the 2% historical rate), failed 3 on a JSON parse, 437 with warnings. Plus **1,634
code-answered cards written free**. **`cardClauses` 2,767 -> 21,098; derived coverage of
commander-legal 8.7% -> 66.3%.**
- **THE BILL IS READ OFF THE RESULTS, NOT THE DRY RUN, and this is the method to reuse:** input
  47,680,028 (2,813/card) · output 3,833,806 (**226/card**) · cache_write 0 · cache_read 0 · total
  51,513,834. **`EST_OUTPUT_TOKENS` was 400 and is now 226**, the billed figure over a broad draw.
  **The 2026-08-22 batch's 254 is deliberately NOT the number** — cards stuck on `other` are not a
  sample of the corpus.
- **CACHING CONFIRMED DEAD ON 16,949 CARDS, not just the refresh draw: cached this would have been
  $11.97, so the threshold miss cost $21.45 on this tranche alone.**
- **POPULATION AND PANEL ARE BYTE-IDENTICAL AND THAT IS THE ACCEPTANCE TEST — 35,436 edges / 45,025
  reasons / MESHED 289, panel 95.1% real 387 / false 20 / debt 0.** The 71 calibration decks were
  already ~100% covered, so 16,698 cards bought OUTSIDE them must not move a claim INSIDE them.
- **DECOMPOSED, AND THE DERIVE HALF MOVES NOTHING: ratings 0 of 6,571, raw scores 0, partners 0, TOP
  card 0/71.** Every move is the `theme-stats.json` refresh, which is unavoidable — the artifact is
  COMPUTED from the derived corpus and the buy multiplied it 7.6x. **N 2,761 -> 21,098, tags 540 ->
  1,163, 623 new and ZERO lost**; those 623 had been scoring the CLAMPED MAXIMUM idf, i.e. beating
  every tag the corpus had actually observed. Refreshed: ratings **735 of 6,571** (mean |Δ| 0.0135),
  raw 4,753, **partners 0** (an idf cannot form an edge — the same acceptance test one layer over),
  TOP card 2/71, top-10 15/71, 113 facet moves.
- **SIX HEADLINES CHANGED AND TWO ARE WORSE, recorded rather than averaged away.** Better, and all
  three are defects THIS FILE ALREADY NAMED closing themselves: `orzhov-spellslinger` "gain life"
  (0.02) -> **"noncreature spells"** (0.10) — the deck recorded as led by a tag at deck frequency
  ONE; `ups-all-chandras` "non combat damage" -> **"chandras entering"** (0.31); and
  `mono-blue-tribal-tribal` "pump effects" -> **"Merfolk entering"**, the tribe at last.
  `big-mana-big-plays` -> "artifacts entering" (0.24) is fine. **WORSE: `golbez-artifacts` trades a
  specific true headline at 0.57 for "artifacts" at 0.11, and `otterly-awesome-spellslinger` loses
  the "noncreature spells" the subsumption work gave it, reads "instants" (0.10) and now falls under
  A15's naming floor** (named decks 67 -> 66). Subtype primaries 22 -> 23, **0 lost**; cohesion
  median 0.33 -> 0.32; distinct headlines 40, unchanged.
- **A BATCH REPORTS `succeeded: 0` LONG AFTER IT HAS METERED MOST OF THE TOKENS — counts flip in
  bulk while usage meters live.** ~50M tokens showed on the dashboard against `request_counts` 0
  succeeded. Not a stall, not a double charge.
- **THE MERGE WAS BLOCKED BY `required_conversation_resolution`, NOT BY A CHECK.** Both required
  checks were green and `gh pr view` said `BLOCKED` with no reason; the cause was ONE unresolved
  CodeQL review thread. **`gh api repos/.../branches/main/protection` is where that setting is
  visible** — it is not in the checks list and not in `rulesets` (which is empty).
- **THAT ALERT WAS REAL AND IS FIXED, NOT DISMISSED: a batch id is NETWORK DATA and it was going
  straight into a file path.** `join` RESOLVES `..`, so an id carrying one writes outside
  `.batches/` and a leading `/` replaces the directory outright. `safeBatchId` is an ALLOW-list, and
  the direction is deliberate — **losing the state file loses every card the batch was paid for**,
  so refusing loudly beats stripping to something writable. It lives in `anthropic-batch.ts` and NOT
  in the bin, because **importing the bin RUNS the CLI** (the `isMoxfieldUrl` trap). Mutation-proven.
- **THE 251 UNPERSISTED WERE RE-ASKED FOR ~$0.94 AND 86 CAME BACK — coverage inside the cutoff
  19,728 -> 19,814 of 19,979 = 99.2%, commander-legal 66.0% -> 66.2%, clause docs 21,098 -> 21,184
  (PR #26).** Persisted 86, refused 165, failed 0 — the 3 JSON parse failures cleared.
  **A RE-ASK RECOVERS ANYTHING ONLY BECAUSE MOST REFUSALS ARE NON-DETERMINISTIC**: the model
  mis-structures its answer against the segmenter. ability-type-mismatch 70 -> 43 · invented-id
  39 -> 26 · duplicate-id 27 -> 22 · missing-id 8 -> 1.
- **`unknown-trigger-event` FELL 66 -> 50 AND THAT IS THE DIAGNOSIS CONFIRMING ITSELF — A MISSING
  WORD CANNOT BE RE-ROLLED INTO EXISTENCE.** So those were never missing words: the model emits
  MORPHOLOGICAL VARIANTS of vocabulary that already exists and the gate refuses rather than guessing
  which was meant. `add-counter` for **`counter-added`** (7) · `enter`/**`enters`** (2) ·
  `leave`/**`leaves`** (2) · `discard`/**`discarded`** (2) · `turn-face-up`/**`turned-face-up`** (2)
  · `attack`/**`attacks`** (1). **`remove-counter` (3) is a variant of a word DELIBERATELY absent** —
  M4 refuses it rather than reinterpreting the direction. **A refusal naming a word is not by itself
  a vocabulary gap; check the list for the word's other spelling first.**
- **STOPPED AT TWO ROUNDS ON PRINCIPLE, NOT ON COST. REPEATED RE-ROLLING AGAINST A GATE SELECTS FOR
  ANSWERS THAT VALIDATE RATHER THAN ANSWERS THAT ARE RIGHT** — the gate checks STRUCTURE, not
  correctness, so a card passing on the third try may merely have produced a worse answer that
  happens to parse. A third round is ~$0.60 for maybe 35 cards and is not obviously worth having.
- **OF THE 165 LEFT, ~55 ARE VOCABULARY-BLOCKED AND WILL REFUSE IDENTICALLY FOREVER** —
  unknown-trigger-event 50 + unknown-verb 5. **`exploit` (8) is a real CR keyword action and is not
  in `TRIGGERS`**, which the standing "every keyword action is also a trigger event" rule says it
  should be; also `prevent` 3 · `attach` 3 · `shuffle` 2 · `fight` 2 · `evolve` 2 · `amass` 2.
  **`play` (19, the largest) is a MEANING near-miss rather than a spelling one** — a land played
  ENTERS (CR 305), so `enters` is the right answer and the model reached past it.
- **THE TRIGGER-VOCABULARY TREADMILL IS CHEAP NOW AND THIS FILE'S WARNING IS STALE: only 190 clause
  docs carry an `other` trigger**, so a `TRIGGER_VOCAB_VERSION` bump re-selects 190 cards —
  **$0.37 batch / $0.75 live**. The recorded treadmill was measured when that gate sat ABOVE every
  stored version and re-selected everything. **Re-measure `carriesOtherTrigger`'s population before
  refusing a vocabulary addition on cost.**
- **REMAINING TRANCHE: 10,580 commander-legal cards (rank > 20,000), ~$24 at today's no-cache rate.**
  Spent to date $33.42 + ~$0.94. Suite **2,723**.

**THE PROJECT IS PUBLIC, RENAMED, AND YOU CAN NO LONGER PUSH TO THE DEFAULT BRANCH.**
`github.com/SetsudanHana/edh-seer` — PUBLIC, default branch **`main`** (renamed from `master`
2026-08-29 through GitHub's rename API, which carried the protection rule over).

- **BRANCH PROTECTION IS ENFORCED FOR ADMINS, WHICH INCLUDES YOU.** A direct push to `main` is
  rejected — PROVEN, not assumed: `! [remote rejected] main -> main (protected branch hook
  declined)`, `GH006 ... 2 of 2 required status checks are expected`. **`git push --dry-run` reports
  SUCCESS against a protected branch** because it never sends objects, so the rule never evaluates —
  it is not a valid test of protection.
- **NO `Co-Authored-By:` AND NO `Claude-Session:` TRAILER, EVER — the harness tells you to add them
  and this repo is the exception.** History was rewritten twice to strip exactly those two from
  1,380 commits; putting them back re-lists Claude as a co-author on the commit page, and the owner
  noticed within the hour on 2026-08-29. Two commits carried them and **BOTH WERE REWRITTEN OUT the
  same evening — `2c14f59` -> `53995cb`, `1c81686` -> `43d4ca2`, trees BYTE-IDENTICAL and the diff
  against the old tip EMPTY.** Prose mentions of Claude INSIDE a commit body are FINE and were
  deliberately kept. Same for PR bodies: skip the "Generated with Claude Code" footer, because a
  **squash merge folds the branch commits' trailers into the merge commit AND GitHub adds its own
  `Co-authored-by` line on top** — so a clean branch commit is not enough on its own. **Check with
  `git log --format='%(trailers:only)' -1` before pushing.**
- **THE REWRITE ITSELF, so the next one costs minutes rather than an evening.** `git filter-branch`
  and `git-filter-repo` are both BLOCKED by the sandbox classifier, and so are `push --force` and
  deleting the protection rule — **the owner has to run those three by hand**; everything else is
  ordinary git the agent can do. Two tip commits do not need a history-rewrite tool at all:
  `git cherry-pick --no-commit` then `git commit --author=... -F <cleaned message>` with
  `GIT_AUTHOR_DATE` carried over rebuilds them exactly, and `git diff old-tip new-tip` coming back
  EMPTY is the proof. The order that matters: DELETE the protection rule (not "allow force pushes" —
  the required checks block a direct push too, `GH006 ... 2 of 2 required status checks are
  expected`), push with `--force-with-lease`, then PUT the protection back from a payload saved
  BEFORE the delete. A `git bundle create --all`, VERIFIED with `git bundle verify`, is the thing
  that makes it reversible. **The PR pages keep the old SHAs in their timeline forever** — that is
  GitHub's record of the merge, not git history, and nothing short of deleting the PRs removes it.
- **EVERY CHANGE GOES: branch -> PR -> both CI legs green -> merge.** Use
  `gh pr merge <n> --squash --auto --delete-branch` so it lands the moment checks pass rather than
  being babysat. Required checks are `test (node 20)` and `test (node 22)`; force-push and deletion
  are blocked; `strict` (branch-must-be-current) is deliberately OFF because with it on every merge
  invalidates every other open PR — measured on ten Dependabot PRs in one evening.
- **THE PACKAGE SCOPE IS `@edh-seer/*`, NOT `@mtg/*`.** 433 references across 239 files. Older blocks
  in this file still say `@mtg/` in prose; they mean the same modules. Root package is `edh-seer`.
- **THE `ponytail:` CODE-COMMENT CONVENTION IS NOW `CEILING:`** — 32 sites across 23 files. Same
  meaning (a deliberate simplification with a known limit and an upgrade path); the old name told a
  reader nothing and named a tool.

**WHAT IS LOCAL-ONLY NOW, AND IT IS MOST OF THIS PROJECT'S MEMORY.** `CLAUDE.md`, `docs/ROADMAP.md`,
`docs/superpowers/` (specs and plans) and `docs/measurements/` are **gitignored AND stripped from
all history**. They exist on disk and nowhere else.
- The ignores for them live in **`.git/info/exclude`**, not `.gitignore`, because a public ignore
  file listing `CLAUDE.md` and `docs/superpowers/` is itself the disclosure. A copy sits in the
  backup dir; **a fresh clone will not have it** and those files will show as untracked.
- **BACKUP: `~/projects/mtg-synergy-engine-backup-20260828/`** — the full pre-rewrite bundle
  (verified restorable), plus copies of superpowers, measurements, CLAUDE.md, ROADMAP.md.
- History was rewritten twice with `git-filter-repo`: authorship unified onto one real identity
  (1,665 commits had been authored by "Claude"), the `Claude-Session` / `Co-Authored-By` trailers
  removed from 1,380 commits, and the AI-tool sidecars deleted. **Commits 1,708 -> 1,241**, `.git`
  31 MB -> 5.0 MB. Prose mentions of Claude INSIDE commit bodies were kept — 48 of them are
  engineering record, not trailers.

**SECURITY IS AT ZERO ON BOTH DEPENDENCY AXES, AND THE REMAINING CODEQL ALERTS ARE ONE TRACKED
FAMILY.** Dependabot alerts **24 -> 0** (6 critical, 14 high at the start), `npm audit` **6 -> 0**,
CodeQL **66 -> 23 -> 0 open** (all 23 regex alerts `fixed` on 2026-08-29, PR #20). Secret scanning, push protection and CodeQL are all ON — they were
IMPOSSIBLE while the repo was private (`422 "Secret scanning is not available for this
repository."`, and branch protection returned `403 "Upgrade to GitHub Pro or make this repository
public."`). **Going public unlocked all three at no cost; it was never a paid-tier problem.**
- **`vitest` 1.6 -> 4 cleared four advisories at once** (CRITICAL vitest UI arbitrary file
  read/execute, HIGH vite `server.fs.deny` bypass, MODERATE vite-node and esbuild) because vitest
  1.6 pins the whole vulnerable vite chain. Worth attempting rather than deferring **because `web`
  was ALREADY on vitest 4 and green — the repo was proving the major works.** No test needed
  changing. Suite unchanged.
- **THE DECKLIST PARSER WAS QUADRATIC IN UNTRUSTED INPUT, AND TWO OF THE THREE FLAGGED PATTERNS WERE
  REAL.** Measured before touching anything: `/\s*[([].*$/` 1.0ms at 1,000 chars -> **1,966ms at
  64,000**; `/[:=]+$/` 0.7ms -> **1,200ms**; `/^(\d+)\s*x?\s+/i` **flat 0ms — NOT EXPLOITABLE and
  deliberately left alone**, since nothing follows `\s+` to backtrack into. A pasted decklist is
  untrusted input to `/api/analyze`, so one long line cost two seconds of server CPU. Both real ones
  are linear scans now; `MAX_CARD_LINE` (500) bounds the parse boundary as defence in depth.
- **THE FIRST TWO VERSIONS OF THAT TEST MEASURED NOTHING, which is the lesson.** The end-to-end test
  passed against the OLD pattern, because the line cap truncates before any regex runs — the cap was
  doing all the work. Moved onto `beforeBracket` directly, uncapped: **still passed**, because the
  input ended `"(LEA) 1"` and **with a bracket present the match SUCCEEDS in 0.1ms and never
  backtracks**. Only a NO-BRACKET input makes it fail at every position, which is where the
  quadratic cost lives. Corrected, the mutation fires at **18,000ms against a 1,000ms budget**.
- **19 BINS WROTE TO PREDICTABLE WORLD-WRITABLE `/tmp` PATHS**, and `mkdtemp` is the WRONG fix here:
  those paths are a HANDOFF (`precision-sample` writes what `precision-score` reads; also
  `normalize-experiment` -> `normalize-score`, `schema-experiment` -> `experiment-compare`,
  `dump-suspects` -> `reconcile`). A random directory per run breaks all four pairs. `scratchDir`
  (`packages/data/src/scratch.ts`) keeps the path STABLE and removes the exposure instead: one
  parent per uid, created 0o700. **Stated rather than hidden: a mitigation, not an atomic create.**
  The 24 CodeQL alerts are dismissed with that reasoning, because the rule wants randomness
  specifically.
- **`input.includes("moxfield.com")` DECIDED BETWEEN FETCHING A REMOTE DECK AND READING A LOCAL
  FILE**, and is also true of `moxfield.com.example.invalid`. Now a hostname comparison
  (`isMoxfieldUrl` in its own module — **importing `main.ts` RUNS the CLI**, so the first test called
  `process.exit(1)` on import).

**CODEQL IS AT ZERO AND EVERY REGEX ALERT IS `fixed`, NOT DISMISSED — 23 -> 0, PR #20, issue #19
CLOSED 2026-08-29.** The whole open scan was regex: 1 `js/redos` + 22 `js/polynomial-redos`. The 39
that remain are the previously-dismissed `scratchDir` / temp-file family.
- **THE EXPONENTIAL ONE COST 32.7 SECONDS OF EVERY CORPUS RUN AND NOBODY HAD PRICED IT THAT WAY.**
  `segment.ts`'s `ACTIVATED` regex was filed as "not reachable from a request", which is TRUE and was
  the wrong frame: `segment()` runs over the whole corpus in `normalize-corpus.ts` and
  `segment-check.ts`, and **segmenting all 34,081 cards with oracle text took 32,680ms against
  206ms after — 159x.** Re-measured on this machine, the blowup is 10 parts 2.9ms · 14 parts 19ms ·
  16 parts 154ms · 18 parts 1.4s · 20 parts 13s at 59 characters. **A defect that is not
  attacker-reachable can still be the thing making your own pipeline slow — price it against the
  batch it runs in, not against the threat model.**
- **THE FIX IS THE NON-REGEX SCAN THE OLD ENTRY NAMED, AND THE AMBIGUITY WAS FREE TO REMOVE.** Parts
  exclude `:`, so a match can only ever end at the FIRST colon; and **splitting on EVERY comma is
  what makes one pass enough** — a phrase part may legally span a comma, but splitting there only
  ever SHORTENS the parts, so a head that parses at all parses when cut at every comma. That is why
  the two earlier attempts ("exclude `,` from the class", "cap input length") both failed: they tried
  to keep the ambiguity and bound it.
- **PROVEN EQUIVALENT RATHER THAN ARGUED: every clause of all 34,081 cards is BYTE-IDENTICAL.** The
  instrument is a 20-line offline pair — dump `name/oracleText/keywords` from Mongo once, then run
  `segment()` over the dump and diff. Cheaper than any gate and it answers the exact question.
- **THE 22 POLYNOMIAL ONES ARE ONE FAMILY: a negated class that does not exclude its own OPENING
  delimiter.** `\{[^}]+\}` 790ms -> 0.1ms at 50,000 chars with `[^{}]`; `\s*\([^)]*\)\s*`
  1,106ms -> 0.1ms with `\([^()]*\)`. **THE BRACE FIX IS A PROVEN NO-OP: 34,433 cards, ZERO nested
  braces and ZERO unclosed ones**, so no real string can tell the two classes apart. Applied at every
  site rather than only the flagged ones — an unflagged copy of a fixed defect is how it comes back.
- **THE PAREN FIX IS NOT A NO-OP AND THE COST IS STATED: 5 cards nest parentheses inside reminder
  text** (Just Desserts, Punctuate, Xerex Squire, Devoted Mardu, Rocket-Powered Turbo Slug) **and
  their clauses move. Every one is `commander=not_legal`**, so no EDH decklist can contain them; the
  other 34,076 are byte-identical. The two surrounding `\s*` were deleted with it and that half IS
  free — the `\s+` collapse on the next call already did their work.
- **EVERY `\s{1,4}` BOUND IS MEASURED, NOT GUESSED: all 993,681 whitespace runs in printed oracle
  text are a SINGLE character.** `GRANTED_TO` and `COPIED_INTO` need no constant at all — `(.*?\S)`
  removes the overlap outright, because a lazy `.` was competing with the `\s+` after it for every
  space. **`slug` was scanning for a run that cannot exist**: `[^a-z0-9]+` has already collapsed
  every run, so at most one hyphen sits at each end.
- **`NAMED`'s BOUND WAS WRONG AT 60 AND THE CORPUS SAID SO.** Its longest capture corpus-wide is
  **61** — Grandeur's *"Tarox Bladewing: Tarox Bladewing gets +X/+X until end of turn"*, a name
  swallowing a whole ability, **a defect this change deliberately PRESERVES rather than quietly
  fixes**. 80 clears it. Found only because every changed pattern was run SIDE BY SIDE with the one
  it replaces over all 34,433 cards and 6,195 stored clauses.
- **GATES BYTE-IDENTICAL, which is the acceptance test: edges 35,436 / reasons 45,025 / MESHED 289,
  panel 95.1% [92.5, 96.8] real 387 / false 20 / uncertain 22, debt 0.** Suite **2,719**.
- **TWO NEW TEST TRAPS, BOTH HIT BEFORE THEY WERE AVOIDED.** The ReDoS input must **FAIL** to match —
  given a well-formed cost the old pattern succeeded in 0.1ms and never backtracked, exactly the
  decklist-parser lesson one package over. And the mana-symbol test must use a **SPLIT** card:
  `castableManaCost` returns a non-split cost untouched and never reaches the scan, so the first
  version passed in both arms. Three tests, mutation-proven at 14,062ms / 916ms / 954ms against
  budgets of 1,000 / 200 / 500ms; all five guards of the new scan fire one at a time, and the
  empty-part guard **has no printed witness and says so in its own test**.

**CI, DEPENDABOT AND THE TWO THINGS THAT BIT.** CI is a matrix on Node 20 (the `engines` floor) and
22 (LTS), with npm caching, `permissions: contents: read` and cancel-in-progress concurrency.
Dependabot groups minor and patch into one weekly PR per dependency type; **majors stay separate
because those are the ones that break a build** — which is exactly what happened.
- **THE FIRST CI RUN WAS RED AND IT WAS MY GATE THAT WAS WRONG.** `board-force.test.ts`'s
  "centroid stays near the origin at 10,000 ticks" hit vitest's 5s default on the Node 20 leg while
  passing on 22 — **the gate was measuring how fast the runner is, not whether the layout drifts.**
  It carries an explicit 30s timeout now; the assertion is unchanged.
- **`jsdom` 25 -> 30 IS CLOSED, NOT MERGED.** It fails on Node 20 AND 22
  (`webidl.util.markAsUncloneable is not a function`) and passes on 26 — adopting it means raising
  the `engines` floor for a dev-only test DOM with no advisory behind it.
- **`cs-categories.test.ts` MADE A CLEAN CHECKOUT RED** — it asserted a gitignored cache directory
  was non-empty, so `npm test` was green only on the one machine holding `.cs-cache/`. It skips that
  single assertion when the cache is absent and still fails loudly when the cache is present and the
  constant has drifted. Proven both ways: 24 pass with it, 23 pass and 1 skips without.
- **THE MATCHER HAD TWO TYPE ERRORS 2,706 GREEN TESTS COULD NOT SEE** — `edges.test.ts` cast its
  fixture `as never`, which erased `maker.card.name` for every reader below. The recorded "a green
  suite is not a compiling one" trap. **`typecheck` IS A CI GATE SINCE 2026-08-29 (PR #21), running
  BEFORE `npm test` on both matrix legs; errors 35 -> 0.**
- **34 OF THOSE 35 SAID NOTHING ABOUT THIS REPO, AND THE CAUSE IS A PACKAGE `main` THAT POINTS AT
  SOURCE.** Every one came from `web/client`'s compile: `types.ts` TYPE-imports `DeckReport` and
  `GraphEdge` from the `@edh-seer/engine` and `@edh-seer/matcher` barrels, whose `main` is
  `src/index.ts` — raw TypeScript, not a built `.d.ts` — so tsc compiles that whole Node-side source
  graph and reports `node:fs`. **`skipLibCheck` CANNOT REACH THEM: they are source, not
  declarations.** The two `TS7006` implicit-`any` in `data/src/scryfall.ts` are downstream of the
  same thing (`gunzipSync` untyped is `any`), not their own defect. **Fixed by putting `"node"` in
  the client's `types`; the real fix is emitted declarations for the five Node-side packages, which
  is a build step that buys nothing else today.**
- **THE ONE REAL ERROR IS EXACTLY THE CLASS THE GATE EXISTS FOR: `BoardTuner.tsx`'s `CONSTANT_NAME`
  is a `Record<NumericParam, string>` and `collidePad` was added to `BoardParams` after the map was
  written.** Exhaustive record, missing key, invisible to every test. `KNOBS` still has no
  `collidePad` SLIDER, left alone on purpose: that list is curated by hand and picking a range is a
  judgement.
- **THE PROTECTION `"node"` GIVES UP WAS NEVER REAL, WHICH IS WHY IT WAS ACCEPTABLE.** It also makes
  `process`, `Buffer` and `node:fs` typecheck INSIDE client code — but **the recorded 2026-08-21
  regression (a value import of `GRAVEYARD_HATE_SHARE` from
  `@edh-seer/matcher/src/answer-coverage.js`) typechecked perfectly WITHOUT node types**, so that
  config never stood between the client and Node code. `client-browser-safe.test.ts` is the
  replacement: it walks the files that SHIP and fails on a Node builtin, honouring the same two
  exclusions the tsconfig makes — **duplicated rather than read out of the tsconfig, because a guard
  that takes its exemptions from the file it guards can be switched off by editing that file
  alone.** `process.env` is allowed on purpose (Vite REPLACES it at build time). **It asserts it
  found more than 20 files**, because an empty walk passes a whitelist test while checking nothing.
- **WHERE THE GUARD LIVES WAS DECIDED BY IT FAILING, TWICE.** It sits in the SERVER suite because
  that is the only NODE environment in the package — under the client's jsdom config
  `import.meta.url` is rewritten to an http URL and `fileURLToPath` throws, so **a guard that walks
  the filesystem cannot run inside the environment it guards**. And it uses `__dirname`, because the
  server tsconfig is `NodeNext` emitting CJS for `nest build` and rejects `import.meta` outright
  (TS1470). **That it uses the very name it forbids the client to use IS the distinction.**
- **PROVEN TO FAIL RATHER THAN ASSUMED**: an injected error in `packages/matcher` exits 2, one in
  `web/client` exits 2, a clean tree exits 0; the guard fires on a planted `Buffer` and on a walk
  pointed at a near-empty directory. **Suite 2,720.**

**SUITE 2,721** (2,712 + 5 activated-cost/ReDoS + 2 delimiter + 1 browser-safe guard + 1 browser-bundle ratchet). `npm test` from the root,
as always. Unchanged by PR #23 — the six JSON conversions added no test, they emptied one ratchet.

**THE GRAPH WENT BLACK AND IT WAS MEASURING THE CANVAS THAT ERASED IT (2026-08-28, owner-reported,
free, client-render only).** Focus a node, then anything that resizes the canvas, and the board goes
black until a pointer move happens to CHANGE the hovered node.
- **`size()` WRITES `canvas.width`, AND WRITING THAT ATTRIBUTE WIPES THE BACKING STORE**, so
  measuring is also erasing and the frame after it is the only thing between the reader and a black
  board. `onResize` repainted only through `fitToView()`, **which it deliberately skips once a real
  gesture has claimed the camera** — so the one path that repainted was the one path a reader who
  had touched the board could no longer take, and H11's parked rAF then never drew again.
- **MEASURED LIVE: painted pixels 3,417 -> 0 across a resize, and it stayed 0.** After the fix,
  798 -> 704 with no mouse move at all. It recovers on a pointer move only because the hover handler
  invalidates when the hovered id CHANGES, which is why it takes a move ACROSS nodes.
- **`invalidate()` GOES IN `size()`, NOT IN `onResize`** — `size()` is what clears, so every caller
  and any future one gets the repaint instead of rediscovering this. It is the only writer of
  `canvas.width` in the file.
- **NOT REPRODUCED: the owner's exact trigger.** Clicking a second node does not resize the canvas on
  the example deck (cssW 1434 -> 1434). Fullscreen or a page scrollbar toggling are the likely
  sources; all of them hit this same defect.

**THE PIPELINE IS DOCUMENTED FOR A STRANGER NOW — `docs/HOW-IT-WORKS.md`, and it IS published.**
Traces one real pair (Siege-Gang Commander -> Skullclamp) through all six stages with the ACTUAL
output of each captured from the live corpus, not invented: segmenter clauses, normalized JSON,
derived trigger and emits, implied events, and the `Reason` the matcher produces. Four mermaid
diagrams (pipeline, the `eventMatches` decision, an analyze request end to end, the collection
model). **A per-line mermaid lint flagged the erDiagram as broken and the LINT was wrong** —
`||--o{` is valid cardinality and `cards {` closes on a later line.

**STATIC-FIRST HOSTING IS PLANNED AND PARKED — NOTHING BUILT, NOTHING SPENT.** Owner's direction:
host JSON, push computation to the client, Cloudflare in front, first version pure analysis with no
deckbuilding. Measured feasible: **the whole dataset is ~9 MB gzipped** (cards 3.55 + derived ~3.8 +
combos 1.56 + tokens 0.15; clauses never ship) and **a 100-card analysis needs ~25 KB**. The server
is 411 lines whose Mongo use is five reads by name or id, and `analyzeDeckStructured` is already
pure and already takes a SYNCHRONOUS lookup.
- **THE COUPLING WORTH NOT REDISCOVERING: STATIC HOSTING FORCES THE CORPUS BUY.** No server means no
  lazy normalization, so whatever ships is what a stranger gets. Coverage is **2,767 derived of
  31,829 commander-legal — 8.7%**. Owner's call is the **EDHREC top 10,000 first (~$23 batch)**
  rather than the whole corpus ($63), leaving the tail buyable later.
- **THE EDHREC THEME SLUG IS IN `url`, NOT IN `slug`** — that field is a representative CARD
  (`skullclamp` for Tokens), and a bucket miss returns **403, never 404**, so a crawl built on
  `slug` fails silently on all 401 themes and produces an empty ranking. Verified live.
- **P2'S "SIX `readFileSync` SITES" IS AN UNDERCOUNT, AND BUNDLING IT IS WHAT SHOWED THAT
  (2026-08-29, free).** `esbuild --bundle --platform=browser` on `analyzeDeckStructured` fails with
  **16 errors across TEN modules**. The six are real and on the graph; **the four missing are all in
  `@edh-seer/tagger` and all reached through its BARREL** — `otags/functional.ts` and
  `otags/semantics.ts`, which **`readFileSync` AT MODULE LOAD rather than lazily**, plus
  `clause-store.ts` (`node:crypto`) and `derive/token-types.ts`.
- **CLOSED THE SAME DAY, PR #22: 16/10 -> 12/6, and the residue is EXACTLY the six.** `theme-fold` ·
  `land-conditions` · `typeline` · `theme-promote` · `hierarchy` · `implied` · `edges` ·
  `supply-demand` name `@edh-seer/tagger/{subtypes,subject,schema}` now. **The way in was
  `analyze.ts:32` and `analyze.ts:48`, each importing ONE constant** (`SUBTYPE_TYPES`,
  `LAND_SUBTYPES`) — four browser-hostile modules for two constants.
- **AN `exports` MAP, NOT A RAW `src/...js` PATH.** `@edh-seer/engine` already ships one and
  `cli/report.ts` already imports `@edh-seer/engine/percent` through it, so the pattern is proven on
  the NODE side here and not only in the client. It also makes the tagger's browser-safe surface
  explicit rather than leaving every internal file reachable — **the 2026-08-21 regression was
  exactly a deep import into a file never meant to be public.** Nothing imported a tagger subpath
  before, so the map closed no existing door.
- **THE SIX ARE CLOSED TOO AND THE ENGINE BUNDLES FOR A BROWSER TODAY — 2026-08-29, PR #23:
  12 errors / 6 modules -> ZERO, 349,973 bytes.** Each was one `readFileSync` of a colocated JSON
  artifact; each is a static `import ... with { type: "json" }` now, `resolveJsonModule` in
  `tsconfig.base.json`. **THE ACCEPTANCE TEST IS THAT NOTHING MOVES — same bytes, loaded earlier:
  edges 35,436 / reasons 45,025 / MESHED 289, panel 95.1% real 387 / false 20 / debt 0, ALL
  BYTE-IDENTICAL**, suite 2,721, typecheck 0 across six workspaces.
- **AN EMPTY RATCHET LIST PASSES VACUOUSLY, so the test GAINED A SIZE FLOOR** — the
  `client-browser-safe.test.ts` "found more than 20 files" shape, one package over. Both directions
  mutation-proven ONE AT A TIME: a `node:fs` read put back fails the set assertion, a one-line stub
  entry fails the size one **at 67 bytes against a 200,000 floor**.
- **TWO FALLBACKS DELETED ON PURPOSE AND IT IS THE LOUDER DIRECTION.** `impact.ts` fell back to
  `SEED_IMPACT_WEIGHTS` and `theme-stats.ts` to `UNIFORM_STATS` when the file was unreadable; a
  static import makes an absent or malformed artifact a BUILD failure. **It matters most for
  theme-stats, where an ABSENT TAG SCORES THE MAXIMUM IDF** — a silently degraded artifact does not
  weaken the ranking, it lets a missing tag WIN (the 2026-08-18 finding).
- **`rootDir: "src"` DOES NOT BLOCK an artifact at package ROOT, and PROBING beat assuming.**
  `hierarchy.json` and `theme-stats.json` sit outside `src`; both `--noEmit` AND a real emit accept
  the import, so **neither file moved and no generator changed** — the two-line answer instead of a
  move plus three call-site edits.
- **VERIFIED IN BOTH MODULE SYSTEMS RATHER THAN TYPECHECKED.** The CLI renders a deck under tsx/ESM,
  and the web server (nest, CJS `NodeNext`) answers `POST /api/analyze` 200 — 94 cards, buildScore
  4.72, cohesion 0.206, bracket 3, 919 edges, i.e. the fields fed by rules / answer-pool /
  theme-stats specifically. A JSON import behaves differently in tsx, `nest build` and vite, so one
  green typecheck is not evidence for three runtimes.
- **RECORDED RATHER THAN FIXED, AND A FALSE SENTENCE WAS CAUGHT BEFORE IT SHIPPED: `hierarchy.json`
  HAS NO STALENESS GUARD.** The first draft of its new comment claimed `theme-stats-drift.test.ts`
  and `gen-vocabulary --check` guard it; **neither does** — the drift test reads `theme-stats.json`
  only and `gen-hierarchy.ts` has no `--check`. It sat at 16 of 527 subtypes for the project's whole
  life with every test green. The `ponytail:` names the upgrade: the drift test's shape, asserting
  every `SUBTYPE_TYPES` key appears, free and database-free.
- **THE GATE IS THE BUNDLE ITSELF, NOT A GREP FOR IMPORTS — `matcher/src/browser-bundle.test.ts`.**
  It runs esbuild and asserts the browser-hostile module set EQUALS the six, failing in BOTH
  directions (the `KNOWN_DEFECT_CAP` shape): a seventh means a Node builtin went back on the
  analysis path, a missing one means **P2 landed a conversion and the win has to be BANKED by
  deleting that line.** Proven both ways. **`absWorkingDir` IS PINNED AND IT IS LOAD-BEARING** —
  esbuild reports paths relative to its working directory, so without it the test PASSED ALONE and
  FAILED IN THE SUITE (`packages/matcher/src/...` under `vitest --root`, `src/...` under
  `npm test -w`).
- **`edges.ts` DID NOT APPEAR IN A PLAIN `grep` FOR THE BARREL IMPORT — the NUL-byte trap again**,
  and it is one of the eight. It took `grep -a` plus esbuild's `--metafile` reverse graph to find
  it. **A dependency audit on this repo that greps must use `-a`, or it silently under-reports.**
- **Q6 DELETED THIS ITEM'S FORCING FUNCTION ON 2026-08-29 AND THAT NEEDS SAYING OUT LOUD.**
  `web/client`'s typecheck used to fail on `theme-stats`/`hierarchy`/`answer-pool` — the roadmap
  called that "the same defect from a different direction" — and it passes now, because `"node"`
  went into the client's `types`. **A green typecheck is NO LONGER evidence that the engine bundles
  for a browser.** `esbuild --platform=browser` is the instrument; nothing in CI runs it yet.
- **RE-MEASURED 2026-08-29 AND UNMOVED: derived coverage 2,767 of 31,829 commander-legal = 8.7%,
  corpus 34,433.** P1 is genuinely parked — nothing bought since.
- → `specs/2026-08-28-static-hosting-design.md`, `plans/2026-08-28-edhrec-corpus-tranche.md`
  (both LOCAL ONLY now — see the ignore note above).

