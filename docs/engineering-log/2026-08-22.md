## State as of 2026-08-22

**A GAP NAMES A COST BAND, AND THE POINT CLAIM IS REFUSED ON MEASUREMENT — F14 shipped, F15 closed
half-shipped and half-refuted (2026-08-22).** "Ramp 9/10 — add ~1" now reads "Ramp 9/10 — add ~1,
typically 2–3 mana".
- **A BAND AND NEVER A POINT: the modal mana value is only 25–42% of the cards in EVERY leaf** (ramp
  modal 2 = 36%, draw modal 3 = 25%, board wipe modal 5 = 33%), so "add a two-mana rock" would be
  wrong about two thirds of the time. That is the `thinnest: tutors` defect one rung along — the hint
  this repo REMOVED on 2026-08-21 for exactly that reason — and measuring is the only thing that
  separated the two proposals.
- **MEASURED PER PARENT, because a suggestion names a parent and never a leaf** (the 2026-08-21
  ruling): Consistency 2–4 (n=1,181, 65% inside) · Ramp 2–3 (n=930, 56%) · Interaction 2–4 (n=1,324,
  64%) · Board wipes 3–5 (n=98, 73%). **`lands` gets NO band** — a land is mana value 0.
- **THE TABLE IS HAND-TRANSCRIBED, SO IT IS GATED: `build-population.ts --cost-bands`** recomputes
  every band from the 71 decks and exits non-zero on drift, proven to fire. `GRAVEYARD_HATE_SHARE`
  shipped as 36/16/6 against a source measuring 39/19/8 because nothing recomputed it; this is that
  guard. The gate reaches the numbers by a DIFFERENT path from the one that produced them
  (`detectBuildCategories` against `report.cards[].roles`) and the two agree exactly.
- **THE CLIENT FIXTURE WAS STALE AND NOBODY HAD NOTICED**: it carried "Removal 7/10 — add ~3", a
  LEAF-level sentence the engine stopped producing on 2026-08-21. A fixture that no longer resembles
  real output stops exercising the renderer it exists for.
- **F15's GRAVEYARD EXPOSURE ALREADY SHIPS** ("Your plan runs through the graveyard. 39 artifacts and
  19 enchantments in the format shut it off, and this deck answers neither"), so the item was half
  closed before it was read.
- **F15's COMBO HALF IS REFUTED, and the shape of the refutation is worth keeping: DEPENDENCE AND
  FRAGILITY ARE ANTI-CORRELATED.** 29 of 71 decks hold a combo; combo is the TOP strategy in **6**,
  and all 6 run **7 to 28 pieces** — redundant, not fragile. The **14** decks with ≤4 pieces are not
  combo decks at all (top strategy tokens/spellslinger/counters/aristocrats). A "your plan depends on
  a fragile combo" warning would be false about both groups, and there is no third group.


**THE BOARD IS ALLOWED TO STOP PAINTING, NOT JUST STOP MOVING — H11 shipped 2026-08-22, and the LIVE
RUN FOUND A HOLE EVERY TEST WAS GREEN THROUGH.** H1 let the simulation settle; the loop still ticked,
drew and rescheduled unconditionally, so a finished board repainted 60 times a second for as long as
the tab stayed open.
- **MEASURED IN A REAL BROWSER: 0 draws over 60 idle rAF frames · 1 draw hovering onto a node and 1
  hovering off · 1 on a wheel zoom · 30 draws over 30 frames with a flow open** (the crawling dash).
  Counted by patching `setTransform`, which `draw()` calls exactly twice a frame. **MEASURE PER rAF
  FRAME, NOT PER SECOND** — headless Chrome throttles rAF and a wall-clock sample read 0 in BOTH arms,
  which reads exactly like a frozen board.
- **THE SIX REFS ARE ONE HOOK, and that is what makes it safe.** `huesRef`, `commandersRef`,
  `matchesRef` and `flowRef` are assigned in the COMPONENT BODY, so any change implies a render — a
  `useEffect` with NO dependency array covers all four and anything added later. Only the camera, the
  hover id and art landing have no render behind them.
- **THE rAF IS NEVER CANCELLED, deliberately**: a missed wake is a frozen board, so the loop stays
  alive and a missed invalidation self-heals on the next one rather than being terminal.
- **THE HOLE: `ReportTabs` BUILDS THE LOADER THE BOARD ACTUALLY USES**, because `GraphView` prefers
  it (it starts warming art the moment the analyze response lands) — and the first fix wired the wake
  to `GraphView`'s own FALLBACK loader. `draw()` reads art by POLLING, and a parked loop never polls,
  so art would have landed and never painted. **A SHARED OBJECT NEEDS A SUBSCRIPTION, NOT A
  CONSTRUCTOR CALLBACK** — `ArtLoader.subscribe`. Every test was green through this.
- `PARK_ALPHA` is d3-force's own default `alphaMin` restated as a named constant, because the board
  drives `tick()` by hand and the test stubs of `createBoardSimulation` do not implement the method.
- **A MEASUREMENT ERROR WORTH THE WARNING: a `vi.fn` `mockReturnValue` IS STICKY ACROSS TESTS.** The
  first version of the still-settling test was measuring a leaked stub (alpha 0, an already-parked
  board) rather than anything it wrote. It stubs its own moving simulation now.
- **H12 IS BLOCKED AND THE BLOCK IS VERIFIED: there is no add verb and no deck editor in the client**
  — `setInspectingId` is written in exactly one place, from a canvas click.
- **H13 IS REFUSED ON ITS PREMISE: `graph-html.ts` and the app board are DIFFERENT GRAPHS.** The
  export renders `buildGraph`'s printed-characteristics graph (~330 nodes: type/subtype/keyword/mana/
  cmc/layout, plus `event:` nodes) for Cytoscape/Gephi; the app renders `projectDeckGraph` (~84 nodes:
  cards and synergy edges). "Pre-settle so the export matches the app" has nothing to match. Written
  into `graph-html.ts` itself, which is what survives of the item.


**A TARGETING RESTRICTION IS A DEMAND NOTHING HERE MODELS — C7's re-enumeration found it, and the
LIST WAS ITSELF THE DEFECT (2026-08-22). PANEL 94.9% -> 95.5% [93.1, 97.1], false 22 -> 19, real
406 -> 405, debt 0.** `SubjectFilter.restricted`, set by `parseSubject` on the printed cue "targets
only", read by `eventMatches` on the CONSUMER alone. `DERIVE_VERSION` 74 -> 75, free.
- **THE ROADMAP'S FALSE-FAMILY LIST WAS HAND-TYPED AND WENT STALE BY 41 CLAIMS IN TWO DAYS** — it
  recorded 63 falses; the panel reads 22. **`panel-score.ts --falses <out>` now dumps every FALSE
  claim with the note it was judged under**, so the work list is regenerated instead of re-typed.
  `PanelScore.falses` carries it next to `unjudged`, because re-deriving it at a call site means
  re-implementing the cache's exact-then-wildcard lookup.
- **TWO DEFECTS STACKED, AND THE FIRST IS WHY THE ANSWER IS REFUSAL RATHER THAN A BETTER PARSE.**
  The type LEAKED out of the relative clause: `parseTypes` sweeps the whole string, so Leyline of
  Resonance derived `[creature, instant, sorcery]` and **Vesuvan Duplimancy — head noun merely "a
  spell" — derived `[creature, artifact]`**, naming what is TARGETED and missing the spells it
  watches. With the noun corrected it still claims every instant in the deck, which is the registered
  "a claim that applies to a card merely for being an ordinary card is false". `replacement.restricted`
  is the same ruling one layer over: keep the ability and its kind, claim no cards.
- **MEASURED: 24 corpus cards print "that targets only", 3 on a trigger subject, 108 reasons across
  the 71 decks** (Leyline 59, Vesuvan 36, Exterminator Magmarch 13). **Edges 35,333 -> 35,234 (-99),
  reasons 44,599 -> 44,491 (-108), the pre-registered figure EXACTLY; MESHED 288 unchanged, build 0
  memberships, ratings 72 of 6,236 (mean |Δ| 0.0025), themes 0 headlines.** ISOLATED: disabling the
  one gate line returns the population to 35,333 / 44,599 byte-identical.
- **ONE REAL CLAIM IS KNOWINGLY LOST AND IS STATED RATHER THAN HIDDEN: Origin of Metalbending is an
  INSTANT** whose mode puts a counter on "target creature you control", so Leyline really does copy
  it and the owner judged it REAL. The engine cannot tell it from the instants aimed at an opponent.
- **READ ON THE CONSUMER ONLY.** A producer's emit never states how a spell was targeted, so the
  field can only ever be a demand — reading it on the producer is the `entersTapped` mistake that
  silently deleted 29 real claims. Both directions pinned by tests proven to fire.
- **A SUB-SHAPE THE OWNER HAS NOW RULED ON TWICE, and no parse can close it: A REMOVAL SPELL'S FILL
  GOES TO THE OPPONENT'S GRAVEYARD BY INTENT, NOT BY TEXT** — Noxious Gearhulk -> Junji ("you would
  very rarely destroy your own creature") and Abstruse Appropriation -> Nulldrifter. Both technically
  satisfiable, both false about how the card is played.
- **THE 19 LEFT**: control/whose side 7 · a board state or condition with no slot 4 (Death Tyrant's
  "ATTACKING creature you control or BLOCKING creature an opponent controls" ×2, Yuna's intervening-if,
  Alchemist's Talent's "cast using a Treasure") · a zone lost at the CLAUSE layer 2 (**Fang, Fearless
  l'Cie watches cards leaving YOUR GRAVEYARD and the clause records a bare `leaves`** — a
  re-normalization, so it SPENDS) · self-reference 2 · wrong effect kind 2 · transform-is-not-entering
  1 · dropped antecedent 1. **At 19 over 424 decided claims these sit inside the rubric's own 25-40%
  judge error; the registered next step is the THIRD BLIND DRAW, not chasing them one at a time.**

**TWO ROADMAP ITEMS IN A ROW DIED ON THEIR PREMISE, SO CHECK THE PREMISE BEFORE OPENING ONE.** G5b's
was wrong three ways; **G2b's was RIGHT and already shipped a day earlier in `2bddda0`** (the
`TOKEN_CREATION` guard in `parseSubject`) with the line left un-ticked — re-measured 2026-08-22,
**ZERO name-shaped `create-token:` keys across the 71 decks** against the 6 it was filed on, the
family now 94 distinct subtypes all real, and the only multi-word key left corpus-wide is `time lord`,
which is the ONE real multi-word subtype in Magic. C7's list was stale the same way. A roadmap line is
a claim about the code, and a stale one is a map of where the work used to be.


**A TRIGGER-DOUBLER CANNOT SAY WHICH TRIGGERS IT DOUBLES — 14 OF 15 DERIVE NO SUBJECT, AND
PANHARMONICON AND ISSHIN ARE BYTE-IDENTICAL (2026-08-22, owner's lead).** The owner named the
family from memory — "cards like Sword of Animist which grant creature trigger on attack and Isshin
can double those triggers" — and it holds.
- **CLOSED 2026-08-22 — `Ability.doubles` now records WHICH triggers a doubler doubles, and a new
  TRIGGER-TO-TRIGGER pass pairs it with cards carrying that trigger. Measured: edges 35,124 ->
  35,333 (+209, inside Fable's pre-registered +200-450), reasons +238, MESHED 288 UNCHANGED, panel
  FALSE 22 -> 22 with 1 new claim left as OWNER debt, build 0 memberships moved, TOP card 0/71.
  All five registered criteria pass, including 0 of 234 cross-class claims and 0 on a consumer with
  no triggered ability. `REFIRE_KINDS` stopped counting the bare kind in the same change, so Isshin
  no longer makes a deck read as an ETB-refire deck.** Original measurement (stale the moment Isshin
  was pulled in, which made it 16/15):
- **MEASURED: `static:trigger-doubling` is 15 abilities and 14 carry NO subject** — Panharmonicon,
  Yarok, Veyran, Naban, Drivnod, Tekuthal, Harmonic Prodigy, Echoes of Eternity, Gandalf the White,
  Wayta, Twinflame Travelers, Starfield Vocalist, Mirror Room, Virtue of Knowledge. Each names a
  DIFFERENT event class — Panharmonicon "creature entering", Drivnod "creature dying", Veyran
  "instant or sorcery you cast", Naban "Wizard entering", Isshin "creature attacking" — and all of
  them collapse to the same thing, so the engine cannot tell any of them apart.
- **PROVEN BY PULLING THE WITNESS IN RATHER THAN ARGUING IT.** Isshin was outside the 71 decks and
  had no clause doc at all; on the owner's ruling it was normalized for **$0.0005** and derives
  `verb=trigger-again`, object `"a triggered ability of a permanent you control"`,
  `static:trigger-doubling`, **subject ABSENT — byte-identical to Panharmonicon.** The qualifier is
  dropped by the CLAUSE layer, so this is not fixable in derive alone.
- **CLAUDE.MD ALREADY RECORDED THIS AND RECORDED IT TOO NARROWLY.** The standing note blames a
  COLOUR filter ("a static narrowed only by COLOUR loses its subject", Echoes of Eternity). Colour is
  one of fourteen: Panharmonicon's narrowing is a TYPE plus an EVENT and is dropped just the same.
- **IT COSTS NO FALSE EDGES TODAY, WHICH IS WHY IT SURVIVED**: with no subject the family forms no
  edges at all, so a trigger-doubler is inert rather than wrong. The cost is silence — an Isshin in
  an attack-trigger deck reads as synergising with nothing.

**A CARD CAN BE PULLED INTO THE CORPUS BY NAME NOW — `--card "<name>"` (owner's ruling 2026-08-22:
"if you need a card, we can always bring it in the corpus even if it is not covered by 71 decks").**
When given, the named cards are the WHOLE scope, so the bill is one card and the dry run says so.
Same need `EXEMPLAR_TERMS` serves for a vocabulary addition, one rung more general: there the witness
is chosen by the WORD it exercises, here by NAME, because the reason you want a card is usually a
DEFECT rather than a word. **This is how the trigger-doubling finding above stopped being an
argument and became a measurement**, and it is the cheapest instrument in the repo.


**THE BATCH API BATCHES THE TRANSPORT, NOT THE PROMPT: 1,408 CARDS FOR $3.24 AT HALF PRICE, AND IT
LANDED IN SEVEN MINUTES (2026-08-22).** `normalize-corpus.ts --batch` submits, `--collect <file>`
persists in a later process. **Persisted 1,380, refused 28, failed 0.**
- **THE HEADER OF THAT FILE FORBADE BATCHING AND THE PROHIBITION IS ABOUT A DIFFERENT THING.**
  `tag-batch-api.ts` put 40 cards in ONE PROMPT, which is the measured cause of the old pipeline's
  dropped and duplicated clauses (32 clauses vs 39-41 for the same cards sent singly). The Batch API
  sends the SAME one-card request per card. `anthropicBody` is shared with the live client and a
  test asserts the submitted params equal it EXACTLY — **proven to fire** by making the submitter
  build its own body. Half price is only a discount if it buys the same corpus.
- **RESULTS COME BACK IN ANY ORDER**, so the collector keys on `custom_id` and never on position.
  Reading them positionally would write each card's clauses onto a DIFFERENT card — the worst
  failure available to this pipeline. The test fixture is deliberately out of order.
- **THE SCOPE WAS DEDUPED BY NAME, WHICH IS NOT DEDUPED BY CARD** — found because the Batch API
  rejects colliding `custom_id`s and the live path never noticed. **2,544 scope names resolve to
  2,541 cards**: Rampant Growth, Reanimate and Exsanguinate are each also listed under a Universes
  Beyond flavor name that `searchNames` merged onto the same card. The live path had been paying
  for three cards twice.
- **MEASURED, AND THE AGGREGATE IS A NEAR-NULL RESULT WORTH STATING PLAINLY: edges 35,211 → 35,143
  (−68), reasons 44,465 → 44,382 (−83), MESHED 288 UNCHANGED.** Re-normalizing 1,380 cards under a
  newer prompt barely moved the population, and moved it slightly DOWN.
- **THE DEBT IS JUDGED AND THE ANSWER IS "NO DETECTABLE CHANGE": PANEL 95.3% → 94.8% [92.3, 96.6],
  real 405 → 405, false 20 → 22, uncertain 22 → 22, DEBT 0** (owner judged all 7 on 2026-08-22).
  **94.8% sits INSIDE the pre-buy baseline's own bound of [94.4, 95.3]**, which had debt 4 — so the
  buy moved panel precision by nothing measurable, and the mid-flight 95.5% was flattered by three
  falses sitting unjudged. **Quote 92.9% debt 0 from here on** (this block's 94.9% was superseded by the 2026-08-23 REAL-column census; kept because the buy's own before/after is measured against it) (94.8% -> 94.9% when the trigger-doubling family's first
  claim, Gandalf the White -> Legolas Greenleaf, was judged REAL by the owner on 2026-08-22 --
  real 405 -> 406, false 22 unchanged).
  - **THE "REAL FELL BY 4" WORRY IS RESOLVED AND IT WAS NOT LOST RECALL** — all four were the
    unjudged claims, and all four came back REAL. Real is unchanged at 405 across the whole buy.
    Checking before believing a recall loss, for the third time in this file.
  - **TWO OF THE THREE FALSES CONFIRM DEFECTS THIS FILE ALREADY NAMES AS UNFIXED**, which is the
    useful part: **Incubation Druid → Evolution Witness** is self-reference on BOTH sides (Adapt puts
    counters on ITSELF, the Witness triggers on counters on ITSELF), and **Misty Rainforest → Yuna,
    Grand Summoner** is the dropped intervening-if — Yuna needs "if it had one or more counters on
    it" and a sacrificed fetchland carries none, the exact case recorded with these exact fetchlands.
  - **THE THIRD FALSE IS AN OWNER RULING THAT BEAT CLAUDE'S READ, and it is worth keeping.**
    **Abstruse Appropriation → Nulldrifter**: Claude leaned REAL on the Goldspan Dragon / Terror of
    the Peaks plain-body precedent, since Nulldrifter really is a nonland permanent that really can
    be exiled and re-cast. **Owner: "Abstruse Appropriation is removal in the first place, you would
    not use it on your own stuff."** The claim presupposes exiling your OWN creature; a removal spell
    is aimed at an opponent's permanent, so the card you may cast is theirs. Judging it real would
    have rewarded an over-general claim that happens to land and hidden the dropped-antecedent defect
    behind it. **A claim can be technically satisfiable and still false about how the card is played.**
  - The four REALs each turn on a printed fact worth not relearning: Danny Pink GRANTS its trigger to
    creatures you control (so Gimli countering itself fires it — a grant is not a self-reference);
    **Raucous Theater's TYPE LINE is "Land — Swamp Mountain"**, so a Swamp-or-Mountain fetch really
    finds it; Genesis Wave's own last sentence puts the unchosen revealed cards in YOUR graveyard.
  → `docs/measurements/panel/verdicts-debt-2026-08-22.jsonl`
- **WHAT IT DEMONSTRABLY BOUGHT: 47 cards gained a verb they had no word for** — `gain-control` 25,
  `monarch` 13, `phase-out` 9 — each previously losing its clause to the escape hatch. **126 cards
  are still on `other`**, which is the next punch list.
- **THE REFUSALS ARE NOT MOSTLY A VOCABULARY PROBLEM, which reframes the obvious next step.** Of 28:
  **ability-type-mismatch 9 · invented-id 6 · missing-trigger 4 · duplicate-id 4 · unexpected-trigger
  3 · unknown-verb 2 · unknown-trigger-event 2 · zone-on-unzoned-verb 1.** Only **4** name a missing
  word (`day-night` as a VERB, `play` and `leave` as TRIGGERS). The rest are the model
  mis-structuring its answer against the segmenter, and the gate refusing rather than banking it.
  Adding those three words buys 4 cards, not a batch.
- **A PROBE CALL SETTLES THE PREFILL SHAPE BEFORE SUBMITTING** — the live client detects an
  unsupported assistant prefill from a real call's ERROR and retries; a batch cannot, and by the
  time it errored it would have cost the whole run.
- **VERIFIED ON A 3-CARD BATCH FIRST**, submit → poll → collect → persist in ~2 minutes, and
  **Robe of Stars came back carrying `phase-out`** — a card measured that same hour as stuck on
  `other`. Vocabulary, transport and gate confirmed together against a real card.
- **NOT CAPTURED, AND IT IS A METHOD MISS: no `ratings-compare.ts --save` baseline was taken before
  the buy**, so the per-card rating effect of 1,380 re-normalized cards is unmeasurable after the
  fact. Take it BEFORE the next spend.

**THE ACTION SIDE OF THREE CONCEPTS WHOSE TRIGGER SIDE ALREADY EXISTED (2026-08-22).**
`gain-control`, `phase-out` and `monarch` are all legal TRIGGERS and none was a legal VERB, so a
card that PERFORMS one lost its clause whole. Same asymmetry `copy` and `create` were each found by.
- **SIZED OVER THE WHOLE ~34k CORPUS** per the 2026-08-15 completeness ruling: gain-control **348
  corpus, 11 of 27 normalized on `other`** · phase-out **60, 7 of 10** · monarch **64, 5 of 15**.
- **THE CONTROL FOR THE MECHANISM IS `initiative`** — the same KIND of concept as `monarch` (a
  designation the game tracks, CR 726 against CR 720) and already a VERB: **23 corpus cards, 8
  normalized, ZERO on the escape hatch.** Having the word is the entire difference.
- **NONE GETS AN `EMITS` ROW**, following `initiative` and `city-blessing`: their trigger
  counterparts carry no `CLAUSE_TRIGGER_TO_VERB` mapping either, so there is no consumer for an emit
  to reach and a guessed event would buy nothing while risking false edges.
- **A GATE THAT HAD STOPPED GATING, found while bumping the versions beside it.**
  `TRIGGER_VOCAB_VERSION` sat at **14 against a NORMALIZE_VERSION of 12** — it was incremented as a
  plain counter rather than set to the prompt version its list changed at, so it sat ABOVE every
  version any doc could carry and re-selected the entire `other`-trigger population on every run.
  The highest stored normalizeVersion in the corpus was 12, so nothing was ever gated out. **That is
  the `--refresh-other` treadmill arriving through the guard written to prevent it.** Lowered to 13;
  pinned by a test proven to fire. `NORMALIZE_VERSION` 12 → 13, `VOCAB_VERSION` 10 → 13.
- `--refresh-other` selection **26 → 158 cards ($0.12 → $0.73)**.

**CLAUDE.MD'S POPULATION FIGURES WERE STALE BY A COMMIT AND NOBODY NOTICED.** This file recorded
35,583 edges / 44,777 reasons; the 2026-08-22 baseline measured **35,211 / 44,465**. The drift is
`e086a32` (the recipient-is-not-a-subject fix), which moved the population and was written up
without its population numbers. **Measure the gates in the same commit that moves them.**

