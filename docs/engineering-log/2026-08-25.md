## State as of 2026-08-25

**TWO FABLE REVIEWS OF THE MANA MODEL LANDED 2026-08-25 AND FOUND REAL DEFECTS — NOTHING IS FIXED.
FOURTEEN ITEMS FILED AS ROADMAP SECTION N; START THERE.** One review took the STATISTICS and the
measured deltas, one took the MAGIC RULES correctness of the text parsing, deliberately split so
neither could rubber-stamp the other.
- **WHAT THEY CLEARED, because it is what makes the rest credible: `payable` was brute-forced against
  an exhaustive backtracking matcher over 200,000 random cases — ZERO mismatches.** `alsoPrice` is
  byte-identical with and without a commander, the 12-turn horizon refuses rather than answering a
  nearer turn, and same-seed across both policy arms is a correct PAIRED comparison. The headline
  deltas reproduce to the decimal.
- **N1: CASTABILITY PRINTS UP TO 300% ON A LEGAL PASTED DECK.** `byCardHits` keys on card NAME and
  increments per COPY per trial-turn — three copies read `byCard = 3.0`, REPRODUCED. **The 71 decks
  are singleton, so every measurement ever taken here was blind to it**, and an arbitrary paste is a
  first-class input.
- **N2: THE TEN REAL FETCHLANDS DO NOT MATCH THEIR OWN CUE — ~110 slots — AND THE TEST THAT PROVED
  IT REWROTE THE CARD'S TEXT.** See the L4a block below, corrected in place.
- **N3: A TRANSFORM CARD'S LAND BACK IS PLAYED AS A LAND (CR 712.4a), ~23 slots.** `land-count.ts`
  already makes this check — **E4's "the `isLand` mistake one layout over" is recorded in this very
  file** — and `goldfish.ts` never learned it.
- **N4: A -6.5pp MANA CORRECTION SHIPPED UNDISCLOSED INSIDE THE COLOUR COMMIT.** Library thinning
  arrived in `e0efd6d` (VERIFIED: two `library.splice` calls `90eed3e` does not have) and moved
  P(>=6 mana at T6) in **59 of 71 decks, ALL DOWN, worst -6.52pp** — correct, and measured nowhere.
  The commit says "gates byte-identical", true of population/panel and silent on the table a reader
  reads. **"Measure the gates in the same commit that moves them", for the third recorded time.**
- **N5: the 2,000-trial noise figure in `goldfish.ts` is 5x optimistic** — claims 0.5pp, measures
  SD 0.8-1.2pp with a 2.58pp max excursion. The conclusion survives; the sentence does not.
- **N7: two of L4a's own figures do not reproduce** — the land half of the decomposition, and
  "the tron branch moves exactly 6 decks". Both corrected in place below.
- **N8: the falsifier defence is SOUND and half the story** — the two-colour stratum (1.87pp) is also
  under the threshold, so 47 of 71 decks clear nothing individually and the feature earns its keep in
  the 25 at three or more colours. The gradient's grouping rule was never stated and moves the table.
- **N15, FOUND BY A THIRD REVIEW THE OWNER ASKED FOR AND THE LARGEST UNMEASURED ERROR IN THE MODULE:
  A FETCH REMOVES THE NEXT LAND IN DRAW ORDER AND EVERY FETCH SAYS "THEN SHUFFLE".** The residual
  library is exchangeable, so a UNIFORMLY RANDOM land should leave; taking the earliest pushes the
  survivors later. **MEASURED A/B over the 71 decks: P(>=6 mana at T6) +1.90pp mean, up in 57 decks,
  worst `naya-spellslinger` +6.92pp.** The model UNDERSTATES mana on exactly the fetch decks the
  land-fetch feature was built for. **ONE LINE PER SITE — and neither adversarial review of this same
  module found it, between them filing fourteen other defects.**
- **THE RAMP ARM HAS NO EXTERNAL CHECK AT ALL, which is why N15 survived.** A1 validates LANDS-ONLY
  against the closed form; the ramp arm is the part doing all the work (median +33.8pp) and nothing
  checks it. An exact DP on a small synthetic deck runs in **33ms** and is that missing anchor (N16).
- **THE OWNER WAS RIGHT AND I WAS WRONG ABOUT THE LAND-DROP CAP (N17).** I claimed it needs the
  prefix; **one card is seen per turn, so `S_t - t` is non-increasing and the cap collapses to
  `min(T, S_T)` — counts ARE enough for lands**, verified to 7.8e-16 against `pAtLeast`. Where the
  count instinct genuinely fails is RAMP: "cast iff pool >= mv" is a threshold whose argument is the
  quantity being computed, so identical counts give different boards. **Replacing the simulator is
  refused on MEASUREMENTS — 6.2s on `fairdrazi` against 63ms, and 6.8M states for the land dimension
  alone once colours enter.**
- **THE METHOD LESSON, and it is the one to carry: A PROBE THAT CALLS THE INNER FUNCTION DOES NOT
  TEST THE GATE THAT SELECTS IT.** `fetchMask` was verified against real cards and `FETCHLAND` — the
  predicate deciding whether `fetchMask` is ever called — was not. Fifth entry in this file's
  "a probe answered a question nobody asked" family, and the first where the probe was RIGHT.

**THE REPORT CAN SAY "YOU CAN CAST THIS" NOW, IN ONE NUMBER — L4a shipped 2026-08-25, free, and
TWO LIVE RUNS FOUND WHAT NO TEST WAS GOING TO. Gates byte-identical (34,141 / 43,156 / MESHED 287,
panel 95.1% real 385 / false 20 / debt 0), which IS the acceptance test: it still feeds no score.**
- **THE REFUSAL THAT COST A SENTENCE.** `castability.ts` reported a mana axis and a colour axis and
  refused to multiply them — correctly, both are driven by the same lands — so **no figure in the
  whole report meant "you can cast this card"**. The simulation asks the board both questions in the
  SAME TRIAL, so the correlation is handled by construction rather than avoided.
- **`castable` IS THE ANSWER, `mana` IS THE DIAGNOSTIC.** A card whose mana is high and whose
  castable is low has a COLOUR problem, not a ramp problem, and those are fixed differently.
  Threshold 5pp in the CLI and the panel; below it the second number says the same thing twice.
- **Hall's condition over the cost's OWN colours**, never a product of per-pip probabilities: a
  hybrid pip is ONE pip either colour satisfies, a dual is ONE mana not two, colourless is the EMPTY
  mask so Sol Ring pays generic and no coloured pip.
- **A FETCHLAND IS A COLOUR FIXER AND A THINNER (owner) — AND THE TEN REAL FETCHLANDS DO NOT MATCH
  THE CUE, so none of this reaches them (Fable, 2026-08-25, roadmap N2).** `FETCHLAND` demands the
  literal *"land card"* and **Scalding Tarn says "an Island or Mountain CARD"** — verified FALSE on
  the whole cycle, ~110 slots, no mask and no thinning. **The test that "proved" it rewrote the
  card's text**, and the probe called `fetchMask` directly rather than the gate. What the rule DOES
  reach is the "basic land card" fetches. **`producedMana` covers 94.5% of
  land slots and the ENTIRE gap is fetchlands**, which correctly produce nothing of their own.
  **Scalding Tarn searches for an "Island or Mountain CARD" — not a basic — so it finds Steam Vents,
  20 lands in `iz-it-izzet` rather than 19 basics; and it is an OR, not an AND.** Swept all 66
  library searchers: exactly ONE card needs the two facts separated, **Seething Landscape** ("a BASIC
  Island, Swamp, or Mountain card", 3 decks), which was counting Watery Grave.
- **THE REGISTERED FALSIFIER FIRED AND IS RECORDED, NOT RELAXED.** It said refuse under 2pp median
  across the 71 decks; measured **1.45pp**. It is the WRONG STATISTIC — 26 of 71 decks are zero- or
  one-colour where a zero gap is the model being RIGHT, so C3's own control arm was counted against
  the feature. **FOURTH time this file records a criterion written wider than the claim it gates.**
  **THE GRADIENT IS MONOTONE: 0.00 / 0.50 / 1.70 / 7.70 / 23.90pp at 0, 1, 2, 3 and 5 colours**,
  worst `unify-all-the-universes` at 40.6pp median card and 82pp at p90.
- **LIVE DEFECT 1, THE SIXTH TIME THIS FILE RECORDS THE SHAPE: `{C/W}` IS PAYABLE WITH COLOURLESS
  MANA.** Ulalek, Fused Atrocity costs `{C/W}{C/U}{C/B}{C/R}{C/G}`, read as a WUBRG demand — so
  `fairdrazi-5-color-less`, a COLOURLESS Eldrazi deck, was told its own commander casts **6% – 9%**
  while the report added *"what is missing is the colours"*. Now **63% – 80%** and the note correctly
  vanishes. **A NUMERIC hybrid (`{2/W}`) KEEPS its colour** — the generic alternative costs MORE, so
  demanding the colour is the under-claiming direction.
- **LIVE DEFECT 2, IN THE BROWSER: the diagnostic line printed "91% – 91%"** — the headline collapsed
  a degenerate range and the second line did not. Both do now, pinned by a test.
- **THE OLD CAVEAT IS DELETED RATHER THAN REWORDED**: *"lands and mana rocks only — land-fetch ramp
  like Cultivate is not counted, so this reads low"*. The simulation models land-fetch ramp; that
  caveat was the measured defect this whole line existed to close. Two stale claims in
  `goldfish.ts`'s own header corrected the same way — **it is no longer colour-blind and
  `deck-math.ts` DOES import it** (for the panel; `pressure.ts` still does not, so no score moves).
- **BOTH POLICY ARMS RUN ONCE** (`manaModel`), a commander is PRICED WITHOUT BEING SHUFFLED IN
  (CR 903.6, `alsoPrice`), and a card past the 12-turn horizon is REFUSED rather than priced at a
  nearer turn. Cost 28.2ms -> 46.8ms on the worst five-colour deck. C1-C7 pass. Suite 2,487.

**A MANA SOURCE IS WORTH WHAT IT TAPS FOR, AND `goldfish.ts` PRICED EVERY ONE AT EXACTLY ONE —
I10b shipped 2026-08-25, free. Headline P(>=6 mana at T6) moves in 56 of 71 decks, ALL UP, mean
+2.23pp, max +7.5pp. Gates byte-identical (34,141 / 43,156 / MESHED 287, panel 95.1% real 385 /
false 20 / debt 0), which IS the acceptance test for a leaf nothing scores.**
- **THE ITEM WAS OPENED FOR TRON AND TRON IS 4% OF THE ANSWER. Decomposed: the ROCK half is +1.96pp
  over 54 decks, the LAND half +0.28pp over 20** (recorded as "+0.22pp over 16" and corrected by an
  independent re-measurement 2026-08-25 — the original decomposition did not sum to its own total). **Sol Ring is in 52 of the 71 decks and was worth
  one mana.** 19 distinct cards upgrade, READ ONE BY ONE, zero false positives.
- **THE FIRST COUNT WAS WRONG THE FAMILIAR WAY: 27 lands print an `Add` of two or more symbols and
  only 9 NET more than one.** A filter land pays `{U/R}` to make two; a payment land pays `{1}`.
  **A COST IS NOT FREE** — the "a RATE is not an AMOUNT" ruling one subsystem over. Also refused with
  reasons: RESTRICTED mana (colour-blind model, so Jegantha's five stay one), a KAROO (two off one
  fewer land is net neutral, bounce unmodelled), any cost that is not exactly `{T}`.
- **A GATE IS ZERO BELOW ITS THRESHOLD, NOT ONE.** Temple of the False God, 16 decks, taps for
  NOTHING under five lands; the incumbent gave it one from turn one.
- **THE TRON BRANCH MEASURED ZERO UNTIL THE OWNER CORRECTED THE MECHANISM.** Drawing three NAMED
  singletons among 13 seen cards is (13x12x11)/(99x98x97) = **0.18%**, so a trio-keyed branch moved
  **0 of 71**. **PLANAR NEXUS IS "every nonbasic land type", so ONE CARD IS AN URZA'S MINE AND AN
  URZA'S POWER-PLANT AT ONCE — and it is in 6 of the 7 tron decks.** Modelled, the branch moves
  **exactly those 6** (+0.4 to +1.2pp at T6, +0.9 to +2.2pp at P(>=8 by T8)); the 7th,
  `colorless-honest-magic`, is the one without a Nexus, which is the attribution confirming itself.
  **Prismatic Omen is NOT tron — every BASIC land type does not include Urza's.**
- **I10's "unreachable" NOTE IS REFUTED BY THE LAYER IT WAS WRITTEN ABOUT.** It recorded that Planar
  Nexus's type-granting static is dropped at derive, so "neither a subtype read nor a name read
  reaches it". **`goldfish.ts` reads PRINTED TEXT by design** (`classifyAccelerant`'s own rule), so
  the owner's acceptance test is reachable there and only there. **CHECK WHICH LAYER A CEILING
  BELONGS TO before recording it as general.**
- **`castability.ts` HAS THE SAME DEFECT AND KEEPS IT, with the reason in the code**: `pAtLeast`
  counts SUCCESSES among draws and a source worth two mana is not a success worth two, so fixing it
  means leaving the hypergeometric — which is what the simulator already is. Under-statement, so it
  stays inside `manaWithRocks`'s job of being an interval's upper bound.
- **CEILING, one card: Worn Powerstone ENTERS TAPPED** and rule 4 pays a rock the turn it lands —
  pre-existing, now two mana early instead of one. 2 decks, the line J12 and M4 both refused on.
  Five guards proven to fire by mutation, plus the Nexus branch. Suite 2,480.

**A PERMANENT ENTERING UNDER A CONTROLLER THE SCHEMA CANNOT NAME NOW CLAIMS NOTHING — I7 shipped
2026-08-25, FREE, and SECTION I IS CLOSED. Edges 34,418 -> 34,141 (-277), reasons 43,500 -> 43,156
(-344), MESHED 287 UNCHANGED, PANEL 94.8% -> 95.1% [92.5, 96.8], false 21 -> 20, uncertain 23 -> 22,
REAL 385 UNCHANGED, debt 0, build memberships 0.** A precision win at ZERO recall cost.
`DERIVE_VERSION` 80 -> 81.
- **THE ITEM'S PRESCRIBED FIX WAS WRONG AND ITS CEILING WAS REAL.** It said *"the fix is `control` on
  the emit"*. **`SubjectFilter.control` is `{you, opp, any}` and the printed fact is "whoever owns the
  target"** — none of the three, since Chaos Warp aimed at your OWN permanent gives YOU the new one.
  So it is a REFUSAL, the resolution `replacement.restricted` and C7's `SubjectFilter.restricted`
  both reached: keep the ability, claim no cards.
- **`any` IS A PERMISSION, NOT A NEUTRAL UNKNOWN** (C6's finding), so it satisfied a `you` demand and
  an `opp` demand alike and **Chaos Warp claimed to put EVERY CREATURE IN YOUR DECK onto the
  battlefield — 13 rows on `eggman`**, *"When Coalstoke Gearhulk enters thanks to Chaos Warp, it
  brings a card back"*, false twice over (a RANDOM top card, under the target owner's control). Now 0.
- **THE CUE IS TWO PRINTED FACTS** — an antecedent controller plus the permanent coming off *"their
  library"* — **73 corpus cards match the library half, 8 derived, READ ONE BY ONE rather than
  sampled. TWO EXCLUSIONS, EACH FOUND BY A CARD THAT WOULD OTHERWISE HAVE LOST A REAL CLAIM:**
  *"under your control"* (**Curse of Unbinding** reveals off the enchanted player's library and the
  creature is YOURS) and *"your library"* (**Demolition Field** and **Tempt with Discovery** have TWO
  puts, and **the clause layer records no owner per action**, so a card-level refusal would delete the
  real half). **"Each player" falls out for free** — Field of Ruin names no antecedent and includes
  YOU. **OVER-CLAIMING ON ONE CARD BEATS DELETING A TRUE CLAIM: that is the correct direction when
  REMOVING**, the mirror of the usual "a missing answer beats a wrong one".
- **BOTH DEPARTING PANEL CLAIMS WERE DIAGNOSED IN THE CACHE ON 2026-08-07 — eighteen days before the
  fix landed.** `Chaos Warp -> Demonic Junker` FALSE (*"Chaos Warp cannot make it enter — it shuffles
  a permanent away and reveals a random top card"*), `Chaos Warp -> Nesting Dragon` UNCERTAIN. **A
  judged verdict's NOTE is a defect report nobody was reading.**
- **THE ONE CLAIM THAT HAD TO SURVIVE DID, FOR A STRUCTURAL REASON.** `Sundering Eruption -> Nesting
  Dragon` on `enters:land` is OWNER-judged REAL and Sundering Eruption is one of the five refused —
  **but Volcanic Fissure is a LAND FACE, so `impliedEvents` gives the card its own entry**. What left
  is the searched basic that goes to the opponent. Checked against the panel, not assumed.
- Ratings 268 of 6,236 (mean |Δ| 0.0090), raw scores 561, partners 300, TOP card 0/71, top-10 7/71.
  Three guards proven to fire by mutation. Suite 2,477.

**I10 IS REFUSED ON TWO DERIVED CARDS, AND MY FIRST CONSUMER COUNT WAS VACUOUS (2026-08-25). No
code, no spend.**
- **THE METHOD ERROR FIRST, because it nearly produced the opposite answer. The first count read the
  stored `subtypes` field, which the MTGJSON merge leaves `undefined` on every card in this family
  (it covers 22,152 of 33,977 cards), so it answered ZERO whatever the decks held.** Read off the
  TYPE LINE it is **25 distinct lands**. **A PROBE THAT READS AN ABSENT FIELD ANSWERS 0 AND LOOKS
  LIKE A FINDING** — fourth time this file records one.
- **THE FAMILY IS FAR MORE LIVE THAN THE ITEM IMPLIED: 25 distinct nonbasic-subtype lands across the
  71 decks · Urza's Saga in 20 (it IS an Urza's land) · Talon Gates of Madara 13 · Scavenger Grounds
  11 · and SEVEN decks run the full Urza tron trio.** Urza's Workshop sits in 3 decks holding 6, 7
  and 7 Urza's lands; `mono-blue-tribal-tribal` runs the whole Locus package.
- **THE SCALING CHANNEL IS GRAVEYARD-ONLY END TO END, BY CONSTRUCTION.** `scalingSubject` opens
  `if (!GRAVEYARD_COUNT.test(text)) return undefined`; the `edges.ts` loop opens
  `if (a.effect.scaling !== "per-graveyard" ...) continue`. So the item's *"may only need the card in
  the corpus"* is FALSE twice over — Urza's Workshop IS derived, with the fact sitting in
  `amount: "for each Urza's land you control"` and **`scalingSubject` absent**, and closing it needs a
  derive branch AND a new edge loop.
- **224 OF 241 SCALING ABILITIES CARRY NO `scalingSubject` (93%) AND MOST OF THAT IS NOT A GAP: 93
  have `amount: "X"` and 46 have no amount at all**, so the subject is not in the string to read.
- **THE GENERAL VERSION IS REFUSED BY C2's SHIPPED GREAT HENGE RULING** (a board count reached 33, 31
  and 46 creatures). C2 left the door open for the discriminating SUBTYPE case — 6 of 100 cards, not
  46 — **but the family has not grown: 12 corpus / TWO derived.** Two cards is the line J12 and M4
  both refused on.
- **"URZA'S POWER-PLANT" IS THE SUBTYPE; THE CARD IS "URZA'S POWER PLANT", NO HYPHEN** — a lookup on
  the item's spelling returns NOT IN CORPUS, which nearly became a false finding.
- **HALF B'S CONDITION IS DROPPED AT THE CLAUSE LAYER, exactly as I9 measured**: Urza's Mine's stored
  clause is ONE clause with TWO `add-mana` actions and the *"If you control…"* is gone, so the engine
  believes the land always adds both. I9 proved for $0.01 that re-buying returns byte-identical
  clauses — a `Clause` has no condition slot.
- **THE OWNER'S ACCEPTANCE TEST IS UNREACHABLE FROM EITHER DIRECTION: Planar Nexus derives two bare
  `mana-generation` abilities**, its type-granting static dropped and its type line a bare `Land`
  with no subtypes.
- **FILED WITH ITS NUMBER RATHER THAN BUILT — the one consequence with a consumer: the mana model
  UNDERSTATES the 7 full-tron decks.** `goldfish.ts` and `manaAudit` price every land at one source;
  assembled tron is 3 lands tapping for 7. **Reopen that before reopening the edge half.**

**I8 IS REFUSED AND ITS OWN INSTRUCTION IS WHAT REFUTED IT (2026-08-25). No code, no spend.** The
item said *"needs the derived doc to diagnose (the card has none), so pull it before writing
anything"* — **the card HAS one, and it reads the controller exactly as printed.**
- **Esper Sentinel derives `trigger {verbs: [cast], subject: {control: "opp", type: [artifact,
  enchantment, planeswalker, instant, sorcery, battle], notType: [creature], scope: "each"}}`,
  effect `draw-card`, `repeats: "per-turn"`.** So `opp` is there, the noncreature narrowing is there,
  and *"first ... each turn"* is carried by the field the resource ledger added. **It is not the C6
  family at all**, whose defect was `parseControl` returning `any` for a phrase it could not read.
- **THE FAMILY IS CLEAN, 19 OF 19, READ ONE BY ONE.** 97 corpus cards print *"whenever an opponent
  casts"*, 15 derived, 19 triggers, **16 carrying `control: "opp"`**; the other three are the same
  cards' OTHER abilities and each correctly `you` — Sauron's combat-damage Army trigger,
  Jin-Gitaxias's *"whenever YOU cast"*, Ghostly Pilferer's *"becomes untapped"*.
- **THE LIVE BEHAVIOUR IS RIGHT RATHER THAN UNDIAGNOSED.** Esper Sentinel sits in 3 of the 71 decks
  and forms NO synergy row in any — correct, since its trigger watches an OPPONENT casting and
  nothing in your deck supplies that. **C6's fix is what makes that true.** It is also correctly
  absent from the cut list, because it fills the `draw` role and D2 protects a card with a role.
- **THE RESIDUE IS MEASURED AND TWO SHIPPED RULINGS ALREADY COVER IT**: *"unless that player pays"*
  is **245 corpus / 18 derived** and is a TAX, which has formed no edge since 2026-08-06; *"where X
  is THIS creature's power"* is **56 / 6** and is the MAGNITUDE axis, refused three separate times.
- **THE NINTH ROADMAP PREMISE CORRECTED BY MEASUREMENT IN THIS RUN.** Eight of the last nine items
  opened had a false premise somewhere — L2's instrument, L1's target, I6's witness, I8's whole
  claim. **Pull the doc, run the count, THEN write.**

**PUTTING A PERMANENT ONTO THE BATTLEFIELD IS NOT CASTING IT, AND EVERY PERCENTAGE IN THE PANEL
PRICES CASTING — I6's into-play half ships as a CAVEAT 2026-08-25, free; its cheat-CAST half is
REFUSED ON ITS OWN FREQUENCY. Gates byte-identical (34,418 / 43,500 / MESHED 287, panel 94.8% real
385 / false 21 debt 0), which IS the acceptance test for a sentence.**
- **THE ITEM'S PREMISE ABOUT ITS OWN WITNESS IS FALSE — THE EIGHTH CORRECTED BY MEASUREMENT IN THIS
  RUN.** I6 says *"Rabble Rousing reads `cardSelection` off its Hideaway reminder text"*. **It derives
  ONE ability and it is a `token-generation` on `attacks`.** No `cardSelection` anywhere; the Hideaway
  reminder derives nothing because `segment.ts` makes a reminder clause inert — the same documented
  behaviour that hid cycling for months. And "Sneak Attack matches NOTHING" is true for a duller
  reason than implied: **neither card is normalized**, both being outside the 71 decks.
- **THE TWO HALVES SPLIT ON THE CONSUMER COUNT.** Cheat into PLAY (a NONLAND permanent from hand onto
  the battlefield): **107 corpus · 8 derived · 10 of 71 decks**. Cheat CAST: **438 corpus · 72 derived
  · 46 of 71 decks**. **A LAND FROM HAND IS EXCLUDED — that is a land drop you already had, i.e.
  RAMP** — and it is 52 of the 159 raw matches.
- **THE CHEAT-CAST HALF IS REFUSED ON FREQUENCY.** 46 of 71 decks (65%) over 64 cards that do not
  hold together — Tibalt's Trickery casts a RANDOM card, Dauthi Voidwalker casts an OPPONENT's exiled
  card, Mizzix's Mastery your own sorcery from the graveyard. **15 deck-slots were a card's own
  SUSPEND reminder**, the card cheating ITSELF in, which K5a's refusal list already covers and which
  it excluded rebound for. A caveat on two thirds of decks is one rung from the registered "a claim
  that applies to a card merely for being an ordinary card is false".
- **THE COINCIDENCE WITH THE DEFECT IS PERFECT AND WAS MEASURED BEFORE THE CODE: ALL TEN decks
  holding a cheat card also hold a nonland at mana value 6+, 8 of them at 8+.** `eggman` is told
  *"Metalwork Colossus 11 — 2%"* while running Dr. Eggman. Correct about casting, beside the point
  about the deck.
- **A LIST AND NEVER A RATE.** How often a deck cheats needs the enabler drawn, alive and holding a
  target — a play model this layer lacks and **I11's goldfish lacks too (it casts no removal and has
  no opponent)**. Same structural refusal J5 made for the commander tax. **No percentage changes**,
  asserted by a test comparing every row with and without the cheat card.
- **THE FAMILY WAS ENUMERATED BY SHAPE, NOT SAMPLED** — creature card 30, permanent card 9, artifact
  card 5, then singletons down to "construct, robot, or vehicle" — and **ZERO of the 107 put the card
  under an OPPONENT's control**, which would have been the wrong sentence.
- **WEB ONLY, AND IT IS INHERITED: `report.ts` has never printed `castability.biases`**, so the whole
  caveat block is invisible in a terminal run. Verified live both ways: `eggman` shows it under the
  Metalwork Colossus row, `draguns` renders the block without it. Suite 2,471.

**THE COLOURED-SOURCE REQUIREMENT SHIPPED WITHOUT THE MULLIGAN AND WAS TELLING 62 OF 71 DECKS THEY
WERE SHORT — L5, 2026-08-25, free. 316 FALSE SHORTFALL CLAIMS REMOVED; gates byte-identical
(34,418 / 43,500 / MESHED 287, panel 94.8% real 385 / false 21 debt 0).**
- **THE ITEM'S OWN WARNING, TURNED INWARD.** L5 cautioned against comparing raw counts to Karsten's
  mulligan-derived thresholds. **`ColorDemand.required` WAS `minCopies` — raw hypergeometric — and
  it was not being compared to anything, it was being SHOWN.** Raw, **{C}{C} by turn 2 "needs" 36
  sources of 99** and **{C}{C}{C} by turn 3 "needs" 44**. Same model that read 37 lands as an 80%
  three-land-drop deck before `mulligan.ts` corrected it to 90.3%.
- **HALF THE ITEM WAS ALREADY BUILT — check that before opening one.** `manaAudit` has priced
  supplied-against-demanded per colour per turn since it was written and reaches the reader through
  `deck-math.ts`. What was wrong was the MODEL behind the number, not the absence of one.
- **THE GENERALISATION IS ONE PARAMETER.** `pLandDrops` hardwires the draw count to `need`; replacing
  it with `seen(turn) - HAND` gives `pByTurn`, **byte-identical to `pLandDrops` on its own diagonal
  (difference 0)**, with `minSources(3,3) === landsForDrops(3) === 37`. Corrected: {C}{C} T2 36 ->
  22 · {C}{C}{C} T3 44 -> 37 · {C} T2 22 -> 16.
- **IT SHIPS AS AN INTERVAL BECAUSE THE KEEP BAND READS LANDS.** Applying it to one colour models a
  player who mulligans on black sources, which nobody does — the corrected end OVER-states the help
  exactly as raw UNDER-states it. Both reach the reader; `met` anchors on the CORRECTED end so the
  report under-claims a shortfall rather than over-claiming one.
- **MEASURED: rows with a shortfall 139/153 -> 68/153 · decks 62/71 -> 40/71 · unmet demands
  458/1,237 (37.0%) -> 142/1,237 (11.5%) · median shortfall 10 -> 5 sources.** It does NOT collapse
  to nothing — 40 decks keep a real one, worst still 24 short, which is the check worth naming.
- **A LIVE BROWSER FOUND TWO THINGS NO TEST WAS GOING TO, THE FIFTH TIME THIS FILE RECORDS IT.**
  (1) The overcommitted warning fired on a SINGLE row — mono-blue `draguns`, *"Together these rows
  want 37 sources from 36 lands, which no deck can hold"* — and **it compares SOURCES against LANDS**
  while `supplied` counts rocks and dorks. **It fired before this change too, at 44 against 36, where
  the absurdity read as rhetoric; correcting the number turned it into a plain false sentence.**
  Guarded on `> 1` row. (2) The caveat printed **"20, 20, 20, 20, 20"** on a five-colour deck whose
  rows share a raw figure. Deduped. Both re-verified live.
- **A VACUOUS GATE WAS CAUGHT BY MUTATING ALL THREE RATHER THAN A SAMPLE**: flipping `met` to the raw
  end left the suite GREEN, because every fixture sat below BOTH bounds. The missing case — a deck
  BETWEEN the two requirements — is the one the whole correction is about, and it is a test now.
- **KARSTEN'S PUBLISHED COLOURED-SOURCE TABLE IS STILL NOT THE CHECK**, and is named rather than
  half-done: it is not in this repo, hand-transcribing it is how `GRAVEYARD_HATE_SHARE` shipped
  wrong, and only now is the comparison commensurable in kind at all. Suite 2,467.

**THE LAND FORMULAS MATERIALLY DISAGREE — MEDIAN 4.6 LANDS APART ON THE SAME DECK — AND L1's OWN
REPLACEMENT METRIC IS `Flat37` WEARING A DERIVATION (2026-08-25). Free; gates byte-identical
(34,418 / 43,500 / MESHED 287).** `bin/land-formulas.ts --probability`.
- **THE ITEM'S "UPGRADED SHAPE" IS REFUSED ON ITS OWN ARGUMENT, and one grep settled it.** It
  proposed scoring formulas by MAE against `L*(deck) = landsForDrops(the deck's curve turn, 0.9)`.
  **`landsForDrops` TAKES NO DECK ARGUMENT** — `DECK = 99` and `HAND = 7` are module constants — so
  it varies only through `need`, in twelve-land steps: **3 drops at 90% = 37 lands · 4 = 49 · 5 =
  56**. Either every target is 37 and **`Flat37` scores a perfect zero**, or a target is 49 and no
  Commander deck can reach it. **"The deck's curve turn" occurs exactly once in the tree — in L1.**
  Pinned by a three-test ratchet on `mulligan.ts`, proven to fire.
- **A PROBABILITY IS A MONOTONE TRANSFORM OF THE LAND COUNT, so the probability scale PRICES the
  disagreement and cannot RANK.** Ranking by distance-in-probability is ranking by distance-in-lands
  squashed. What it buys is an ABSOLUTE reading with no fit in it — which is the target
  contamination L1 correctly refuses (the owner reads the guides being scored).
- **THE ABSOLUTE ANSWER, AND IT IS THE FIRST ONE THIS REPO HAS: the median deck makes its first
  three land drops 88.2%** (p25 87.1 · p75 89.3 · range 81.2-94.7) **and its first four 69.7%**. The
  convention is confirmed at **90.3% for 37 lands**, and the owner's decks at a median 35 sit ~2pp
  under it by choice.
- **THE CONVERSION RATE: at 36-38 lands ONE LAND IS WORTH 0.98pp of P(3 drops) and 2.02pp of P(4).**
  Every land-count argument in the genre can be priced with it.
- **MY OWN REGISTERED HYPOTHESIS WAS REFUTED IN THE INTERESTING DIRECTION.** I expected one
  tradition to collapse to a couple of points; the spread between six recommendations ON THE SAME
  DECK is **median 8.3pp / max 35.7pp**, or **median 4.5pp / max 12.9pp excluding RambIe** (20-74
  lands, an outlier the others do not share) — about **4.6 lands**, worst on
  `judith-tokens-connoiseur`. **At four drops it doubles: median 8.7pp, max 21.9pp.** §2's "twelve
  sources agree, n≈1" is about the SHAPE of the answer, not the number.
- **THE ORDER IS GENEROSITY, NOT QUALITY, AND THE LABEL SAYS SO** — more probability is not better,
  and a 60-land deck makes every drop and loses every game. Stable across keep bands `{2,3,4}` /
  `{2,3,4,5}` / `{3,4}`, which is L1's own prediction holding. Same describe-never-grade ruling
  `BracketPanel` and `LegalityPanel` ship.
- **STILL UNANSWERABLE: which formula is RIGHT.** Contaminated target, no discriminating information
  on either scale, no game outcomes anywhere in the repo. The question is PRICED, not closed.
  Suite 2,464.

**RAMP GETS AN EXPENSIVE COMMANDER DOWN TWO TURNS SOONER AND A CHEAP ONE BY EXACTLY NOTHING — L2
shipped 2026-08-25, free, and THE ITEM NAMED AN INSTRUMENT THAT CANNOT SEE ITS OWN QUESTION. Three of
four registered criteria FAILED; every gate byte-identical (34,418 / 43,500 / MESHED 287, panel 94.8%
real 385 / false 21 debt 0), which IS the acceptance test for a leaf bin.** `bin/commander-ramp.ts`.
- **`commanderCast` MOVED IN 0 OF 70 DECKS, EXACTLY 0.00pp, AND IT IS STRUCTURAL.** The item filed
  the counterfactual against `castability.commanders`, whose headline is `pAtLeast(mv, LANDS, ...)` —
  swapping a nonland ramp slot for another nonland changes no argument of that call. Its upper bound
  moves and sees **410 of 855 nonland ramp slots (48.0%)**, because **a land-fetch spell produces no
  mana of its own** and `isManaSource` correctly refuses it. **Samut's 14 nonland ramp slots are
  land-fetch 8 · dork 2 · rock 1 · unclassifiable 3** — three of fourteen visible, reading
  **34.1% -> 34.1%**. `goldfish.ts` models land-fetch and answers it: **turn 6 -> turn 8**, identical
  under both play policies. **CHECK WHICH INSTRUMENT CAN SEE THE QUESTION, not just whether the claim
  is true** — the premise held and the plan did not.
- **THE ANSWER IS A MONOTONE GRADIENT, and correlation could not reach it: median delay 0 · 1 · 1 · 2
  and mean 0.00 · 0.55 · 1.11 · 1.86** across commander-MV buckets <=3 / 4 / 5 / >=6, over 74 priced
  commanders at 20,000 trials. `r(commanderMV, ramp)` is 0.14 because an expensive commander sits in
  an expensive deck; the counterfactual has no collinearity to fight.
- **R1 FAILED AND ITS FAILURE IS THE OWNER'S OTHER HALF MEASURED.** It demanded a delay in a MAJORITY
  OF ALL DECKS (33 of 74) — but the mean delay at commander MV <= 3 is **exactly 0.00 turns**, which
  is *"a 1-2 mana commander needs none to cast it early"* confirmed. **The criterion was written
  wider than the claim it gates**, the third time this file records that shape (A3's criterion 2,
  1b's "real UNCHANGED").
- **R3/R4 FAILED AS REPORTING INSTRUCTIONS.** The two policies disagree about the turn in 14 of 74
  decks, so both arms print on every row; and **5 of 74 decks flip their median turn by one between
  seeds**, every one sitting where the median board mana is within a point of the commander's cost.
  **The bucket medians are BYTE-IDENTICAL across seeds**, so the gradient is quotable and a single
  deck's "+1" is not. More trials shrink a stepping median and cannot remove it.
- **TWO CENSORED ROWS WOULD HAVE PRINTED A FALSE `+0` ON THE TWO DECKS RAMP MATTERS MOST TO.**
  `the-capitoline-triad` and `colorless-spellslinger` run a mana-value-10 commander neither arm
  reaches in 10 turns, so differencing two `>10` medians yields zero; they print **61% -> 96%** and
  **64% -> 95%** of trials never getting there instead. They still enter R1/R2 at +0, under-stating,
  left alone rather than corrected after the numbers.
- **THE ITEM'S SECOND FINDING IS FALSE: "the Ramp floor of 10 discriminates for nobody" — 13 of 71
  decks sit under it.** The medians hold (13/13/13/15). The real criticism is that `BUILD_PARENTS`
  records Ramp p25-p75 as **10-17**, so the floor IS this corpus's p25 and flags the owner's own
  bottom quartile by construction.
- **NOTHING READS IT INTO A SCORE** — the `goldfish.ts` condition. Colour-blind, a ceiling under a
  mana-maximising policy, and **197 of the 897 `ramp` members stay live in both arms** because the
  model cannot classify them, so every delay is a lower bound. Four guards proven to fire by
  mutation. Suite 2,461.

**LEGALITY REACHES THE WEB, AND A LIVE BROWSER FOUND A FALSE POSITIVE ON THE TOOL'S OWN EXAMPLE DECK
(2026-08-25). Free; population 34,418 / 43,500 / MESHED 287 and panel 94.8% debt 0 byte-identical,
which is the acceptance test for a panel over data the report already carried.** `report.legality`
had been on `DeckReport` since J4 and rendered in the CLI only, so a paste into the UI — the surface
the whole feature was built for — got no legality report at all. `LegalityPanel.tsx`, LEADING the
Overview column, because every number below it is computed over a list the format would not let you
play.
- **THE DEFECT NO TEST COULD HAVE CAUGHT, and it is the fourth time this file records that sentence.**
  **A Moxfield export lists the commander in the DECKLIST as well as naming it**, so the same card
  arrives twice — and **the tool's own example deck did exactly that**. Counted as slots, two Krenko
  entries read as TWO COMMANDERS, and the pair then failed every licensing test because a card does
  not partner with itself: the panel flagged *"Krenko, Mob Boss · Krenko, Mob Boss"* as an illegal
  pairing. **Dedupe by NAME — one card is one commander.** The unit suite was green through it, and
  the calibration corpus cannot see it: `parseDecklistSections` puts the head block in `commanders`
  and NOT in `deck`, so no CLI run ever produces the shape.
- **THE EXAMPLE DECK WAS FIXED RATHER THAN THE FINDING SUPPRESSED.** Its `decklist` repeated the
  commander, making it 51 slots with a genuine duplicate nonbasic. The remaining finding — *"50
  cards, and a Commander deck is exactly 100"* — is TRUE of a truncated demo list and stays.
- **THE CAVEAT IS ON SCREEN AND IS ASSERTED BY TEST, not left in a comment**: a report and not a
  verdict, five rules of many, and an empty list means nothing was FOUND rather than that the deck is
  legal. A panel of complaints sitting one column from two real scores out of five reads as a grade
  unless the copy refuses to be one — the same ruling `BracketPanel` already ships.
- **CARD NAMES ARE CAPPED AT EIGHT, as the CLI caps them**: a colour-identity finding on a badly
  pasted deck can name dozens, and a list that long stops being read. The CLI's own "four rules of
  many" was stale after J12 and now reads five. Verified live on a deliberately illegal paste — all
  four rules fired and every other panel still rendered. Suite 2,452.

**WHICH TWO CARDS MAY LEAD A DECK TOGETHER — J12's partner half shipped 2026-08-25 as the FIFTH rule
of J4's legality report, free, and SECTION J IS CLOSED. Every gate byte-identical (34,418 / 43,500 /
MESHED 287, panel 94.8% real 385 / false 21 debt 0), which IS the acceptance test for a report that
forms no edge.**
- **THE CR-DERIVED LIST WAS SHORT BY THREE, AND COUNTING FIRST IS THE ONLY REASON THE RULE IS
  RIGHT.** The roadmap named five abilities off CR 702.124; the corpus prints **SIX forms —
  Partner 70 · Partner with <name> 54 · Partner—Friends forever 7 · Partner—Character select 6 ·
  Partner—Survivors 4 · Partner—Father & son 2** — plus Choose a Background 32 and Doctor's companion
  27 (17 legendary Doctors). **So the rule reads the LABEL after the em dash and pairs any two cards
  printing the same one.** A hard-coded list would have flagged three legal pairings, and a seventh
  group printed next year works without a code change.
- **A REPORT THAT CANNOT LICENSE A PAIR FLAGS IT, so a missing form is a FALSE POSITIVE ON A LEGAL
  DECK** — the exact failure the naive 903.3 reading already made once, five decks deep. That is why
  the six forms were enumerated before the regexes were written rather than after.
- **IT FIRES ON 0 OF THE 71 DECKS**, like J4's other four rules: all four two-commander decks are a
  "Choose a Background" creature opposite a Background, checked card by card. Built for the arbitrary
  pasted list. Verified live in the CLI on a deliberately illegal pair (Krenko + Haunted One).
- **J12's OTHER CLAIM IS FALSE AND THE CODE ALREADY DISAGREED WITH IT.** The item said *"a partner
  pair is two independent 21-damage clocks — which makes J2's refused threshold swap even more wrong
  for those decks"*. **`commanderDamage` ALREADY loops over every commander and emits one row each**;
  it has since J2 shipped. No two-commander deck in the corpus detects voltron, so a unit test is the
  only instrument that can see it — the position `SubjectFilter.named` has held since it shipped.
  **That is the SEVENTH roadmap premise corrected by measurement in one day.**
- **SIX BRANCHES PROVEN TO FIRE BY MUTATION**, one per licensing shape plus the three-commander rule.
- **NOT WIRED INTO THE WEB, and it is J4's gap rather than this one**: `report.legality` is rendered
  in the CLI only, so a paste into the UI still gets no legality report. Suite 2,451.

**A BACKGROUND'S ENTIRE PRINTED PURPOSE IS BUFFING THE OTHER COMMANDER, AND THREE OF THE FOUR IN THE
CORPUS FORMED NO EDGE AT ALL — J12's Background half shipped 2026-08-25, FREE, one condition.
Edges 34,414 -> 34,418 (+4), reasons 43,495 -> 43,500 (+5), MESHED 287 UNCHANGED, panel 94.8% real
385 / false 21 / uncertain 23 debt 0 BYTE-IDENTICAL, build 0 memberships, ratings 6 of 6,236 (raw
scores 9, partners 7, TOP card 0/71, facets 2).** `DERIVE_VERSION` 79 -> 80.
- **THE GATE WAS RIGHT AND ITS TEST WAS ONE FIELD TOO NARROW.** `effectSubject`'s grant branch reads
  the recipient off the CLAUSE TEXT (`grantRecipient`) and then demanded a **SUBTYPE**, because
  *"creatures you control gain haste"* reaches the whole deck and is the registered ordinary-card
  claim. **"Commander creatures you own" is ONE card, or two** — the opposite of a whole-deck claim —
  and `combatNarrowsOffType` already ships that exact reasoning for the same `commander` field. One
  clause: `s.subtype === undefined && s.commander !== true`.
- **THE WITNESSES GO 0 · 0 · 0 · 1 -> 0 · 1 · 1 · 2. Exactly 4 of the 71 decks run two commanders and
  ALL FOUR run a Background** — `burakos-crashing-the-party` (Haunted One), `draguns` (Feywild
  Visitor), `draguns2` (Candlekeep Sage), `sarevok-lord-of-pain` (Cultist of the Absolute).
- **THE ONE THAT ALREADY WORKED NEVER PASSED THROUGH THIS GATE, which is why the defect hid.** Cultist
  of the Absolute says *"Commander creatures you own GET +3/+3"* — a `modify-pt`, whose subject comes
  from the action OBJECT. The other three say *`have "<quoted ability>"`* — a `grant-ability`, whose
  subject comes from the clause text and hit the subtype test. **The family splits 19 `get` / 26
  `have "…"` over 45 corpus cards, 8 derived.**
- **THE FILED CUE WAS WRONG AND UNDERSTATED THE FAMILY FIVEFOLD.** The roadmap measured
  *"commanders you own/control get/have"* at **9 corpus / 0 derived**; these cards print **"Commander
  creatureS you own"**, and on the corrected cue it is **45 / 8**. The same slip in the sweep's own
  first probe read 0 buffing cards in the calibration set before it was corrected — a regex answering
  a question nobody asked, for the fourth time in this file.
- **ALL FIVE NEW CLAIMS WERE ENUMERATED AGAINST PRINTED TEXT, not sampled**: Feywild Visitor ->
  Renari · Candlekeep Sage -> Ganax · Cultist of the Absolute -> Sarevok · and **two Backgrounds
  sitting in the 99** of `nashi-sole-survivor` (Agent of the Shadow Thieves, Sword Coast Sailor),
  which are still real — the grant says "Commander creatures you own", and Nashi is one wherever the
  Background sits. Both had been rating **0.0** and now rate 0.8 and 0.7.
- **HAUNTED ONE IS STILL 0 AND IT IS A PAID-LAYER CEILING, NOT A DERIVE ONE.** Its stored clause 1 is
  a bare `{id: 1, abilityType: "static"}` with NO actions — the model dropped the grant entirely — so
  nothing exists for a subject to attach to. **5 of the 50 granted-clause parents are actionless**
  (Mesmeric Sliver, Enduring Vitality, Haunted One, Resonating Lute, Galazeth Prismari) and derive
  could synthesize the grant from the parent's SEGMENT text for free — but only **2 of the 5** would
  pass the subtype-or-commander gate, the other three being whole-deck claims that are correctly
  refused. **Two cards is the consumer count this file refuses on**, so it is recorded and not built.
- **"AN EXTRA KEYWORD ABILITY" WAS A FALSE SENTENCE and is now "an extra ability".** `grant-ability`
  covers a printed keyword AND a whole quoted ability, and the KIND cannot tell them apart — Feywild
  Visitor hands its commander a combat-damage TRIGGER. CR 702 makes a keyword an ability, so the
  wider word is true of both and nothing is lost on the cards the old phrase described correctly.
- **BOTH HALVES OF THE GATE PROVEN TO FIRE**: reverting the carve-out fails the commander test,
  dropping the subtype half fails the Anger test. Verified live in the CLI. Suite 2,444.

**J10 IS REFUSED ON THE CONSUMER COUNT AND ITS CENTRAL CLAIM IS INVERTED (2026-08-25). No code
beyond one stale figure corrected in passing.**
- **THE NUMBERS: the item says "51 corpus cards … against 15 printing `eminence`". Measured: 14 carry
  a zone CONDITION and 12 print Eminence** (102 mention the command zone at all). Its "exactly ONE of
  each is derived" half holds — only **Inalla** is normalized, and The Ur-Dragon is not.
- **DROPPING A CONDITION DOES NOT CREATE A REQUIREMENT — IT REMOVES ONE**, and that reverses the
  direction of the defect. The item reads *"Inalla derives its trigger with the zone condition
  DROPPED, so the engine thinks it needs Inalla on the battlefield"*. It derives `trigger: {enters,
  "another nontoken Wizard you control"}` with NO condition, so the engine claims the synergy
  UNCONDITIONALLY. The printed condition is a DISJUNCTION — *"in the command zone **or** on the
  battlefield"* — true of a commander in every state except graveyard or exile. **The unconditional
  reading is CLOSER to the truth than any naive recording would be**, and recording "needs Inalla on
  the battlefield" would make the cut-list deck-fit row fire wrongly.
- **THE SURFACE THE ITEM WANTS ALREADY EXISTS AND THE ITEM SAYS SO**: `deck-math.ts`'s
  `commandZoneCaveat` prints "part of this commander works from the command zone, so it is doing
  something before you can cast it", and its own comment calls that "the honest surface while the
  derive fix (J10) is unbuilt".
- **SO THE DERIVE FIX HAS NO CONSUMER**: 14 corpus cards, ONE normalized, and that one already
  derives the more-correct answer. The Ur-Dragon's cost-reduction shape — the case that WOULD form
  real edges with the zone fact missing — is **not normalized and not in any of the 71 decks**, so
  the risk is hypothetical. Same call as M4's subject fix and J9.
- **`deck-math.ts`'s "89 corpus cards" was stale and now reads 84**, re-measured. The
  1-of-75-commanders figure holds and is still Inalla.

**"AVAILABLE EVERY GAME" IS TRUE ONCE — the commander tax ships as a CAVEAT and not a number
(2026-08-25, roadmap J5). Free; every gate byte-identical (34,414 / MESHED 287, panel 94.8% debt 0,
build 0), which is the acceptance test for a sentence.**
- **WHAT THE READER WAS BEING TOLD.** The report says *"plus <commander> from the command zone, every
  game"*, and `availability.ts` and `deck-math.ts` both price a command-zone answer at **P = 1**. All
  true, and all of it invites being read as free and repeatable. The line now carries *"free the
  first time only — each recast from the command zone costs {2} more (CR 903.8), and nothing here
  models how often it dies"*.
- **A NUMBER IS REFUSED FOR A STRUCTURAL REASON, not for effort.** The tax follows how many times the
  commander has been CAST from the zone, which follows how many times it has DIED — and **nothing
  here simulates a game. I11's goldfish model casts no removal and has no opponent**, so the one
  simulator in the repo cannot answer it either.
- **SHIPPED AS DATA ON THE REPORT AND NOT WRITTEN INTO EACH RENDERER**, because **no subpath of
  `@mtg/matcher` is safe to value-import from client code** (the recorded 2026-08-21 regression) and
  a second copy of a sentence is how two surfaces start disagreeing. `format.ts` gains
  `COMMANDER_TAX_PER_CAST`, `commanderTax(castNumber)` and the caveat — the function because every
  future reader of 903.8 wants the TOTAL paid by the Nth cast rather than the increment.
- **`cr-section-sweep.ts`'s 903 row was stale within the hour and is corrected here** — it still said
  colour-identity legality was unmodelled, which J4 shipped. A coverage instrument nobody re-reads
  becomes a map of where the work used to be.

**J9 IS REFUSED ON MEASUREMENT AND ITS HEADLINE COUNT WAS WRONG BY SEVEN TIMES (2026-08-25). No
code, no spend.** The item read *"if you control a commander" — 203 CORPUS CARDS, 33 DERIVED, THE
BIGGEST FAMILY IN THE SWEEP*. **Measured: 29 corpus cards, 18 normalized.** The neighbouring counts
show where 203 came from — **448 cards mention a commander at all**, **39 say "control a/your
commander"**. It is a cycle and a half, not the biggest family.
- **THE FREE-CAST HALF IS ALREADY HANDLED BY THE ONE CONSUMER THAT COULD USE IT.**
  `castability.ts`'s REFUSALS list has carried "alternative cost — the card can be cast for free"
  since K5a, and its comment names **Deadly Rollick and Flawless Maneuver by hand**. The cycle is 5
  cards and each is already refused a castability figure rather than priced at a mana value it will
  not pay.
- **"ONE FIX SERVES BOTH [J9 AND I6]" IS REFUTED BY A GUARD THAT ALREADY EXISTS.** K5a's regex is
  `cast this (spell|card)(?! from exile) … without paying its mana cost` precisely to SEPARATE them:
  540 corpus cards match the bare phrase and **19 deserve it**, because 487 use it about ANOTHER card
  — I6's population — and 34 are rebound, where the first cast costs its printed mana.
- **WHAT DEADLY ROLLICK LOSES IS MOSTLY BY DESIGN.** The item says "free removal reads as ordinary
  removal"; it actually derives ONE ability with an empty effect kind and its `exile target creature`
  derives NOTHING — because the vocabulary is PAYOFF-CENTRIC and has no removal kind and no
  counterspell kind, which `effect-kind.ts` states outright. **1,235 of 5,467 derived abilities (23%)
  carry an empty effect kind**, so the family is not special.
- **THE CONDITION ITSELF BUYS NOTHING, AND THAT IS THE REFUSAL.** `conditionCares` is a CLOSED MAP
  feeding tag re-weighting, archetype membership and the deck-fit cut-list row. **Every EDH deck has
  a commander**, so the deck-fit row can never fire, no commander archetype exists, and re-weighting
  a demand every deck meets is the registered "a claim that applies to a card merely for being an
  ordinary card is false" one rung over.
- The **Will cycle** loses only "you may choose BOTH" — a MAGNITUDE fact, refused three times — and
  **Loyal Apprentice's Lieutenant line** is the standing intervening-if refusal from 2026-08-15.
- **FIVE ROADMAP PREMISES HAVE NOW BEEN CORRECTED BY MEASUREMENT IN ONE DAY** (I4's missing amount,
  I1's unsplittable regex, M3's "the gate refused", M4's "refused" that was INVERTED, and J9's 203).
  **Check the premise before opening an item** — this file already said so twice, and the rate today
  is one in three.

**DECK LEGALITY SHIPS AS A REPORT AND NEVER A GATE — and the naive reading of CR 903.3 flagged FIVE
of the 71 decks, all five FALSE (2026-08-25, roadmap J4). Free; every gate byte-identical (34,414 /
43,495 / MESHED 287, panel 94.8% debt 0, build 0), which is the acceptance test for something that
reads inputs already present and refuses nothing.** `matcher/src/legality.ts` -> `report.legality`,
four rules: 903.5a size · 903.5b duplicate nonbasic · 903.5c/d colour identity · 903.3 who may lead.
- **A REFUSAL IS THE WRONG FAILURE DIRECTION HERE**, the `cutCandidates` ruling one layer over: a
  partial paste is a normal thing to hand this tool, and refusing to analyse it is a worse product.
- **THE FIVE FALSE POSITIVES ARE WHY THE CARVE-OUTS EXIST, and they were measured BEFORE shipping.**
  Four are **Backgrounds** — Haunted One, Feywild Visitor, Candlekeep Sage, Cultist of the Absolute,
  all `Legendary Enchantment — Background`, a legal second commander — and the fifth is **Will
  Kenrith**, a Legendary PLANESWALKER whose own text says *"Will Kenrith can be your commander"*.
  **31 corpus Backgrounds, 49 cards printing that line.** It UNDER-REPORTS on purpose.
- **A BACKGROUND IS ACCEPTED WITHOUT CHECKING ITS PARTNER prints "Choose a Background"** — the
  pairing is J12's question, and flagging a legal Background because this function cannot see its
  partner is the same false positive one step later.
- **THE COLOUR CHECK IS SKIPPED WHEN NO COMMANDER WAS IDENTIFIED**, never run against an empty
  identity: otherwise every coloured card is flagged and the report is about the PARSER, not the deck.
- **IT FIRES ON NOTHING IN THE CALIBRATION CORPUS**: all 71 decks are exactly 100 cards, carry no
  duplicate nonbasic and stay inside their identity. Built for the arbitrary pasted list, like I9's
  `bfz` row. Verified on a deliberately broken paste — 34 cards, Sol Ring twice, Brainstorm in
  mono-red — all three reported, analysis still ran.
- **A VACUOUS TEST WAS CAUGHT BY MUTATION.** The basic-land exemption could not fire because the
  fixture gave every filler copy a DISTINCT name, so no basic was ever a duplicate; with 94 copies of
  one `Mountain` the mutation fails three tests. **Three of four carve-outs fired first time — mutate
  all of them, not a sample.**

**THIRTY-ONE DECKLIST LINES IN THE CALIBRATION CORPUS WERE RESOLVING TO THE WRONG CARD, AND IT WAS
NOT A LATENT RISK — I3 shipped 2026-08-25, free.** *"Rampant Growth"* resolved to **Studious
First-Year // Rampant Growth**, a Bear Wizard; *"Reanimate"* to **Grave Researcher // Reanimate**;
*"Regrowth"*, *"Exsanguinate"* and *"Seething Song"* likewise to a prepare card's front face. Which
doc won was decided by `findOne` order. `toCardDoc` now indexes the whole name and the FRONT face
only, `ingestFlavorNames` stops writing an empty key, and `bin/migrate-search-names.ts` brings the
persisted docs in line.
- **COLLIDING KEYS 79 -> 54, THE DEFECT ITSELF 27 -> 3.** The three survivors are SPLIT cards whose
  FRONT face equals a whole card's name — Bind, Smelt, Armed — which front-face-only cannot fix and
  which is Magic's own naming. The rest of the 54 are same-named Un-set variants and punctuation
  twins, a different class.
- **A MIGRATION AND NOT A RE-INGEST, because `searchNames` HAS TWO WRITERS**: `toCardDoc` builds it
  and `ingestFlavorNames` `$addToSet`s flavor names onto it AFTERWARDS. A re-ingest rebuilds the
  first and DROPS the second. 864 docs changed, 867 back-face keys and 4 empty keys removed.
- **EVERY PART OF THE POPULATION MOVE IS A CORRECTION**: edges 34,554 -> 34,414 (-140), reasons
  43,657 -> 43,495 (-162), MESHED 288 -> 287, build **27 memberships** (Rampant Growth replacing
  Blazing Firesinger // Seething Song in `ramp`).
- **PANEL real 391 -> 385, AND ALL SIX ARE CLAIMS ABOUT CARDS THE DECKS DO NOT CONTAIN** — read one
  by one. `Grave Researcher // Reanimate -> Archaeomancer` is a CREATURE'S ETB in a deck holding the
  SORCERY Reanimate. **Six cached verdicts had been judged REAL against a fiction.** Precision
  94.9% -> 94.8%, false 21 unchanged, dropped verdicts 685 -> 691.
- **IT CORRECTS A DIAGNOSIS THIS FILE ALREADY CARRIED.** The 2026-08-22 batch-API note says *"Rampant
  Growth, Reanimate and Exsanguinate are each also listed under a Universes Beyond flavor name that
  `searchNames` merged onto the same card"* — **the same three names**, and the mechanism was at
  least incomplete: they were not merged onto the same card, they resolved to a DIFFERENT one.
- **A METHOD ERROR WORTH KEEPING: THE FIRST CHECK RAN THE WRONG ARM.** A probe asking "does any line
  resolve to a different NAME" answered 0 — because it ran AFTER the migration, so both states were
  post-fix, while the population had already moved by 140 edges. **Isolating it needed BOTH states**:
  restoring the back-face keys reproduced the old population byte-for-byte, and diffing resolved
  `_id`s named all 31 lines. **Comparing a state against itself proves nothing**, and this is the
  third time that shape is recorded here.

**"NO SubjectFilter CAN NAME AN ABILITY" IS TWO GAPS AT TWO LAYERS, NOT ONE — M5 verified and
RECORDED where the ceiling lives (2026-08-25). No code, no spend, no gate touched, and section M is
CLOSED.** The item asked for exactly this and was right to: it is the ceiling behind "Gogo forms no
edge to The Millennium Calendar", so it belongs in `specs/2026-08-14-resource-ledger-design.md` §10
rather than in a superfriends item.
- **CONFIRMED TO THE CARD: The Chain Veil rates 0.2 with ONE partner in `ups-all-chandras`**, a
  21-planeswalker deck, and that partner is `enters:artifact`. Only its end-step downside derives.
- **THE FAMILY IS 16 CORPUS CARDS, SPLIT 14 / 2, and the halves are blocked at different layers —
  which is why a subject slot alone would close only the larger one.**
- **ABILITY COPIERS, 14 cards / 7 derived actions** (Gogo, Lithoform Engine, Strionic Resonator, The
  Peregrine Dynamo, Abstruse Archaic): they DO derive, `copy-spell` with
  `{control: "you", scope: "target"}`. They cannot say the target is an ABILITY, so the subject is
  UNTYPED — and **measured over the 71 decks it claims NOTHING rather than everything**: Gogo has 4
  partners and Lithoform Engine 13, and **not one reason on either is a `copy-spell`**. A SUBJECT-slot
  gap failing in the honest direction, which is worth knowing before anyone "fixes" it.
- **EXTRA-ACTIVATION GRANTERS, 2 cards, blocked ONE LAYER EARLIER ON THE VERB.** The Chain Veil
  derives `{verb: "other", object: "<the whole sentence>"}` — no verb covers granting an extra
  activation, so the action never reaches a subject at all. Verrak, Warped Sengir is the other.
- **SECTION M, END TO END**: M1/M2 shipped (superfriends label + the planeswalker theme key), M3
  shipped for $0.15 (`activate` is a trigger event), M4's free half shipped (the inverted
  counter-removed trigger) with its remainder refused on the consumer count, M5 recorded.

**`activate` IS A TRIGGER EVENT NOW — $0.15, 32 cards re-asked, 27 persisted / 5 refused / 0 failed
(2026-08-25, roadmap M3). THE FIRST SPEND SINCE THE 08-22 BATCH, and the treadmill it was warned
about is now MEASURED rather than warned about: it bought FOUR cards.** `NORMALIZE_VERSION` 13 -> 14,
`TRIGGER_VOCAB_VERSION` 13 -> 14, `NORMALIZE_MIN_COMPATIBLE` untouched.
- **THE ITEM'S PREMISE WAS WRONG IN THE USEFUL DIRECTION, and checking before spending is what
  caught it.** The roadmap said "the gate refused rather than guessing, which is correct". **The gate
  refused NOTHING**: all four members answered `{event: "other", subject: "you activate a loyalty
  ability of a Chandra planeswalker"}` with `unknownTriggers: []`. The model took the ESCAPE HATCH
  and put the whole sentence in the SUBJECT — **the fourth time this session** the pipeline has left
  a fact in a string beside the event name (`counter-removed`, `taps-for-mana`, `loses-the-game`,
  `damage-dealt`).
- **SIZED AGAINST THE GAME per the completeness ruling: 36 corpus cards print an activation trigger
  and only 6 are loyalty-specific** — Burning-Tree Shaman, Harsh Mentor, Runic Armasaur,
  Illusionist's Bracers, Verrak, Ertha Jo. The loyalty reading is 17% of the family.
- **IT WORKED AND THE SUBJECT NARROWED TOO**: all four came back `{event: "activate", subject: "a
  loyalty ability of a Chandra planeswalker"}` — the escape hatch is gone and the subject is the
  thing rather than the sentence.
- **IT MAPS TO NO ENGINE VERB, deliberately**, joining `search` / `becomes-target` / `unlocked`:
  nothing EMITS an activation, so it forms no edges and lands in `unknownTriggers`. **Four cards now
  refuse VISIBLY where they were silently empty**, which is what a future demand gets built against.
- **THE TREADMILL, PRICED: of the 32 selected, 4 improved, 23 came back IDENTICAL and 5 were
  refused** — clause triggers on `other` fell 30 -> 26. That is what a TRIGGERS-only bump costs, and
  `TRIGGER_VOCAB_VERSION`'s own comment predicts it without a number.
- **ONE REFUSAL INDEPENDENTLY CONFIRMS M4**: *"Regenerations Restored: unknown-trigger-event —
  `remove-counter` is not in TRIGGERS"*, the same gap M4 recorded an hour earlier, surfaced by the
  gate on a different card.
- **MEASURED: edges 34,545 -> 34,554 (+9), reasons 43,634 -> 43,657 (+23), MESHED 288 UNCHANGED,
  panel 94.9% debt 0 UNCHANGED, build 0, ratings 27 of 6,236.** The +9 is NOT the activation family
  — those four form no edges either way — it is the other 23 re-asked cards moving slightly at v14.
- **`TAGGER_PROVIDER` WAS UNSET IN `packages/tagger/.env`, so every bin that spends silently fell
  back to Ollama** — the exact trap this file already recorded. The dry run said `provider: ollama`
  until it was set on the command. **The line was added to the .env on 2026-08-26**, after the trap
  had fired three times; a silent fallback produces a corpus answered by the wrong model.
- **A PIPELINE FINDING: `unknownTriggers` IS COMPUTED AND THEN DISCARDED AT THE PERSISTENCE
  BOUNDARY.** `deriveCardTags` returns `{oracleId, schemaVersion, promptVersion, model,
  characteristics, abilities}` and drops it, so a refusal is invisible to anything reading
  `cardTagsDerived`; the audit bins recompute it via `deriveAbilities`. Verifying M3 against the
  stored doc read `[]` and looked like a failed fix.

**A TRIGGER THAT FIRES WHEN COUNTERS LEAVE WAS DERIVED AS ONE THAT FIRES WHEN THEY ARRIVE — the
roadmap called it refused and it was INVERTED, which is the opposite failure direction (2026-08-25,
M4's free half). Edges 34,549 -> 34,545 (-4), reasons 43,638 -> 43,634 (-4), MESHED 288, panel 94.9%
debt 0, build 0, ratings 1 of 6,236.** `DERIVE_VERSION` 78 -> 79, free.
- **THE CLAUSE ALREADY CARRIED THE FACT, IN ITS SUBJECT.** Chandra, Fire Artisan normalizes to
  `{event: "counter-added", subject: "loyalty counters removed from Chandra"}`: the model reached for
  the nearest legal event and left the truth in the subject. **That is the fourth time this pipeline
  has put a DIRECTION in a string beside the event name** — `taps-for-mana`, `loses-the-game` and
  `damage-dealt` are the same shape, and all four are read free at derive time.
- **REFUSED RATHER THAN REINTERPRETED**: `counter-removed` is not in `TRIGGERS`, and adding it is a
  vocabulary bump that re-selects the whole `other`-trigger population and SPENDS. A visible refusal
  beats a banked near-miss.
- **READ ON THE SUBJECT AND NOT ON THE CARD TEXT, deliberately** — a planeswalker names BOTH
  directions in different sentences ("counters are removed" in the trigger, "put a loyalty counter"
  in a loyalty ability), so a card-scoped test would have refused the ADDING half too. That is the
  `triggerHasCue` clause-vs-card-scope lesson one guard over.
- All four removed claims are `ups-all-chandras`, and **Fire Artisan keeps her five REAL relations**
  (cast, cost-reduction) — checked pair by pair. One clause corpus-wide has this shape; **9 corpus
  cards print a trigger on counters being removed**, so this is the one the normalizer answered.
- **THE CHANNEL NOW READS EXACTLY AS M4's TITLE SAYS: SIX SUPPLY EMITS, ZERO DEMAND.** The three rows
  a first probe counted as demand are the same cards' own `effect.subject` — supply counted from the
  other field. **COUNT THE CONSUMERS BEFORE BELIEVING A DEMAND FIGURE, including your own probe's.**
- **THE SUBJECT FIX IS REFUSED ON THE CONSUMER COUNT.** All SIX emits are untyped, not just Chandra,
  Acolyte of Flame's as the item filed — every one reads `{control: "any", counter: "loyalty"}`,
  dropping "each red planeswalker you control", "target commander that entered this turn", and the
  SELF recipient on Teferi, Kaito and Lolth. **With zero consumers they form zero edges, so fixing
  them buys nothing today** — and four of the six are self-referential, the untyped-wildcard shape
  that would matter the moment a demand exists. Reopen when M3 gives the channel a consumer.

**THE GOLDFISH MODEL REACHES A READER, AND FINDING OUT WHICH QUANTITY THE POLICY MOVES CHANGED THE
DESIGN MID-BUILD (2026-08-25, I11's report wiring). It feeds NO SCORE — ratings 0 of 6,236, raw
scores 0, partner counts 0, population/panel/MESHED/build byte-identical — which is the condition the
whole model shipped under.**
- **THE FIRST CUT PRINTED "15% - 15%" ON EVERY ROW.** A policy interval per turn is empty: **the two
  policies agree on every median and disagree only in the TAIL.** So the rows carry a median with
  p25/p75 beside it (C7), and the INTERVAL is reported on **P(6 mana by turn 6) — the cell the
  whole-item falsifier was measured on**. On `samut.txt` it reads **55% - 62%**. Reporting the range
  where it collapses would have hidden the sensitivity exactly where nobody looks.
- **THE TWO READOUTS OF THE SAME CELL ARE RECONCILED RATHER THAN LEFT TO THE READER.** The commander
  line already said its figure "reads low"; **the engine now knows by how much**, so it names the
  better number: *"34% – 43% … (the simulation below models that ramp and reads 55% – 62% for the same
  cell)"*. **ONLY WHEN IT IS LITERALLY THE SAME CELL** — the commander row's `turn` IS its mana value
  and the headline is fixed at six-on-six, so they coincide only for a six-mana commander and `gisa`
  correctly gets no pointer. Same trap as the land row reading "33 in deck" above "lands 37/36".
- **A DEGENERATE RANGE COLLAPSES TO ONE FIGURE**, never "100% – 100%" — the rule `castability.ts`
  already ships. **Found in a live browser on the example deck, not in any test.**
- **2,000 TRIALS, CHOSEN AGAINST THE WIDTH OF THE THING IT SITS INSIDE** — within ~0.5pp of the
  20,000-trial answer, inside a policy interval whose median width is 7.0pp, at ~48ms per analyze.
  A number whose noise rivals its own stated width is misleading; one an order of magnitude under it
  is not. That is the defensible way to pick a Monte Carlo sample size.
- **THE CAVEATS ARE ON SCREEN, NOT IN A TOOLTIP**: the range is named as the PLAY POLICY, the high end
  is called a ceiling no real deck plays to, and the colour blindness is stated outright. **C10 reaches
  the FIELD NAMES too**, asserted against the serialized report — a field name is exactly where a
  banned word creeps back in.
- **A MEASUREMENT GAP IN `12d5bcb` IS CORRECTED HERE: the `theme-stats.json` regeneration that
  shipped with M1/M2 moves 266 RATINGS and 4,682 raw scores** (partner counts 0, TOP card 0/71). That
  commit measured the artifact's THEME effect — 2 headlines — and not its ratings effect, and idf
  feeds `buildAxis`. The refresh was correct and necessary; its blast radius was understated.
  **MEASURE THE GATES IN THE SAME COMMIT THAT MOVES THEM**, for the second time in this file.

**ONE EFFECT KIND CARRIED 2,704 FALSE PAIRS, AND THE TWO ARCHETYPE TABLES DISAGREED ABOUT IT —
J2 and J3 shipped 2026-08-25, both free. Gates byte-identical throughout (34,549 / 43,638 / MESHED
288, panel 94.9% debt 0, build 0), as they must be: `CATEGORY_MATCH` is read only by
`groupEdgesByArchetype`, and the commander-damage row forms no edge.**
- **J3: `voltron-auras` LABELLED 27 OF 71 DECKS -> 4, and 2,780 pairs -> 76.** The roadmap recorded
  "~18 of 71"; **re-measured before touching anything it read 27 — it had GROWN** since I9 and C7
  moved the reason population, which is why the week plan's stale-line audit flagged it.
- **`pump` WAS THE WHOLE INFLATION: 2,704 of the 2,780 pairs.** `mechanisms.ts`'s own header says the
  archetype signatures "EXCLUDE broad shared kinds (damage, draw-card, pump)"; `ARCHETYPE_SIGNATURE`
  does exclude it and `CATEGORY_MATCH` kept it. **THE TWO TABLES DISAGREED AND THE ONE THAT KEPT
  `pump` INFLATED.** The DEFINING half was always right — an Equipment or Aura entering IS the
  signal; what was wrong is that ONE Equipment makes the category survive and `pump` then swept every
  anthem in the deck into it.
- **DROPPING `counter-placement` TOO WAS BUILT FIRST AND COSTS FOUR GOLD PAIRS — the compass ratchet
  said so rather than any argument.** All That Glitters, Ethereal Armor, Ancestral Mask and Sage's
  Reverie all reach Setessan Champion on `enters:enchantment` + `counter-placement`, the
  enchantress-voltron relation the category is for. **The tag is `enters:enchantment`, not
  `enters:aura`, because a reason keys on the CONSUMER's demand.**
- **G3's SECOND LESSON CONFIRMED AGAIN: a wrong category silently DELETES the right one beside it.**
  `dedupeNearIdentical` drops a group whose pairs sit inside a bigger one's, so **`fight-club` and
  `voltron-mill` get `tokens-go-wide` back** (61 -> 63 decks). Measure the group changes, never just
  the label count.
- **J2: 21 COMMANDER DAMAGE SHIPS, AND NOT AS A THRESHOLD SWAP.** The item's refutation stands and
  shaped the design: swapping `STARTING_LIFE` 40 -> 21 would move the clock ~2 turns earlier and the
  number would be a LIE, because `pressureCurve` is TOTAL BOARD POWER while commander damage comes
  from ONE creature. **A RANGE and never a point** — bare (the commander alone) to kitted (carrying
  everything the deck can attach, a wild ceiling).
- **THE TWO TABLES AGREE, WHICH IS THE ONLY CROSS-CHECK AVAILABLE HERE: the four decks that get a
  commander-damage row are EXACTLY the four J3's fix leaves in `voltron-auras`** —
  `who-is-the-reddest-of-them-all` (power 2, **11 bare -> 1 kitted**, 29 attachables) ·
  `nashi-sole-survivor` (2, 11 -> 1, 35) · `voltron-mill` (The Mindskinner, power 10, **3 -> 1**, 20)
  · `obeka-upkeep-shenanigans` (2, 11 -> 3, 9). Three of them are also the only three of the old nine
  that carried real archetype confidence.
- **THE ROW IS GATED ON THE DECK'S OWN DETECTED ARCHETYPE, not on a new threshold**, so the module
  cannot disagree with `detectArchetypes` about what the deck is. A 1-power commander in a
  spellslinger deck needs twenty-one connections — true, useless, noise.
- **A RATE IS NOT AN AMOUNT, and it bit here exactly as it bit I4 one subsystem over**: Ethereal
  Armor's "gets +1/+1 FOR EACH enchantment you control" contains the literal `+1/+1` and is not a
  `+1`. An unreadable bonus contributes ZERO; a `*` power yields no row at all.

**THE MANA MODEL SHIPPED AND ITS OWN FALSIFIER FIRED — mana availability is a POLICY property at
this deck's scale, so the point readout is WITHDRAWN and an INTERVAL ships (2026-08-25, roadmap I11).
That is the registered recordable outcome, not a failure.** `matcher/src/goldfish.ts` +
`bin/goldfish-report.ts`, free. **Nothing imports the model into a score** — the condition the K7/J7
reconciliation holds under. Every standing gate is byte-identical and LABELLED STRUCTURALLY VACUOUS:
a leaf module nothing imports cannot move them.
- **C8 TRIGGERS AT 84% OF THE EFFECT THE MODULE EXISTS TO MEASURE.** Over the 71 decks at 20k trials,
  holding up two mana before casting an accelerant moves the headline cell (P(≥6 mana at turn 6)) by
  **up to 27.6pp against a MEDIAN RAMP SIGNAL of 32.7pp**, where the demotion rule fires at 50%. The
  bin prints a PAIR of policies and never one number: the greedy arm is a CEILING, the hold-up-2 arm
  is nearer how the owner's decks are actually played, and the truth is between them. Interval width
  p25 4.1pp · median 7.0pp · p75 10.2pp. Same resolution `castability.ts` already reached.
- **THE HEADLINE DEFECT IS CONFIRMED, BY A WIDER MARGIN THAN THE SPEC MEASURED.** On `samut.txt` the
  shipped `castability.ts` pair reads **[34.1%, 43.5%] against a simulated 62.2%** at turn 6, and
  `castability.ts:126` still promises "the truth is between them". **THE MECHANISM REPRODUCES
  EXACTLY**: the bin finds **11 accelerants on that deck and only 3 are ones `manaWithRocks` can
  see** (Birds of Paradise, Enduring Vitality, Springleaf Parade) — the other 8 are land-FETCH
  spells, which produce no mana of their own and which `isManaSource` correctly refuses.
- **CRITERION R FIRES ON THE SPEC'S OWN TABLES, WHICH IS THE PROCESS DEFECT IT WAS WRITTEN TO CATCH,
  CATCHING ITSELF.** §3/§5 recorded a median ramp value of **+8.7pp** and "higher in 61 of 71"; the
  shipped bin measures **+32.7pp and 71 of 71**, accelerants per deck **min 3 · median 9 · max 17**
  against the recorded median 13, and Samut **62.2%** against 55.8%. Those probes were session
  scratchpad. **The bin is the recheckable source now — re-measure before quoting the spec's tables.**
- **C4's NEGATIVE ARM FAILS AND IS RECORDED RATHER THAN RELAXED**: `nashi-sole-survivor`, the one deck
  at the corpus minimum of 3 accelerants, moves **11.7pp** against a registered <5pp. A synthetic
  3-rock deck misses it the same way (**+9.4pp**), measured and left in the test file. Both point
  where C8 points: at this scale the number is policy-and-variance dominated.
- **EVERY MECHANICAL ANCHOR PASSES**, which is what makes the verdict a POLICY verdict rather than a
  broken simulator: **A1** the capped off-diagonal — the closed form on the diagonal within 1.5pp and
  **exactly 0 above it**, which is the half `pAtLeast` gets wrong (it answers 55.5% for four mana on
  turn three, off three land drops) · **A2** rule 4's rules-correct timing (a rock pays the turn it
  lands per CR 302.6, a dork waits) · **A3** the all-tapped deck as the same curve shifted a turn ·
  **C2** the land-drop cap checked from OUTSIDE by A1 · **C3** the tapped predicate firing both ways
  THROUGH the simulator · **C6** determinism with `Math.random` banned by test · **C9** one
  classifier, imported from I9 · **C10** no output naming "castable" while the model is colour-blind,
  asserted against the result's own field names.
- **THE POLICY IS THE MODEL, and every output is a CEILING UNDER A MANA-MAXIMISING POLICY.** Rule 3
  casts every accelerant it can afford; the owner's own decks hold mana up for interaction.

**A LAND THAT NEVER TURNS ON READ EXACTLY LIKE A LAND WITH NO CONDITION — I9's remainder shipped
2026-08-25, free, and EVERY GATE IS BYTE-IDENTICAL (34,549 / 43,638 / MESHED 288, panel 94.9% debt 0,
build 0 memberships, ratings 0 of 6,236), which IS the acceptance test here for a reason worth
stating: the deck-level rows form no edge by construction and the G family measures ZERO cards in the
71 decks.**
- **THE PAIRWISE PASS STATES THE POSITIVE AND CAN SAY NOTHING WHEN THE ANSWER IS ZERO.** "Rootbound
  Crag enters untapped because you run Steam Vents" is an edge; a Crag with no supplier forms no edge,
  shows no row, and the SILENCE is indistinguishable from a land with no condition printed on it.
  **The zero-fan warning had NO SURFACE AT ALL — it was a probe finding, never rendered**, so
  `draguns`' Mistrise Village (a BLUE land wanting Mountain or Forest, in an island deck, tapped every
  game forever) was known to the instrument and never to a reader. `report.landConditions`, rendered
  in the CLI and in the web Overview.
- **A REASON, NEVER A GATE** — the card still taps for mana and still rates, the same discipline the
  unmet `conditionCares` row already keeps.
- **TEMPLATE C RIDES THE SAME SURFACE and stays inert on this corpus exactly as recorded: 24 of 71
  decks run a `bfz` land and ZERO fail.** It names no member (every basic contributes equally), so
  the row is the deck-level count. Built for the pasted dual-heavy list, not for these.
- **THE G FAMILY IS READ NOW, ON BOTH HALVES**: `basicTypeDemand` reads "as long as you control a
  <basic type>" off ANY card, taking the same SUBTYPE edge `check`/`verge` take plus the same unmet
  row. **22 corpus cards, ZERO of them lands, ZERO in the 71 decks** — so it forms no edge and fires
  no warning here, and its unit test is the only instrument that can see it (the standing
  `SubjectFilter.named` has, at 13 cards).
- **A LAND'S OWN CONDITION IS ASKED FIRST**, so a card that is both is never reported twice. Three
  gates proven to fire: counting any land instead of BASICS for `bfz`, reading no G demand, and
  dropping that order.
- Verified in a live browser on `draguns` and in the CLI. Suite 2,413.

**A 21-PLANESWALKER DECK READ AS SPELLSLINGER AND AN 18-WALKER CONTROL DECK READ AS TOKENS —
superfriends exists now, and the theme stops naming a character (2026-08-25, roadmap M1/M2,
owner-reported). FREE, both. Population/panel/MESHED byte-identical, `build-population.ts` 0
memberships; ratings 29 of 6,236 (mean |Δ| 0.0005), raw scores 143, partner counts 0, TOP card
0/71.**
- **M1: SUPERFRIENDS IS A CARD TYPE COUNT AND NOTHING ELSE.** Every other `ARCHETYPE_SIGNATURE` row
  keys on a MECHANISM and this archetype has none — what makes a deck superfriends is that a third of
  it is planeswalkers. `ArchetypeSignature.cardTypes` + `CardSignal.cardTypes` + one row.
- **NO NEW THRESHOLD WAS NEEDED, AND THE DISTRIBUTION IS WHY: planeswalkers per deck across the 71
  are 0 on FIFTY, 1 on fourteen, 2 on five — and then 18 and 21.** The most any non-superfriends deck
  runs is 2 (3.4% of its nonlands) against 28.1% and 32.8%, so every threshold between them gives the
  identical answer and the EXISTING `ARCHETYPE_FLOOR` (0.08) separates them by a factor of eight.
  The A11 tripwire is satisfied with the widest margin any archetype here has, and nothing was
  invented to make it pass.
- **ONE SCORE MOVES AND IT IS THE MISCOUNT LEAVING**: `mono-blue-plainswalker-control` buildScore
  **5.000 -> 4.815**, because `tokens` carries `boardWipe: -2` and an 18-planeswalker CONTROL deck
  was being told it needed ONE board wipe (2/1, a perfect 5) rather than three. **NO
  `ARCHETYPE_TARGET_DELTAS` ROW FOR SUPERFRIENDS, deliberately** — real doctrine says the archetype
  wants MORE wipes, and that is the uncalibrated claim `BASE_TARGETS` warns about.
- **M2: A PLANESWALKER'S SUBTYPE IS A CHARACTER NAME AND THE DECK'S IDENTITY IS THE CARD TYPE.**
  A9's fan-out emits one key per subtype and the card-type key only when there is NO subtype — right
  for a creature (a Wizard deck wants `enters:wizard`, not `enters:creature`) and wrong for a walker.
  **MEASURED: `mono-blue-plainswalker-control` runs EIGHTEEN walkers and split them `enters:jace` 7 /
  `enters:teferi` 3** — a 7-card theme named over an 18-card deck. **Exactly ONE headline moves**:
  "jaces entering" (0.11) -> "planeswalkers entering" (0.28).
- **ADDITIVE, NEVER A REPLACEMENT** — planeswalker subtypes are real typal identities, so
  `enters:chandra` survives and a payoff naming one still finds it. **PLANESWALKER-ONLY, and the
  restraint IS the design**: pushing every card's type would give every creature `enters:creature`,
  the universal-bucket failure three theme designs died on — proven by mutation, since doing so fails
  two OTHER standing tests.
- **`ups-all-chandras` DELIBERATELY DOES NOT MOVE**: all 21 of its walkers ARE Chandras, so
  `enters:chandra` 20 beats `enters:planeswalker` 21 on tf-idf and "chandras entering" is the more
  specific TRUE name.
- **`theme-stats.json` REGENERATED IN THE SAME CHANGE** (the three-times-recorded staleness failure:
  an absent tag scores the MAXIMUM idf). It also cleared accumulated drift, **559 -> 540 tags**, from
  the `DERIVE_VERSION` moves since it was last written — **DECOMPOSED rather than folded in**: the
  refresh alone moves 2 headlines and M2 on top of it moves the one above.
- **"LOST a subtype-level primary: 1" IS THE POINT HERE, NOT A REGRESSION.** The criterion A9 and A11
  registered fires on `enters:jace -> enters:planeswalker`, which is the fix.
- **NOT THE "GRAVEYARD MATTERS" GROUPING the owner also saw.** That surface is `CATEGORY_MATCH`
  (`groupEdgesByArchetype`), a DIFFERENT table, and it is still unfixed. M1's own line said to decide
  which surface the complaint is about first, and it was right to.

**WHICH TABLE THIS DECK IS FOR — WotC's Commander Brackets ship, and EVERY GATE IS BYTE-IDENTICAL,
which IS the acceptance test (2026-08-25, roadmap L3, threshold 6 on the owner's call). Edges 34,549
/ reasons 43,638 / MESHED 288, panel 94.9% debt 0, build 0 memberships, ratings 0 of 6,236.** A JOIN
over two facts the engine already had, so it forms no edge, reads no rating and moves no score.
`matcher/src/brackets.ts` -> `report.bracket`, rendered in the CLI and in the web Overview.
- **DEFINITIONAL, NOT EMPIRICAL, and that is why this was the one item of twelve community sources
  worth code**: brackets are a published RULE about what a deck may CONTAIN, so there is nothing to
  fit, tune or refute. "Refutation" does not apply to a lookup.
- **THE PREMISE CHECK RAN FIRST AND BOTH DOUBTED FACTS WERE ALREADY IN THE DATA.** **INFINITE vs
  FINITE is the combo's `result` TEXT** — `ComboDoc` is `{cards, result}` with no flag — and
  **103,343 of 106,605 corpus combos (97%) say "Infinite"**, the rest genuinely finite ("Lock,
  Prevent all damage that would be dealt to you"). Of the 29 calibration decks holding a combo, **27
  hold an infinite one**; `codie` and `orzhov-spellslinger` hold combos with none, which is exactly
  the distinction bracket 1-2 turns on. **PIECE COST is a join**: `cards.length` is the count and the
  corpus carries `manaValue`.
- **`gameChanger` WAS THE THIRD FIELD FOUND ON `CardDoc` AND DROPPED BY `docToCard`**, after
  `producedMana` and `allParts`. It had been recorded as "stored, unused" since the card-graph work —
  and it was unused BECAUSE it never reached `Card`. **Check that join before recording a field as
  unused.** It reads **53**, matching the published list exactly.
- **THRESHOLD 6 CHOSEN FROM THE DISTRIBUTION, NOT FROM TASTE**: over the 71 decks the bands read
  33/28/10 at <=4, **33/26/12 at BOTH <=5 and <=6**, 33/20/18 at <=7, 33/19/19 at <=8. Five and six
  agreeing exactly is a plateau and six is its upper edge — the most generous reading of "cheap" that
  reclassifies nothing. Shipped bands **33 / 26 / 12**. What a sweep can establish here is
  INSENSITIVITY across the range a reader would argue about, and it does.
- **THREE BANDS AND NOT FIVE, and the missing precision is stated in the product**: 1 vs 2 is about
  how the deck was BUILT and 4 vs 5 is a META judgement, neither a checkable list.
- **IT DESCRIBES AND NEVER GRADES.** The panel sits one column from two real scores out of five, so
  "Bracket 4" reads as 4/5 unless the copy says what the number is about. A 4-5 deck is not a worse
  deck than a 1-2 deck; it is a deck for a different table.
- **A MISSING MANA VALUE PRICES A COMBO AT ZERO, which reads it as CHEAPER and keeps the deck in the
  HIGHER band** — a resolution failure must not flatter a deck into a lower bracket than its contents
  allow.
- Verified live in a browser (example deck, Bracket 3, "2 infinite combos") and in the CLI across all
  three bands. Six tests, four gates proven to fire; suite 2,407.

**FOUR DETECTOR DEFECTS, ONE GATE, AND TWO OF THE FOUR PREMISES DID NOT SURVIVE BEING MEASURED
(2026-08-25, roadmap I1/I2/I4/I5 — the "I-config batch"). Population, panel and MESHED BYTE-IDENTICAL
(34,549 / 43,638 / 288, panel 94.9% debt 0), which is the acceptance test: not one of these four
forms an edge.** Ratings 16 of 6,236 (mean |Δ| 0.0007) with **raw scores 0 and partner counts 0** —
the movers are all the `doubleDuty` 1.15x premium flipping for a card that lost its only ROLE.
Suite 2,400.
- **I5, IMPULSE DRAW IS NOT CARD SELECTION — the pattern said so outright.** `selection`'s third
  alternative was literally the impulse template, so Light Up the Stage and Jeska's Will were filed
  as scry. **cardSelection 92 -> 78, impulseDraw 0 -> 14, nothing else moved.** Selection REORDERS
  what you will draw; impulse draw ADDS cards you may cast, usually only this turn — nearer draw, and
  with a DEADLINE draw does not have. **BOTH LEAVES SIT INSIDE CONSISTENCY, so the Consistency union
  moved in 0 of 71 decks and `buildScore` is untouched**: a labelling fix, measured as one.
- **I2, A SENTENCE ABOUT THIS SPELL'S OWN COST CAN NEVER BE A TAX** (stax 30 -> 28). Fixed at the
  SOURCE in `costDirection`, and **the two cards leak through the CLAUSE-TEXT FALLBACK differently**,
  which is why it is one guard and not a strive rule: Call the Coppercoats' object carries no
  direction word so the fallback reads `more` off "This spell costs {1}{W} more", while Blasphemous
  Edict's ALTERNATIVE cost has a clause saying "each opponent" and fired the opponent cue on a
  sentence about neither cost nor opponents' costs. A self REDUCTION survives, and so does the
  fallback's original job (Sapphire Medallion names no "this spell"). `DERIVE_VERSION` 77 -> 78.
- **I4's FILED PREMISE WAS FALSE, and this is the third roadmap line in a week to die on one.** It
  read "the engine has both halves already (`manaValue` and the emit's amount)". **A
  `mana-generation` ability carries NO amount and NO emit at all — Dark Ritual and Manamorphose
  derive BYTE-IDENTICALLY**, the same shape the trigger-doubling family was found by. The CLAUSE
  holds it (`action.object` = `"{B}{B}{B}"`, **537 add-mana actions corpus-wide**), so the net test
  reads ORACLE TEXT through a named `oneShotNetsMana` op and **deriving `Ability.amount` for add-mana
  is the root-cause fix and its own item** — `sentence.ts` renders that field into reason TEXT, and
  `lines.ts` and `supply-demand.ts` read it too. **Ramp 293 -> 291** (Manamorphose, Rapturous
  Moment); **attainment moves in exactly ONE deck**, `codie` 0.90 -> 0.80, the other four being over
  target. Three guards, each found by a wrong cut and each proven to fire: **one-shots only** (a dork
  nets 0 the turn it lands and is still ramp), **a PERMANENT FACE is not a one-shot however the other
  half is typed** (Bramble Familiar // Fetch Quest's `{T}: Add {G}` is a dork and the type-line union
  says "Sorcery" — the `isLand` mistake one layout over), and **a RATE is not an AMOUNT** ("Add {R}
  for each card in target opponent's hand" is the most explosive ritual in the format).
- **I1 IS HALF SHIPPED AND THE OTHER HALF IS REFUSED AS CONFIG, on its own measurement: the family
  splits 79 GRANTED · 20 SELF-KEYWORD · 41 UNSPLITTABLE BY ANY REGEX.** "This spell can't be
  countered" and "phases out" are not keywords at all, and **Delighted Halfling grants with no cue**
  ("Legendary creature spells you cast can't be countered" — neither "gains" nor "has"). Shipped only
  what the printed keyword list settles: **protection 140 -> 113 (-27)**, Sylvan Caryatid and Stonecoil
  Serpent and both Emrakuls leaving Interaction. 27 rather than 20 because **"protection from Humans"
  IS the printed keyword `protection`**. **The Interaction union moves in 22 decks and ATTAINMENT in
  exactly ONE** (`mishra-claimed-by-tokens` 10 -> 9) — every other deck was over target on removal
  alone, which is what made shipping the narrow slice safe. The residue needs the derive-side read of
  the grant's SUBJECT the item itself named as "the honest version", and `effect-kind.ts` gives those
  keywords NO kind today on purpose.
- **A GRANT ALWAYS WINS, CHECKED FIRST**, because a card can print a keyword AND hand it out, and
  handing it out is the Interaction fact.

**AN ABILITY DISCOUNT IS ABOUT THE ABILITY'S COST AND NEVER THE CARD'S — CR 118.7 on the activated
side, and reading the wrong string was wrong in BOTH directions (2026-08-25, roadmap 1c).
PANEL 94.7% -> 94.9% [92.3, 96.6], FALSE 22 -> 21, real 390 -> 391, uncertain 23, debt 0. Edges
34,568 -> 34,549 (-19), reasons 43,684 -> 43,638 (-46), MESHED 288 UNCHANGED, build 0 memberships.**
Matcher-only, FREE, no re-derive. Same rule B3 ships for CAST costs, one layer over.
- **EXACTLY ONE CLAIM LEFT THE PANEL AND ONE ARRIVED, which is the two directions in one measurement.**
  **Thought Vessel costs `{2}` and its only ability is `{T}: Add {C}`** — the card looks discountable
  and the ability is not (owner-judged FALSE 2026-08-23, OVERTURNING the 2026-08-20 cached REAL:
  "the consumer has activated abilities" is necessary and not sufficient). **Executioner's Capsule
  costs `{B}` and its ability costs `{1}{B}, {T}, Sacrifice this artifact`** — the SPELL-side guard
  was refusing a claim that is real, and its cached REAL was sitting on a dropped claim.
- **TWO GATES, DECOMPOSED, because they are different facts.** (1) CR 118.7: no generic mana in any
  activated cost, no reduction — **-17 edges / -33 reasons**, and it is what cuts Thought Vessel.
  (2) **THE PRINTED FLOOR, which BOTH derived reducers carry** ("can't reduce the mana in that cost
  to less than one mana"): an ability costing one mana cannot be reduced at all, so a Signet
  (`{1}, {T}: Add …`) is not discounted however much generic it prints — **a further -2 edges / -13
  reasons**, panel identical in both arms. **Gated on the producer's own printed cue and NOT on
  "costs less to activate"**, because a reducer without a floor really would take that ability to
  zero. Read as "some ability SURVIVES the floor", which is what keeps Executioner's Capsule (two
  mana, reduced to one, not LESS than one).
- **COUNTED BEFORE IT WAS BUILT, and the count is the reason the population barely moves.** 90 corpus
  cards print the ability-reducer cue against **2 in the derived corpus** (Forensic Gadgeteer,
  Training Grounds); of 792 derived cards carrying an activated ability, **488 have no generic mana
  in ANY of them** — every land, every `{T}: Add` rock, Sol Ring included — and **56 more are floored
  out**. Only the artifacts and creatures those two reducers actually reach formed a claim, so the
  effect is 19 edges and not 500.
- Ratings 37 of 6,236 (mean |Δ| 0.0009), raw scores 52, partner counts 19, TOP card 0/71, top-10
  0/71, facets 0. Suite 2,397. **Three mutations proven to fire**: dropping either gate, and reading
  `c.card.manaCost` in place of the ability costs.
- **CEILING, unchanged from B3: the tax interaction is not modellable pairwise.** A third card that
  taxes the ability adds generic and the reduction bites; every claim here is about two cards.

**A QUOTED ABILITY GRANTED TO A TOKEN IS THE TOKEN'S, AND THE CARD WAS CLAIMING IT TOO — the round-4
census's biggest false family, closed FREE (2026-08-25, roadmap 1b). PANEL 92.9% -> 94.7%
[92.0, 96.4], FALSE 30 -> 22, real 392 -> 390, uncertain 24 -> 23, debt 0. Edges 35,792 -> 34,568
(-1,224), reasons 45,044 -> 43,684 (-1,360), MESHED 288 UNCHANGED.** `segment.ts`'s
`grantedToOwnToken` selects a `granted` clause whose PARENT creates a token; `deriveAbilities` drops
the whole clause. `DERIVE_VERSION` 76 -> 77, 2,761 re-derived, no spend.
- **THE WHOLE CLAUSE GOES, NOT JUST ITS TRIGGER.** What is quoted on a created token is a COMPLETE
  ability — Vivi's Persistence's Wizard both watches the cast and deals the damage — so keeping the
  effect and dropping the trigger would leave the card claiming to do a thing it never does.
- **94 GRANTED CLAUSES OVER 92 CARDS, AND THE CUE SELECTS 23 WITH ZERO FALSE POSITIVES against the
  other 69**, read one by one rather than sampled. The 69 are five shapes that all KEEP their
  ability: a grant to a CLASS you control (Bello, Enduring Vitality, Galazeth), an EQUIPMENT or AURA
  granting to its host, a COPY-SELF grant ("except it has that ability"), a grant to a class an
  OPPONENT controls, and an EMBLEM. The cue reads the PARENT and never the granted text, because
  "token" routinely appears inside the granted ability itself — Kaito, Cunning Infiltrator's emblem
  creates a Ninja and its parent merely says "You get an emblem with that ability".
- **THE PRE-REGISTERED "real UNCHANGED" CRITERION FAILED AND IS RECORDED RATHER THAN RELAXED.**
  It was written wider than the rule it gates — the A3 failure again. It assumed the 8 banked falses
  were the family's whole panel footprint; **three more claims of the IDENTICAL shape carried older
  verdicts**: Chandra's Ignition -> Vivi's Persistence and Meathook Massacre II -> Cornered by Black
  Mages, both USER verdicts REAL from ROUND 2 (2026-08-07), and Circle of Power -> Kuja, owner-judged
  PARTIAL in round 3. All three predate tokens-as-nodes (2026-08-16), and the owner's own round-3
  note says why: *"Kuja creates token that trigger on casting non creature spells, that is why I
  wanted at some point to include the tokens on the graph."* **Round 4 is the newest owner ruling on
  this family and it governs; rounds 2 and 3 are superseded on it.**
- **THE RELATION IS RELOCATED, NOT LOST, AND THAT IS CHECKED RATHER THAN ARGUED.** The Wizard token's
  own derived row carries `triggered / cast`, the Bird's (Summon: Fat Chocobo) carries `enters` — and
  a live CLI run of `kuja-spellslinger` still reads **"Vivi's Persistence — synergizes with 68
  cards"**, the reasons now naming the actor correctly: *"When Artist's Talent is cast, **Wizard**
  deals 1 damage"*. **THE PANEL CANNOT SEE A TOKEN-MEDIATED CLAIM BY CONSTRUCTION** — its pairs name
  two real cards, which `panel-score.ts:41` already states — so a `real` fall here is the instrument's
  blind spot and not lost recall. Third time in this file that checking beat believing a recall loss.
- **`build-population.ts` MOVED EXACTLY ONE MEMBERSHIP AND IT IS THE FIX CONFIRMING ITSELF: Kibo,
  Uktabi Prince leaves `tribal-tribal`'s `ramp`.** Its printed mana ability is `"{T}, Sacrifice this
  token: Add {R} or {G}"` — on the BANANA, not on Kibo, who produces no mana. Same shape as the
  E-family's `wincon:go-wide` 18 -> 17.
- **AN EMBLEM IS THE SAME WRONG SENTENCE ONE OBJECT OVER AND IS LEFT ALONE ON PURPOSE** (13 of the
  94): an emblem has no node for the relation to move to, so refusing it would DELETE a claim rather
  than relocate one, and no panel claim witnesses the defect.
- Ratings 225 of 6,236 (mean |Δ| 0.0053), raw scores 589, partner counts 16, TOP card 0/71, top-10
  3/71, facets 17. Suite 2,396 passing, 0 failed. Both directions pinned by tests PROVEN TO FIRE.
- **A POPULATION FIGURE QUOTED FROM A DOCUMENT WAS WRONG AGAIN**: the week plan's baseline read
  35,803 / 45,067 off the roadmap's I9 lines; `population-compare.ts` measures **35,792 / 45,044**.
  The plan flagged its own figure as unre-measured, which is the only reason it did not propagate.

