## State as of 2026-08-28

**A FACE THAT DOES NOT PRINT THE TOKEN DOES NOT MAKE IT — the last open faces-as-nodes item closed
2026-08-28, free, matcher-only. Edges 35,442 -> 35,436 (-6), reasons 45,031 -> 45,025 (-6), MESHED
289 UNCHANGED, panel BYTE-IDENTICAL (95.1% real 387 / false 20 / debt 0), build 0 of 33 memberships,
ratings 6 of 6,239 (mean |Δ| 0.0001, raw 10, partners 10, TOP card 0/71). Suite 2,705.**
- **`allParts` IS A CARD-SCOPED SCRYFALL FACT and `faceDeckCards` spreads it onto every face**, so
  both faces of a transforming maker claimed everything the card can make. Two consequences, and the
  one that MOVED is the over-claim: **6 false `creates:` rows, every one `Reflection of Kiki-Jiki
  -[creates]-> Goblin Shaman`** — the token Fable of the Mirror-Breaker's CHAPTER I makes, in six
  decks. Each was the only reason on its pair, so 6 edges left with them, which is the attribution
  confirming itself.
- **THE FACE'S OWN PRINTED TEXT IS THE FACT**: a `create` sentence naming the token (`facesCreating`).
  **The SENTENCE test is load-bearing** — a payoff face routinely says the token's name
  ("Sacrifice a Treasure:") and saying it is not making it.
- **A CARD-SCOPED FALLBACK WHEN NO FACE NAMES IT, and it is the conservative direction.** Scryfall's
  typeless "Copy" row is never named in printed text, and a face whose text was never stored names
  nothing either; attributing to NO face would make the token read PARTNERED off its own maker,
  which is the over-claim the partner scan exists to avoid. **17 multi-face token makers in the 71
  decks: 13 create from exactly one face, 3 from both (Fable, Indulge // Excess, Ajani), 1 from the
  back alone (Mirrorhall Mimic).**
- **`tokenCreators` IS FACE-KEYED AGAIN, and the 2026-08-27 physical-name fix is SUPERSEDED rather
  than reverted.** That fix was right while attribution was card-scoped — a real per-face answer is
  what makes the face name correct now, so the partner scan compares endpoints directly instead of
  translating both sides to the physical card.
- **THE PARTNER HALF HAS NO WITNESS IN THESE 71 DECKS AND SAYS SO: unpartnered tokens 231 of 580,
  UNCHANGED.** The one candidate was the Fable pair, and removing the false `creates:` edge removed
  the only edge that could have counted — so the unit test is this half's only instrument, the same
  position `SubjectFilter.named` has held since it shipped. Three mutations fire (attribution,
  fallback, the partner-scan translation).

**A CARD-WIDE STATIC RELATES TO A PERMANENT ONCE — the MESHED double-count filed on 2026-08-27
CLOSED 2026-08-28, free, matcher-only, no re-derive. MESHED 332 -> 289, reasons 45,246 -> 45,031
(-215), edges 35,607 -> 35,442 (-165), PANEL BYTE-IDENTICAL (95.1% [92.5, 96.8], real 387 / false 20
/ uncertain 22, debt 0), build 0 of 33 memberships, ratings 116 of 6,239 (mean |Δ| 0.0065, raw 167,
partners 159, TOP card 0/71, top-10 2/71, 4 facet moves). Suite 2,699.**
- **A PERMANENT SHOWS ONE FACE AT A TIME (CR 712.3a), so faces-as-nodes was pairing a wide anthem
  with each printed face and counting one claim twice.** Both rows stamp back to the same physical
  name and **NEITHER IS FALSE** — each is true of the face it names — so this is a COLLAPSE, not a
  refusal. `staticClaim` in `directedReasons` is the old loop body extracted as a function; the loop
  then asks it again for the consumer's EARLIER faces and skips a claim one of them already makes.
- **MEASURED BEFORE BUILDING: 217 duplicate rows across the 71 decks — `static:cost-reduction` 154 ·
  `static:pump` 60 · `static:type-grant` 3** — and the population fell by 215, the two-row difference
  being rows that were duplicates of each other.
- **THE FIRST SATISFYING FACE KEEPS IT, NEVER FACE 0 UNCONDITIONALLY.** A modal DFC with a Sorcery
  front and a Creature back is reached only by its BACK face, so anchoring on the front would DELETE
  the claim rather than collapse it. Both halves proven to fire by mutation, one at a time.
- **MESHED LANDS AT 289 AND THE +2 OVER THE PRE-FACES 287 IS ATTRIBUTED RATHER THAN ROUNDED AWAY.**
  Group rows now Omo 75 · Serah 55 · Maskwood 53 · Jodah 53 · Serah 53 = 289, fan-out unchanged
  (73/55/51/53/53). **Omo, Queen of Vesuva prints TWO type-grant statics — one for lands, one for
  nonland creatures — and a creature//land MDFC satisfies one with each face** (Glasspool Mimic //
  Glasspool Shore, Hydroelectric Specimen // Hydroelectric Laboratory). Two DIFFERENT abilities, so
  the collapse correctly keeps both; pre-faces they produced BYTE-IDENTICAL sentences about the
  combined card and `dedupeReasons` merged them, which is where the missing 2 went. Maskwood's
  pre-existing triple-count (3 rows, one consumer) is untouched, as before.
- **EDGES FALL BY MORE THAN A DEDUPE WOULD SUGGEST BECAUSE A FACE IS A NODE**: when a back face's
  ONLY claim was the duplicate, that face-pair edge goes entirely, and the claim lives on the front
  face's edge. **THE VISIBLE COST IS A BACK-FACE ROW RATING 0.0** in `report.cards` (Optimus Prime,
  Autobot Leader 0.8 -> 0 and 7 like it) — the physical card keeps its rating on its front row.
  Stated rather than hidden: it is the price of counting one claim once.
- **THE PRODUCER SIDE IS LEFT ALONE, ON A MEASUREMENT.** Two producer faces each printing their own
  static are two distinct printed abilities. The residue is **75 rows, ALL `static:cost-reduction`
  and ALL one card** (Serah Farron // Crystallized Serah, a reducer on each face), and
  cost-reduction is exempt from the mesh census, so it moves no gate.
- **THE WRONG NOUN IT SURFACED IS FIXED IN THE SAME PASS (free, sentence-only).**
  `staticGrantSentence` rendered every `type-grant` as *"an extra creature type"*, so Omo's LAND
  grant read *"Omo gives Glasspool Shore an extra creature type"*. `typeGrantNoun` reads the
  SUBJECT when it names one of the two card types this family reaches, else the CONSUMER'S OWN type
  line — **6 of the 13 derived type-grants carry no type at all** (Glasspool Mimic, Copy Land, Minas
  Morgul) and Eluge's derives a bare `{subtype: island}`. **UNDECIDED MEANS NO NOUN**: a card that
  is BOTH (Dryad Arbor) reads *"an extra type"*, which is true of every member. Verified live on
  `mono-blue-tribal-tribal`: *"Omo gives Glasspool Shore an extra LAND type"* beside *"Omo gives
  Glasspool Mimic an extra CREATURE type"*, both faces of one card, each correct. **Population,
  panel and ratings ALL BYTE-IDENTICAL (35,442 / 45,031 / MESHED 289, 95.1% debt 0, ratings 0 of
  6,239)** — `claimCount` keys on (tag, text), so a text change that merged or split a row would
  have moved the scores, and none did. Suite 2,702. Two mutations fire (subject read, consumer
  read).

**AN ENTER-AS-A-COPY REPLACEMENT IS A REASON TO BE BLINKED, AND THE CARD ASKED FOR NOTHING — §26.3's
"flicker + enters-as-copy" closed 2026-08-28, free, matcher-only. Edges 35,454 -> 35,607 (+153),
reasons 45,055 -> 45,246 (+191), MESHED 332 and PANEL BYTE-IDENTICAL (real 386 / false 20 /
uncertain 22, debt 1 — no new panel claim), build 0 of 33 memberships, ratings 150 of 6,239
(mean |Δ| 0.0120). Suite 2,697.**
- **Sakashima the Impostor derives exactly `{kind: static, effect: {kind: clone}}` — no subject, no
  emit** — so a flicker that makes it re-enter (and re-choose what it copies) had nothing to satisfy.
  `Sakashima <-> Waterbender's Restoration` 0 reasons -> `enters:creature`.
- **MATCHED ON THE PRINTED CUE, NOT THE DERIVED KIND, which is the C4 ruling for this same family.**
  31 derived cards print it and arrive as `static/clone` (27) and `static/copy-spell` (4)
  indifferently; **0 of the 31 carried a self `enters` trigger.** 66 corpus cards, 17 of 71 decks,
  548 candidate pairs, max fan-out 16 against the mesh cap of 50.
- **ALL 66 WERE READ, AND 2 ARE A FALSE POSITIVE THE NAIVE CUE TAKES: Essence of the Wild ("CREATURES
  YOU CONTROL enter as a copy of this creature") and Infinite Reflection.** Both apply the
  replacement to OTHER permanents, so a self demand is a claim the card does not make. Anchoring on
  the printed self template ("you may have … enter as a copy", 62 of 66) refuses both. **It
  under-claims on two and says so** — The Mimeoplasm's "if you do, IT enters as a copy" sits in a
  second sentence, and a cue loose enough to read a bare pronoun would catch every TOKEN in the
  corpus.
- **THE FIRST VERSION WAS WIDER THAN THE CHANNEL IT WAS JOINING, AND ONLY MEASURING SHOWED IT.** With
  an untyped trigger subject, **Reality Shift got in 21 times** — its emit is a bare
  `{enters, control: any}` because its target MANIFESTS an unknown top card, and **a face-down
  manifested permanent is a 2/2 with no abilities (CR 708.2), so no copy replacement applies.**
  **VERIFIED AGAINST THE COMMITTED TREE RATHER THAN ARGUED: `Reality Shift -> Solemn Simulacrum`
  forms 0 reasons today while `Dance of Many -> Solemn Simulacrum` forms 4** — so the untyped demand
  was accepting an emit the existing self-ETB channel already refuses. Carrying the card's own
  printed TYPE closes it (224 pairs -> 192). **TYPE ONLY, NEVER SUBTYPE**: a blink emits "a creature
  you control", which cannot promise a Shapeshifter, so a subtype demand would refuse the whole
  flicker class this exists for.
- **THE ATTRIBUTION CONFIRMS ITSELF: TOP card moved in exactly one deck —
  `hidetsugu-and-kairi-like-to-multiply` — and top-10 in `can-i-copy-your-homework`,
  `cult-of-clones`, `everything-is-a-land` and `big-mana-big-plays`. Every mover is a clone deck.**
- **THE 40 PRODUCERS WERE READ AND ONE IS QUESTIONABLE, PRE-EXISTING, AND CHECKED RATHER THAN
  ASSUMED.** Ghostly Flicker 17 · Ghost Vacuum 15 · Identity Thief 14 · Wormfang Turtle/Newt 14 each
  · Animate Dead / Reanimate / Breach the Multiverse 9 each, then the blinks. **`Dance of Many`
  emits `{type: creature, token: false}` for a clause that CREATES A TOKEN copy** — a derivation
  mis-typing that already reaches ordinary self-ETB creatures on master, so this adds clones to a
  channel that carried it rather than introducing it.
- **A FALSE SENTENCE AGAIN, AND AGAIN CAUGHT BY READING THE OUTPUT.** The generic self grammar says
  "it triggers", and **a replacement effect never triggers (CR 614.1c)** — what makes a clone worth
  blinking is that the copy choice is made again against the board as it now is.
  `enterAsCopySentence` says that. **Two sentence fixes in two consecutive changes: read the prose a
  new edge family generates, every time.**
- **FACE-SAFE FOR FREE**: `faceDeckCards` gives every face its own `oracleText`, so Glasspool Shore
  (the LAND back of Glasspool Mimic) is not handed a clone demand. Four mutations proven to fire.
→ `docs/measurements/2026-08-28-enter-as-a-copy/README.md`

**THE PANEL IS DEBT 0 AGAIN AND THE OWNER'S READING OF ONE CARD OPENED TWO GAPS (2026-08-28).**
The proliferate demand's single new panel claim — `Arcbound Slith -> Surge Conductor` — is
**owner-judged REAL**: *"so true, but slith puts counters on itself with modular and dealing damage,
and on death modular allows you to put counters it had to other artifact creature."*
**PANEL 95.1% [92.5, 96.8], real 386 -> 387, false 20 UNCHANGED, uncertain 22, DEBT 0** — quote it
unbounded again. `panel-build.ts` folded it (1,210 -> 1,211 verdicts, pairs.json byte-identical),
which is the C11 rebuild-is-safe path rather than the retired merge-by-hand workaround.
- **THE VERDICT WAS RIGHT AND THE NOTE WAS A DEFECT REPORT, exactly as the 2026-08-20 finding said a
  judged note can be.** Slith derives ONE ability: trigger `combat-damage` with `self: true`
  (correct), emitting `counter-added {control: "any", counter: "+1/+1"}` — **the SELF is dropped**,
  when the card says "put a +1/+1 counter ON IT".
- **BOTH "GAPS" I FILED OFF THAT NOTE WERE WRONG, AND THE CORRECTIONS ARE THE FINDING (same day).**
- **GAP 2 — "MODULAR DERIVES NOTHING" IS FALSE. `modular` HAS BEEN IN `KEYWORD_EMITS` ALL ALONG**
  (`modular: [{verb: "counter-added", counter: "+1/+1"}]`), and it works: **Arcbound Worker, whose
  entire printed text is Modular 1, emits `counter-added {control: "you", counter: "+1/+1",
  implied: true}` and forms real edges** — to Fathom Mage (*"When Arcbound Worker gets a counter,
  Fathom Mage draws you 1 card"*) and to Surge Conductor through the proliferate demand shipped an
  hour earlier. **MY PROBE READ `cardTagsDerived.abilities[].emits`, WHICH IS STORED DERIVED DATA,
  AND `keywordEvents` IS A MATCHER FACT DELIBERATELY NOT MERGED THERE** — its own doc comment says
  so. Sixth entry in this file's "a probe answered a question nobody asked" family, and the first
  where the probe was wrong about a feature that already shipped. **CHECK `implied.ts` BEFORE
  RECORDING A KEYWORD AS UNREAD.**
- **GAP 1's COUNT IS RIGHT AND ITS FRAMING IS WRONG. 1,517 corpus cards print "put a counter on
  it/this"; 105 are derived and 84 carry no `self` on any `counter-added` emit** (Wall of Limbs,
  Dragonspark Reactor, Primal Amulet, Mindsplice Apparatus, Heartfire Hero, Alesha). **But that is
  not an unnoticed defect — `counterAddMatches` takes a PERMISSIVE path for an untyped counter
  producer on purpose**, and its own comment states the reason: *"an add-counter's subject is parsed
  from its object — '+1/+1' — which describes the COUNTER, never the permanent receiving it"*.
  **Stamping the recipient would push all 84 onto the strict `subjectMatches` path, which the same
  comment records as having already broken The Great Henge once.** So the honest status is a KNOWN,
  REASONED design point rather than open work, and I filed it as a defect without reading the
  matcher that consumes it.
- **THE VERDICT AND THE CLAIM ARE UNAFFECTED.** Slith's combat-damage emit supplies a counter, and
  modular supplies a second one, so the proliferate edge stands twice over. **A card can be right for
  a reason its derivation does not hold — and a reader can be wrong about which layer holds it.**

**A PROLIFERATE HAD A SUPPLY AND NO DEMAND — Track 3's §26.3 "proliferate -> poison counters" miss
closed 2026-08-28, FREE, matcher-only, no re-derive. Edges 34,758 -> 35,454 (+696), reasons
44,104 -> 45,055 (+951), MESHED 332 UNCHANGED, panel real 386 / false 20 / uncertain 22 ALL
UNCHANGED with DEBT 1, build 0 of 33 memberships, ratings 175 of 6,239 (mean |Δ| 0.0105). Suite
2,690.**
- **`impliedCounterEvents` has made a proliferate SUPPLY an untyped `counter-added` since it
  shipped; nothing made it ASK for one.** So Radstorm ("Proliferate.") and Virulent Silencer ("that
  player gets two poison counters") were two PRODUCERS with nothing between them. Proliferate gives
  each chosen permanent another counter of each kind ALREADY THERE (CR 701.29), so a board with no
  counter makes the card do nothing — that is a demand for a counter SOURCE.
  `proliferateAbilities` is the same shape `keywordAbilities` gives a printed keyword: a synthetic
  consumer ability `directedReasons` already knows how to read.
- **MEASURED BEFORE BUILDING, and every guard came from a number.** 24 derived cards emit
  `proliferate` (102 corpus), in 14 of 71 decks, 766 (proliferate x counter-source) pairs — the
  registered UPPER bound, and +696 edges landed under it because some pairs already had an edge.
  **Max fan-out per producer is 16 (`venser`) against the mesh cap of 50, so MESHED holding at 332
  IS the acceptance test.**
- **A PROLIFERATE CANNOT BE THE ORIGIN OF THE COUNTER IT MULTIPLIES**, or two proliferate cards edge
  to each other over a counter neither made. **Excluded at the EVENT level, and a card-level rule
  was measured and refused: 6 of the 24 ALSO author a real `counter-added`** (Sword of Truth and
  Justice, Yawgmoth, Lulu, Tidus, Tromell, Patrolling Peacemaker) and a one-line card test would
  have deleted all six genuine origins.
- **IMPLIED MINUS AUTHORED, not implied alone.** `producerEvents` dedupes identical events, so if a
  card authors a `counter-added` byte-identical to its own implied one, an implied-only exclusion
  deletes a printed origin. **0 of 24 collide today** — the guard is kept anyway and its own test
  says it has no printed witness, because the shape is plainly printable and the failure direction
  is a DELETED true claim.
- **A MUTATION DID NOT FIRE AND THAT IS THE FINDING.** The first "both proliferates and authors"
  test used Sword of Truth and Justice's shape — an authored counter carrying `+1/+1`, which never
  collides with an untyped implied one — so it **passed in BOTH arms and measured nothing**.
  Replaced with the actual collision fixture. Four mutations now fire, one at a time.
- **A FALSE SENTENCE WAS CAUGHT BY READING THE OUTPUT, NOT BY A TEST.** The first working version
  rendered the witness as *"When Virulent Silencer gets a counter, Radstorm triggers"* — the Silencer
  does not GET a counter, it puts poison counters on a PLAYER, and Radstorm is a SORCERY, which never
  triggers. **`subjectNoun` cannot rescue it**: a `counter-added` emit is routinely untyped, so
  `producerCanBeSubject` cannot refuse a producer that really could carry a counter — the residual
  that gate's own comment records. Same shape as the Austere Command fix (the edge right, the prose
  naming the wrong object). `proliferateSentence` says what happens, and **the reason population did
  not move when the text changed** (45,055 both sides), so no `(tag, text)` row merged or split.
- **DEBT 1, LEFT FOR THE OWNER ON PURPOSE: `Arcbound Slith -> Surge Conductor`.** Slith is an
  Artifact Creature — Slith with Modular 1 and "whenever this creature deals combat damage to a
  player, put a +1/+1 counter on it"; Surge Conductor proliferates whenever another nontoken
  artifact you control enters. Claude judging a family it has just built is the weakest evidence
  available (the standing 2026-08-18 ruling). Until judged, panel precision is bounded [94.8, 95.1].
- **CEILING, the one `keywordAbilities` already carries**: only edge formation reads this, so a
  proliferate card still does not count as a counter payoff for `cardCaresTags`.

**TRACK 3's §26.3 LIST RE-MEASURED CARD BY CARD, AND THE 2026-08-26 AUDIT GUESSED THE WRONG ONE.**
Each witness pair was run against the current engine rather than re-drawing the recall frame (which
needs ~150/stratum and owner judging). **Ozolith is CLOSED** — `The Ozolith <-> Wick, the Whorled
Mind` forms 2 `leaves:creature` reasons, attributable to no single fix. **`historic recursion` is a
CEILING, not a defect, and C2c never touched it**: `The Eighth Doctor <-> The Fifth Doctor` still
reads 0 and will after any derive fix, because **The Fifth Doctor's only two triggers are
`end-step`**. The recursion channel demonstrably works (`Reanimate -> Archaeomancer` 2 reasons;
`Regrowth -> Archaeomancer` correctly 0, a return to HAND making nothing enter) — what miss #75
claims is "this recursion could return that card" independent of any trigger, which is **the tutor
ceiling §8.6 already records**. **12th roadmap premise corrected by measurement.**
- **THE TWO SAC-OUTLET MISSES ARE BLOCKED ON AN OWNER RULING, NOT ON A DEFECT.** The outlets DO
  derive their sacrifice/dies emits — that is SUPPLY. What is missing is that **a COST is a DEMAND**,
  which nothing here records. **2,033 corpus cards print a sacrifice inside an activation cost; 425
  abilities over 174 derived cards — but 249 of the 425 are `this <thing>`, the card sacrificing
  ITSELF, and 97 more name a BARE TYPE.** Both §26.3 witnesses are bare-type ("Sacrifice a creature",
  "Sacrifice a permanent"), **precisely the shape the shipped tutor rule already refuses**. Building
  it for its own witnesses means OVERTURNING that ruling, not extending it.
- **`grep` STILL SILENTLY SKIPS `edges.ts` WITHOUT `-a`.** `file` calls it *"Java source, Unicode
  text, UTF-8 text"*, and a plain `grep -n "proliferate" packages/matcher/src/edges.ts` returns
  NOTHING while `grep -an` finds four lines. The NUL-byte trap this file records as fixed is back in
  a new form, and it made one of my readings wrong today. **Use `grep -a` on that file.**
- **THE CLAUSE/DERIVED CORPUS IS 2,767, NOT THE 2,541 THIS FILE STATES ELSEWHERE.** Re-counted
  directly off Mongo 2026-08-28.
→ `docs/measurements/2026-08-28-proliferate-demand/criteria.md`

