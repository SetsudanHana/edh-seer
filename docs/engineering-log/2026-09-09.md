# 2026-09-09 — The trigger vocabulary is complete against the rules, and the rest of the corpus is bought

**The buy: 10,595 commander-legal cards, $23.63, one batch.** `msgbatch_01XPsc6NgZ4Dfsf31uYM15DH`,
submitted 07:58 UTC, ended about 09:05 — it sat queued at 0 succeeded for an hour where the
16,949-card batch of 08-29 finished in 17 minutes, then completed. All 10,595 succeeded; persisted
10,497, refused 92 (0.87%), failed 6 on JSON. Bill read off the results, not the dry run: input
37,894,118 (3,576/card), output 1,872,941 (176/card), cache 0. `cardClauses` 21,730 → 32,227;
**31,731 of 31,829 commander-legal cards are read in full.** On the 71 decks nothing moved (panel
98.7% / 91.4%, compass 41/55, edges 35,688), which is what buying the tail should look like.

**Before the buy, the prompt's words were argued from the Comprehensive Rules** (PR #275), because
normalization is a one-way ratchet. Method, reusable: a census of every printed trigger head by
verb over the 31,829 commander-legal cards, a probe per rule-defined event, and for each family what
the ALREADY-BOUGHT clauses had recorded (`research/tagger/trigger-head-census.ts`,
`trigger-event-probe.ts`, `near-miss-clauses.ts`). Seventeen TRIGGERS words (117 → 134), each with
its rule: reflexive 603.12c (252 cards, 115 of the 183 `other` triggers), damaged 120 (122; 82 had
been stored as damage-dealt), state 603.8 (50; Dark Depths stored as counter-added), counter-removed,
mana-spent, loses-game, returned-to-hand (5, all unbought), unattached, put-into-library,
becomes-renowned, plotted, resolves, becomes-saddled, foretell, give-gift, mentors, solved. Judged
and not added, with the reading written down: "add mana" is tapped-for-mana; an extra turn is the
step's own event; "you get {E}" is add-counter (CR 107.14 — 57 bought clauses had said add-mana).
`derive/trigger-completeness.test.ts` ratchets both ways and asserts every homonym family is named
in the prompt's new HOMONYMS rule.

**The refresh's refusal list is a vocabulary instrument** (PR #276). The $0.73 refresh of 171 cards
proved the words and refused six; one refusal invented `loses-game` as a VERB, because winning or
losing the game had no action word. 117 cards print it, 109 bought, recorded as `lose-life` on 30+
— Nine Lives and Slaughter Pact joined to every life-loss payoff. `win-game` / `lose-game` at v19,
110 cards re-asked by name for $0.48, zero lose-life left. The buy then surfaced two more small ones
the same way: `unattach` as a verb (4 cards), `evolve` as a trigger (2). Under $1 to close.

Total spent today: $24.84.
