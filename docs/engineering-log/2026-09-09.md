# 2026-09-09 — The trigger vocabulary is complete against the rules, and the rest of the corpus is bought

**The buy: 10,595 commander-legal cards, $23.63, one batch.** `msgbatch_01XPsc6NgZ4Dfsf31uYM15DH`,
submitted 07:58 UTC, ended about 09:05 — it sat queued at 0 succeeded for an hour where the
16,949-card batch of 08-29 finished in 17 minutes, then completed. All 10,595 succeeded; persisted
10,497, refused 92 (0.87%), failed 6 on JSON. Bill read off the results, not the dry run: input
37,894,118 (3,576/card), output 1,872,941 (176/card), cache 0. `cardClauses` 21,730 → 32,227;
**31,731 of 31,829 commander-legal cards are read in full.** On the 71 decks nothing moved (panel
98.7% / 91.4%, compass 41/55, edges 35,688), which is what buying the tail should look like.

**Before the buy, the prompt's words were argued from the Comprehensive Rules** (PR #275), because
normalization is a one-way ratchet. Method, reusable: a census of every printed trigger head by
verb over the 31,829 commander-legal cards, a probe per rule-defined event, and for each family what
the ALREADY-BOUGHT clauses had recorded (`research/tagger/trigger-head-census.ts`,
`trigger-event-probe.ts`, `near-miss-clauses.ts`). Seventeen TRIGGERS words (117 → 134), each with
its rule: reflexive 603.12c (252 cards, 115 of the 183 `other` triggers), damaged 120 (122; 82 had
been stored as damage-dealt), state 603.8 (50; Dark Depths stored as counter-added), counter-removed,
mana-spent, loses-game, returned-to-hand (5, all unbought), unattached, put-into-library,
becomes-renowned, plotted, resolves, becomes-saddled, foretell, give-gift, mentors, solved. Judged
and not added, with the reading written down: "add mana" is tapped-for-mana; an extra turn is the
step's own event; "you get {E}" is add-counter (CR 107.14 — 57 bought clauses had said add-mana).
`derive/trigger-completeness.test.ts` ratchets both ways and asserts every homonym family is named
in the prompt's new HOMONYMS rule.

**The refresh's refusal list is a vocabulary instrument** (PR #276). The $0.73 refresh of 171 cards
proved the words and refused six; one refusal invented `loses-game` as a VERB, because winning or
losing the game had no action word. 117 cards print it, 109 bought, recorded as `lose-life` on 30+
— Nine Lives and Slaughter Pact joined to every life-loss payoff. `win-game` / `lose-game` at v19,
110 cards re-asked by name for $0.48, zero lose-life left. The buy then surfaced two more small ones
the same way: `unattach` as a verb (4 cards), `evolve` as a trigger (2). Under $1 to close.

Total spent today: $24.84.

**A self-or-class trigger is two triggers, and a recursion re-fires a death.** The v4 recall draw's
blink family was two misses. "Whenever this creature or another enchantment you control enters"
(Fear of Sleep Paralysis, and every constellation card) had its self half stripped so the class half
could not union with it — which also deleted the card's own ETB, the one thing a flicker re-fires.
Derive now keeps a self twin beside the class half, inheriting the class half's origin zone: without
that, River Kelpie's "enters from a graveyard" became a plain self ETB and two owner-judged FALSE
claims (Phantasmal Image and Omni-Changeling → Kelpie) came back through the copy pass on the first
derive, so the copy pass now refuses a from-zone self ETB too — a copy never enters from a
graveyard. The death side had no channel at all: nothing emits a second death, so a new
`refires:dies` pass compares a from-graveyard `enters` emit against a consumer with its own death
trigger by printed characteristics (Sheoldred, Whispering One → Vindictive Lich; Reanimate → Blood
Artist), and the panel recovered Necromancy → Gixian Puppeteer on it. DERIVE_VERSION 130. On the 71:
edges 40,464 → 40,656, reasons 53,518 → 54,285, meshed 289 unchanged, panel 98.7% / retention 91.6%
(384 held, 35 lost), compass 41/55, judging debt 20 → 23. The commander page shows neither relation
by the 09-05 ruling that a card's own trigger is not a demand on the other 99; the deck report does.

## Evening — the three recall-v4 leftovers, one PR each (#296, #297, #298)

All three turned out derive-side, and two of the three roadmap guesses about WHERE the defect sat
were wrong (#151 was called matcher-side; #28 was called a `flipPerspective` question).

**#120, a damage multiplier on fight damage.** The CR 614 damage template captured the VICTIM
("a permanent or player") as the trigger subject, because when it was written a damage emit
recorded only its object. Since 2026-08-27 an authored damage emit carries `GameEvent.dealer` and
`eventMatches` compares a damage trigger against the DEALER, so Fiery Emancipation demanded
`permanent` and Khalni Ambush's typeless fight dealer could never satisfy it — nor could any
authored damage emit satisfy any multiplier. The source phrase before "would" is now the subject,
the damage half of `restricted` is retired (Gratuitous Violence's "a creature you control" is a
demand `subjectMatches` checks on the dealer), and a fight's dealer is a `creature` (CR 701.14a).
Left open: a dealer carries no colours and not the card's own type, so Torbran's "a red source"
and a creature's own ping stay silent under a creature-only multiplier. DERIVE_VERSION 131: edges
40,656 → 40,729, reasons 54,285 → 54,373.

**#151, a DFC back face's type.** Scryfall files the fifteen Transformers under layout
`transform`, which `playableFaces` reads as front-face-only — right for a werewolf, wrong for a
card CR 702.162 lets you cast converted. The printed keyword "More Than Meets the Eye" is read off
`keywords`, the back face implies its own `cast` and `enters`, and Tails sees Starscream's Vehicle
entering. `goldfish.ts` keeps its own front-only rule for castability (CEILING). DERIVE_VERSION
132: edges 40,729 → 40,854, reasons 54,373 → 54,612.

**#28, an opponent's graveyard filled by an edict.** Two defects. Hellish Rebuke grants "sacrifice
this permanent" to PERMANENTS YOUR OPPONENTS CONTROL and derived it as the instant's own death:
`grantedRecipientOf` (PR #294) accepted only creature-shaped recipients, and its rewrite sat in the
general trigger branch, which a `damage-dealt` trigger never reaches. The recipient may now be a
permanent, the rewrite is shared by both branches, and it follows the EMITS and the granted "you"
(CR 113.8) — so Rebuke's `dies` is `{control: opp, type: permanent}` and its life loss is the
opponent's. Valgavoth derived NOTHING: its replacement is Leyline of the Void's sentence, refused
on purpose, and its "play cards exiled with" is a play from exile no verb names. A card printing
BOTH halves (Valgavoth, Dauthi Voidwalker — two in the corpus) is a static `graveyard-recursion`
over `control: opp`, the shape Memory Plunder already carries; Leyline and Anafenza still derive
nothing. DERIVE_VERSION 133: edges 40,854 → 40,930, reasons 54,612 → 54,703, and the panel lost
one FALSE claim (Rebuke's sacrifice feeding a recursion over YOUR graveyard): precision 98.7% →
99.0% on 439 claims.

Across the three: retention 91.6% (384 held, 35 lost) and the ratchet unchanged, compass 41/55
and anti-compass 5/5 unchanged, judging debt 23 unchanged. #90 (flash timing) and #91 (shuffle)
recorded as inexpressible and closed. theme-stats regenerated (#299, 29 keys move, all
attributable); static-out rebuilt and deployed, live manifest `v-464b5f6ddbf7`.

Two process notes worth more than the fixes. `derive-corpus` derives only docs below the current
`DERIVE_VERSION`, so a second derive-side edit under an already-derived version is a silent no-op —
the first #120 measurement was of half the change until `--force` re-ran it. And the CR ledger in
`rules-invariants.test.ts` still files fight under 701.12, which the 2026-08-19 CR numbers as
Exchange; fight is 701.14. Not corrected here.

## Later that evening — the debt is paid, and two of its verdicts moved code

The 23 debt claims were judged by the owner off a sheet: `rejudge-sheet.ts --verdict debt`, a mode
added for it (the roadmap had said `#calibrate`, which draws random pairs by stratum and cannot
serve a named claim; the first cut of the mode returned every unjudged claim in the 71 decks, 36,272
rows, before it was narrowed to the panel's own pairs). 17 real, 5 false, 1 uncertain; debt 0.

**`refires:dies` is gone.** PR #295 had built it that morning for recall v4 #145 (Sheoldred
returning Vindictive Lich "so it can die again"), and all three of its panel claims came back FALSE
with the ruling: *"the edge should be just reanimation — sure it can die again, but that is the
point of reanimation in the first place."* A death trigger firing after a reanimation is what
reanimating a creature is, not a second relation. The pass made 242 reasons on the 71 decks across
52 consumers, Zulaport Cutthroat alone 31 of them — the self twin PR #295 gave a self-or-class dies
trigger had made every aristocrats payoff a "comes back to die again" consumer. Removed: edges
40,930 → 40,802, panel false 10 → 7, and Necromancy → Gixian Puppeteer goes back on the known-lost
list it had been "recovered" from on exactly this claim. #145 stands as not-an-edge, not as a miss.

**"Defending player" is an opponent as an actor.** Reaper King → Kibo, Uktabi Prince claimed
`fodder:artifact` off "defending player sacrifices an artifact of their choice": `recipient.ts` knew
"each opponent" and "target opponent" and not the combat phrasings, so the sacrifice defaulted to
Kibo's controller. 25 corpus cards print it, the annihilator Eldrazi among them. DERIVE_VERSION 134:
edges 40,798, panel false 6, precision 98.5% [96.9, 99.3] on 435 claims, retention 91.5% (388 held,
36 lost), compass 41/55.

**Open ruling, `fodder:artifact`.** Two more falses — Surgehacker Mech → Threefold Thunderhulk,
Syr Ginger → Trading Post — against one real (Midnight Crusader Shuttle → Daretti) and one uncertain
(Aether Spellbomb → Sai, "you can just pay 1 and sacrifice Spellbomb to its own ability"). The pass
says any artifact CARD is fodder for an outlet that names artifacts; the verdicts say a card with its
own job is not. Which property carries the ruling is the owner's to name before code is written.

Also found and not fixed: `sentence.ts` prints "When Sidisi, Regent of the Mire dies" for Sidisi's
"sacrifice a creature ... other than Sidisi" — `other` is set for "another X" and "each other", not
for "other than". Right edge, wrong sentence.
