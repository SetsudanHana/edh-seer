## State as of 2026-08-23

**PRECISION IS AN ENGINE FIGURE AGAIN, AND IT IS LOWER: PANEL 95.5% -> 92.9% [90.0, 95.0], real 405
-> 392, false 19 -> 30, uncertain 22 -> 24, DEBT 0 (round 4, the REAL-column CENSUS, 2026-08-23).**
The owner judged **all 293** live panel claims Claude's rubric called REAL and they had never seen,
blind, with no cached reasoning on the page. **Disagreement 4.4% strict (13/293) [2.6, 7.4], 3.8%
lenient (11/291)** — inside the ≤8% registered after round 1, which read **25% strict on this same
stratum**. What changed is the RUBRIC, not the engine.
- **BOTH TERMS OF THE RATIO ARE NOW OWNER-JUDGED** — the false column by the 2026-08-15 census, the
  real column by this one — so the withdrawal standing since 2026-08-07 is discharged.
  **Quote 92.9% [90.0, 95.0], debt 0, owner-denominated, from here on.** The panel's own ceiling is
  unchanged: 895 frozen pairs, 674 cached verdicts on claims the engine no longer makes, and a change
  outside those pairs still moves it by exactly zero.
- **ALL 293 WERE BANKED, NOT ONLY THE 13.** "Owner-truth on the same terms as the false column" is
  only true if the column is owner-judged END TO END, and banking the agreements is what makes the
  draw's own `USER VERDICT` exclusion cover them — otherwise a future draw re-shows a row and
  measures the owner's MEMORY. `verdicts-useragreement-r4-2026-08-23.jsonl`, `implied` copied from
  the row each verdict supersedes. **THERE IS NO ROUND 5**; a further draw would measure the owner
  against themselves. The `uncertain` column (24 rows) is what is still mixed-rubric.
- **A QUOTED ABILITY GRANTED TO A TOKEN DERIVES AS THE CARD'S OWN TRIGGER — 8 of the 13, and it is
  DOUBLE-COUNTED rather than merely wrong.** **Vivi's Persistence is an INSTANT carrying
  `triggered / cast "a noncreature spell"`** — printed inside the quotes of "create a 0/1 black
  Wizard creature token with '...'". The TOKEN already derives that trigger, so every noncreature
  spell edges to the card AND to its token. Owner, six times in near-identical words: *"the token
  that X produces triggers on it, not this instant."* Vivi's Persistence x3, Cornered by Black Mages
  x2, Lindblum Industrial Regency, Summon: Fat Chocobo (a Bird token watching landfall).
  **MEASURED: 62 derived abilities over 52 of the 2,761 derived cards; 196 corpus cards print
  `token with "`, 44 derived.** **`segment.ts` ALREADY MARKS THESE** — `kind: "granted"` with a
  `parentId` — so the fix is DERIVE-SIDE and FREE. **Not all 52 are the token shape**: Bello, Bard of
  the Brambles grants to YOUR OWN creatures, where the trigger is real and belongs to the creatures,
  so a blanket refusal is a second wrong answer.
- **CR 118.7 WAS NEVER APPLIED TO THE ACTIVATED SIDE, and it overturns a cached REAL.** #270
  Forensic Gadgeteer -> Thought Vessel: Gadgeteer prints its own floor ("can't reduce the mana in
  that cost to less than one mana") and **Thought Vessel's ability is `{T}: Add {C}`, no mana in the
  cost at all**. The 2026-08-20 family work judged it REAL on "the consumer has activated abilities",
  which is necessary and not sufficient. Same rule as B3, one layer over.
- **THE REMOVAL-SPELL FILL RULING LANDS FOR THE THIRD AND FOURTH TIME**: All Is Dust -> Desecrate
  Reality and Summon: Primal Odin -> Witch's Cottage. Odin's is fixable (its emit's `control` is
  printed "opponent controls" against Cottage's "your graveyard"); All Is Dust's is intent, not text.
- Two `partial`s, both known unrepresented restrictions: Chainer triggers only on creatures **not
  cast from hand** (no derived field records a cast's SOURCE ZONE), and **Gollum returns only
  ITSELF** — one more family-C witness.
- -> `specs/2026-08-06-judge-agreement-design.md` "THE ANSWERS", `docs/measurements/2026-08-23-agreement-r4/`

