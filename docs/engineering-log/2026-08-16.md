## State as of 2026-08-16

**THE FIRST SPEND SINCE THE CORPUS BUY: 76 CARDS RE-NORMALIZED FOR $0.33, AND IT PAID FOR ITSELF BY
BREAKING A GATE (2026-08-21).** `--refresh-other` selected 76 stale docs; **69 persisted, 7 refused,
0 failed**, and the refusals are the gate working (one named the next gap outright: `"reveal" is not
in TRIGGERS`). `NORMALIZE_MIN_COMPATIBLE` untouched — nothing was re-bought.
- **`create` IS A TRIGGER EVENT NOW (CR 701.6), and the pipeline named its own missing word.** With
  the segmenter fix in place the model finally answered Mirkwood Bats with BOTH events and the
  persist gate refused the card: **`unknown-trigger-event — "create" is not in TRIGGERS`**. Identical
  to `copy`'s story five lines above it in that list — a legal VERB as an ACTION, missing from the
  trigger side. Added, mapped `create → create-token` in `CLAUSE_TRIGGER_TO_VERB`,
  `TRIGGER_VOCAB_VERSION` 12 → 13. **Mirkwood Bats now carries clause 2 `create / a token` AND clause
  3 `sacrificed / a token`.**
- **THE BUY BROKE THE MESH GATE AND THE GATE CAUGHT IT: MESHED 288 → 405, and the +117 was exactly
  two cards.** Re-normalizing gave **Raphael, the Muscle** ("Double all damage that creatures you
  control WITH COUNTERS ON THEM would deal") the subject `{creature, you, all}` = 60 claims, and
  **Mjölnir, Hammer of Thor** ("Double all damage EQUIPPED CREATURE would deal") `{creature, any,
  all}` = 57. **60 + 57 = 117.** A counter presence and an attachment are both printed, both real and
  both unrepresentable in a `SubjectFilter` — the Excalibur shape (a condition after the subject
  fanning one card to 97 consumers), one layer over.
- **FIXED BY REFUSING THE CLAIM, NOT BY WIDENING THE FILTER**: a multiplier verb (`double`/`triple`)
  whose object carries an unrepresentable narrowing keeps its KIND and claims NO cards — CR 614's own
  `replacement.restricted` rule ("a claim it cannot check is the wrong-answer direction this repo
  refuses") applied to the static pass. `DERIVE_VERSION` 66 → 68, re-derived free.
- **NET, AND THE GATES HELD: edges 35,527 → 35,583 (+56), reasons 44,720 → 44,777 (+57), MESHED 288
  UNCHANGED, panel 94.3% [91.7, 96.1] real 411 / false 25 / uncertain 23 debt 0 BYTE-IDENTICAL.**
  Suite 2,285.
- **WHAT THIS COST TO LEARN: about a third of a dollar, and it bought a vocabulary word, a derive
  guard, and proof that the mesh gate catches a corpus regression the tests cannot see.**

**"NOTHING TRIGGERS ON TOKEN CREATION" IS FALSE, AND AN OR-TRIGGER LOSES A BRANCH — owner's
correction, from memory, against a claim this file and the roadmap both carried (2026-08-21).**
**Mirkwood Bats: "Whenever you create OR SACRIFICE a token, each opponent loses 1 life" derives
`trigger = sacrifice:{control: you, token: true}` and drops the create half.** It is in the owner's
own `smooth-criminal` deck.
- **13 more corpus cards print a "whenever you create …" trigger and NONE is in the derived corpus**,
  so the "a `create-token:` payoff count is always 0" that justified G2's payoff exception was an
  artifact of a derive defect PLUS the derived corpus being 2,541 of ~34,000 — the same understatement
  `becomes-blocked` produced (0 in the decks, 164 corpus-wide).
- **THE CHANNEL IS OTHERWISE POPULATED: 27 derived abilities carry a `token: true` trigger subject**
  — dies 9 · leaves 6 · combat-damage 4 · attacks 2 · enters 2 · sacrifice 1 · create-token 1 ·
  non-combat-damage 1. What fails is the OR shape, which should be sized as "whenever X or Y"
  generally rather than as a `create` special case. → roadmap G2c
- **METHOD NOTE, and it is the fourth time in this file: A COUNT OVER THE DERIVED CORPUS IS NOT A
  COUNT OVER MAGIC.** The claim was inherited from a roadmap line, repeated in a refusal record, and
  used to justify a design decision, and no one re-measured it until the owner read a card.

**AN ARISTOCRATS DECK IS ITS PAYOFFS, NOT THE REMOVAL SPELL THAT EMITS A SACRIFICE — the archetype
detector never learned the Sorin lesson, and a SHIPPED SCORE was reading the inflated number
(2026-08-21).** `detectArchetypes` counted supply and demand alike, so a control deck's removal
package WAS its aristocrats confidence — and `build.ts:325` feeds
`max(conf(reanimator), conf(aristocrats))` into `answerCoverage` as `graveyardVulnerability`.
- **MEASURED: 815 of the 974 aristocrats matches across the 71 decks are SUPPLY-ONLY, against 159
  cares-backed.** A signature row can now be `demandDefined`, and only `aristocrats` is: a card
  matching via `cardCaresTags` counts 1.0, supply-only counts `PRODUCER_SHARE` 0.35 — the same
  constant and the same reasoning as `rankFreq`. **Aristocrats tops 28 decks → 11; labels in use
  7 → 10.**
- **A TREASURE MAKER IS RAMP, NOT A TOKENS DECK** — the identical `RESOURCE_TOKENS` exclusion
  `wincon.ts` already ships for go-wide. **227 of the 774 token matches are resource-only, 218 of
  them Treasure.** A card whose kind fired with NO `create-token:` tag is deliberately NOT excluded:
  silence is not proof, and a silent exclusion is the wrong failure direction.
- **THE SHIPPED CONSUMER MOVED, WHICH WAS THE POINT: buildScore mean 4.485 → 4.468, median 4.563 →
  4.556, coverage mean 0.914 → 0.904, and the vulnerability term's max effect 0.1672 → 0.0925 over
  15 decks (was 16).** Criterion 4 stays comfortably enabled. `graveyardVulnerability` is RIGHT IN
  KIND and was miscalibrated: under Rest in Peace a creature never DIES (it is exiled), so an
  aristocrats deck really is wrecked by graveyard hate — what was wrong was charging that to decks
  whose aristocrats confidence was removal noise.
- **THE ABSTENTION DEFECT BELOW IS NOW CLOSED — A15 SHIPPED THE SAME DAY.** `THEME_NAME_FLOOR` 0.10
  and `ARCHETYPE_LEAD_FLOOR` 0.25, both taken from the DISTRIBUTIONS and not from correctness (a
  floor cannot predict correctness here: hits median cohesion 0.31, misses 0.33, and the worst wrong
  headline sits at 0.70 and is a TRUE sentence). **Theme abstains on 10 of 71, archetype leads on 23
  of 71, and 5 of the 6 control decks now name NOTHING.** The tag survives the withdrawn claim ("no
  dominant theme (strongest: proliferate)") and an ABSENT `dominant` means "no opinion" and names
  the deck. `buildScore` byte-identical. One registered criterion failed by ONE DECK and is recorded
  rather than tuned: `otterly-awesome-spellslinger` was a headline hit and abstains at 0.06.
  **AND MASTER HAD BEEN CARRYING A TYPE ERROR SINCE THIS FIX MERGED**: `ARCHETYPE_SIGNATURE`'s
  annotation never learned `demandDefined`, invisible to 872 green tests — the recorded
  `vitest does not typecheck` trap, fixed in A15's commit.
- **TWO REGISTERED CRITERIA FAILED AND ARE RECORDED AS REFUTED, NOT RELAXED.** (1) "Aristocrats tops
  none of the 6 owner-named control decks" — it still tops 4, confidences halved (zenos 0.43 →
  0.20) and the LABEL unmoved. **THE NAMING LAYERS WERE THE ONLY CODE IN THIS REPO FORBIDDEN TO
  ABSTAIN** — `detectArchetypes` always returns a top label (Goodstuff at confidence 0 can never
  win), `rankThemes` always crowns a head, `winconReport` always picks a `primary` — so on a deck
  with no positive identity, argmax returns whatever ranks second. That is the general defect, and
  it is why gating removes a false CONFIDENCE and never a false LABEL. (2) "no label tops more than
  15 of 71" — Tokens inherited the vacancy, 24 → 26.
- **A MEASUREMENT OF MINE WAS WRONG AND IS CORRECTED HERE: `analyzeDeckStructured`'s 5th parameter
  is the `ComboIndex`, and a probe that omits it disables the floor-exempt `combo` archetype
  entirely.** Scored without it the detector read 5/37 against the owner's own deck names; scored
  properly it is **7/37 → 8/37**, and `acererak-combo` — which I had recorded as a miss — reads
  **Combo 0.44 with the win route `combo` at 60%**. **29 of the 71 decks carry a detected combo.**
  Any probe calling that function must pass the index.
- **WHAT "WHAT IS THIS DECK" ACTUALLY IS, and it reframes three refused designs (Fable, 2026-08-21):
  a THREE-SLOT sentence — win route (what ends the game) · engine (what fuels it) · means (what buys
  time) — and each instrument already answers exactly ONE slot.** `winconReport` is the win route,
  the theme headline is the engine, the BUILD interaction counts are the means. The owner's own deck
  names sample different slots ("Judith **Tokens** Connoiseur" is the engine, "Acererak **Combo**"
  the win route, "Fandaniel Mono Black **Control**" the means), so the headline's 17/37 is not "46%
  accurate" — it is ~68% accurate WITHIN the engine slot and 0% outside it by construction. Every
  grading exercise so far scored a one-slot instrument against a composite.
- **WIN-ROUTE NAMING IS REFUTED BEFORE ANY CODE, on its own registered criterion.** "No wincon class
  is primary in more than 12 of 71": measured **go-wide 29, burn 27**, combo 10, voltron 5, and
  win-route agreement with the owner's names is **3/37**. `burn` covers a Zulaport drain and a
  Lightning Bolt alike, so the wincon vocabulary carries a LARGER inflation than the archetype one
  this fix corrects. Recorded; the free dump was the whole cost.
- **THE OWNER'S RULING THAT STARTED IT: "the theme of the deck is what wins you a game, aristocrats
  win you a game, where control nope, control needs some additional winconditions like thassas
  oracle or laboratory maniac where control is means to pull it off."** Reproduced in the data:
  `alt-win` fires on 4 decks in the control sample and 3 are owner-named control decks, and it is
  BINARY-exempt from the wincon floor because one Thassa's Oracle IS the plan.
- **GROUND TRUTH EXISTS AND IS NOW USED: `packages/cli/decks/calibration/deck-names.json`**, the
  owner's own Moxfield names, which the corpus README already declared ground truth. 37 of the 71
  carry a strategy word. **Register 17/37 (theme headline) and 8/37 (archetype) as the regression
  baseline every future naming change states a before/after against — never a target to climb**,
  since the mapping is the judge and tuning the judge is the Goodhart failure the panel exists to
  catch.

**A SELF ETB TRIGGER WANTS A FLICKER, NOT ITS OWN CARD TYPE — the theme tag was MIS-KEYED, and
FABLE'S REVIEW INVERTED WHICH SIDE CARRIES THE DEMAND (2026-08-21, roadmap G1, owner's call).**
`cardCaresTags` never checked `subject.self`, so "When THIS creature enters, draw" was demand for
`enters:creature` at FULL weight against `PRODUCER_SHARE` 0.35 for supply. Owner's ruling: it IS
demand -- for flicker, copy and Panharmonicon-style trigger doubling -- so the tag named the wrong
class. "Wizard entering, and trigger of specific card self entering are 2 different things."
- **DROPPING IT IS A WASH, AND THAT IS WHY THE RE-KEY EXISTS.** The one-liner moved 18 headlines,
  cohesion up on 9 and down on 9, mean 0.310 -> 0.326, subtype primaries 24 -> 22.
- **WHICH SIDE CARES DECIDES EVERYTHING.** Keyed with the ETB card CARING, `etb-refire` headlines
  **11 of 71 including a deck with ONE flicker effect** -- the universal-bucket failure three theme
  designs died on. Keyed with the RE-FIRER caring (the ETB card supplies at producer share), it
  headlines **4, every one with 7+ re-firers**: `mono-blue-blink` (26 ETB / 18), `everything-is-a-
  land` (21), `can-i-copy-your-homework` (15), `cult-of-clones` (7). Corpus 369 self-ETB cards and
  109 re-firers of 2,745, idf **1.77** -- it cannot win by rarity either. **Projected BEFORE the
  code was written, and the projection is what refused my own first design.**
- **FIVE OF SIX REGISTERED CRITERIA PASS; SUBTYPE PRIMARIES 24 -> 21 FAILS, and it ships anyway on
  the owner's call with the failure recorded** (the A9 precedent). Four of the five losses are the
  self-demand removal itself, inherent to the rule. `inalla` is byte-identical -- its eminence is a
  CLASS subject ("whenever ANOTHER nontoken Wizard"), which is the acceptance test.
- **A TIMING KEY AND A PLACEHOLDER CAN NO LONGER NAME A DECK** (A12, shipped alongside):
  `upkeep:any` headlined 2 decks and `cast:__none__` was a placeholder competing as a theme.
  `demoteUnrankableHeadline` is a post-rank guard beside the promotion rule. **The first cut was
  wrong on the one deck that earns it** -- `obeka-upkeep-shenanigans` runs Obeka, Splitter of Seconds
  (`extra-phase:upkeep`), so a phase headline survives when the deck SUPPLIES that phase; a BEGINNING
  phase counts (CR 501), an extra TURN does not. 1 of 71 headlines moved. `describeTag` also had no
  phrase for `static:`, so a CLONE deck headlined the literal word "static"; now "clone effects".
- **GATES: population and panel are unreachable** (a theme tag forms no edge), **build is
  byte-identical** (buildScore mean 4.485 / median 4.563 / perfect 7), census unchanged.
  `theme-stats.json` REGENERATED in the same change -- an absent tag scores maximum idf, which is
  the 2026-08-18 staleness failure.

**A TUTOR IS NOT A GRAVEYARD CARD — `top-manipulation` no longer defines Graveyard Matters, and the
bogus category had been EATING a correct one (2026-08-21, roadmap G3).** `effect-kind.ts` maps
search, scry, surveil AND mill to one kind, and `CATEGORY_MATCH` let it define the category, so
every fetchland read as a graveyard card. One table entry; labelling only.
- **MEASURED over the 71 decks: graveyard-matters 70 decks -> 63, pairs 7,301 -> 4,095 (-3,206),
  top group changed in 11 of 71** — `abzan-defenders`, a WALLS deck, led with "Graveyard Matters 92"
  and now reads Draw Engine. **The real graveyard decks keep it and barely move**: smooth-criminal
  192 -> 189, acererak 137 -> 129, hidetsugu 119 -> 107, **voltron-mill 14 -> 13**, which is the
  self-mill worry answered by measurement rather than argument.
- **THE DEDUPE IS WHY THIS COST MORE THAN A LABEL.** `dedupeNearIdentical` drops a group whose pairs
  sit >=90% inside a BIGGER group's — and every fetchland pair is in landfall AND was in
  graveyard-matters, which was bigger. **Landfall decks were losing their own label: 17 -> 20
  decks (+62 pairs)**, power-matters 0 -> 1, reanimator 3 -> 5. A wrong category is not merely noise
  in its own row; it silently deletes the right one next to it.
- **`mill-self` IS DEAD CONFIG AND WAS ALREADY** — it keys on `enters-graveyard:*` and **0 of the 71
  decks carry one such reason**, measured directly. The stub's proposed fallback would buy nothing.
- **NOTHING SCORED MOVED, structurally**: `CATEGORY_MATCH` is read only by `groupEdgesByArchetype`,
  whose output reaches `report.archetypes` and the web board and nothing else — `detectArchetypes`
  reads `ARCHETYPE_SIGNATURE`, a different table. Matcher 861 passed (incl. the `derive-compass`
  gold-pair ratchet, which did not fire), web 434.

**INTERACTION SCORES COVERAGE x COUNT NOW — FIVE OF THE SIX REGISTERED CRITERIA PASS OUTRIGHT;
CRITERION 5 PASSES ON 2 OF ITS 3 NAMED MUTATIONS, THE THIRD STRUCTURALLY UNREACHABLE — measured, not
asserted (2026-08-21, corrected in the whole-branch review's final fix wave: the earlier "ALL PASS"
headline said so four bullets before admitting criterion 5's third mutation does not fire, the exact
overstatement this project's own documentation discipline exists to catch).**
**A SECOND CORRECTION LANDED THE SAME DAY, RESIDUAL TO THE FIRST: `GRAVEYARD_HATE_SHARE` shipped as
36/16/6 against a stated provenance ("the corpus cards matching `graveyardHateRecurring`") that
actually measures 39/19/8 — the original probe nested that rule inside a DIFFERENT rule's branch
and counted the intersection, a strict subset never matching its own documented source. Corrected
to 39/19/8; every figure below that depends on it is re-measured and marked. `gen-answer-pool.ts
--check` now also gates both hand-transcribed tables against a live corpus sweep, not just the pool
artifact, so this class of drift cannot ship silently again.**
`Interaction`
attainment was `min(count/target,1)`; it is now that times a
pool-and-demand-weighted `coverage` share of the five permanent answer classes, so fourteen
creature-removal spells and zero artifact answers no longer scores full marks. → owner ruling
2026-08-21, `specs/2026-08-21-answer-coverage-design.md`, `answer-coverage-population.ts` (new,
free).
- **MEASURED: decks at full Interaction attainment 66 of 71 (count alone) -> 46 of 71
  (coverage-weighted)** — a real fall, criterion 2's whole test, unaffected by the
  `GRAVEYARD_HATE_SHARE` correction. `answer-pool.json` (generated, free) says a mono-black deck's
  artifact pool is 56 against white's 215, the smallest of any identity, so missing artifact
  removal costs it **10.6-10.7% of coverage, never more, on all 9 named mono-black decks measured**
  (criterion 3, re-measured against the corrected table; was stated as 10.2-10.4%) — never the ~20%
  a uniform read would charge.
- **§6'S HEADLINE PREDICTION MISSED, ON ITS OWN NAMED WITNESS, BY OVER 16x — the blend ships
  enabled anyway, correctly, against the registered predicate.** §6 predicted total coverage would
  be "nearly inert for exactly the decks it was designed for"; on `sarevok-lord-of-pain`, §6's own
  witness, coverage moves **0.1672 — over 16x** criterion 4's 0.01 disable threshold (re-measured
  against the corrected `GRAVEYARD_HATE_SHARE` 39/19/8; was 0.1739, over 17x, against the wrong
  table — the miss stands either way). That is the prediction failing, not a near-miss it survives.
  What §6 actually COMPUTED was a narrower quantity — artifact's own share of the coverage
  denominator, 10.6% -> 10.7% (was stated 10.5% -> 10.2%, a fall, against the wrong table; the
  corrected table makes it a slight RISE instead) — which reproduces exactly, but §6 stated its
  conclusion about *total* coverage without taking the step from one to the other. The real
  mechanism: total coverage moved because the OTHER classes' demands recompose as `v` rises
  (`land`'s `hateShare` is 0, so its demand and weight collapse; `creature`'s is 0.591, so its
  demand and weight rise), not because the artifact term §6 analysed did anything. Right
  arithmetic, wrong quantity. Criterion 4 itself still PASSES on 16 of 71 decks exceeding 0.01
  (unchanged by the correction), so the term ships live as designed — that verdict does not depend
  on §6 having called it.
- **NOTHING THAT READS EXISTING EDGES MOVED, and one stale figure was found and isolated rather
  than carried forward.** `population-compare.ts` / `panel-score.ts` / `ratings-compare.ts` /
  `build-population.ts --diff` are byte-identical before this plan's first commit and after its
  last (35,527 edges / 44,720 reasons / MESHED 288, panel 94.3% debt 0, ratings 0 of 6,236, 0
  category memberships), checked via a `git worktree` at the pre-plan commit. **The 44,507 this
  file quoted for reasons is stale by +213, and the drift PREDATES this plan** — traced to this
  branch's earlier "human-readable-report-p1" phase-1 work (mana symbols, the reason-sentence
  module), confirmed by checking out the merge-base with `master`, which still reads 44,507.
- **ONE OF THREE "PROVEN TO FIRE" MUTATIONS ISN'T LIVE, and that is recorded rather than papered
  over.** `poolShare` stubbed to 1 and the blend replaced by baseline alone both fail a test on
  mutation, confirmed by actually mutating and reverting. The third named mutation
  (`covered` loosened from `count >= 1` to `>= 0`) cannot be observed by any test: `build.ts`'s
  `detectAnswerClasses` only ever creates a class's Map entry on its first matching card, so a
  zero-count entry never exists to loosen the threshold against — the same `>= 1`-vs-`>= 0`
  distinction the `named` and `zone` fields have both needed elsewhere in this project, except here
  the data structure makes it structurally unreachable rather than merely untested.
- **THE GENERATED ARTIFACT'S RATCHET IS THE SAME SHAPE `gen-vocabulary.ts --check` ALREADY
  SHIPS**: a free, DB-less structural test (`answer-pool.test.ts`, 8 tests — monotone under
  identity superset, all 32 x 6 present) runs in every `npm test`; a corpus-driven `--check`
  (needs Mongo) is the separate CI step that catches the corpus itself moving. Ran live: zero
  drift against the current 34,433-card corpus.
- Full suite **2,254 passed, 2 failed, 7 skipped** — both failures are pre-existing d3-force timing
  tests (`GraphView.test.tsx`, `board-force.test.ts`) in files this plan never touched (`git diff`
  empty), one of them timing out even run alone rather than only under parallel load.
- **CEILING, severity is unrankable.** The corpus can COUNT hate pieces and cannot RANK them —
  Rest in Peace and Scavenging Ooze are both one row of the 66 `graveyardHateRecurring` matches,
  though one is a total shutoff and the other eats one card a turn. `coverage` cannot tell a
  format-defining answer from a marginal one, only whether the class has any answer at all.

**A PANEL REBUILD IS SAFE NOW, AND THE HEADLINE NO LONGER MOVES WITH ROW ORDER — C11 closed
(2026-08-20). PANEL 94.3% [91.7, 96.1], real 411 / false 25 / uncertain 23, debt 0.** Three faults,
each found by the previous fix exposing it, and the number moved DOWN twice on the way — which is how
you know the rule was not chosen to flatter it.
- **THE AUTHORITY ORDER WAS CARRIED BY ROW ORDER ALONE.** The cache is append-ordered, a rebuild
  replays sources ALPHABETICALLY, and the two disagreed on 8 claims — the panel reading **92.0%
  against 93.8% with identical verdicts**. Measured: of **64 claims whose duplicate rows disagree,
  44 are the owner overriding an earlier Claude verdict**, notes saying so outright. `mergeVerdicts`
  now encodes it — **a USER verdict is never overwritten by a Claude one, in either order** — and
  three claims corrected the moment it did, all of them an owner verdict beating a LATER Claude one
  (Rook Turret, Optimus Prime, Junji).
- **`implied` IS PART OF A VERDICT'S IDENTITY, NOT A TIE-BREAK.** A verdict records the MECHANISM it
  was judged against, and `scorePanel` owes a claim again when they disagree. The first dedupe
  collapsed rows differing on it and **cost two judged claims** (Hornet Nest → Enduring Innocence,
  Aragorn → Prowl) — each had an OWNER verdict on the authored mechanism and a Claude verdict on the
  implied one, which are answers to different questions. `verdictKey` keeps them apart.
- **AND THE LOOKUP WAS COARSER THAN THE STORAGE, which is what actually made a rebuild unsafe.**
  `scorePanel` consulted the cache by TRIPLE while verdicts are stored per mechanism, so whichever
  row sat LAST won: a lossless rebuild that changed no verdict still moved the score 93.6% → 94.3%.
  It now takes the EXACT mechanism and falls back to a row with no `implied`, which is the wildcard
  that field's absence has always meant.
- **PROVEN, not asserted: two consecutive rebuilds and a RANDOMLY SHUFFLED cache all read 94.3%,
  debt 0, 1,202 verdicts, 0 lost and 0 changed.** Row order now carries no meaning at all. The cache
  is deduped once, 1,689 lines → 1,202 verdicts, each surviving row sitting where it was actually
  made — ordering by FIRST appearance instead flipped three owner verdicts and is the second wrong
  turn recorded here.
- **`panel-build.ts` folds the cache in LAST** (it is the resolved record; sources are provenance and
  replay alphabetically) and refuses to write fewer DISTINCT claims than are cached. **Adding
  verdicts by rebuilding is safe again**; the earlier "merge into `verdicts.jsonl` directly" workaround
  is retired.

**THE COST-REDUCTION FALSES WERE RUBRIC DEBT, AND THE OWNER CLEARED THEM: PANEL 91.5% → 93.8%
[91.2, 95.7], real 400 → 410, FALSE 37 → 27, debt 0 (2026-08-20).** All ten judged TRUE on the CLAIM
SENTENCE ("X's cost reduction applies to Y") under the 2026-08-18 ruling that a cost reduction IS a
pairwise claim. Nine were Claude's `generic` verdicts from the era when the family was excluded as "a
deck role"; the proof they were stale is that the SAME producers already carried the owner's own REAL
verdicts on other consumers (The Darkness Crystal false → Dismember but real → four others; Ruby
Medallion false → Nesting Dragon but real → Rapacious Dragon). The tenth, Eluge → Pongify, was stale
by DERIVATION instead. → `docs/measurements/panel/verdicts-useragreement-r3-2026-08-20.jsonl`
- **THE JUDGING SHEET IS AN INSTRUMENT NOW** (`bin/rejudge-sheet.ts`): `--tag` + `--verdict` writes a
  markdown sheet, a JSONL template AND a self-contained interactive page that carries the facts a
  family's verdict turns on — for this family the consumer's MANA COST, TYPE LINE and COLOURS, which
  the plain worksheet omits and the owner had to look up by hand on 2026-08-19.
- **THE CLAIM SENTENCE GOES FIRST AND THE CACHED VERDICT IS FOLDED AWAY.** The first cut of the page
  showed neither — the cached note sat where the claim belonged, so the last thing read before
  clicking was Claude's old reasoning. `Calibrate.tsx` had already settled this: it hides the
  engine's reasons until asked, because seeing what the engine believes anchors the verdict to it.
- **DO NOT RUN `panel-build.ts` TO ADD VERDICTS — MERGE INTO `verdicts.jsonl` DIRECTLY.** A rebuild
  took judging debt 0 → 26. Two separate faults, and the first report of them was WRONG:
  - **28 unique verdicts exist ONLY in the generated cache** (in-session judgements paid against
    worksheets never kept as their own file). Fixed: the cache is folded in as a source and a ratchet
    refuses to write fewer DISTINCT CLAIMS than are already cached — measured after the fix, a
    rebuild loses ZERO.
  - **THE RAW LINE COUNTS LIED, AND SO DID I.** "1,689 → 1,084" looked like 605 lost verdicts; **577
    of it was DUPLICATE LINES COLLAPSING** — the cache holds 1,689 lines for 1,112 distinct claims.
    **Count distinct claims before reporting a loss.**
  - **STILL OPEN, and the reason a rebuild is not safe to run today**: the file resolves duplicate
    conflicts by APPEND ORDER (chronological, last wins) while a rebuild resolves them by SOURCE
    order, and the two disagree on **8 claims** — a rebuild reads 92.0% where the file reads 93.8%.
    Neither number is wrong; they are two different tie-breaks over conflicting judgements that were
    never reconciled. The file's order is the semantics in force, so the file is authoritative until
    that is settled.
  - A verdict file is folded ALPHABETICALLY, so a re-judge must be named to sort AFTER the file it
    supersedes — hence `verdicts-useragreement-r3-2026-08-20.jsonl`.

**THE ARRIVAL SLOT — how an object got here is a PROPERTY OF THE EVENT, so it narrows the producer
(2026-08-20). PANEL 90.5% → 91.5% [88.5, 93.8], false 42 → 37, real 400 UNCHANGED, debt 0.**
The one intervening-if shape that does not need an evaluator: `SubjectFilter.notCast` and
`.entersTapped`. `DERIVE_VERSION` 65 → 66, free.
- **Satoru, the Infiltrator** — "whenever Satoru and/or one or more other nontoken creatures you
  control enter, IF NONE OF THEM WERE CAST OR NO MANA WAS SPENT to cast them" — claimed every
  creature in the deck; **Deep Gnome Terramancer** wants lands entering "WITHOUT BEING PLAYED";
  **Amulet of Vigor** and **Tiller Engine** want a permanent that "ENTERS TAPPED". Five panel falses,
  all five removed, nothing else touched.
- **THE PRODUCER SIDE IS INFERRED, NEVER A SECOND FIELD**: a TOKEN was never cast and an entry FROM A
  STATED ZONE was put there rather than cast, so `token === true || fromZone !== undefined` answers
  it — **644 of 748 derived `enters` emits qualify**, and a card's own implied entry (104) is the
  cast Satoru excludes by name. Only `entersTapped` needs stamping on both sides, from the same
  printed cue `ARRIVES_TAPPED` already uses.
- **AND IT BIT THE SAME NERVE A FOURTH TIME.** In the self-trigger identity gate a producer's emit is
  the FILTER against the consumer's PRINTED characteristics — so stamping `entersTapped` as SUPPLY
  turned it into a demand that the consumer CARD be tapped, which no card is, and **silently deleted
  29 REAL self-ETB claims the moment the field existed**: Eldrazi Confluence blinking Solemn
  Simulacrum, Fungal Fortitude returning Gray Merchant of Asphodel. Caught by the panel losing 2 real
  claims and by a −581 reason cut that four cards could not explain. **`zone`, `counter`, `commander`
  and now `entersTapped`: A FIELD THAT DESCRIBES THE EVENT MUST BE STRIPPED WHEREVER AN EMIT IS USED
  TO IDENTIFY A CARD.** Pinned by a regression test proven to fire.
- **MEASURED after the strip: edges 35,761 → 35,527 (−234), reasons 44,811 → 44,507 (−304), MESHED
  288 unchanged, build categories 0, ratings 135 of 6,236 (mean |Δ| 0.0056), TOP card 1/71.**

**AN INTERVENING-IF CONDITION IS A DEMAND ON THE DECK — recorded, and MEASURABLY INERT, which is the
finding (2026-08-20, owner's framing).** The owner's reframing is right and worth keeping: "we should
only care about the condition, cause it is a consuming edge, and the effect, which is producing" —
Yuna should score more in a counters deck, Warlock Class is an aristocrats payoff, a card wanting a
red permanent in a deck with none is a bad card. `Ability.conditionCares` records the demand and
`cardCaresTags` unions it. `DERIVE_VERSION` 63 → 65, free.
- **A CLOSED MAP, NEVER AN EVALUATOR** — the 2026-08-15 refusal stands. Four printed shapes:
  counter-presence → `counter-added:any`, "a creature died this turn" → `dies:creature`, "you
  attacked this turn" → `attacks:any`, "a <type> entered this turn" → `enters:<type>`. **15 derived
  cards carry one** (counter-added 13, attacks 3, enters:planeswalker 2, dies:creature 1). Yuna now
  cares about counters, Warlock Class about creatures dying, Oath of Liliana about planeswalkers.
  Colour and life conditions emit NOTHING: a colour is not a theme any card supplies.
- **EVERY GATE IS BYTE-IDENTICAL — 35,761 / 44,811 / MESHED 288, panel 90.5% real 400 / false 42
  debt 0, build categories 0 — AND SO IS EVERYTHING ELSE: ratings 0 of 6,236, headlines 0 of 71,
  cohesion 0, census 0.** Isolated by disabling the one line and re-running, not assumed.
- **WHY IT CANNOT MOVE A RATING, and this is the part worth knowing before the next attempt.**
  `rankFreq` only RE-WEIGHTS tags already in `deckFreq` (a cared-about tag counts full instead of
  `PRODUCER_SHARE` 0.35), and **`buildAxis` reads `deckFreq` and `themeStats`, never the cares side**
  — so the axis, `axisWeight` and the rating are all unreachable from here. More fundamentally,
  **`synergyRating` is EDGE-derived, and a condition forms no edge by design**, so "scores more in a
  counters deck" is not deliverable through the rating at all without inventing the pairwise claim
  the owner's own framing says does not exist.
- **CHANNEL (1) IS NOW BUILT AND IT MOVES EXACTLY ONE DECK.** `detectArchetypes` is CARD-based, and
  `CardSignal.themeTags` now unions `conditionCares`. `cardThemeTags` already carried a card's
  trigger verbs, so "whenever a creature dies" always read as aristocrats — but **Warlock Class
  triggers on the END STEP and names the deaths only in its intervening if**, so the payoff was
  invisible to every archetype. **Measured: 1 of 71 decks moves —
  `braids-mono-black-enchantress`, which runs Warlock Class, aristocrats 0.397 → 0.413** (one card
  of 63 joining). The ORDER does not change, so `computeBuild`'s archetype-adjusted targets are
  untouched and `build-population.ts` reports 0. Population, panel and ratings all byte-identical.
  **`CATEGORY_MATCH` in `mechanisms.ts` was the wrong table** — it matches REASONS, and a condition
  forms none; `ARCHETYPE_SIGNATURE` is the card-based one.
- **CHANNEL (2) IS BUILT TOO — a condition the deck cannot meet is a cut-list REASON.** "If you have
  a card that cares about red permanents and have none, it is not a very good card in the deck."
  `conditionCares` states the demand as a theme tag and `deckFreq` already counts the supply, so this
  is a JOIN, not new analysis. **FOLDED, because supply is keyed FINER than demand**: a planeswalker
  themes at its SUBTYPE (`enters:liliana`) while Oath of Liliana asks for `enters:planeswalker` —
  the same `tag === want || fold(tag) === want` predicate `computeCohesion` settled on. The card's
  OWN tags are excluded, so Warlock Class cannot satisfy its own demand.
  **Measured: exactly ONE card is flagged across the 71 decks** — `braids-mono-black-enchantress`
  running **Oath of Liliana** with zero planeswalkers, agreeing with the independent 1-of-33 probe
  taken before any code was written. Renders live: *"its condition needs planeswalkers entering, and
  nothing in the deck provides that."*
- **A REASON, NEVER A GATE, AND OATH OF LILIANA IS PRECISELY WHY.** Its end-step Zombie half is dead
  text in that deck — but its ETB ("each opponent sacrifices a creature") is UNCONDITIONAL and real,
  so it still rates 1.5 and keeps "its best edge is on your main theme". **A rule that demoted a card
  for one unmet condition would throw away a working card**; the honest surface is to print the fact
  and let the reader judge, which is the same discipline the rest of the cut list keeps.
- **STILL OPEN**: (1) ARCHETYPE membership —
  `mechanisms.ts` reads effect kinds and tags, and Warlock Class should count toward aristocrats;
  (2) DECK FIT — a condition naming something the deck lacks is a cut-list row. **Measured: 33
  card-slots across the 71 decks carry a deck-checkable condition and exactly ONE is unsatisfiable,
  `braids-mono-black-enchantress` running Oath of Liliana with ZERO planeswalkers.** One in 71
  because these are the owner's own well-built decks — the same self-comparison trap `BASE_TARGETS`
  records — and it will be far commoner on an arbitrary pasted deck.
- **A BACKSPACE BYTE ATE FOUR REGEXES AND THE TESTS WOULD NOT HAVE CAUGHT IT.** Writing the file
  through a non-raw heredoc turned every `\b` into a literal 0x08, so `/\bcounters?\b/i` became a
  match on a backspace character: it compiled, ran, and returned zero. `file` says "with
  overstriking" and `od -c` shows it. **This is the third time this trap has fired in this repo
  (memory `regex-via-heredoc-mangles-escapes`) — check `file` on any source written through a
  heredoc before believing a zero result.**

**`SubjectFilter.notSubtype` — A NEGATED SUBTYPE HAD NOWHERE TO GO (2026-08-20). PANEL 90.3% →
90.5% [87.4, 92.9], false 43 → 42, real 400 UNCHANGED, debt 0.** `notType` holds CARD TYPES and is
tested through `expandTypes`, so "target **non-Dragon** creature card" lost its exclusion entirely
and **Junji, the Midnight Sky claimed to reanimate Hidetsugu and Kairi, a Legendary Creature — Ogre
Demon Dragon** — precisely the card the text excludes. Derive-side, free, `DERIVE_VERSION` 62 → 63.
- **MEASURED FIRST: 65 distinct subtypes are negated across 247 corpus cards, 14 in the derived
  corpus** — non-Human, non-Zombie, non-Dragon, non-Angel, non-Sliver and 60 more. A bigger family
  than several shipped earlier today.
- **NO CONTAMINATION, and it is free rather than lucky**: `parseSubtypes` tokenises on `[a-z'-]+`,
  so "non-dragon" arrives as ONE token and can never join the positive list — a card cannot demand a
  Dragon and exclude one at the same time. `noncreature` (notType), `nonbasic` and `nontoken` keep
  their own slots, each pinned by a test.
- **NO ABSTENTION CLAUSE, unlike `notType`.** A card type can arrive as an umbrella ("a spell" says
  nothing about whether it is a creature), which is why the type check abstains; a subtype never
  can, and a producer listing no subtype simply cannot carry a negated one.
- **EXACTLY ONE CLAIM MOVED — the target — and the pair correctly claims NOTHING else**, because
  Junji genuinely cannot reanimate a Dragon. Edges 35,768 → 35,761 (−7), reasons 44,821 → 44,811
  (−10), MESHED 288, build categories 0, ratings unchanged from the previous step. Three tests, the
  gate proven to fire.
- **CR 700.16 `worthy` NOW HAS ITS MISSING SLOT AND STAYS UNBUILT**: "legendary, ISN'T A VILLAIN, and
  is red and/or white" needs `legendary` + `colors` + this field, and all three now exist — but it is
  **4 corpus cards**, so composing them is not worth a rule until the family grows.

**A RECURSION THAT RETURNS ONLY WHAT IT PUT THERE ITSELF IS FED BY NO OTHER CARD — panel family C
(2026-08-20). PANEL 88.8% → 90.3% [87.2, 92.7], false 51 → 43, real 403 → 400, uncertain 24 → 22,
debt 0. THE THREE REAL ROWS ARE DUPLICATE TAGS, NOT LOST RELATIONS — verified pair by pair.**
- **THE FAMILY WAS MIS-NAMED "ORIGIN" AND SIX OF ITS SEVEN ARE THE `exiled with` SHAPE ONE RUNG
  ALONG**: the returned set is defined by the CONSUMER'S OWN earlier action, so no producer fill can
  put anything in it. Three printed templates, each measured corpus-wide: **"from among those"**
  (Ripples of Undeath mills three and returns one OF THOSE — and so does **Nashi**, which was sitting
  in the family-A residue), **"put into a graveyard this way"** (Necromantic Selection, 24 corpus
  cards) and **"put there … this turn"** (Gerrard's Hourglass Pendant, 49 corpus cards).
- **AND THE STRUCTURAL HALF, which took THREE tries to get right.** An untyped recursion on an
  ability carrying its own graveyard-entry trigger returns exactly what that trigger saw ("whenever a
  creature you control with a +1/+1 counter on it dies, return THAT CARD") — **21 derived cards**:
  Marchesa, Kaya's Ghostform, Feign Death, Luminous Broodmoth, Optimus Prime, Shirei. It sits AFTER
  the trigger-match skip, so it only drops fills the trigger cannot see: Kefka milling a creature for
  Marchesa, whose trigger needs a counter.
- **TWO WRONG VERSIONS AND TWO WRONG DIAGNOSES, all recorded in the code.** The blanket version also
  refused fills the trigger DOES see, costing 3 REAL claims on Meathook Massacre II (an UNRESTRICTED
  "whenever a creature you control dies, return that card", so a sacrifice outlet really does enable
  it). I then blamed that on "a gap in the `dies` channel" — **also wrong: I compared pairs against
  decks that do not contain the producer at all.** `smooth-criminal` is the only deck holding those
  three pairs, and `dies:creature` carries every one. **CHECK THAT A DECK CONTAINS BOTH CARDS BEFORE
  READING A MISSING CLAIM AS A MISSING CHANNEL.**
- **THE REAL COUNT FELL WITHOUT A RECALL LOSS, and that distinction is the whole verdict.** Each of
  the three removed REAL rows was a `graveyard-recursion:any` DUPLICATE of a surviving
  `dies:creature` row on the same pair — checked pair by pair, not inferred. The only relation
  genuinely gone is Kefka → Marchesa, which is FALSE. **A panel counts claim ROWS, so a tag
  consolidation reads as lost recall; look at the pair before believing it.**
- **MEASURED: edges 36,197 → 35,768 (−429), reasons 45,529 → 44,821 (−708), MESHED 288 unchanged,
  build categories 0 moved, ratings 1,102 of 6,236 (mean |Δ| 0.0467), TOP card 5/71.** Two tests,
  both proven to fire.

**"EXILED WITH THIS CARD" IS A SET ONLY THE PRODUCER CAN ENUMERATE — panel family B (2026-08-20).
PANEL 88.4% → 88.8% [85.5, 91.4], false 53 → 51, real 403 UNCHANGED, debt 0.**
- **ALL FOUR SELF-TRIGGER FALSES ALREADY CARRIED `self: true`** — derivation was right and the leak
  was matcher-side, which is the opposite of where I looked first. Reading the four showed **four
  DIFFERENT root causes**, not one family, so only two were closable together.
- **THE CLOSABLE PAIR.** Gisa, Glorious Resurrector and The Darkness Crystal both emit a bare
  `enters: creature` for "put all creature cards exiled with <me> onto the battlefield", so they
  claimed to fire the self-ETB of every creature in the deck. **185 corpus cards say "exiled with",
  10 of them derive an enters/cast emit.**
- **GATED ON THE OPPONENT CUE, AND MEASURING IS WHAT FORCED IT: refusing all ten would delete REAL
  claims.** Ghost Vacuum exiles from ANY graveyard and Colfenor's Urn exiles YOUR OWN dying
  creatures, so both really can return a deck-mate and fire its trigger. Gisa and The Darkness
  Crystal are replacement effects on a creature **an opponent controls** dying, so the set can never
  hold a card of yours. TWO printed facts are required, and either alone is wrong.
- **MEASURED: edges 36,214 → 36,197 (−17), reasons 45,627 → 45,529 (−98), MESHED 288, build
  categories 0 moved, ratings 514 of 6,236, TOP card 2/71.** Test proven to fire.
- **THE OTHER TWO ARE DIAGNOSED AND NOT FIXED, each needing its own slot.** **Junji → Hidetsugu and
  Kairi**: Junji returns "target **non-Dragon** creature card" and **Hidetsugu and Kairi is a
  Legendary Creature — Ogre Demon Dragon**, so the exclusion is real and printed — but `notType`
  holds card TYPES and Dragon is a SUBTYPE. **`notSubtype` is the missing slot, and it is the SAME
  one CR 700.16 `worthy` needs** ("isn't a Villain"), so one field closes two items.
  **Abstruse Appropriation → Nulldrifter**: "you may cast THAT card for as long as it remains
  exiled" — the antecedent restriction is dropped and the emit reads as a class; 14 corpus cards.
  **Chaos Warp → Demonic Junker**: "the OWNER of target permanent … they put it onto the
  battlefield" is a random top card under someone else's control, and the emit carries neither fact.

**A NEGATIVE MODIFIER IS NOT AN ANTHEM, AND AN ABILITY DISCOUNT NEEDS AN ABILITY — panel family E
(2026-08-20). PANEL 87.8% → 88.4% [85.1, 91.0], false 56 → 53, real 403 UNCHANGED, debt 0.**
- **`debuff` IS A NEW EFFECT KIND, fixed at the SOURCE because FIVE readers were wrong.** `modify-pt`
  mapped to `pump`, which is right for "+2/+2" and the exact opposite of the truth for "-2/-2" —
  **measured, 30 of 301 derived pump abilities (10%) carry a negative amount** (Massacre Wurm, Toxic
  Deluge, Heartless Summoning, Doomwake Giant, Tragic Slip), against 186 corpus cards printing
  "get -N/-N". `mechanisms.ts` used `pump` for FOUR archetypes and `wincon.ts` for the go-wide
  finisher, so a matcher-side gate would have left every one of them wrong. Weight 0.4, between
  `damage` 0.2 and `forced-sacrifice` 0.6.
- **RELABELLING IS ONLY HALF A FIX, and the panel said so immediately**: with the new kind alone,
  Curse of Death's Hold → Entity Tracker survived with a NEW TAG and turned a judged FALSE into
  judging DEBT. The static applies-to pass says "P's static applies to C", which claims C is
  IMPROVED, so a `debuff` must form NO edge — what these cards relate to is the OPPONENT'S board,
  which has no node.
- **REFUSING FORENSIC GADGETEER OUTRIGHT WAS BUILT FIRST AND WAS WRONG — it deleted THREE REAL
  CLAIMS.** "Activated abilities of artifacts you control cost {1} less to activate" really does
  discount Thought Vessel, Transmutation Font and Dross Skullbomb, each judged REAL with the note
  "X is an artifact with activated abilities". What it cannot reach is a consumer with NO activated
  ability — Servo Schematic (all triggered) and Stridehangar Automaton (all static), the two judged
  FALSE. **So the printed cue selects the PRODUCER and the CONSUMER'S OWN ABILITY LIST decides.**
  2 derived cards, 83 corpus cards printing "less to activate".
- **MEASURED: edges 36,253 → 36,214 (−39), reasons 45,759 → 45,627 (−132), MESHED 288 unchanged,
  ratings 487 of 6,236 (mean |Δ| 0.0159), TOP card 2/71.** `build-population.ts` moved **exactly one
  membership — `wincon:go-wide` 18 → 17, dropping Massacre Wurm and Heartless Summoning**, which is
  the archetype gate confirming the fix rather than a regression: neither is a go-wide finisher.
- **WHAT IS LEFT OF FAMILY E, and neither is a code fix.** **Sludge Monster ×2** says "base power and
  toughness 2/2" — a SET, not a modifier, with a POSITIVE amount, so the sign rule cannot see it;
  **14 derived cards** share that shape and a blanket refusal would delete Mirror Entity, whose
  "creatures you control have base power and toughness X/X" is a real finisher. **Eluge → Pongify is
  a STALE VERDICT, not a defect**: the note reads "Eluge reduces no costs", which was true of the
  derivation when it was judged and is false now — Eluge derives a real `cost-reduction` on the first
  instant or sorcery each turn, and Pongify is an Instant. It needs an owner re-judge.

**AN UNSTATED CONTROLLER IS YOU, NOT A WILDCARD — the panel's biggest genuinely-broken false family,
measured and closed (2026-08-20).** `parseControl` returns `any` for anything it cannot read and
`matcher/subject.ts` treats `any` as a PERMISSION, so YOUR OWN draw satisfied "whenever an OPPONENT
draws a card". **Measured: 3,157 of 4,137 emits (76%) carry `any`, against 108 triggers demanding an
opponent.** Derive-side, free, `DERIVE_VERSION` 58 → 61.
- **PANEL 86.5% → 87.8% [84.5, 90.5], false 63 → 56, real 403 UNCHANGED, debt 0.** Seven claims
  removed, **all seven FALSE and none real** — Orcish Bowmasters ×2, Mind's Eye ×2, Faerie
  Mastermind, Bloodchief Ascension, Revel in Riches.
- **A BLANKET DEFAULT WOULD BE WRONG, which is why `CONTROLLER_DEFAULT` is a VERB LIST.** Of the
  6,729 `any` action objects in the clause corpus **6,503 (96.6%) name no player at all** — but
  "destroy target permanent" and "tap target creature" name none either and are routinely aimed at an
  OPPONENT'S board. Only verbs the rules pin to the controller are in: draw · mill · discard ·
  sacrifice (CR 701.17a — you may sacrifice only what you control) · search · scry · surveil ·
  add-mana · create (CR 111.2) · gain-life · lose-life. **`enters` is deliberately OUT**: a fetchland
  and Chaos Warp ("the OWNER … puts it onto the battlefield") cannot share a default.
- **READ THE SENTENCE, NOT THE OBJECT — the first cut cost two REAL claims and the measurement is the
  only reason I know.** Testing `action.object` alone deleted **Dark Deal → Orcish Bowmasters** and
  **Ruin Grinder → Scrawling Crawler**: both are "EACH PLAYER draws", the action object is merely
  "cards", and the player is named earlier in the sentence. Arm 1 scored 87.9% by deleting them; arm 2
  scores 87.8% and keeps them, **which is the better arm** — precision that rises by deleting real
  claims is the Goodhart failure this panel exists to catch.
- **AN EMPTY CLAUSE TEXT IS "SAY NOTHING", NEVER "GUESS YOU".** `deriveAbilities` passes `""` when a
  clause has no text, so the guard tests TRUTHINESS and not `!== undefined` — the first version fired
  on the empty string and broke the standing Pongify case, whose Ape token goes to the destroyed
  permanent's controller and reads as yours from the object text alone.
- **MEASURED: edges 36,660 → 36,253 (−407), reasons 46,323 → 45,759 (−564), MESHED 288 unchanged,
  build categories 0 moved, ratings 398 of 6,236 (mean |Δ| 0.0134), partner counts 375, TOP card
  2/71, top-10 17/71.** The removal profile is the proof: every top consumer is opponent-facing —
  **Faerie Mastermind 114 · Orcish Bowmasters 100 · The Unagi of Kyoshi Island 44 · The Meathook
  Massacre 35 · Mind's Eye 35 · Kamber 26 · Zurzoth 24 · Bloodchief Ascension 24**, each verified
  against printed text.
- **FIVE OF THE ELEVEN SURVIVE, each on a verb held back on purpose**: Misty Rainforest → Deep Gnome
  Terramancer and Gisa → Demon's Disciple need `enters`; The Sibsig Ceremony → Massacre Wurm needs
  `dies`; The Water Crystal → Nashi needs graveyard-recursion; and **Priest of Forgotten Gods →
  Orcish Bowmasters is the guard being conservative** — its clause says "each opponent sacrifices …
  you draw a card", so a player IS named in the sentence and the default correctly declines.
- **THE PANEL'S FALSE FAMILIES ARE NOW ENUMERATED** (all 63 read and grouped, 2026-08-20): consumer
  qualifier / intervening-if **25** · control **11 → 5** · cost-reduction "applies to any ordinary
  card" **9, the price of the owner's own 2026-08-18 ruling, not a bug** · origin (a fill is not a
  death) **7** · wrong effect kind **6** · self-trigger leak **4**. Fixing all of them reaches ~100%,
  but **judge error is 25-40%**, so the last families are inside the rubric's own noise — the third
  blind draw comes before chasing them.

**A CLASS SUBJECT SATISFIES A `historic` DEMAND NOW, AND THE FIX WAS TWO HALVES — the second one only
measurement could find (2026-08-20, roadmap C2c).** `historic` is stamped off a card's own type line,
so only a SELF event could ever satisfy a historic demand: "whenever an ARTIFACT you control dies" is
historic supply by CR 700.6 and did not match. Matcher-only, free, no re-derive.
- **`impliesHistoric` reads a CLASS subject, and EVERY MEMBER OF AN OR-LIST MUST QUALIFY.** `type` is
  an OR (`expandTypes` unions it), so `{type: [artifact, creature]}` is satisfied by a plain creature
  and guarantees nothing; `allTypes` is an AND, so one member is enough. **Measured against a `some`
  reading: it buys 4 claims and ALL FOUR ARE WRONG** — Evereth, Viceroy of Plunder ("sacrifice
  another creature or artifact"), Midgar, City of Mako ("an artifact or creature"), and Tibalt's
  Trickery, an INSTANT answering a historic-SPELL trigger.
- **THE SECOND HALF: `selfFillTypes` skipped the stamp whenever the clause had already named a
  type.** A card saying "sacrifice THIS CREATURE" arrives with `type: creature` and hit the early
  return, so **Burnished Hart — an ARTIFACT CREATURE — put an artifact in the graveyard and no
  historic payoff could see it.** Otawara, Boromir and Sojourner's Companion are the same shape.
  `self` means the object IS this card, so its printed SUPERTYPES are known facts whatever noun the
  clause used; the flags are now stamped on both paths. **The first measurement hid this**: it read
  32 new claims, and the halves together read 62.
- **MEASURED: edges 36,614 → 36,660 (+46), reasons 46,261 → 46,323 (+62), MESHED 288 unchanged,
  panel 86.4% → 86.5% [83.1, 89.3], real 401 → 403, FALSE 63 UNCHANGED, debt 0** — a recall win at
  no precision cost, and both new panel claims already carried a cached REAL verdict. Build
  categories 0; ratings 26 of 6,236 (mean |Δ| 0.0010), partner counts 46, TOP card 0/71, top-10 2/71,
  facets 0. **All 62 claims were enumerated and read against printed text**, every one an artifact or
  legendary hitting a graveyard. Witnesses: Abstergo Entertainment 2.5 → 3.3 in `voltron-mill`, The
  Capitoline Triad 3.1 → 3.5. Four tests, both halves proven to fire.
- **TWO ROADMAP LINES WERE FALSE AND ARE TICKED.** "Keyword-action gap list — our canonical verbs
  cover 18; investigate · populate · explore · goad · connive · discover · incubate · plot · forage ·
  venture all canonicalize to nothing" had been wrong since 2026-08-15, when the whole CR 701 list
  became verbs — and it nearly got re-ranked as live work today. The keyword census entry below it
  is now scoped to what a keyword EMITS, since both vocabulary halves are closed. **A stale roadmap
  line is not inert: it is a map of where the work used to be.**

**TRIM MODE — "I'M 5 OVER, WHAT GOES?" — AND THE SOL RING FAILURE CAME BACK ONE LAYER UP
(2026-08-20, roadmap D2).** `cutCandidates` FILTERS, so it returns 0-12 rows and is **empty on 18 of
the 71 decks**: the right answer to "is anything here doing nothing" and no answer at all to "I must
cut five". `trimOrder` ranks every cuttable card weakest-first and always has an Nth row, each
carrying what argues it STAYS. `report.trim` ships the WHOLE order so a caller slices its own N
without a round trip; CLI `--trim N`, web a 3/5/10 control.
- **MEASURED: 0 decks short of 5 rows against 18 with an empty cut list; mean 61.6 rows per deck;
  unprotected rows median 2, min 0, max 14; 51 of 71 decks have FEWER THAN 5 unprotected rows**, so
  the mode routinely has to show a protected card and say why.
- **THE FIRST CUT REBUILT THE SOL RING DEFECT, and only the 71-deck run showed it.** Letting an
  over-target category STRIP a role's protection made `burakos-crashing-the-party`'s top five its
  ENTIRE RAMP PACKAGE — Honor-Worn Shaku, Arcane Signet, Dark Ritual, **Sol Ring**, Springleaf Drum —
  all rated 0.0, all tied, their order decided by mana value. The deck really is ramp 16/10 and
  "cut five ramp cards" is defensible arithmetic, but **"cut THESE five" is an attribution nothing
  here can make**. A role now protects whatever the count says and the surplus rides on the
  protection TEXT: "fills ramp — ramp is at 16 against a target of 10, so there is room here".
  Pinned by a test proven to fire.
- **THE SURPLUS IS USED HERE AND REFUSED IN `cutCandidates`, deliberately.** A caller who ASKED for
  five rows is getting five, and refusing the surplus entirely would rank a surplus-category rock
  level with an under-target draw spell. Order within a category is still rating, partners, cost,
  then NAME — so the renderers say outright that tied rows are alphabetical and that nothing ranks
  two ramp cards against each other.
- **Gates untouched, as they must be for a pure read over existing inputs: 36,614 / 46,261 /
  MESHED 288, panel 86.4% debt 0, ratings 0 of 6,236.** Verified in a live browser: four unprotected
  rows then a combo piece shown WITH its protection. Eight tests.

**CR 700.9 `modified` AND 700.12 `outlaw` ARE SUBJECT FILTERS NOW, AND NOTHING MOVES — WHICH IS THE
ACCEPTANCE TEST (2026-08-20, roadmap D1a).** Derive-side, so FREE: `DERIVE_VERSION` 56 → 57, 2,745
rows re-derived, no spend. Both NARROW a subject that without a slot reads as its bare noun — "other
outlaws you control have haste" (Vihaan, Hellspur Posse Boss) is an anthem on EVERY creature, the
Favorable Winds shape `SubjectFilter.keyword` was built for.
- **`outlaw` IS A PRINTED FACT AND IS SET ON BOTH SIDES.** CR 700.12 is five creature types wearing
  one noun (Assassin, Mercenary, Pirate, Rogue, Warlock), so `isOutlaw` reads the type line beside
  `isHistoric`. **20 corpus cards, ZERO in the derived corpus** — pure insurance, and it cost nothing
  to buy.
- **`modified` IS A BOARD STATE AND IS DEMAND-ONLY.** CR 700.9 is "has a counter, is equipped, or is
  enchanted by an Aura its controller controls" — the same class as `counter`, whose arrival broke
  three identity gates comparing it against a type line. No printed producer can state it, so the
  family UNDER-CLAIMS on purpose: "whenever a MODIFIED creature you control deals combat damage" is a
  false sentence about an unmodified creature. **44 corpus cards, 40 with a "modified <noun>"
  subject, 2 derived.**
- **THE SUPPLY SIDE WAS SCOPED AND THEN DROPPED ON A MEASUREMENT.** CR 700.9's one printed case is a
  permanent that ENTERS WITH COUNTERS, which lives in the card's ABILITIES while `impliedEvents` sees
  only `Characteristics`. Widening that signature buys **zero edges**: the only derived consumer of a
  `modified` subject is Kodama of the West Tree, whose trigger is `combat-damage`, and
  **`combatSelfSupplied` already refuses every baseline producer for it** — Kodama forms **0
  combat-damage reasons** either way (its 13 reasons in `bant-1-1` are all `enters`/`ramp-target`/
  `cast`). **Check whether an existing gate already neutralises the defect before building the half
  that would fix it.**
- **MEASURED: 36,614 / 46,261 / MESHED 288 byte-identical, panel 86.4% [83.0, 89.2] real 401 / false
  63 / uncertain 24 debt 0 identical, build categories 0, ratings 0 of 6,236.** Four tests, the
  outlaw gate proven to fire.
- **`worthy` (CR 700.16) REFUSED ON SHAPE, not on its 3-card count**: "legendary, isn't a Villain, and
  is red and/or white" decomposes into `legendary` + `colors` + a NEGATED SUBTYPE, so the honest gap
  is a `notSubtype` slot rather than the noun.

**THE CR VOCABULARY SWEEP RAN, AND THE RULES UPDATE WAS A NULL RESULT (2026-08-20, roadmap D1's
first half — NO SPEND).** `fetch-comp-rules.ts` pulled **MagicCompRules-20260819** over the cached
20260807. Regenerated and diffed: **keyword actions 69 → 69, keyword abilities 194 → 194, CR 7xx
sections 147 → 147 — nothing added, removed or retitled.** The word lists are complete against the
CURRENT game, not merely against the version they were built from, and that is the first recheckable
null result this project has for a rules update. → `specs/2026-08-15-vocabulary-completeness-stub.md` §8
- **CR 703 (turn-based actions) and CR 116 (special actions) — the two axes the stub recorded as
  NEVER SWEPT — are swept, all 13 and all 12 rows judged.** Scheme-setting, rolling to visit
  Attractions, the planar die and conspiracies are excluded on **LEGALITY**, never on demand.
  Damage removal (703.4p) and mana emptying (703.4q) carry **0 corpus cards** in a trigger clause.
  The only real hole 116 leaves is `plot`, already tracked as MTGJSON lag.
- **FIVE TRIGGER WORDS ADDED, counted over the WHOLE ~34k corpus per the 2026-08-15 ruling.**
  `put-into-graveyard` **104 cards / 44 in a trigger clause** · `tapped-for-mana` 32/23 ·
  `loses-control` 10/8 · `phases-in` 45/6 · `becomes-crewed` 4/4. **The graveyard one is the batch:**
  `dies`, `milled` and `discarded` split ONE event by ORIGIN, so a card watching every origin had to
  pick one and be wrong about the rest — Syr Konrad, the Grim names three in a single line. It maps
  to **`enters-graveyard`, which is origin-blind**; mapping it to any one origin would be a narrower
  claim than the card makes. **The other four map to NOTHING on purpose** and land in
  `unknownTriggers` — a refusal beats a near-miss. `TRIGGER_VOCAB_VERSION` 11 → **12**;
  `NORMALIZE_MIN_COMPATIBLE` untouched, nothing re-bought.
- **THE ENLARGED PROMPT IS PRICED, which the stub asked for and nobody had done: +79 characters,
  ≈22 tokens per card, ≈$0.05 over the 2,665-card scope and ≈$0.60 over a whole-corpus 34k re-buy.**
  The completeness argument costs under a dollar even at full scale. Dry run at the new vocabulary:
  19 cards / $0.07, **not spent**.
- **A GENERATED FILE WAS HOLDING HAND-WRITTEN CODE, and regenerating deleted it.** `gen-vocabulary.ts`
  rewrote `derive/subtypes.ts` and **removed `joinMultiWordSubtypes`** — the Time Lord splitter from
  A6, typed into a generated file on 2026-08-19 — taking **3 importers and 20 tests red at once**.
  It is emitted by `renderSubtypesModule` now. **Anything that belongs beside generated data belongs
  in the RENDERER, never in its output**, and nothing caught it for a day because the generator only
  runs when the rules or MTGJSON move.
- **THE SHRINK-ONLY RATCHET FIRED IN THE DIRECTION NOBODY USUALLY SEES**: MTGJSON caught up on
  `storied` (keyword abilities 222 → **223**) and `cr-completeness.test.ts` failed until the entry was
  DELETED from `MTGJSON_LAG`, which is exactly what a stale-exemption guard is for. Lag 6 → 5, then
  **→ 0 the same day: `gen-vocabulary.ts` now UNIONS MTGJSON's catalogue with the CR's own 702
  headings**, so the lag class cannot refill. **223 → 228** — plot · space sculptor · start your
  engines! · visit · ∞. Three heading shapes normalize and all three are live: "Daybound and
  Nightbound" is ONE heading for TWO keywords, "∞ (Infinity)" loses its gloss because the card prints
  only the symbol, everything else is verbatim including the "!" in "Start Your Engines!".
  **THE CARD COUNTS FILED WITH THAT ITEM WERE WRONG BY TWO ORDERS OF MAGNITUDE**, from the
  oracle-text probe whose own header warns it is an upper bound: on the `keywords` field, **start
  your engines! is 46 not 238, plot 40 not 43, and visit / space sculptor / ∞ are ZERO** — visit is
  Attractions-only (CR 702.159a) and space sculptor is one card, so neither is in the gameplay corpus
  at all. **"Count the consumers, not the printed word", a third time, and this time inside my own
  write-up.** Buys nothing today by construction (0 corpus cards say "with plot" or "with visit") and
  the re-derive proves it: `DERIVE_VERSION` 57 → **58**, population **36,614 / 46,261 / MESHED 288**,
  panel **86.4% debt 0**, ratings **0 of 6,236**, all byte-identical.
- **THREE STATUS ROWS IN `cr-section-sweep.ts` WERE STALE, so the coverage instrument understated the
  engine** — 903 said `SubjectFilter.commander` "does not exist" (shipped 2026-08-15), 704 listed the
  legend rule as open (closed 2026-08-19), 707 claimed nothing of CR 707.2 was modelled (same day).
  OPEN rows **18 → 15**. **A coverage instrument nobody re-reads becomes a map of where the work USED
  to be.**
- **LEFT OPEN AND NOT GATED ON THE BUY**: `modified` (49 cards), `outlaw` (20) and `worthy` (2) are
  CR 700.x concepts with no `SubjectFilter` slot — and a subject filter is DERIVE-side, hence FREE,
  so they never needed the corpus buy at all.

**A COUNT NARROWED BY `historic` IS NOT A WILDCARD — the scaling loop was refusing a calibration
deck's own COMMANDER (2026-08-20, roadmap C2b).** "This spell costs {1} less to cast for each
historic card in your graveyard" derives `{historic: true, zone: graveyard}` with no type and no
subtype, and the untyped-count guard — written to refuse Riverchurn Monument's honestly untyped
"cards in their graveyard" — refused it too. **THE POPULATION IS EXACTLY THREE untyped scaling
subjects in the derived corpus**: Riverchurn (still refused, correctly), Rite of Flame (`named`,
refused on purpose) and The Capitoline Triad. One condition, matcher-only, free, no re-derive.
- **MEASURED: edges 36,603 → 36,614 (+11), reasons 46,248 → 46,261 (+13), MESHED 288 unchanged,
  panel 86.4% [83.0, 89.2] real 401 / false 63 / uncertain 24 DEBT 0 BYTE-IDENTICAL,
  `build-population.ts` 0 card-category memberships moved, ratings 7 of 6,236 (mean |Δ| 0.0002),
  raw scores 14, partner counts 9, TOP card 0/71, facets 0.** All 13 claims sit in
  `the-capitoline-triad`; the commander goes **2.4 → 3.1**. Urza's Saga sacrifices ITSELF, so a Saga
  really does hit the graveyard; the mills and discards reach it through the untyped-PRODUCER
  wildcard `graveyardFillMatches` already grants every typed demand.
- **THE TAG KEYS `scales:any` ON PURPOSE.** `themeSubjectKey` has no `historic` branch and adding one
  would re-key two OTHER live subjects (Abstergo Entertainment, Samwise Gamgee) — frozen-panel debt
  for a cosmetic gain. The guard is what refuses a wildcard; the key is a grouping label.
- **THE WIDER READ WAS BUILT, MEASURED AND FILED RATHER THAN SHIPPED.** `historic` is stamped only by
  `selfSubject`, so "an **artifact** you control dies" — historic supply by CR definition — fails the
  check while the same card putting ITSELF in passes. Teaching `subjectMatches` the type inference
  takes the same deck **13 → 21**, but it widens a shared predicate for every historic consumer
  (Jhoira, Basim, Glóin, Rona, The Sixth Doctor) and needs its own before/after. → roadmap C2c.
- **C2a REFUSED IN THE SAME PASS, on the count the roadmap asked for.** A STAT-GATED self reduction
  would discriminate, and there are **2 of 364 self-reducers corpus-wide**: Bolt Bend (a binary gate)
  and Spectral Denial (a per-creature count). The other three cards printing both cues are not the
  shape — Orysa gates on a board TOTAL (every creature contributes, the Great Henge defect), Rally
  the Monastery's stat clause is an unrelated MODE, Not of This World gates on the countered spell's
  target. **The blocker is not the subject slot**: Bolt Bend derives `scaling` and `scalingSubject`
  both UNSET because its reduction is a CONDITION on a fixed amount, and the condition channel was
  measured and refused on 2026-08-15. `reducesItself` refuses it today and that refusal is correct.
  If the family grows, build Spectral Denial's shape — it needs no condition slot.

**`edges.ts` WAS NOT GREPPABLE FOR MONTHS.** `claimCount` used a literal NUL byte as its
composite-key separator, so `file` reported the source as `data` and **grep skipped the matcher's
biggest file without saying so** — three searches returned "no matches" against 1,301 lines that
plainly contained the term. Now the `\u0000` escape: same string at runtime, file stays text.
**A silent empty grep is indistinguishable from a real absence — check `file` when a search of a
known-large file comes back clean.**

**WHAT YOUR LIBRARY IS WORTH TO A PAYOFF THAT READS A RANDOM CARD OFF THE TOP — AND WHY IT IS NOT AN
EDGE (2026-08-19, roadmap C5).** Hidetsugu and Kairi's death trigger exiles the top card, drains for
its MANA VALUE, and free-casts it if it is an instant or sorcery. That is why its deck runs **21 of
63 nonlands at mana value 6 or more, every one an instant or sorcery** — a deliberate construction
the engine said nothing about.
- **THE PAIRWISE EDGE WAS CONSIDERED AND REFUSED ON THIS REPO'S OWN RULE, and the refusal is the
  interesting half.** A tutor earns its edges by CHOOSING — which is exactly why a bare-type tutor
  ("a creature card") is already refused for naming no particular card. **This trigger chooses
  nothing**: the card exiled is whatever is on top, so a claim about Skull Storm would be true of
  every instant and sorcery in the deck equally, which is the registered "a claim that applies to a
  card merely for being an ordinary card is false". The only thing separating Skull Storm from
  Pongify here is MAGNITUDE, and magnitude has no channel that ships — the supply:demand discount has
  been refused three times. So it is reported at DECK level naming no member, the same shape
  `deckSlack` uses for a surplus after the cut list flagged Sol Ring.
- **BOTH MEANS, NEVER ONE.** `meanManaValue` counts the WHOLE library because **a land really is
  exiled and really does drain 0**, and `nonlandMeanManaValue` says what a hit is worth when it is
  not one. The owner's "READ MV WITHOUT LANDS, ALWAYS" ruling is about a QUALITY DENOMINATOR, where
  no cost term could ever touch a land; this is the opposite situation and dropping lands here would
  flatter the deck by 53%. `landShare` states how often the trigger pays nothing.
- **MEASURED: 8 of 71 decks carry such a card, 9 rows** — Hidetsugu ×2, Keen Duelist ×5, Sin Prodder,
  Yuriko, the Tiger's Shadow. Witness: **2.63 mana a hit, 4.02 when it is not a land, 37% of the time
  it is one, 46% of the library instant-or-sorcery**. The predicate is two PRINTED facts —
  "top card of **your** library" plus an ability amount naming a mana value — which is what excludes
  the 25-card mana-value family's other members: an opponent's library (**Nashi** exiles "the top card
  of EACH PLAYER's library"), a spell being cast (Imp's Mischief, Thor), a permanent you sacrifice
  (Imskir Iron-Eater, Megatron). **Keen Duelist is kept and it is symmetric** — your top card sets
  what the OPPONENT loses — so the number is right and the card's own downside is not modelled.
- **POPULATION, PANEL AND MESH BYTE-IDENTICAL (36,603 / 46,248 / 288, 86.4% debt 0), which IS the
  acceptance test**: this forms no edge and reads no rating. Rendered in the CLI (`=== Off the top
  ===`) and in the web panel under "How you win", **verified in a live browser**, 5 new tests.

**A COPY OF A LEGEND FIRES ITS ENTRY TRIGGER AND THEN DIES TO A RULE NOBODY PRINTS (2026-08-19,
roadmap C4). FOUND BY THE OWNER, on their own deck, and the engine was reading a clone deck as a
reanimator deck.** Two facts follow from one copy effect and the engine could state NEITHER.
**CR 707.2**: the copy has the copied card's abilities, so the copied card's OWN entry trigger fires
again. **CR 704.5j**: if the copied permanent is LEGENDARY, a STATE-BASED ACTION immediately puts one
of the two into the graveyard, firing its own death trigger. The legend rule is printed on no card,
so no amount of clause work can ever reach it — the same shape as a Saga's own sacrifice
(`sagaEvents`), except a Saga at least prints a reminder. One pass in `directedReasons`; matcher-only,
free, no re-derive, no spend.
- **THE DECK MEASURED FIRST, WHICH IS WHY THE SCOPE IS RIGHT.**
  `hidetsugu-and-kairi-like-to-multiply` runs **28 cards carrying a permanent-copy cue and 18 of them
  had degree ≤ 1** — their only edge a medallion cost reduction. Its commander rated **3.29 with 17
  partners**, every one reanimation, sac outlets or medallions; **not one clone relation**. It now
  rates **5.27 with 38** and is the deck's top card, reading "Stunt Double copies Hidetsugu and Kairi;
  the legend rule puts one of them into the graveyard, triggering its death ability".
- **MATCHED ON THE PRINTED CUE, NOT AN EFFECT KIND, and that was measured rather than assumed.**
  The derived kinds do not separate the family: **Rite of Replication derives `token-generation`
  byte-identically to a 1/1 Soldier maker**, and the one structural marker (`scope: "target"` with
  `token: true`) reads **38 corpus cards of which 4 are Role/Aura tokens ATTACHED to a target**, not
  copies of it. `token` is DROPPED from the type test because the field means opposite things on the
  two shapes — what is CREATED on "create a token that's a copy of target creature", what is COPIED
  on a populate effect. Population: **497 corpus cards carry a copy cue (1.4%)**.
- **THREE REFUSALS, EACH FOUND BY A FALSE CLAIM ON THE PANEL, NOT BY READING.** (1) A
  **TYPE-REWRITING** copy: Astral Dragon copies a NONCREATURE permanent and derives
  `{type: creature, subtype: dragon}` — the token, not the target — so it claimed every Dragon in a
  Dragon deck and was the single biggest rating mover before the guard. Additive wording is left
  alone on purpose (Phantasmal Image's "an Illusion IN ADDITION TO its other types" is still a copy
  of the creature), as is a stat-only change (Saw in Half's halved power). (2) **POPULATE** copies a
  TOKEN, never your commander. (3) An **UNTYPED** copy subject: Court of Vantress derives TWO
  `token-generation` abilities and only the second names `["artifact","enchantment"]`, so taking the
  first made it claim to copy a creature. Only a `clone` static defaults to creature — Stunt Double
  derives no subject at all and every printed member of that family copies a creature at minimum.
- **THE WIDER READ WAS BUILT AND REFUSED ON ITS OWN MEASUREMENT.** Merging EVERY typed branch into
  an `anyOf` recovers Saheeli's Artistry's creature mode (its artifact mode derives first) and costs
  a panel FALSE — **86.4% → 86.2%, false 63 → 64**. Under-claiming is the correct failure direction,
  so it is reverted with the number in the code rather than tuned.
- **MEASURED: edges 36,041 → 36,603 (+562), reasons 45,396 → 46,248 (+852), MESHED 288 UNCHANGED,
  panel 86.4% real 399 / false 63 / uncertain 24 IDENTICAL with DEBT 2, `build-population.ts` 0
  card-category memberships moved, headline 0 of 71 and cohesion 0, ratings 417 of 6,236 (mean |Δ|
  0.0243, raw scores 369, partner counts 342), TOP card 2/71, top-10 14/71.** Suite green; 7 new
  tests and **both gates PROVEN TO FIRE** (removing the legendary gate fails the nonlegendary test,
  removing the enters gate fails the "becomes a copy" one).
- **DEBT IS 0 AND THE FAMILY COST NO PRECISION — both claims judged REAL, at the owner's request.**
  **Panel now reads 86.4% [83.0, 89.2], real 401 / false 63 / uncertain 24** (was real 399 with the
  two unjudged). `Inalla → Thundertrap Trainer`: the Trainer is a nontoken Otter WIZARD, so eminence
  makes a token copy and CR 707.2 fires its "look at the top four" a second time. `Saw in Half →
  Ojer Axonil`: **the RELATION is real and the MECHANISM is partly mis-stated** — the death trigger
  fires from the DESTROY, and the two half-size token copies are both legendary so 704.5j then fires
  it again. Judged real on the Goldspan Dragon / Terror of the Peaks precedent (a plain-body reading
  survives a mechanism change). The second firing returns nothing: a token that leaves the
  battlefield ceases to exist (CR 111.7).
- **CEILINGS, all single cards, all in the code**: `control` is ignored, so a card making an OPPONENT
  copy something over-claims — gating it would delete Saw in Half's real claim to buy Espers to
  Magicite's false one, 1-1; Coiling Rebirth's copy is conditioned on the creature NOT being
  legendary, which no derived field records; Mirage Mockery stays refused by a DERIVE-side defect
  ("artifact or creature" derived as `allTypes`, demanding both).

**AN MDFC WAS BEING PAID FOR TWICE, AND THE TYPE LINE CANNOT TELL ONE FROM A TRANSFORM CARD
(2026-08-19, roadmap E2/E3/E4 — the follow-ups to E1 below).**
- **E4, the one with a measured population.** `landInputs` hardcoded `mdfcUntapped: 0` /
  `mdfcTapped: 0` while Karsten's published coefficients (−0.74 untapped, −0.38 tapped) sat unused,
  AND `isLand` is a type-line test, so an "Instant // Land" counted as a FULL land in `actual` —
  while `producedMana` carries the BACK face's colour, so a cheap one also reached the
  `rampPlusDraw` accelerant net. **`layout` IS THE GATE and measuring forced it**: the type-line
  test alone also catches **Treasure Map, Dowsing Dagger, Thaumatic Compass, Ojer Axonil and Growing
  Rites of Itlimoc**, every one a TRANSFORM card whose land back is reached by transforming a
  permanent already in play and can never be PLAYED — the same distinction `FRONT_FACE_ONLY` draws
  in derive. The ZNR pay-3-life cycle counts UNTAPPED; that is the cycle the coefficient was fitted
  on. **MEASURED: 55 of 71 decks run a land-back MDFC, target moves in all 55, mean −1.49, largest
  −4, mean (actual − target) −0.87 → −1.68** — decks read slightly MORE land-short, which is right,
  since a tapped MDFC is 0.38 of a land and was credited as 1.0. **TWO NOTIONS OF "LAND" NOW COEXIST
  ON PURPOSE**: the type-line one every other reader uses, and the regression's, where an MDFC is a
  spell worth a fraction of a land. `build.ts` keeps its own `lands` category and `BASE_TARGETS`, so
  `buildScore` is untouched.
- **E2: cost reached the cut list as a COLUMN and a TIEBREAK, never a gate.** Ties break toward the
  EXPENSIVE card — a 9-drop nothing connects to costs a turn as well as a slot. **Candidacy
  unchanged; ORDER moved in 23 of 71 decks and the VISIBLE rows in exactly 1** (`hidetsugu` swaps
  Quasiduplicate for Stolen Identity at the 12-row limit — a tiebreak at a boundary, not a new
  candidate).
- **E3: three caveats that stated a direction now state a SIZE.** Draw understatement **+10.9pp** at
  five cards ahead, **+19.9pp** at ten, `minCopies` at 50% moving 4 → 3 (`hypergeometric.ts`) · the
  ramp → late-clock → **flattered availability** cascade, the only bias in this layer running toward
  flattering the deck (`deck-math.ts`) · **`pressure.ts` has NO MANA BUDGET AT ALL** — `MV <= turn`
  gates each creature independently, so turn 6 fields every drawn 6-drop at once, which is a deeper
  optimism than the "nobody blocks" it already stated and the reason ramp is refused there.
- **E6's other half followed: COST NOW SITS BESIDE THE RATING on the Cards table** —
  `CardSynergy.manaCost` / `.manaValue` / `.castability`, rendered as `{5}{B}{B}` over
  `22% – 40% by T7` against a synergy rating of 3.7. **A LAND OR A REFUSED COST RENDERS AN EM DASH,
  NEVER 0%** — `deckCastability` drops both, and a 0% would tell a reader they cannot cast the card.
  Two facts side by side, never multiplied: the same ruling `castability.ts` ships for its own axes,
  and the reason cost stays OUT of `synergyRating`.
- **A LIVE RUN FOUND WHAT NO TEST COULD: the panel read "33 in deck" above a chip reading
  "lands 37/36".** Both correct — Karsten prices an MDFC as a spell, the build category counts it by
  type line — and the pair reads as a broken report. The lands row now says
  "4 modal DFCs counted as spells, not lands" (`DeckMath.lands.mdfc`), rendered only when the deck
  runs one. **Run the app after a change that moves a number two readouts share.**
- Gates unchanged throughout: **36,041 / 45,396 / MESHED 288 · panel 86.4% debt 0**. Eleven new
  tests. → `specs/2026-08-19-clock-and-mana-model-review.md` §9

**A DARK RITUAL WAS A FULL BLACK SOURCE FOR A TURN-1 `{B}` DEMAND — the two castability axes counted
different universes, and the mana axis is now a RANGE (2026-08-19, roadmap E1).** Owner's observation
about ramp/rituals/fast mana/cheap draw, reviewed by Fable, measured here. →
`specs/2026-08-19-clock-and-mana-model-review.md`
- **THE DEFECT NOBODY HAD NAMED.** `cardCastability`'s mana axis counts LANDS ONLY; its colour axis
  and `manaAudit.supplied` counted **every library card with `producedMana`** — rocks, dorks AND
  one-shot rituals, at zero delay. `isManaSource` refuses an instant/sorcery unless a permanent type
  also appears on the type line (an "Instant // Land" modal DFC keeps its land half). Definitional,
  not probabilistic: a one-shot is not a source at ANY confidence. **Measured: 139 of 3,197
  `producedMana` library cards are one-shots; `supplied` moves on 103 of 153 colour rows over 53 of
  71 decks; 59 of 1,250 demands flip met → unmet (one-directional by construction); 9 colours acquire
  a `worst` row and 0 change identity.**
- **`manaWithRocks` IS AN UPPER BOUND AND MUST NEVER BECOME THE HEADLINE** — lands plus the rocks
  already castable (`manaValue < turn`), which themselves need lands, so it over-states exactly as
  lands-only under-states. Shipping BOTH turns the documented "the two biases partly cancel, which is
  exactly why neither is safe to drop" trap into a stated interval. **Width over 4,142 rows: mean
  8.7pp, median 5.2pp, p90 22.7pp, max 49.3pp; over the TOP-4 rows the reader actually sees, mean
  20.7pp** (widest `fairdrazi-5-color-less` / Breaker of Creation **16% – 65%**). Collapses to one
  figure on 18.9% of rows and the UI prints one number there rather than "78% – 78%".
- **RAMP IS REFUSED IN `pressure.ts`, AND THAT IS THE REVIEW'S MAIN RESULT.** `expectedPower` has NO
  MANA BUDGET — it deploys every drawn creature with `MV ≤ turn` simultaneously and never sums mana
  values against mana available, so ramp corrects a constraint that does not exist. **The cascade
  nobody had written down**: the ramp gap makes `measuredClock` read LATE for a ramp deck, so
  `deck-math.ts`'s `turn` is too big, so `seen(turn)` is too big, so availability is OVERSTATED —
  the opposite direction to every other stated bias, and it flatters exactly the decks the owner
  plays. Also refused, each with its reason: any ritual coefficient (wrong SHAPE — one turn's mana,
  and Karsten's 0.28 bucket already absorbs them empirically), a closed-form `seen(T)` correction
  (the panel judges CLAIMS and `ratings-compare` judges RATINGS — nothing here can judge a
  PROBABILITY, so a goldfish simulator would have to come first and would then BE the correction),
  and a tapped-land term (play policy).
- **COST AS A QUALITY DENOMINATOR: MEASURED, DECLINED FOR THE RATING, and the owner ruled on how to
  read it.** On the shipped `2 × authority ≥ score` predicate over 6,236 rated deck-cards:
  **nonland majority-payoff mean MV 3.43 vs feeder 3.04, medians IDENTICAL at 3, p75 identical at 4,
  separation entirely in the tails — MV≥5 23.0% vs 16.1%, MV≤2 34.6% vs 45.0%.** **READ MV WITHOUT
  LANDS, ALWAYS** (owner, 2026-08-19): the all-cards figure REVERSES the sign (payoffs 2.34, feeders
  2.51) purely because lands are MV 0 and classify majority-payoff more often, and no cost term would
  ever touch a land. So a multiplicative cost term fails the registered "zero majority-payoff falls"
  criterion a fourth time — but on a 0.39-mana tail difference, so it is recorded as **predictably
  fails, NOT unpassable by construction**, and no fourth sweep is worth building. Cost belongs on the
  cut list as a column and a tiebreak (never a gate) and beside the castability figure — the same
  never-multiply ruling `castability.ts` already ships for its own two axes.
- **GATES BYTE-IDENTICAL AND STRUCTURALLY SO: edges 36,041 / reasons 45,396 / MESHED 288, panel 86.4%
  [82.9, 89.2] real 399 / false 63 / uncertain 24, debt 0.** `computeDeckMath` is a leaf
  (`analyze.ts:682`) and nothing outside it imports `castability.js` or `mana-audit.js`. Six tests,
  **all proven to fire** — reverting `isManaSource` to `() => true` and loosening `manaValue < turn`
  to `<= turn` fails exactly 3.


**A COST REDUCTION NEEDS GENERIC MANA TO REDUCE — 703 reasons refused, and panel FALSE fell by 2
(2026-08-19, roadmap B3).** CR 118.7: a reduction cannot take generic mana below zero, so "spells you
cast cost {1} less" does NOTHING to a card costing exactly `{U}`. **Found by the OWNER**, from two
`uncertain` verdicts on the panel debt, not by any instrument here.
- **TWO EXEMPTIONS, BOTH REAL, BOTH MEASURED — a blanket guard would have been a wider defect than
  the one it closes.** (1) A **COLOURED-PIP** reducer genuinely discounts a `{U}` spell — Defiler of
  Flesh, Defiler of Dreams, Eluge, and Morophon's `{W}{U}{B}{R}{G}` makes it FREE. **17 claims kept.**
  (2) An **ADDITIONAL COST** is added BEFORE a reduction is subtracted — **CR 601.2f**: "the total
  cost is the mana cost …, PLUS all additional costs and cost increases, AND MINUS all cost
  reductions." **Everflowing Chalice is `{0}` with Multikicker {2}**: kicked twice it totals {4} and
  a reducer takes it to {3}. **The owner caught this mid-build after the first cut refused it.**
- **THE ADDITIONAL-COST LIST IS THE 17 KEYWORDS CR 702.x DEFINES WITH ONE**, plus `offspring` and
  `tiered`, which the owner named and which POSTDATE the rules file. **Deliberately NOT split into
  mana and non-mana**: the CR states most with a `[cost]` placeholder so the split is not verifiable
  from the rules text, and **a wrong REFUSAL deletes a real claim while a wrong keep costs a
  handful**. 120 of the 4,304 zero-generic corpus cards (2.8%) carry one.
- **`{0}` IS A NUMERIC SYMBOL CARRYING ZERO GENERIC.** The first `hasGenericMana` tested for a DIGIT
  rather than an AMOUNT and kept Mishra's Bauble, Urza's Bauble and Everflowing Chalice — 19 claims.
  Caught because the probe and the guard disagreed and the probe was right.
- **MEASURED: edges 36,595 → 36,041 (−554), reasons 46,099 → 45,396 (−703), MESHED 288 unchanged.
  Panel 86.0% → 86.4% [82.9, 89.2], real 400 → 399, FALSE 65 → 63, debt 0** — two of the removed
  claims had already been hand-judged FALSE, which is the strongest evidence available that the
  refusal is right. Ratings moved 468 of 6,236, **partner counts 398** (a partner really does vanish
  when its only relation was an impossible discount), TOP card 1/71. Themes: **0 headlines, cohesion
  0, census 0** — cost reduction carries no theme tag.
- **CEILING**: the tax interaction is unmodelled and is not modellable pairwise. A THIRD card that
  taxes the spell adds generic and the reduction bites — which is exactly why the owner judged the
  two witnesses `uncertain` and not `false`. Every claim here is about two cards.

**THE PANEL DEBT IS 0 AND THE COST-REDUCTION FAMILY IS VINDICATED — judged by the OWNER,
2026-08-19.** The 16 unjudged claims were all `static:cost-reduction`, deliberately left for the
owner because the family was admitted an hour earlier by the same reasoning that would have judged
it. **Verdicts: 14 real, 2 uncertain, ZERO false.**
- **PANEL NOW READS 86.0% [82.6, 88.9], real 400 / false 65 / uncertain 26, DEBT 0** (was 85.6%
  [82.0, 88.5], real 386 / false 65 / uncertain 24, debt 16). **Quote 86.0% debt 0 from here on** —
  the figure is a bounded interval again rather than one with a hole in it, and `false` did not move
  by a single claim.
- **THE JUDGING PAGE JOINED FACTS THE TOOL'S OWN WORKSHEET DOES NOT CARRY.** `panel-score.ts
  --worksheet` writes producer/consumer/tag/claim plus ORACLE TEXT only, and a cost-reduction
  judgement turns on the consumer's COLOUR, TYPE and MANA COST. Also: the flag is `--worksheet`;
  `--rejudge` dumps every live claim (~475), and the roadmap had named the wrong one.
- **A REAL DEFECT FELL OUT OF THE TWO `uncertain` VERDICTS, and it is the owner's own rules
  knowledge, not the engine's**: **a cost reduction cannot reduce a cost with NO GENERIC MANA**
  (CR 118.7 — a reduction cannot take generic below zero), so "spells cost {1} less" does nothing to
  a card costing exactly `{U}`. K-9, Mark I and Nashi are both that shape. **Measured: 740 of 5,482
  cost-reduction reasons across the 71 decks (13.5%), over 208 distinct consumers** — Baleful Strix
  `{U}{B}`, Birds of Paradise `{G}`, Archmage's Charm `{U}{U}{U}`, Ayara `{B}{B}{B}`.
  **NOT a straight refusal, and the owner said why**: a TAX effect on the table adds generic and the
  reduction then bites, which is why both are `uncertain` rather than `false`. Cutting 13.5% of a
  family admitted three weeks ago needs registered criteria and a before/after. → roadmap B3.

**THE GREAT HENGE'S REVERSE RELATION IS REFUSED — it is the same defect with the arrow turned round,
and counting is what showed it (2026-08-19, roadmap C2).** The standing item said big creatures make
the Henge cheaper, so it is a `scales:`-family CONSUMER of power rather than a producer of discounts.
That sentence is TRUE and it does not survive being counted: **"greatest power among creatures you
control" restricts NOTHING**, so every creature satisfies the subject and the reverse edge reaches
**33, 31 and 46 creatures** in the three calibration decks that run it — the IDENTICAL fan-out as the
defect `reducesItself` was written to fix, where the Henge claimed to discount 45 creatures and became
its deck's top card over 129 false reasons.
- **THE POPULATION IS 10 SELF-REDUCERS BY THE PRINTED CUE AND THEY SHARE NO SHAPE**: a board count
  including the OPPONENT's creatures (Blasphemous Act, in 15 decks), max-POWER (Great Henge) and
  max-MANA-VALUE (Sunderflock) readings, a stat THRESHOLD (Bolt Bend), event counts within a turn
  (Rowdy Research, Blood for the Blood God!), graveyard counts (Furygale Flocking, The Capitoline
  Triad) and a creature-TYPE-DIVERSITY count (Valiant Changeling). **Three of the ten derive no
  subject at all.** There is no one rule here, which is the second reason to refuse.
- **THE GRAVEYARD SUBSET ALREADY HAS ITS CHANNEL AND USES IT** — Furygale Flocking earns **11
  `scales:instant` reasons** in `izzet-big-mana` today, because `cost-reduction` left
  `ROLE_NOT_SYNERGY` on 2026-08-18 and the scaling loop stopped excluding it.
- **A STALE COMMENT ABOUT A LIVE GATE, FIXED.** The scaling loop still said "cost-reduction is the
  rule's own first member" of `ROLE_NOT_SYNERGY` and cited The Capitoline Triad as the example.
  **Both false since 2026-08-18**: the set is `{tax, win-game, extra-turn, extra-phase}`, and
  Capitoline is refused by the UNTYPED-COUNT guard one line above, not by that one.
- **TWO ITEMS FILED RATHER THAN BUILT, both one card, both "count the consumers first"**: a
  STAT-GATED self reduction WOULD discriminate (Bolt Bend's "a creature with power 4 or greater" is
  a claim about big creatures specifically, and `SubjectFilter.stats` exists to say it) but its
  threshold derives nowhere; and the scaling loop's untyped-count guard refuses **The Capitoline
  Triad `[historic]`**, where artifact/legendary/Saga is a real narrowing it cannot see. The other
  untyped-but-narrowed refusal, Rite of Flame `[named]`, is correct on purpose.

**A SPECIFIC PRIMARY MEASURES ITSELF; A GENERAL ONE MEASURES ITS FAMILY — and cohesion now reports
BOTH (2026-08-19, roadmap A10).** `computeCohesion` keyed on `fold(primary)` and counted the whole
folded family whatever the primary was. Right for the case it shipped for — a token deck splitting
across 22 `create-token:<subtype>` keys read 3 of 63, 0.05 "unfocused" — and wrong once subtype-level
primaries went 6 → 24 across A4, A9 and A3: `inalla` read **"wizards entering · 0.71" where 0.71 was
the share of the deck that is CREATURE-ish**. The label named one thing and the number measured
another.
- **ONE PREDICATE, NO BRANCH**: `tag === primary || fold(tag) === primary`. A general primary IS its
  own fold key, so every family member still counts and its number is **byte-identical** — measured,
  **0 of the 22 movers has a general primary**. A specific primary is not its own fold key, so
  nothing folds INTO a leaf and only the tag itself counts.
- **THE QUESTION COULD NOT BE ANSWERED WHEN IT WAS FILED**: folding down would have read 0.14 on
  `inalla`, an artifact of the first-subtype rule. **A9 is what made it answerable** — `enters:wizard`
  is now 39 cards, so cohesion reads **0.60** and is HAND-CHECKABLE against the decklist, which the
  family reading never was.
- **OWNER'S CALL: report BOTH rather than re-cut a threshold on a scale that had already moved twice
  today.** `Cohesion.familyScore` ships beside `score`; the CLI prints it only when they differ, i.e.
  only when the primary is specific. `cult-of-clones` reads **"daleks entering · 0.08 (its wider
  family: 0.46)"** — five Daleks inside a creature deck, which is a real and actionable thing to be
  told and which one number cannot say. `inalla` reads 0.60 / 0.71: specific AND broad, which is what
  a tribal deck is. The web client renders only the headline and needed no change.
- **THE THIN NUMBERS ARE TRUE, and they expose an A3 property worth knowing**: promotion fires at
  ≥30% of the HEADLINE's support, not of the DECK, so a deck with a weak generic headline promotes
  onto a weak specific one. Raising that bar was considered and DECLINED — it would re-open a rule
  that cleared six registered criteria three commits earlier, and the pair of numbers states the
  situation better than a refusal would.
- Label buckets 26/31/14 → **35/28/8**. Ratings **0 of 6,236**, population **36,595 / 46,099 /
  MESHED 288**, panel **85.6% debt 16** — all unchanged.

**A8 IS CLOSED BY MEASUREMENT, NOT BY CODE.** The dominated-headline curve after A9 and A11:
**4 of 71 decks at 30% sibling support, 2 at 50%, ZERO at 70% and ZERO at 100%** (was 9 / 5 / 3 / 1
when A3 was built). The four survivors are the payoff guard WORKING — `flashy-azula` ←
`enters:human`, `mono-blue-blink` ← `enters:wizard`(13), `enchanting-rani` and `braids` ←
`enters:aura` — every one a sibling with ZERO payoffs. Nothing in those decks watches for a Wizard or
an Aura specifically, so the engine correctly declines to over-specify.

**LEGENDS MATTER IS A THEME AND HAD NO KEY — the A9 failure, fixed and judged on its own terms
(2026-08-19, roadmap A11). ALL SEVEN REGISTERED CRITERIA PASS.** `enters:legendary` existed only by
accident: `splitTypeLine` does not separate supertypes, so `Characteristics.types` for "Legendary
Creature — Human Rogue" is `["legendary","creature"]` and `themeSubjectKey`'s `list(type)[0]`
fallback fires **only when the subject has no subtype at all**. The tag therefore meant "a legendary
permanent WITH NO SUBTYPE entered". `SubjectFilter.legendary` already carried the fact (09ce98d);
only the theme key was missing.
- **MEASURED: exactly 1 of 71 headlines changes** — `marchesa-legends-matter` **"humans entering"
  (0.98) → "legendary permanents entering" (0.98)**, and **NO deck in the corpus reads "humans
  entering" any more**. That deck carries `enters:legendary` on **62 of its ~63 nonlands**, so 0.98
  is honest and not a clamp; its previous headline was its most common creature type at 28.
- **THE TRIPWIRE DID NOT FIRE.** Every EDH deck has a commander and most run several legends, so this
  tag lands in all 71 and could have become the next universal bucket — the failure that got the
  family-grouped design refused. Registered cap: primary of at most 3 decks. **Measured: 1.**
- **TWO THINGS FIXED ALONGSIDE, both the same principle one rung up the type line.** `foldThemeTag`
  folded a SUPERTYPE into a card type — `legendary` is neither a card type nor a subtype, so
  `SUBTYPE_TYPES` could not place it and it fell to `hierarchy.json`, whose scraped `legendary` key
  lists `creature` first, so a legends deck's family was the CREATURE family (identical shape to
  `enters:land` in A5). Supertypes are now their own family: 17 decks' cohesion moved, all small
  falls, each a deck that had been counting legends inside its creature family. And `describeTag`
  rendered it **"legendarys entering"** — an `IRREGULAR_PLURAL` table now covers it plus the creature
  types whose plural is the same word (`mono-blue-tribal-tribal` had read "merfolks entering").
- Ratings moved **125 of 6,236** (mean |Δ| 0.0025), raw scores 986, partner counts 0, TOP card 1/71,
  16 facet moves. Population **36,595 / 46,099 / MESHED 288** and panel **85.6% debt 16** unchanged.
  `themeSubjectKey` untouched throughout this run, so no reason was ever re-keyed.

**A HUMAN WIZARD IS BOTH — the implied entry now advertises EVERY subtype, and `inalla`'s
`enters:wizard` goes 9 → 39 (2026-08-19, roadmap A9). ONE REGISTERED CRITERION FAILED and it ships
anyway, on the owner's call, with the failure recorded rather than relaxed.**
`themeSubjectKey` returns ONE key and resolves subtypes with `list(subtype)[0]` — the first as
PRINTED — so a "Human Wizard" keyed `enters:human` and never `enters:wizard`.
- **`themeSubjectKey` ITSELF WAS NOT TOUCHED AND MUST NOT BE.** It keys REASON tags, and the frozen
  panel keys its cached verdicts on `producer|consumer|tag`; the last attempt to compose extra facts
  into that key cost **22 judging debt while changing no theme**. Only the IMPLIED entry fans out —
  it has no Reason of its own. A card's AUTHORED emits keep first-subtype keying, so they stay
  consistent with the reason tags their edges carry. Panel debt stays 16.
- **THE FRAGMENTATION HAZARD REGISTERED IN THE SPEC FIRED, AND THE GUARD IS MEASURED.** A CHANGELING
  carries the whole `CREATURE_SUBTYPES` list (`extractCharacteristics`), which is right for MATCHING
  and nonsense as a THEME: the first cut took `tribal-tribal` from **"shapeshifters entering" to
  "bears entering"** and nearly doubled the census, **4,500 deck-tags → 8,706**. A changeling now
  advertises its PRINTED type (`subtypes[0]`). After: census 5,061, `enters:shapeshifter` 27 against
  `enters:bear` 2.
- **PASSED: `inalla` `enters:wizard` 9 → 39**, beating `enters:creature` 21 outright with no
  promotion needed · distinct headlines **33 → 36** · subtype-level primaries **20 → 24, none lost**
  · modal still 9 · population **36,595 / 46,099 / MESHED 288** and panel **85.6% debt 16**
  unchanged. Five headlines changed and four are plainly right: **`abzan-defenders` → "walls
  entering"** (the standing A8 witness, whose `enters:wall` had 0 payoffs and could never be
  PROMOTED — it now ranks first outright), `lynde-cursing-mistress` → "curses entering",
  `otterly-awesome-spellslinger` → "otters entering", `eggman` → "constructs entering". The fifth,
  `mono-blue-tribal-tribal` "merfolks" → "wizards" (12 against 10, and those Merfolk are Merfolk
  Wizards), moves an A3 witness and is recorded rather than left to be found.
- **FAILED, RECORDED, NOT RELAXED: `marchesa-legends-matter` still reads "humans entering", and A9
  makes it WORSE — `enters:human` 15 → 28.** The criterion gated a defect A9 cannot reach:
  marchesa is a LEGENDS deck and **`legendary` is a SUPERTYPE with no theme key of its own**.
  `enters:legendary` catches only legendary permanents with NO subtype (it sits at 10) because a
  Legendary Human Rogue keys `human`. Fanning out over SUBTYPES cannot help a deck whose theme is a
  SUPERTYPE. → roadmap A11.
- Ratings moved **206 of 6,236** (mean |Δ| 0.0040), raw scores 1,575, partner counts 0, TOP card
  1/71, 30 facet moves. `theme-stats.json` regenerated, **528 → 568 tags**.

**COHESION IS A SHARE OF CARDS, AND IT WAS COUNTING TAGS — 5 decks read exactly 1.00 because the
clamp was hiding an over-count (2026-08-19, roadmap A7).** `computeCohesion` summed `deckFreq` across
the primary's folded family, which counts a card **once per tag it carries in that family**. Once
every permanent's own entry became a theme tag (A4), the sum ran PAST the card count and
`Math.min(1, familyFreq / nonlandCount)` clamped it — so the headline number was a ceiling wearing
the costume of a share. It now counts DISTINCT CARDS, from the per-card tag sets `analyze.ts` already
had.
- **MEASURED: decks reading 1.00 go 5 → 0 and the clamp is now unreachable** (max 0.98). Median
  0.40 → **0.35**, p25 0.14, p75 0.56. Witnesses: `inalla` 1.00 → **0.71**, `mono-blue-blink`
  1.00 → 0.71, `ashling-lorwyn-only` 1.00 → 0.76, `gengar` 0.92 → 0.60, `magda` 0.84 → 0.56.
  41 decks moved, **0 headlines changed**, ratings/raw scores/partners/facets **0**, population
  **36,595 / 46,099 / MESHED 288** and panel **85.6% debt 16** unchanged.
- **THE LABELS DO NOT NEED RE-CUTTING — the MEASURE was wrong, not the thresholds.** At the honest
  numerator, unfocused / focused / highly split **29 / 28 / 14** across the 71 decks on the incumbent
  0.3 and 0.6, which is a defensible spread. That was the open question A7 was registered to answer,
  and the answer is no change.
- **The flat engine keeps the old sum on purpose** — it holds no per-card tag sets, and the parameter
  is optional so its numbers do not silently move.
- **STILL OPEN, and the two are coupled.** (a) A SPECIFIC primary still measures its whole FAMILY:
  `inalla` reads "wizards entering · 0.71", and 0.71 is the share of the deck that is CREATURE-ish,
  not Wizard. Folding down instead would read 0.14 — which is wrong for a different reason, namely
  (b). (b) **THE FIRST-SUBTYPE RULE**: `themeSubjectKey` takes `list(subtype)[0]`, so a "Human
  Wizard" keys `enters:human` ONLY. That is why a WIZARD deck counts `enters:human` 15 against
  `enters:wizard` 9 — and why `marchesa-legends-matter`, a LEGENDS deck, reads **"humans entering"**.
  → roadmap A9, A10.
- **A CORRECTION TO THE A3 RECORD, found here.** A3's registered criterion 2 was written as "NO deck
  may read 'humans entering'" and recorded as PASSING; it did not hold — `marchesa-legends-matter`
  reads it, from A4, and the promotion rule neither causes nor can fix it (that headline is already
  specific, and the rule only fires on a general one). What the rule actually guarantees is the
  narrower "nothing is PROMOTED onto a zero-payoff sibling", which is what was measured. **The
  criterion was written wider than the rule it gates, and the check was run against the rule's
  outputs instead of against the corpus.**

**THE THEME LINE FINALLY SHIPS SOMETHING — a narrow PROMOTION rule, and "creatures entering" is no
longer the modal headline of EDH (2026-08-19, roadmap A3).** Three ranking rewrites were refused on
this line; this is a POST-RANK GUARD that only the head of the list can move through, so
`rankThemes` is untouched and a fourth universal-bucket regression is structurally impossible.
- **THE PREMISE HAD ALREADY MOVED, and measuring it first is what rescoped the work.** The
  DOMINATED-HEADLINE criterion registered in the family spec §9 was measured for the first time and
  read **9 of 71 decks at 30% sibling support, 5 at 50%, 3 at 70%, 1 at 100%** — not the crisis the
  fourth design was scoped against, because A1, A4, A5 and A6 had already taken the modal headline
  **16 → 10** and distinct headlines **21 → 29** with no ranking change at all. Two of the four
  registered witnesses landed for free.
- **THE HUMAN TRAP, and the payoff guard is the whole rule.** The highest-support specific sibling is
  **`enters:human`** in three of the nine dominated decks — Human rides along with almost every tribe
  — so promoting on FREQUENCY alone names a Wizard deck "humans entering". **`enters:human` has ZERO
  census presence in every one of them: 0 surplus, 0 payoffs, 0 baseline.** Nothing in Magic cares
  that a Human entered. `enters:wizard` has 2 payoffs, `enters:merfolk` 6. So the test is not "is it
  specific" but "does something in this deck WATCH for it" — and it is only measurable because **A2**
  admitted static and token-mediated payoffs into the census.
- **THE RULE**: promote sibling S over headline H when H's subject is GENERAL (a bare card type or
  `any`), S has the same verb and a subject that is a subtype OF H's card type (**`SUBTYPE_TYPES`,
  the CR assignment — never `hierarchy.json`'s co-occurrence count**), `deckFreq(S) ≥ 0.3 ×
  deckFreq(H)`, and **`payoffs(S) > 0`**.
- **SWEPT {0.3, 0.5, 0.7}; 0.5 and 0.7 FAIL criterion 1** (inalla's `enters:wizard` is 9 of 21, so
  only a share ≤ 0.43 reaches it). **The sweep was NOT extended below 0.3 to chase a number.**
- **MEASURED at 0.3: exactly 4 headlines change and all four are right** — `inalla` → **"wizards
  entering"**, `mono-blue-tribal-tribal` → **"merfolks entering"** (both registered witnesses),
  `acererak-combo` → "zombies entering", `cult-of-clones` → "daleks entering". **Modal headline 10 →
  9 and it is no longer "creatures entering", which falls to 6**; distinct **29 → 33**; subtype-level
  primaries **16 → 20, none lost**. `ratings-compare` against a no-promotion arm: **ratings 0, raw
  scores 0, partner counts 0, TOP card 0/71, facets 0** — `buildAxis` reads `deckFreq` and
  `themeStats`, never the ranking. Population **36,595 / 46,099 / MESHED 288** and panel **85.6%
  debt 16** unchanged. Cohesion moved **0**: a promoted sibling folds into the same family.
- **WHAT IS NOT CLAIMED. `the-capitoline-triad` → Eldrazi is still unsatisfied and this rule CANNOT
  reach it** — `enters:eldrazi` has 0 payoffs, the guard refuses it, and the refusal is correct on
  the evidence: nothing in that deck watches for an Eldrazi. **Nine decks stay dominated**, five by a
  zero-payoff sibling; `abzan-defenders` is plainly a Walls deck to a human reader and no card in it
  triggers on a Wall entering. Closing that means finding demand the engine cannot express, not
  lowering the share. `describeTag` pluralises naively ("merfolks entering") — cosmetic, unfixed.

**"TIME LORD" IS ONE SUBTYPE, NOT TWO — and the deck it broke is a TIME LORD TRIBAL DECK
(2026-08-19, roadmap A6).** Every type-line splitter in this repo split the subtype part on
whitespace, so "Legendary Creature — **Time Lord** Doctor" produced `["time", "lord", "doctor"]` —
two subtypes that do not exist and one that does. **THREE splitters had the same bug**:
`tagger/characteristics.ts:splitTypeLine` (which feeds the persisted `Characteristics.subtypes`),
`matcher/typeline.ts:parseTypeLine`, and `matcher/hierarchy.ts:buildHierarchy`. One helper,
`joinMultiWordSubtypes`, longest-match-first over `SUBTYPE_TYPES`, now serves all three.
- **EXACTLY ONE MULTI-WORD SUBTYPE EXISTS IN ALL OF MAGIC** ("Time Lord"), pinned by a test, so the
  join is bounded and a hypothetical three-word subtype works without a code change.
- **THE 36 ROWS THAT ALREADY CARRIED THE JOINED "time lord" ARE CHANGELINGS** — they take the whole
  `CREATURE_SUBTYPES` list from MTGJSON and never pass through a splitter. 23 real Time Lord cards
  carried the split. Two producers of the same field, disagreeing.
- **`it-is-time` IS A MOROPHON / KINDRED DISCOVERY TIME LORD TRIBAL DECK, and it could not see its
  own tribe.** Kindred Discovery ("As this enchantment enters, choose a creature type. Whenever a
  creature you control of the chosen type enters or attacks, draw a card") resolves its chosen type
  per deck, and the value it resolved to was the broken split. After the fix **50 reasons name
  `enters:time lord` / `attacks:time lord`** — Morophon, Realmwalker and The Tenth Doctor all
  supplying it — and the deck's headline reads **"time lords entering", cohesion 0.59**.
  **This completes the A1/A4 story rather than overturning either**: the tag really was
  chosen-type-RESOLVED (A1, which is why it was absent from `theme-stats.json` by construction) AND
  its value really was the whitespace split (A4). Both halves were needed.
- **MEASURED, and it is the first of these theme fixes to move the POPULATION**: edges 36,588 →
  **36,595 (+7)**, reasons 46,090 → **46,099 (+9)**, MESHED **288 unchanged**, panel **85.6% real
  386 / false 65 / uncertain 24, debt 16 unchanged**. Exactly **1 of 71 headlines** moved
  ("times entering" → "time lords entering"), cohesion moved **0**, subtype primaries **16, none
  lost**. Ratings moved 83 of 6,236 (mean |Δ| 0.0053), raw scores 49, partner counts 5, TOP card
  0/71, top-10 1/71, 3 facet moves — every one in `it-is-time`, whose breadth goes **1 → 2.2** and
  anchoring **3.6 → 5**.
- **`DERIVE_VERSION` 55 → 56 and the corpus was RE-DERIVED (free, no model): 2,745 rows.**
  `hierarchy.json` regenerated (527 → 544 keys; `time` and `lord` are gone, `time lord` is present)
  and `theme-stats.json` with it, since both are computed from the field that changed.

**A PERMANENT'S OWN ENTRY IS NOW A THEME TAG — modal headline 16 of 71 → 10, distinct headlines
21 → 29, and it is the best headline distribution this project has measured (2026-08-19, roadmap
A4).** `cardThemeTags` read a card's ABILITIES only, so a permanent contributed nothing by simply
entering: **36 of `braids-mono-black-enchantress`'s 75 nonlands ARE enchantments and 8 carried
`enters:enchantment`**. Built off `impliedEvents`, so the tag says exactly what the matcher claims.
- **A LAND'S OWN ENTRY IS THE MANA BASE, NEVER A THEME — and the first cut proved it by failing.**
  With no exclusion the corpus themed **38 of 71 decks on a basic land type** ("islands entering"
  ×13, "swamps entering" ×8), because ~35 basics out-count every real theme. Same ruling that keeps
  a fetchland naming Swamp outside synergy. A landfall deck still themes `enters:land` through the
  payoffs that TRIGGER on it and the ramp that AUTHORS it.
- **ENTRY ONLY.** `impliedEvents` also pushes `cast` for every nonland and `attacks`/`combat-damage`
  for every creature; adding those makes every nonland a `cast:` theme card, a much larger claim.
  `cardCaresTags` is untouched, so an implied entry enters `rankFreq` at `PRODUCER_SHARE` (0.35).
- **KEYED AT THE SUBTYPE, and the type-level alternative was BUILT AND MEASURED, not argued.**
  Criteria registered before either number existed (`specs/2026-08-19-implied-entry-theme-tag-design.md`
  §4). SUBTYPE: braids **0.14 → 0.51**, subtype-level primaries **6 → 16 with 0 lost**, modal **10**,
  distinct **29** — all four pass. TYPE: **2 subtype primaries LOST** (`magar-spellslinger`
  `enters:treasure` and `rocket-the-mechanist` `enters:vehicle` both collapsing into
  `enters:artifact` — the exact failure the family-ranking design was refused for) and distinct
  headlines **20**, under the incumbent. Deleted, not flagged. Fragmentation is not a hazard because
  `computeCohesion` FOLDS, so a deck's Dragons count inside the creature family regardless.
- **MEASURED: cohesion median 0.22 → 0.40, unfocused 45 → 29, highly focused 4 → 24, 13 headlines
  changed.** Two of the four witnesses registered for the fourth design land on their own:
  **`ashling-lorwyn-only` → "elementals entering" (1.00)** and **`draguns` → "dragons entering"
  (0.70)**; also magda → Dragons 0.84, fight-club → Dinosaurs, tribal-tribal → Shapeshifters (1.00,
  correct — changelings are every creature type), voltron-mill → Equipment, yuna → Sagas.
  `ratings-compare`: ratings **1,137 of 6,236** (mean |Δ| 0.0259), raw scores 3,156, **partner counts
  0** (a theme tag never forms an edge — what moved is the AXIS), TOP card 2/71, top-10 16/71, 100
  facet moves. Population **36,588 / 46,090 / MESHED 288** and panel **85.6% debt 16** unchanged.
  `theme-stats.json` regenerated in the SAME change — **431 → 528 tags** — because it is COMPUTED
  from `cardThemeTags` and leaving it is the staleness mechanism this repo has been bitten by 3×.
- **EXPOSED, NOT FIXED — read before trusting a headline.** `typeline.ts` SPLITS A MULTI-WORD SUBTYPE
  ON WHITESPACE: "Legendary Creature — **Time Lord** Doctor" becomes `time` + `lord`, so `it-is-time`
  themes **`enters:time`**, a parse artifact — and its `attacks:lord`/`enters:lord` have the SAME
  origin, which CORRECTS the A1 note guessing they were chosen-type-resolved. **Exactly ONE
  multi-word subtype exists in all of Magic** ("Time Lord"), so the fix is bounded. Also: `legendary`
  reaches a subject key as if it were a type; **cohesion reads 1.00 on several decks** because
  `computeCohesion` measures the FAMILY of the primary, so a creature-subtype primary is scored
  against "share of the deck that is a creature" — newly common at 16 subtype primaries, and the
  cohesion LABELS want re-cutting against this distribution. One regression row recorded rather than
  smoothed: `naya-spellslinger` "creatures entering" (0.56) → "enchantments entering" (0.14).

**A SUBTYPE'S CARD TYPE IS AN ASSIGNMENT, NOT A CO-OCCURRENCE COUNT — and the fold had been asking
the wrong one (owner's observation, 2026-08-19).** `matcher/hierarchy.json` is built by SCRAPING
printed type lines, so it records every card type a subtype has been printed BESIDE: `forest` reaches
`creature` through Dryad Arbor ("Land Creature — Forest Dryad"), `treasure` and `clue` reach it
through artifact creatures. `foldThemeTag` then took the first match in `TYPE_PRIORITY`, which begins
with `creature`.
- **OWNER'S CLAIM VERIFIED AGAINST MTGJSON `CardTypes.json`: LAND ∩ CREATURE = ∅**, 0 of 18 × 350.
  The ONLY subtypes claimed by more than one card type in all of Magic are the six that are both
  instant and sorcery — **adventure · arcane · chorus · lesson · omen · trap** — which is CR's
  spell-type list by design.
- **THE TWO MAPS DISAGREE ON 19 OF 453 SUBTYPES, and the disagreements are the ones this repo cares
  about**: artifact→creature **treasure · clue · food · equipment · book** · land→creature
  **forest · island · locus · mountain** · enchantment→creature **saga · background · shrine** ·
  artifact→land **vehicle** · sorcery→instant arcane/lesson/trap · plus adventure, luxior, wrenn.
  **This reframes a recorded result**: CLAUDE.md blamed the family sweep for `magar-spellslinger`
  (`enters:treasure`) and `rocket-the-mechanist` (`enters:vehicle`) collapsing into
  "creatures entering" — `treasure → creature` is THIS bug, in the fold, independent of any ranking.
- **`SUBTYPE_TYPES` (generated, free, from the same MTGJSON source as the rest of the vocabulary)**
  is now the fold's first question; `hierarchy.json` stays the FALLBACK, because it carries 73 keys
  the authoritative list does not (multi-word type lines split on whitespace) and a missing answer
  would silently unfold. **`expandTypes` is deliberately UNTOUCHED** — "what can this subject denote"
  really is the co-occurrence question, and changing it moves population; its own item.
- **A SECOND, PRE-EXISTING BUG FELL OUT, found by measuring and not by reading: `enters:land` folded
  to `enters:creature`.** `hierarchy.json` is keyed on the words after a type line's em dash, which
  includes the CARD TYPES themselves, and `h["land"]` lists `creature` first — so a landfall deck's
  family was the creature family. `foldThemeTag`'s own doc comment had always claimed a card-type
  value is left alone; the code did not do it. Now guarded on `ALL_CARD_TYPES`.
- **MEASURED: ratings 0 of 6,236 · raw scores 0 · partner counts 0 · census 0 · headline 0 of 71 ·
  population 36,588 / 46,090 / MESHED 288 · panel 85.6% debt 16.** The fold feeds ONLY
  `computeCohesion` (`rankThemes` is passed a fold only when `alpha > 0`, and it ships at 0).
- **COHESION MOVED IN 43 DECKS, 42 OF THEM DOWN — median 0.35 → 0.22, unfocused 31 → 45, 14 decks
  crossing the label — AND THAT IS THE OLD NUMBER BEING EXPOSED, NOT A REGRESSION.** The old family
  key absorbed everything, so cohesion was measuring "share of the deck that is a PERMANENT", not
  "share on theme". Witness: `braids-mono-black-enchantress`, primary `enters:enchantment`, family
  BEFORE = enchantment 8 + creature 12 + zombie 5 + land 2 + treasure 2 + horror/snake/faerie/
  shapeshifter/rat 1 each → 0.54; family NOW = enchantment 8 + aura 1 → **0.14**.
- **WHY THE HONEST NUMBER IS THIN, AND IT IS THE NEXT ITEM: a permanent's own implied entry is not
  typed by its own card type. 36 of that deck's 75 nonlands ARE enchantments and only 8 carry
  `enters:enchantment`** — Phyrexian Arena and Phyrexian Reclamation carry no `enters` theme tag at
  all, Leyline of the Void and Oath of Liliana carry `enters:any`. Fixing that should take the
  enchantress deck to roughly 36/75 and it moves `deckFreq`, hence headlines and ranking, so it is
  measured on its own rather than hidden inside this diff (owner's call: ship the fold fix first).
  Until then the cohesion LABELS (unfocused < 0.3 · focused < 0.6 · highly ≥ 0.6) are calibrated
  against the inflated scale and read too harshly.
- **Rode along with the regeneration, additive and unrelated**: MTGJSON keyword abilities 220 → 222
  (`affinitycycling`, `halflingcycling`). Subtype lists unchanged at 402 permanent.

**THE MEMBERSHIP CENSUS WAS BLIND TO THE TRIBAL CASE, TWICE OVER — subtype-level loop-closing tags
17 → 63, and NOTHING THAT SHIPS MOVED (2026-08-19, roadmap A2).** Prerequisite for any loop-based
theme ranking: `themeMembership` split every card into surplus/payoff/baseline off `allReasons`, and
`allReasons` could see neither a STATIC payoff nor a TOKEN-mediated relation.
- **A STATIC'S KIND PICKS THE VERB IT PAYS OFF (owner's ruling).** An anthem's Reason carries
  `static:pump` and never an event tag, so a LORD — the archetypal tribal payoff — was a payoff of
  nothing. `STATIC_PAYOFF_VERB` maps **pump · keyword-grant · type-grant · speed-increase · animate
  → `enters:`** (they improve permanents ON the battlefield) and **cost-reduction → `cast:`** (it
  does nothing for a creature already in play). **An unmapped kind credits NOTHING** — `tax` is
  opponent-facing, and four kinds sit at ≤2 instances. The five mapped kinds are **305 of the 313
  statics carrying a usable subject (97.4%)**.
- **THE ONE-RULE VERSION WAS BUILT, MEASURED AND REFUSED**: crediting every candidate tag sharing
  the subject key gives 1,075 credits, of which **317 (29.5%) land on a REMOVAL verb** (`dies` 139 ·
  `sacrifice` 110 · `leaves` 37 · `discard` 13 · `mill` 11 · `enters-graveyard` 7) where the claim
  INVERTS — an anthem does nothing when a creature dies. Read the subject off the CARD, not off a
  Reason, so the credit stands where no producer exists to draw an edge — same rule
  `authoredSurplusTags` follows.
- **TOKEN HOPS ARE RE-STAMPED ONTO THE REAL CARDS.** `cardEdges` excludes every token-touching edge
  by design, so a token deck's whole plan sat on the excluded set. `addHop` now also emits a census
  copy with the hop's two REAL card names, token flags cleared, and **`impliedProducer` cleared ONLY
  when the producer was the token** — a token's own `enters` is implied, but the card that made it
  authored that supply, so for the card it is SURPLUS not baseline. These Reasons reach the census
  and nothing else: not `report.edges`, not `archetypes`, not the graph.
- **MEASURED, and DECOMPOSED because neither half dominates.** Baseline 3,624 deck-tags / 1,502
  payoff credits / 10,102 surplus / 528 tags closing a loop / **17 subtype-level**. Both halves:
  **payoffs +226, surplus +532, 80 tags newly close a loop, subtype-level 17 → 63 (3.7×)**. Static
  alone: payoffs **+191**, surplus **0**, subtype 17 → **44**. Token half by difference: payoffs
  **+35**, surplus **+532**, subtype 44 → **63**.
- **EVERYTHING THAT SHIPS IS BYTE-IDENTICAL, MEASURED NOT ARGUED**: headline **0 of 71 changed**,
  cohesion moved 0, ratings **0 of 6,236** (raw scores 0, partners 0, TOP card 0/71, facets 0),
  population **36,588 / 46,090 / MESHED 288**, panel **85.6% debt 16**. That IS the acceptance test —
  at the shipped settings nothing ranks by the census, so a correct census change must move nothing.
- **`bin/theme-compare.ts` NOW CARRIES THE CENSUS IN ITS SNAPSHOT**, because it is the only reader
  that can see a change nothing ranks by; the previous loop work's figures were prose-only and not
  recheckable, which the adversarial review filed as a process defect.
- **A FALSE CREDIT, FOUND BY READING THE WITNESSES AND LEFT STANDING**: `enters:forest` gains a
  payoff in 4-5 decks and it is always **Yavimaya, Cradle of Growth** ("Each land is a Forest in
  addition to its other land types"), derived `static:keyword-grant` with subject
  `{type: land, subtype: forest}`. Its subject is what the grant CREATES, not a class it benefits
  from — same subject SHAPE as an anthem, opposite direction. ~5 of 226 credits, and a derive-level
  subject question rather than a census one.

**AN ABSENT TAG NO LONGER OUT-SCORES EVERY REAL ONE — `globalIDF` CLAMPS, and the fix moved exactly
the deck the review predicted (2026-08-19, roadmap A1).** `log((N+1)/(count+1))` gave a tag missing
from `theme-stats.json` **7.918** against an observed maximum of **7.225** (N 2,745, minCount 1, 204
of 431 tags count-1), so absence scored as extreme rarity. It now clamps to the RAREST OBSERVED tag,
memoized per stats object; an EMPTY `counts` (`UNIFORM_STATS`) still degrades to the flat
`log(N+1)`, which a naive clamp turns into 0 and which one of the two new tests pins.
- **A WHOLE CLASS OF TAGS IS ABSENT BY CONSTRUCTION, not by staleness.** `deckFreq` carries
  chosen-type-RESOLVED tags (`resolveChosenTypes`) while `gen-theme-stats` computes over raw card
  documents, so `attacks:lord` / `enters:lord` / `tribe:lord` read count 0 forever. Same mechanism
  as the `combat-damage` staleness of 2026-08-18, except structural — and
  `theme-stats-drift.test.ts` cannot see it, because it checks FAMILIES, not tags.
- **MEASURED: 1 of 71 headlines moved.** `it-is-time` (Morophon) themed **"lords attacking" at
  cohesion 0.0159 — ONE card of 63** and now reads **"forests entering" 0.35**. Subtype-level
  primaries LOST **0**, modal "creatures entering" **16 of 71 unchanged**, 21 distinct headlines
  unchanged, cohesion median 0.33 → **0.35**, unfocused 32 → 31. Ratings moved **2 of 6,236** (raw
  scores 24, partner counts 0, TOP card 0/71, top-10 0/71, 1 facet move), all inside that one deck.
  Population **36,588 / 46,090 / MESHED 288** and panel **85.6%, real 386 / false 65 / uncertain 24,
  debt 16** unchanged — the acceptance test, since idf is read only by `axis.ts` and the theme
  ranking and can never form or delete a claim.
- **BLAST RADIUS MEASURED OFF THE SAVED BASELINE, not argued: 5 distinct absent tags reach some
  deck's top-5, over 4 of 71 decks, 1 primary.** `attacks:lord` · `enters:lord` · `attacks:island` ·
  **`cast:__none__` · `counter-added:__none__`** — the last two are a literal placeholder subject
  competing as a theme, surfaced here and NOT chased; its own item.
- **CEILING, and it is why A2/A3 stay open.** The clamp BOUNDS the defect and does not remove the
  class: a chosen-type tag still scores the highest idf any tag can, so it still out-ranks every tag
  the corpus observed more than once. Making `attacks:lord` count what it means is the census work.


**FAMILY-GROUPED THEME RANKING — BUILT, SWEPT, REFUSED, SHIPS AT `alpha: 0` (2026-08-19).** Fable's
design: rank folded families as `S_max + α·Σ(rest)` and name each by its tf-idf MASS-dominant child
(mass, because rarity concentrates it — count share was the rule already measured to fail).
Criteria were registered in `specs/2026-08-19-theme-family-ranking-design.md` §4 BEFORE any number
existed: **modal headline ≤ 8 of 71**, all **6** subtype-level primaries kept, the Samut witness not
regressing, no cohesion > 0.9 without a primary change, population/panel/ratings byte-identical.
- **SEVEN CELLS MEASURED, NONE WITHIN 4× OF PASSING.** `α ∈ {0.2, 0.3, 0.5}` at massShare 0.5 →
  modal **53 / 57 / 61 of 71 (75-86%)** against a baseline of 16, subtype primaries 6 → 1-2.
  Sweeping the second parameter at the gentlest α: massShare 0.35 / 0.25 / **0.15** → modal
  **45 / 38 / 34** (48% at best) with subtype primaries 3 / 4 / **7**. `α = 0` reproduced the
  baseline **byte-identically, 0 of 71 primaries changed** — the wiring acceptance test.
- **WHY IT CANNOT WORK, and the design did not anticipate it**: the naming rule decides a family's
  LABEL, but the RANK still goes to the family, and `enters:creature` is genuinely the biggest
  family in nearly every EDH deck. Folding by (verb, card type) MANUFACTURES a universal bucket,
  and IDF cannot rescue it because folding sums the members' frequency while the family key's own
  idf is low. Lowering `massShare` re-splits labels and even recovers tribes (7 > baseline 6) but
  cannot change which family ranks first — which is exactly what criterion 1 measures.
  **Mass-share naming was the right answer to the wrong half of the problem.**
- **The same five decks lose their subtype primary at every massShare ≥ 0.25** — `draguns2`
  (`enters:dragon`), `everything-is-a-land` (`static:clone`), `it-is-time` (`attacks:lord`),
  `magar-spellslinger` (`enters:treasure`), `rocket-the-mechanist` (`enters:vehicle`), all
  collapsing into `enters:creature`.
- **`bin/theme-compare.ts` IS THE LASTING OUTPUT** — the first instrument that can see a deck's
  headline at all. Every other gate watches a different axis, which is how the theme line was wrong
  for the project's whole life and how a stale IDF artifact went unnoticed for eleven hours of
  commits. Baseline: **71 decks, 21 distinct headlines, modal "creatures entering" 16 (23%),
  cohesion median 0.33, 6 subtype-level primaries.**
- **THE LOOP RANKING WAS THEN BUILT AND ALSO REFUSED, and it is the measurement that ends the
  line.** Owner's argument: "if you are really a tribal deck you also have consumers that benefit of
  given tribe." Ranked by `min(supply, payoffs)` off the surplus/baseline/payoff split
  `themeMembership` already computed and nothing had read: **modal 21 of 71 with supply = authored
  surplus, 23 of 71 with supply = surplus + baseline** — both WORSE than the 16 of doing nothing,
  subtype primaries 6 → 1 in each. (The first cut had a real error worth keeping: for a TRIBAL
  theme the supply is the deck's own creatures, which are IMPLIED and therefore baseline, so
  counting authored surplus alone reads `enters:dragon` as a tiny loop. Fixing it did not help.)
- **WHY ALL THREE DESIGNS FAIL, MEASURED: `enters:creature` closes a REAL loop in 61 of the 71
  decks (86%), median 4 payoff cards.** It is not a bucket manufactured by folding, not an artifact
  of tf-idf, and not a hole in the payoff gate — it is a TRUE statement about most EDH decks, and no
  per-tag scoring function can demote a true statement. The criterion asks the headline to
  DISCRIMINATE between decks, which cannot be answered from one deck's own numbers.
- **A FOURTH ATTEMPT NEEDS A CORPUS-RELATIVE QUANTITY**: "is this loop bigger here than in a typical
  deck". `globalIDF` attempts that against the wrong population — document frequency over CARDS,
  while the question is about DECKS. A deck-level statistic would demote `enters:creature` by
  construction. Two hazards: it can only be computed over the 71 decks, which are one owner's
  collection and not a meta, and it is a fourth artifact that can go stale. The alternative is to
  accept 16 of 71 as an honest distribution and re-register the criterion at a believed value.

**A PASTED FLAT EXPORT NOW FINDS ITS COMMANDER — and the SORT is the signal, not the position
(2026-08-18).** Moxfield's plain text export ("1 Samut, the Driving Force (DFT) 367") carries no
header and NO BLANK LINE, so the headerless convention rule (commander, blank line, the 99) could
not fire and every pasted deck came back with **zero commanders** — re-running for real users the
whole cascade CLAUDE.md already records for the 71 files: `COMMANDER_TF_BOOST` never fires,
`markCommander` stamps nothing, and `pressure.ts` prices the commander by draw probability instead
of command-zone availability.
- **THE RULE**: no header, no blank line, ≥30 card lines, **lines 2..n in alphabetical order and
  the head NOT** — one card deliberately placed outside a sorted list. 1 or 2 head cards, the same
  allowance the blank-line rule makes for partner pairs. "First line is the commander" alone was
  refused as a rule: it would crown the first card of any list.
- **VERIFIED BEFORE WRITING IT: fires on the owner's file, and on ZERO of the 71 calibration
  decks** — all 71 carry the blank line and take the existing path, so `ratings-compare` moves
  nothing (436 / 4,950 / 0, identical to the figures from the IDF regeneration alone) and
  population is unchanged.
- **IT UNDER-DETECTS ON PURPOSE.** A commander that sorts first ("Atraxa" ahead of an alphabetical
  99) breaks no order, so nothing is inferred and the deck reads as it does today — a MISSING
  commander, never a wrong one. Same for an export sorted by type or mana value. Six tests pin it,
  three of them refusals.
- **The owner's deck end to end**: commanders `[Samut, the Driving Force]`, deck 99, `isCommander
  true`, theme "creatures entering" cohesion **0.83 highly focused**, breadth 4.2, SYNERGY 3.8,
  and the clock now prices from the command zone (turn 10, `turnSource: clock`).
  The web path calls the same parser (`data.module.ts:139` → `analyze.service.ts:35`), so a paste
  into the UI gets it too.

**SAMUT'S "UNFOCUSED" DECK WAS A STALE ARTIFACT, NOT A RANKING FLAW — and an ABSENT tag scores the
MAXIMUM IDF, so staleness does not weaken a tag, it makes it WIN (2026-08-18, found by Fable on
review).** `globalIDF` is `log((N+1)/(count+1))`, so a tag missing from `theme-stats.json` scores
**7.84** while `enters:creature` scores 2.02. The whole `combat-damage` family was missing: the
artifact was regenerated at **2026-08-15 00:31** (`0c59087`) and the `combat-damage` table row
landed **eleven hours later** at 11:30 (`651cf0f`). So a **3-card** `combat-damage:creature`
(3 × 7.84 = 23.5) beat a **6-card** `enters:creature` (6 × 2.02 = 12.1) and became that deck's
primary theme. **Third time a stale artifact has starved this engine** — `hierarchy.json` at 16 of
527 subtypes, and this same generator reading the flat `cardTags`.
- **REGENERATED (free, no model, no spend): N 2,541 → 2,745, tags 402 → 431, families 23 → 25**,
  `combat-damage:creature` count 0 → 79 and idf **7.84 → 3.54**.
- **THE DECK THAT PROMPTED ALL OF THIS IS FIXED BY THE BUG FIX ALONE**: Samut reads
  **"creatures entering", cohesion 0.83, "highly focused"**, with **breadth 4.2 and SYNERGY 3.8
  PRESERVED** — the thing the reverted axis fold destroyed. Corpus-wide cohesion median 0.25 →
  **0.33**, unfocused 37 → 32.
- **A DRIFT RATCHET NOW GUARDS IT** (`theme-stats-drift.test.ts`): every `VERB_VOCAB` family must
  appear in the committed artifact. Pure file read, no database, so it runs in the normal suite —
  which is what the other two stale artifacts lacked. **PROVEN TO FIRE**: against the pre-regen file
  it fails with exactly `['combat-damage']`.
- **MEASURED COST: ratings moved 436 of 6,236 (mean |Δ| 0.0084), raw scores 4,950, partner counts
  ZERO, top card changed 3/71, 66 deck-facet moves, mean breadth 2.43 → 2.53.** Population
  (36,588 / 46,090 / MESHED 288) and panel (85.6%, debt 16) unchanged, as they must be: IDF touches
  ranking and the axis, never a claim.
- **STILL THE OPEN RANKING QUESTION, now measured on a SOUND instrument**: the modal headline is
  **"creatures entering" on 16 of 71 decks (22.5%)**, 21 distinct headlines in all, and the tribal
  decks other than `wick-changelings` ("rats entering") still read generically. Fable's proposed
  next step is family-grouped ranking scored `S_max + α·Σ(rest)` and named by **tf-idf MASS share**
  rather than count share — mass is what separates a tribe (rare tag, high idf, concentrated mass)
  from a 22-way token spread, and count share is the rule already MEASURED to fail. Registered
  criteria before any sweep: no headline may cover more than 8 of 71 decks · every deck whose
  primary names a subtype today must keep one · population/panel/ratings byte-identical. Unstarted.

**COHESION MEASURES THE FAMILY NOW — AND FOLDING THE AXIS TOO WAS TRIED THE SAME HOUR AND REVERTED
ON ITS OWN MEASUREMENT (2026-08-18).** A deck whose plan is "make tokens" splits across **22
`create-token:<subtype>` keys** (owner's Samut list: 79 distinct theme tags, folded to the verb
**create-token 34 · enters 60** of ~63 nonlands), and `computeCohesion` counted only the primary
TAG — reporting 3 of 63, **0.05 "unfocused"**, for a deck that plainly is not.
`matcher/src/theme-fold.ts` maps a subtype tag to its card-type family (`hierarchy.json` already had
the map) and cohesion sums whatever folds together. **READ-TIME FOLD, NEVER A RE-KEY**:
`themeSubjectKey` is untouched, so the frozen panel's `producer|consumer|tag` verdicts stay attached
— the 22-debt lesson from composing facts into the tag key.
- **MEASURED: cohesion median 0.15 → 0.25 across the 71 decks**, and **ratings / raw scores /
  partner counts / deck facets ALL MOVED BY EXACTLY ZERO**, population 36,588 edges / 46,090
  reasons / MESHED 288 unchanged — the acceptance test, since nothing that feeds a rating changed.
  Witnesses: burakos 0.05 → 0.54 ("creatures dying"), braids 0.54 ("enchantments entering"),
  ashling 0.76.
- **THE FULL FOLD — axis AND ranking as well — IS MEASURABLY WORSE AND MUST NOT BE RETRIED AS IS.**
  Collapsing many axis keys into one made that key normalise to 1.0 and pushed every other family
  down: the Samut deck went **breadth 4.2 → 2.0, SYNERGY 3.8 → 2.9** — worse on the very deck that
  prompted the work — and **9 of 12 sampled decks, every tribal one among them, themed the
  identical "creatures entering"**, which is the `UNIFORM_STATS` failure again (7 of 8 decks themed
  "draw") and the 38e5248 regression (five tribal decks losing their tribe) in one move. A
  dominance rule naming a family by its biggest child does NOT save it: a tribe competes inside
  `enters:creature` against every other creature subtype and rarely clears half. **Cohesion is a
  SHARE, which a family answers correctly; the axis is a RANKING, where one universal bucket
  destroys the signal.** `theme-fold.ts` ships with its guards tested (tribe/static/counter never
  fold) so a second attempt starts from measured ground.
- **STILL OPEN, and it is the RANKING, not the fold**: the Samut deck still reads cohesion 0.05
  because `rankThemes` picks `combat-damage:creature` — a 3-count, high-IDF fragment — as primary,
  and cohesion measures the family OF THE PRIMARY. Fixing the number for that deck means changing
  which tag ranks first, which is exactly the move that flattened tribal naming. Unstarted, and it
  needs a shape that ranks families without naming them generically.

**COST REDUCTION IS A PAIRWISE CLAIM AGAIN — the 2026-08-06 ruling OVERTURNED by the owner
(2026-08-18): "your cost reducing card is as good as many cards it can reduce."** The old argument
was that a Medallion makes the identical claim in every deck. The overturning one: it does nothing
in mono-red BECAUSE there is nothing to reduce, which a pairwise edge states correctly by forming
ZERO edges there and forty in a mono-blue deck — the colour and type restrictions already live on
the subject, so `subjectMatches` does that work for free. `cost-reduction` leaves
`ROLE_NOT_SYNERGY`; **`tax`, `extra-turn` and `extra-phase` stay**, because Ghostly Prison protects
you the same whatever the other 99 are.
- **MEASURED: edges 32,220 → 36,588 (+4,368), reasons 40,612 → 46,090 (+5,478), MESHED 288
  UNCHANGED.** Ratings moved 2,771 of 6,236 (44%), mean |Δ| 0.196 · raw scores 3,085 · partner
  counts 2,556 · top-10 membership 25/71 · 63 deck-facet moves · mean breadth 2.31 → 2.43.
  The deck that prompted it (Samut, the Driving Force): **breadth 2.0 → 4.2, SYNERGY 2.7 → 3.8,
  Samut 2.8 → 3.9 with partners 40 → 61** over 53 cost-reduction edges.
- **MESHED HELD AT 288 ONLY BECAUSE THE FAMILY IS EXEMPTED FROM THE CENSUS** (`DELIBERATELY_WIDE`
  in `mesh.ts`, counted CLEAN so the buckets still sum). Left in, it reads **3,420** and an
  ACCIDENTAL mesh — the only thing that report exists to show — is invisible inside the total. Keep
  that set at one member unless a second family earns it with its own measured ruling.
- **THREE GATES, because the static applies-to pass never carried this family**: a land is PLAYED,
  not cast (CR 305.1, checked against the type union so an Instant // Land modal DFC keeps its
  edge) · a reducer aimed at OPPONENTS' spells is tax pointing the other way · the reason reads
  "X reduces what Y costs", not the generic applies-to sentence.
- **A SELF REDUCTION'S SUBJECT IS THE MEASURING STICK, NOT WHAT IT DISCOUNTS — caught by the owner
  on the first day, and it is the self-reference family again.** The Great Henge ("This spell costs
  {X} less to cast, where X is the greatest power among creatures you control") derives
  `cost-reduction` with subject `{type: creature, control: you}` — the creatures that SET X — and
  read as an ordinary reducer it claimed to discount 45 creatures and became its deck's top card.
  **Measured: 129 false reasons across the 71 decks, every one The Great Henge, of 5,596
  cost-reduction reasons (2.3%); corpus-wide the family is 11 of 64 cards deriving a cost reduction
  (17%).** `reducesItself` refuses them on the printed cue "this spell costs" / "this ability
  costs". **Ten of the eleven match that cue and the eleventh is not a self-reducer at all** —
  Mutated Cultist's "the NEXT SPELL you cast this turn costs {1} less" is genuinely other-reducing,
  which is why the looser "costs {1} less to cast for each" phrasing is NOT a cue: it appears on
  both shapes. After the guard, reasons 46,219 → 46,090 and panel debt 18 → 16. **The Great Henge
  still rates 5** — on its real ability ("whenever a nontoken creature you control enters, put a
  +1/+1 counter on it and draw a card"), which does relate to every creature in the deck.
  **THE RELATION IS REAL IN REVERSE and is NOT captured**: big creatures make the Henge cheaper, so
  it is a `scales:`-family CONSUMER of power, not a producer of discounts. Its own item.
- **OPEN, AND THE OWNER'S CALL — panel debt is 16, not 0.** The sound reducers land 16 unjudged
  claims on frozen panel pairs; precision reads **85.6% [82.0, 88.5]**, truly bounded [82.3, 86.1]
  until they are judged. Claude judging a family it has just admitted is the weakest evidence
  available, so these were deliberately left for the owner (`panel-score.ts --rejudge <out>`).
- **ALSO OPEN: 5 of 71 decks now rate a reducer as their top card** (Primal Amulet ×2, Artist's
  Talent, Defiler of Dreams, The Sibsig Ceremony, Bontu's Monument — The Great Henge's two are now
  earned by its ETB payoff). The registered test for whether damping beyond the existing √ is
  needed. Evidence against: **Ugin, the Ineffable reaches 65 cards and still rates 2 at rank
  14/97** — a wide fan does not by itself top a deck.

**SUPPRESSION IS A TRADE, AND IT WAS BEING MADE FOR NOTHING — the placeholder-token hole, found by
the owner on their own deck (2026-08-18).** Token mediation deletes a maker's direct "a token
enters" edge because the token NODE re-supplies it one hop later. **Second Harvest** ("For each
token you control, create a token that's a copy of that permanent") lists exactly one token part:
Scryfall's **placeholder "Copy" row — type line `Token`, no card type, its own oracle text saying
"(This token can be used to represent a token that's a copy of a permanent.)"**. That node
satisfies no typed subject and `impliedEvents` gives it no `enters` either (its type line names no
permanent type), so the direct edge was deleted and the hop it was traded for landed nowhere.
Measured before the fix in `naya-spellslinger`: **Second Harvest rated 0.3 with ONE partner and was
invisible to Caretaker's Talent** ("whenever one or more tokens you control enter, draw a card") —
which has 29 partners of its own in that deck. The owner reported it as a cut-list false positive;
the cut list was reading the engine correctly and the engine was wrong.
- **THE FIX IS `hasMediatingToken` (`tokens.ts`), and it covers TWO orphan classes, not one.**
  Suppress only when the producer actually has a token part a node can be built from. **163 of the
  995 ingested token rows are typeless** (`Token`, `Card`, `Boss` — placeholders, ad cards,
  minigame cards) and **349 corpus cards reference one**. Second Harvest is the "only typeless
  refs" class; the other class is **no token parts at all — 26 card-slots over 12 distinct cards in
  the 71 decks**, almost exactly the COPY family CLAUDE.md already lists as out of scope (Saw in
  Half, Court of Embereth, Coiling Rebirth, Stormsplitter, Determined Iteration, Skitterbeam
  Battalion, Cybership, Kibo) plus Sheoldred's Edict, Lich-Knights' Conquest, Gaius van Baelsar and
  Caretaker's Talent itself. Both were losing the same trade.
- **MEASURED: edges 32,182 → 32,220 (+38), reasons 40,563 → 40,612 (+49), MESHED 288 UNCHANGED**
  (the hard gate held), **panel 88.2% real 382 / false 51 / uncertain 22, debt 0 — identical**.
  Ratings moved 41 of 6,236, mean |Δ| 0.0012, raw scores 57, partner counts 56, **TOP card changed
  in 0 of 71**, 8 deck-facet moves. Small and exact: it restores a relation for a narrow family
  rather than widening anything.
- **Witness after: Second Harvest 0.3 → 0.4, partners 1 → 2, axisWeight 0.118 → 0.488** (it is now
  ON the deck's theme, which is what a token doubler in a token deck should be), edging Caretaker's
  Talent directly — and it **left the cut list**, which is now empty for that deck.
- **THREE TESTS PIN IT, and two EXISTING mediation tests had to change** — their fixtures had no
  `allParts` at all, so they were asserting suppression in exactly the state that now (correctly)
  refuses to suppress. They now list the Treasure they make, which is what the two-hop path they
  test presupposes. Suite **1,978 passing, 7 skipped, 0 failed**.

**THE CUT LIST SHIPPED — "which cards is this deck not using?" — and MEASURING IT TWICE IS WHAT
MADE IT HONEST (2026-08-18).** `matcher/src/cut-list.ts`, `report.cutList` + `report.slack`,
rendered in the CLI (`=== Cut candidates ===`) and the web Overview (`CutList.tsx`). A JOIN over
what every analysis already computed — `synergyRating`, `axisWeight`, BUILD roles, combo
membership, land status — not new analysis. **A card is a candidate only when ALL hold**: not a
land, not a commander, not a combo piece, **fills no BUILD role at all**, **carries no
`ROLE_NOT_SYNERGY` effect kind**, `synergyRating ≤ 1.0`, `axisWeight < 0.25`. Every row prints its
three reasons in plain words, and the section says outright it is not a verdict.
- **THE FIRST CUT FLAGGED SOL RING, and the 71-deck run is the only reason anybody knows.** The
  original rule let a card through when every role it fills sat in an OVER-TARGET category —
  `burakos-crashing-the-party` runs ramp 16/10, so Sol Ring, Arcane Signet, Dark Ritual and Cabal
  Ritual all qualified. **Nothing in this repo ranks two ramp cards against each other**; the
  build layer counts a category's members and never sorts them. So the surplus is real and the
  attribution is invented. Any card with a role is now protected outright, and the surplus is
  reported at DECK level instead — `slack`, "ramp 16/10 (+6)", which names the category and never
  a member.
- **THE SECOND FALSE POSITIVE WAS THE ENGINE'S OWN SILENCE.** Jet Medallion was flagged with 0
  partners — because `ROLE_NOT_SYNERGY` makes cost reduction form no edge BY DESIGN ("Sapphire
  Medallion in mono-red does nothing" is a deck-construction fact a pairwise claim cannot carry).
  Reading a deliberate silence as evidence of uselessness is the worst mistake this list can make.
  Cards carrying any of the five role kinds are now protected.
- **MEASURED LIST SIZE, all 71 decks, uncapped**: first cut median **10** per deck (max 39) →
  role-protected median **4** → plus deck-role protection **median 3, mean 4.3, max 19, and 20 of
  71 decks yield NOTHING** — which is the right answer for a tight deck. The rating gate binds only
  below 1.0: thresholds 1.0, 1.5 and 2.0 give identical counts, because a card rating above 1.0
  essentially always has an on-axis edge.
- **THE REMAINING FALSE POSITIVES ARE COVERAGE, NOT LOGIC, and the module says so.** Bitterblossom
  is a candidate in an aristocrats deck — printed text (fetched, not recalled): "At the beginning
  of your upkeep, you lose 1 life and create a 1/1 black Faerie Rogue creature token with flying."
  It rates 0.3 with 1 partner because a body arriving each upkeep only connects through `enters`;
  nothing implies that a creature DIES. Second Harvest is the same shape from the other direction
  — the COPY-token family is explicitly out of scope. **A relation the engine cannot express looks
  exactly like a card doing nothing**, which is why every row ships with its argument attached.
- **FOUND WHILE VERIFYING IT: the CLI had been running the PRE-TOKEN ENGINE.** `main.ts` never
  passed `tokenTags` to `analyzeDeckStructured`, so no token nodes, no two-hop mediation, and
  different partner counts and ratings from the same deck the web server rates — caught by the CLI
  and the measurement bin disagreeing about one card (Obsessive Pursuit 4 partners vs 6). Fixed;
  the two surfaces now report the same engine.
- **GATES UNCHANGED**, as they must be for a pure read: population **32,182 / 40,563 / MESHED
  288**, panel **88.2%, debt 0**. Suite **1,975 passing, 7 skipped, 0 failed**, including 8 new
  unit tests. (`GraphView.test.tsx > fit to view` failed once under full-suite parallel load and
  passed on three re-runs — a pre-existing timing flake, not this change.)

**THE SHIPPED SCORING TABLE WAS MISSING SEVEN EFFECT KINDS, AND THE RATCHET WATCHED THE WRONG FILE
(fixed 2026-08-18).** `loadImpactWeights()` returns `impact-weights.json` — that file, not
`SEED_IMPACT_WEIGHTS`, is what scores production. It carried **27 kinds against SEED's 34**, and a
kind absent from it does not fall back to the seed: it scores `UNKNOWN_KIND_WEIGHT = 0.2`.
**MEASURED BLAST RADIUS BEFORE THE FIX: 1,411 of the 71 decks' 40,563 reasons (3.48%)** —
`keyword-grant` **846** (45 decks, 296 distinct consumers, 0.3 vs 0.2) · `type-grant` **419** (11
decks, 248 consumers, 0.45) · `graveyard-hate` 101 (23 decks, 0.3) · `extra-combat` 30 (2 decks,
**0.9 — a 4.5× under-weight**) · `win-game` 11 · `extra-turn` 2 · `extra-phase` 2.
- **THE "UNREACHABLE" COMMENT IN SEED WAS FALSE, and measuring is what showed it.** It claimed
  `win-game`/`extra-turn`/`extra-phase` can never carry a Reason because they sit in
  `ROLE_NOT_SYNERGY`. That set is applied in the STATIC and SCALING passes only, so a TRIGGERED
  ability with one of those kinds still forms an event edge in pass 1 — and `win-game` is
  re-admitted outright when it names what it counts. All three appear in the population. Comment
  corrected in place.
- **EFFECT, measured with `ratings-compare` against a pre-fix snapshot: ratings moved 459 of 6,236
  (7.4%), mean |Δ| 0.0121 · raw scores 850 · partner counts **0** (weights cannot move an edge) ·
  TOP card changed in **1 of 71** · top-10 membership in 9 · 3 deck-facet moves.**
  Witnesses, all three read from corpus oracle text and not from memory: **Moraug, Fury of Akoum
  3.3 → 5.0 in `rakdos-landfall`** — "there's an additional combat phase after this phase" is
  `extra-combat`, in the deck built to trigger it, and it becomes that deck's top-rated card ·
  **Hellkite Tyrant 1.7 → 2.8** ("if you control twenty or more artifacts, you win the game" — the
  re-admitted `win-game` shape) · **Maskwood Nexus 3.6 → 4.2** (`type-grant`). Falls in the same
  deck (Nesting Dragon 5 → 4.4, Bloodghast 4.6 → 4.1) are renormalization, not demotion — a rating
  is `score / deckMax` and Moraug took the denominator.
- **THE THREE STANDING GATES DID NOT MOVE, as they must not**: population **32,182 edges / 40,563
  reasons / MESHED 288**, panel **88.2% real 382 / false 51 / uncertain 22, debt 0**. Weights are
  read only by `impactEdgeWeight` in the ratings pass; they cannot form or delete a claim.
- **BOTH TESTS THAT SHOULD HAVE CAUGHT THIS WERE THEMSELVES THE DEFECT.**
  `impact-coverage.test.ts` asserted coverage against SEED only; a new test now asserts it against
  `loadImpactWeights()`, and it was PROVEN TO FIRE by deleting `keyword-grant` and watching the
  SEED test stay green while the new one failed. `impact.test.ts` asserted
  `Object.keys(w.kinds).length === 27` — a pinned count that pinned the gap itself; replaced with
  the same coverage property. **A count test can only ever pin whatever number was current.**
  Suite green, 1,967 passing / 7 skipped / 0 failed.

**TOKENS ARE FIRST-CLASS NODES — the 9-task plan shipped, `b2a7c9f~1..HEAD`, 16 commits.** Two
witnesses drove it, both the owner's: Revel in Riches derived a `win-game` threshold of
`atLeast: 10` with an UNTYPED subject — knew the number, not the noun — while Deadly Dispute
made the Treasures it counts; Kuja, Genome Sorcerer's whole payoff lives on the TOKEN it creates,
not on Kuja. The common shape — the relation runs through an object with no node — either
collapsed into a wrong direct edge or vanished entirely. **6.5% of the pre-plan population (2,474
of 37,787 reasons) was already token-mediated and mis-stated as a direct edge.**
Spec `docs/superpowers/specs/2026-08-15-tokens-as-nodes-design.md`, ledger
`.superpowers/sdd/2026-08-15-tokens-as-nodes/progress.md`.
**Baseline chain, so this block and the one below it do not read as contradicting each other**:
the plan started from **29,658 edges / 37,787 reasons / MESHED 221** — the prior block's 33,286
predates `322d129` (2026-08-15, same day, before this plan), which deduped six identical
Farseek→Plains-shaped edges per copy-stack down to one edge carrying the count, dropping
33,286 → 29,658. Not a discrepancy; an intervening commit.

- **A threshold now records WHAT it counts.** `Trigger.thresholdSubject` parses the same sentence
  `thresholdFor` already reads — "ten or more **Treasures**" → `{subtype: treasure}` — and
  `win-game` leaves `ROLE_NOT_SYNERGY` exactly when it names something; an untyped one stays
  excluded, correctly, since "this card wins the game" says the identical thing beside every card
  in the deck. Review found three real defects in the first cut (match-selection divergence
  between the number and the noun, a zone exclusion too narrow to catch "in your graveyard", no
  self-reference guard), all closed by `selectThreshold()` becoming the single match-selection
  loop so the number and the noun cannot describe different sentences.
- **IDENTITY IS EXACT, NOT A NAME-AND-TYPELINE GUESS.** The spec's own claim that `allParts`
  settles "which Wizard" per card was FALSE, and the spend was stopped for the owner before it
  happened: **127 referenced token keys, 50 matching more than one token row, 41 of those
  disagreeing on oracle text** — including Kuja's own Wizard, the project's headline witness. The
  fix is `printingId`, not name+typeLine: `ingest-tokens` records the printing ids that collapse
  into each oracle-id token row, `RelatedPart` keeps its printing id, and
  `card.allParts[].printingId ∈ token.printingIds` is exact — ambiguity 50 → 0, 41 → 0, verified
  independently by the reviewer.
- **THE BATCH: 94 tokens, $0.18, persisted 94 / refused 0 / failed 0** (94, not the spec's
  estimated 87 — the exact-identity fix surfaced tokens the ambiguous key had been silently
  dropping). Witnesses verified post-spend against stored text: Kuja's Wizard carries
  `{trigger: cast "a noncreature spell", verbs: [deal-damage]}`; Treasure → `[sacrifice,
  add-mana]`; Clue → `[sacrifice, draw]`.
- **TOKENS DERIVE THROUGH THE SAME PIPELINE AS A CARD** — the owner's call, rejecting
  identity-only (it fixes Revel and leaves Kuja's token inert). 94 rows, 0 missing,
  `characteristics.token === true` on all 94 with no code path that can skip the override.
  Population byte-identical to the pre-buy baseline, the acceptance test. 59 of 94 derive zero
  abilities and every one was READ, not sampled: 55 keyword-only (inert at derive time for cards
  too — keywords are read from `characteristics.keywords` at match time) and 4 statics. No hidden
  derivation gap.
- **TOKEN NODES AND CREATION EDGES.** Two pre-existing defects were found only because a token's
  characteristics finally ran through this code path: `docToCard` silently dropped `allParts`
  (the feature derived NOTHING on the first re-derive — reading UNCHANGED is what exposed it), and
  tokens received an implied `cast` against CR 111.7 (**1,115 false reasons, more than every other
  token family combined**). Two review rounds followed: round 1 found 242 of 934 token refs (26%)
  resolving to no derived row — a vanilla Bird got no node at all, against spec §4's requirement
  that every token the deck can make gets one — and synthesized them; round 2 found the round-1
  leak-fix keyed `cardEdges` on card NAME, silently deleting real cards from archetypes and theme
  membership in the 10 of 71 decks where a card shares its name with a token it creates
  (archetypes 35,068 → 35,140, +72, the wrongly-deleted cards returning).
  **MESHED 221 → 288, ACCEPTED**, not chased back down: the reviewer re-derived the whole 71-deck
  mesh diff twice and found exactly one group newly over cap (Maskwood Nexus, `static:type-grant`
  — its oracle text, "creatures you control are every creature type," genuinely reaches them, and
  fanOut was already 46 against the cap of 50 before any token work tipped it to 52), against four
  groups CLAUDE.md already documented as correct legendary/tax anthems. Strongest evidence the
  filter discriminates rather than over-matching: the 242 synthesized vanilla tokens moved
  Serah/Jodah by **exactly zero** — only the two printed LEGENDARY tokens (Ravage, Laserbeam) did.
- **TOKENS MEDIATE — one fact, one path, owner's call.** A token-producing card no longer edges
  directly to a token-carer; it edges to the token, the token edges to the carer, and a CR 614
  multiplier still points at the CARD performing the action, never the token it makes (spec's own
  ruling, following the same-day 614 work: pointing a doubler at the token would claim it doubles
  tokens that already exist). edges 32,583 → **32,182 (−401)**, reasons → **40,561 (−557)**,
  **MESHED 288 UNCHANGED — the hard gate held**, panel 88.0% → **88.2%**, judging debt 2 → **0**
  (both claims judged real by the owner: the PLAIN-BODY reading survives a mechanism change — a
  Hornet Nest or Aragorn satisfying a class subject is still real even though the cached verdict
  had been made against the direct-edge mechanism — on the Goldspan Dragon → Terror of the Peaks
  precedent). **Two guards were load-bearing and both were VERIFIED, not assumed**: a verb-blind
  suppression rule would have wrongly caught a CR 614 doubler (same `subject.token === true`
  field, different verb), and a producer-blind one would have broken the second hop, because a
  token's own implied `enters` also carries `subject.token === true`.
  **`isolated-cards.ts` was fixed in the same task** — it built zero token nodes without
  `tokenTags`, structurally blind to the two-hop path it exists to measure, and an instrument
  quoted in the docs must not silently read a population nothing ships: **lands 552 → 548
  (30.9% → 30.7%), nonlands 489 → 441 (11.0% → 9.9%), distinct isolated 412 → 380** — below the
  PRE-token baseline, the owner's stated deckbuilding goal ("avoid cards floating around without
  any connection where they really should have one") measured.
- **TOKEN NODES ON THE GRAPH, unpartnered ones behind a toggle — scope expanded, owner's call.**
  The brief's three files would have shipped a chip over nothing: `data.module.ts` never passed
  `tokenTags` (the web path built ZERO token nodes), and `projectDeckGraph` builds its nodes off
  the deck array alone, so token edges land in `offDeckReasons` and get dropped even once the
  lookup is wired. Fixed by exporting `collectTokenNodes` (one dedupe path, not re-implemented in
  the server) and threading `loadTokenTags` into both the `graph` and `analyze` deps. Population
  byte-identical: edges 32,182 / reasons 40,561 / MESHED 288. Suite 1,917 green.
  **Follow-up, same day: a token whose NAME collides with a real card was merging into that
  card's node** — **92 of 661 distinct token names collide, 8 of the 71 calibration decks carry a
  live witness.** `nodeId()` + `TOKEN_ID_PREFIX` give a token its own node id; `isToken` rides the
  Reason (`producerIsToken`/`consumerIsToken` via `stampSides`), `ProjectedNode` and
  `WireGraphNode` chain. reasons 40,561 → 40,563, MESHED and panel unchanged, suite 1,921 green.
- **FINAL STATE, freshly measured 2026-08-16** (not carried forward from the ledger — every
  number below matches the ledger's own last-recorded figure exactly, confirmation not
  correction). `population-compare.ts`: **edges flat 84,039 → derived 32,182, reasons flat
  113,715 → derived 40,563, MESHED flat 14,342 → derived 288.** `panel-score.ts`: **real 382 /
  false 51 / uncertain 22, PRECISION 88.2% [84.8, 90.9], debt 0.** `isolated-cards.ts`:
  **lands 548/1,785 (30.7%), nonlands 441/4,451 (9.9%), distinct isolated 380.** `npm test` across
  all 7 packages: **1,921 passed, 7 skipped, 0 failed** — the `.cs-cache/` environmental exception
  did not even fire in this checkout; the 7 skips are unrelated network/integration tests.
- **TASK 8 IS NOW VISUALLY VERIFIED — live browser, 2026-08-18.** API :3001 + UI :5173,
  `burakos-crashing-the-party` analysed in the UI (RESOLVED 100/100). Token nodes DRAW: grey disc,
  DASHED rim, the token's own name above and a small `token` tag below. The TYPE legend counts
  `token 7`, and the **LONE TOKENS (1)** chip flips it to `token 8` — the unpartnered Copy token
  appearing, which is the toggle's whole job. Clicking a token opens a real detail panel
  (`Treasure` / `token artifact — treasure`, 5 partners) with FEEDS and FED BY split correctly:
  `Burakos, Party Leader → Treasure` / `Grim Hireling → Treasure` (creates:treasure) feeding it,
  `Treasure → Nadier's Nightblade` (leaves:any) fed BY it — **the two-hop mediation path rendered
  on screen**, not merely in the population figures. The API is the same shape: 93 nodes / 215
  edges, 8 with `isToken`, 17 edges touching a token.
- **TOKEN ART IS JOINED — on the ORACLE id, never the name (2026-08-18).** A token node's
  `tags.oracleId` IS its `tokens` row `_id` (that is the key `loadTokenTags` synthesizes against),
  so the graph dep resolves art with one `$in` over the deck's tokens and hands
  `attachRolesAndArt` a `tokenArtById` map keyed by NODE id. **A token reads art only from that
  map and never from `docs`** — the name path would have handed the Treasure token the art of a
  card called Treasure, the same 92-of-661 collision `nodeId` exists to prevent, and the two new
  tests pin both directions. Measured on `burakos-crashing-the-party`: token nodes carrying
  `artCrop` **0/8 → 8/8**; the `tokens` collection is **995/995** with an `artCrop`, so coverage is
  not deck-specific. Verified in the browser: art paints inside the dashed rim, and the inspector
  shows the whole token card (`Token Artifact — Treasure`, "Sacrifice this token: Add one mana of
  any color") — `cardImageUrl`'s `/art_crop/` → `/normal/` rewrite resolves for tokens too (HTTP
  200, checked). Server-only: no matcher, derive or population change. Suite **1,923 green**.
  **ponytail ceiling**: two DIFFERENT tokens sharing a name already collapse to one node id (that
  is `nodeId`'s granularity, not the join's) — last art wins; give a node its oracle id to separate.
- **STILL OPEN.** Seen in the same browser pass: the Treasure panel repeats `Nadier's Nightblade
  triggers on a permanent leaving the battlefield; Treasure supplies it` **three times,
  byte-identical** — that is the pre-existing one-trigger-many-effects duplicate (the Charitable
  Levy shape already named in the 2026-08-15 block), not a token defect, but a token node is where
  a user meets it first.
- **THE RATINGS PASS NOW WALKS THE TWO HOPS, and the instrument is what found the hole.** Task 7
  made tokens MEDIATE, but the directional pass that produces `score`/`partnerCount`/`authority`
  iterates `unique` — the real cards — so a relation living on a token node was invisible to it.
  **100 cards across the 71 decks had ZERO directed partners while carrying token edges, 43 of them
  having had partners before mediation.** Witness: **Caretaker's Talent** ("whenever one or more
  tokens you control enter, draw a card") held **27 token edges and rated 0 with 0 partners** in
  `naya-spellslinger` — a token payoff reading as synergising with nothing in a token deck, which is
  a WRONG sentence, not a missing one. Fixed by TRAVERSAL, not a second copy of the fact: the reasons
  stay on the token edges and the pair borrows them, in **both** directions (maker → token → payoff,
  and buff → token → maker, both of which were direct edges before mediation).
  **MERGED INTO THE PAIR'S DIRECT REASONS rather than credited per token** — `impactEdgeWeight` takes
  the max per distinct tag, so a maker whose four token types all satisfy one `enters:any` trigger
  scores it ONCE, exactly as the single direct edge did. Crediting per token would have paid it four
  times.
- **WHO GETS THE TOKEN DECIDES WHETHER THE HOP EXISTS — `createsForYou`, caught by measuring.** The
  first cut lifted **Beast Within and Generous Gift 0 → 2.0** in `naya-spellslinger`: their Beast /
  Elephant goes to the permanent's CONTROLLER, an opponent, and a payoff says "tokens YOU control".
  The clause layer already knew (`control: "opp"` on the `create-token` emit). The GRAPH EDGE keeps
  ignoring control on purpose — "this card makes that token exist" is true for Beast Within, which is
  why the token is on the board at all; what is false is the ratings claim built on top of it. So the
  traversal gates and the edge does not. `control: "any"` counts as ours (a card saying only "create
  a 4/4 Dragon" makes it under its own controller).
  **MEASURED, against the shipped tree: ratings moved 889 of 6,236 (14.3%), mean |delta| 0.044 ·
  raw scores 821 · partner counts 754 · TOP card changed in 4 of 71 decks · top-10 membership in 20.**
  Caretaker's Talent **0 → 3.33 with 29 partners**, each reason naming the mediating token
  ("Caretaker's Talent triggers on a permanent entering; Saproling supplies it"). Elemental Eruption
  **0 → 4.8 with 55 partners** in the same deck — its Dragon token has PROWESS, so every noncreature
  spell feeds it; that relation never existed before tokens were nodes, since the trigger is printed
  on the token and not on the card. **Population and panel byte-identical — edges 32,182 / reasons
  40,563 / MESHED 288, panel 88.2% real 382 / false 51 / uncertain 22, debt 0** — the acceptance
  test, since this changes only what the ratings pass READS. Suite **1,931 → 1,933** (`7847d04`'s
  message says 1,923 → 1,925: that added the 2 new tests to a STALE total taken before the
  ratings instrument's own 8 landed. The per-package sum is the figure.)
- **ONE TRIGGER WITH A CHAIN OF EFFECTS IS ONE CLAIM — `claimCount`, free, and the count is the
  only thing that collapses.** Archon of Cruelty's single entry trigger ("target opponent sacrifices
  a creature, discards a card and loses 3 life; you draw a card and gain 3 life") derives SIX reasons
  identical in tag and text, differing only in `effectKind` — forced-sacrifice · (none) ·
  player-life-loss · draw-card · lifegain · drain — so every reanimation spell in the deck scored 6
  against it while a one-payoff creature off the identical trigger scored 1.
  **MEASURED across the 71 decks: 9,268 of 40,563 reasons (22.8%) sit in a duplicate (tag, text)
  group** — 4,010 groups over 3,985 edges (12.4%), sized 2× 3,012 · 3× 808 · 4× 160 · 6× 30 — and
  **8.9% of all reasons (3,609) carry NO `effectKind` at all**, led by `enters:creature` 829 ·
  `enters:any` 335 · `cast:spell` 205. That is chain LENGTH driving the ranking, which is the
  magnitude axis in disguise; magnitude lives in `impactEdgeWeight` (max per distinct tag, already
  immune) and in the open edge-magnitude item.
- **THE REASON OBJECTS ARE NOT COLLAPSED, AND THAT IS THE WHOLE DESIGN.** `dedupeReasons` cannot
  merge them because **`effectKind` is load-bearing for archetype detection** — `mechanisms.ts`
  matches on it, and Archon's six carry aristocrats' `forced-sacrifice`, `drain` and
  `player-life-loss` beside `draw-card` and `lifegain`. Dropping five of six would silently narrow
  every detector that reads them. So `claimCount` changes what the SCORE counts and the wire dedupes
  what the READER sees (`reasonTexts`), and the objects survive untouched.
  **WHAT MOVED: the top-10 edge list in 63 of 71 decks and the single top edge in 43** — what the
  graph and the inspector lead with. **WHAT DID NOT: ratings, byte-identical** (889/821/754 against
  the shipped tree, the same figures as before this change — `impactEdgeWeight` never counted the
  duplicates), **population 32,182 edges / 40,563 reasons / MESHED 288, panel 88.2% debt 0, and
  `build-population.ts` reports 0 card-category memberships moved** — the detector gate, run
  before/after precisely because `effectKind` was at risk. Build suggestions unchanged in 71 of 71
  decks (68 of which have non-empty suggestions, so the null result is real and not vacuous).
  Suite 1,933 → **1,937**.
- **THE RATINGS BLIND SPOT IS NOW MEASURED, AND IT WAS REAL — `bin/ratings-compare.ts`, free.**
  Every other gate watches a different axis: `panel-score.ts` measures CLAIMS,
  `population-compare.ts` edges and reasons, `build-population.ts` category membership. **None of
  them reads a per-card rating**, so `322d129` and the token mediation both asserted their ratings
  effect from the code path rather than measuring it — the identical sentence, twice.
  Modes: `--save` / `--against` (the general before/after) and `--tokens-off`. The diff is a pure
  module (`ratings-diff.ts`, 8 tests) so the comparison is testable without Mongo.
  **MEDIATION'S MEASURED COST, against a tree with the suppression line disabled: ratings moved
  461 of 6,236 (7.4%), mean |delta| 0.016 · raw scores 299 · partner counts 278 · TOP-rated card
  changed in 2 of 71 decks (colorless-spellslinger, venser) · top-10 membership in 9 · 37 deck
  facet moves.** Small, concentrated, and not zero.
- **`--tokens-off` IS NOT THE PRE-TOKENS ENGINE, and finding that out is why the number above is
  measured the way it is.** Omitting `tokenTags` removes the token NODES only; the mediation rule
  (`e.subject.token === true && t.verb !== "create-token" && !p.isToken && !c.isToken`) keys on the
  producer's SUBJECT, so the direct shortcut stays suppressed in BOTH arms. Measured: **0 raw-score
  and 0 partner-count moves between the arms**, which is the directional pass saying so — it runs
  over `unique` (real cards) and never reads a token edge. What DOES move is the axis pass, which
  reads every edge: 10 ratings and 5 decks' breadth, all through the `doubleDuty` 1.15x premium
  flipping. **A rating is DECK-RELATIVE** (`score / deckMax`, rounded to 0.1), so a uniform lift
  moves nothing and the tool prints that caveat on every run — read "raw scores moved" beside
  "RATINGS moved" before calling a change inert. It is also why one card falling (Caretaker's
  Talent 2.99 → 0 in naya-spellslinger) lifts a dozen others in the same deck by renormalization
  rather than on their own merits.
- **Out of scope, unchanged from the spec.** The 11 COPY-family token creators (Saw in Half,
  Court of Embereth, Coiling Rebirth, Stormsplitter, Magar of the Magic Strings, Determined
  Iteration, Caretaker's Talent, Skitterbeam Battalion, Cybership, Kibo) keep their direct edges —
  a token copy's identity is the copied permanent, not a canonical token row. Token-created
  tokens are also out of scope.

- **EDGE MAGNITUDE — SWEPT AND REFUSED TO SHIP, 2026-08-18.** The supply:demand discount
  (`m = min(1, GLUT/R)^BETA`, `packages/matcher/src/magnitude.ts`, a 5-task plan) is fully built and
  wired per-tag/per-direction into `analyze.ts`'s directional pass, gated on `impact-weights.json`'s
  `magnitude.beta` — and ships at **`beta: 0`, the term OFF**, because no swept value cleared the
  criterion registered in the spec BEFORE any number was seen (§5): total inversions must FALL and
  the count of scarce payoffs whose rating FELL must be ZERO. Swept `BETA ∈ {0.25, 0.5, 0.75, 1.0}`
  against `packages/matcher/src/bin/supply-demand.ts --inversions`'s 469-shape baseline (the
  instrument lives in `packages/matcher/src/rank-inversions.ts`), all 71 decks, and **RE-SWEPT the
  same day against the corrected 3,426 baseline** (next bullet): part 1 held at every value
  (inversions 3,426 → 3,363 / 3,363 / 3,324 / 3,332 — Δ **not monotonic** in `BETA`, a
  threshold-crossing count renormalising a deck-relative score, not a clean dose-response, and it
  does not soften part 2; 0.25 and 0.5 landing on the same 3,363 is a CHECKED coincidence, the two
  snapshots differing on 106 of 796 payoff ratings). **Part 2 failed at all four, cleanly and
  monotonically, and got WORSE as `BETA` rose — payoffs that fell 114 → 190 → 250 → 280**, over
  109 / 181 / 238 / 267 distinct deck-card pairs. The criterion was not relaxed, widened or
  reinterpreted to manufacture a pass.
- **THE 3,463 BASELINE WAS STALE, AND THE RE-SWEEP AGAINST THE FIX IS DONE (2026-08-18): raw figures
  moved, no conclusion did.** `supply-demand.ts`'s `side()`
  pushed a token's BARE name into a row's `names`, and 92 of 661 distinct token names collide with a
  real card's — so a token payoff silently borrowed the same-named card's `synergyRating` instead of
  reading unmeasurable. Fixed with the graph's own `nodeId(name, true)` / `TOKEN_ID_PREFIX`, the
  identical collision-guard `nodeId` already exists for. Re-measured at `beta: 0` (curve off, isolates
  the naming bug alone): 469 shapes unchanged, **inversions 3,463 → 3,426 (−37, 1.1%), unmeasurable
  65 → 68 payoffs / 918 → 941 feeder comparisons**, 799 → 796 payoff rows. The four `BETA` values
  were then re-run against that baseline and the bullet above IS that re-sweep. **The ratings
  baseline at `beta: 0` came back BYTE-IDENTICAL to the pre-fix one**, so only the `beta > 0` arms
  could move — ratings moved 1,126 / 1,824 / 2,233 / 2,511 of 6,236, mean |Δ| 0.020 / 0.036 / 0.049 /
  0.060, raw scores **3,984 at every value (was 3,973** — the same fix wave's `analyze.ts` change from
  the deduped `unique` to `resolved`), top card 2/71, top-10 11 / 16 / 17 / 21, partner counts and
  deck facets 0 throughout. **`shapes` and `unmeasurable` holding constant across a sweep is
  invariant BY CONSTRUCTION, not confirmation of anything** — the ratio is built from tags and
  reasons and `ratingByName` is keyed on every card, so neither can move when only `beta` does. The
  earlier write-up read that constancy as evidence the token exclusion is inert to the curve; it
  cannot be. Method: `.superpowers/sdd/2026-08-18-edge-magnitude/resweep-report.md`.
- **THE FAMILY THE DESIGN CALLED HARDEST TO ARGUE AGAINST STILL LOST.** `graveyard-recursion` is 35
  of the 114 fallen payoffs at the gentlest swept value (31%), second by ONE row behind `enters` at
  36, and the largest family at every higher `BETA` (64 of 190, 82 of 250, 96 of 280). The pre-fix
  text said "34, the largest single family" and that superlative is **not recheckable** — the first
  sweep saved no per-`BETA` snapshots. Read the family table as descriptive, never causal: a row is
  attributed to the shape it SITS in while the mechanism is bleed onto a DIFFERENT tag. The 36
  `enters` rows were READ — ordinary ETB payoffs losing the same 0.05-0.15 (Fleshbag Marauder and
  three siblings 2.07 → 1.955 in `burakos-crashing-the-party`), sharing **zero** (deck, card) pairs
  with the graveyard-recursion rows, so the two families are disjoint sets of cards. The named
  witnesses all reproduce post-fix —
  Eternal Witness, Takenuma Abandoned Mire, Malakir Rebirth, Tomb Fortress, Yuna Hope of Spira, each
  losing 0.05-0.15 rating points. Traced to `edges.ts`'s reanimator-consumer loop: the shape is
  supply-glutted (median 6:1, 128 of 157 rows over 3×) because a recursion spell IS correctly fed by
  every card that could land in the graveyard — discard, mill, sacrifice, dies triggers all count —
  which is exactly how a reanimator deck is built on purpose; spec §6 named this family "least
  obviously wrong to be glutted." **The loss is not the recursion spell's own credit being discounted
  directly** — §4.5 says a supply glut should hit the FEEDER's credit, not the payoff's. It is
  CROSS-TAG BLEED: a card's `score` sums its credit across every tag it participates in, and these
  same recursion payoffs also sit on a glutted FEEDER side somewhere else in their own deck (a
  reanimation target is itself frequently an ETB/dies/leaves producer for something else), so
  discounting that other role drags the card's total score down, and with it its rating in the
  scarce-payoff role the instrument was watching.
- **THE THREE GATES DID NOT MOVE — THE ACCEPTANCE TEST, NOT A NULL RESULT.**
  `population-compare.ts` 32,182 edges / 40,563 reasons / MESHED 288, `panel-score.ts` 88.2% debt 0,
  `build-population.ts` 0 category memberships moved — MEASURED once, at the shipped `beta: 0`, and
  STRUCTURALLY unreachable at any other swept value: the discount only alters what the ratings pass
  READS (an `impactEdgeWeight` multiplier), never an edge, a reason or a claim text, and
  `build-population.ts`'s own call chain (`detectBuildCategories`, `winconReport`, `pressure.ts`,
  `land-count.ts`) never imports `magnitude.ts` or `impactWeights` at all. Ratings DID move under the
  sweep (1,114 → 2,495 of 6,236 across the four values, raw scores 3,973 moved at every value since
  the multiplier is discount-only), which is the diff the inversion instrument exists to JUDGE rather
  than merely count.
- **CEILINGS CARRIED FORWARD FROM THE DESIGN, UNCHANGED BY THE MEASUREMENT** (spec §6):
  `EVENTS_PER_ROUND` is provisional and unchecked — `once` (fires once per GAME) and `per-cycle`
  (fires every round) share one constant, and nothing gates them even though they feed the ratio.
  Tokens are counted at the implied rate; `loadTokenTags` is not joined into the census (3.3% of
  supply sides, 13.8% of demand). `amount` is capped at 10 so one "create twenty" cannot decide its
  deck's ratio alone. `BETA` and `GLUT` were seeded and swept against these 71 decks only — a true
  storm deck's 53:1 casting supply IS its engine, and nothing here says otherwise for an archetype
  the calibration set doesn't contain.
- **Reopening this needs a different shape, not a different number.** A per-role score — so a card's
  scarce-payoff credit cannot be bled down by its own glutted-feeder role elsewhere in the same deck
  — is the concrete next step if this comes back. Sweeping further values of `BETA` will not fix a
  defect that gets measurably WORSE as `BETA` rises.
- **THE REOPENING HAPPENED THE SAME DAY, AND `beta` STILL STAYS 0.** The per-role plan
  (`docs/superpowers/specs/2026-08-18-per-role-score-design.md`) shipped `payoffRating`/
  `feederRating` beside the headline (byte-identical at `roleBlend: 1`, the acceptance test for
  Tasks 1-5), re-registered Part 2 to protect only majority-payoff cards' HEADLINE rating (gating
  the new `payoffRating` directly would be vacuous — it can only rise under a discount-only term),
  measured the protected set at **732 of 796 payoff rows, 92.0%**, and swept `BETA` again against
  it. **Part 1 clears at every value; Part 2 fails at every value, monotonically worse as `BETA`
  rises — 100 → 170 → 221 → 250 fallen protected rows of 732 examined (13.7% → 23.2% → 30.2% →
  34.2%).** `magnitude.beta` stays 0.
  **Not a coincidental repeat of the same failure — a structural one.** At `roleBlend: 1`, the only
  value this sweep runs at, the headline formula (`score = authority + feederLift`) is
  byte-identical to the blended score the earlier sweep tested, so the identical cross-tag/cross-
  role bleed reaches the headline exactly as before: a majority-payoff card by composition can still
  sit on a glutted FEEDER role on a DIFFERENT tag elsewhere in its own deck, and discounting that
  role's credit still drags the summed score, and the headline, down. All five witnesses above
  (Eternal Witness, Takenuma, Malakir Rebirth, Tomb Fortress, Yuna) reproduce at the identical deltas
  in the per-role sweep's fallen list, now additionally confirmed `protectedPayoff: true`. **The
  per-role work's real contribution is the INSTRUMENT — a majority-payoff/majority-feeder
  classification that did not exist before — not a change to what ships at `roleBlend: 1`.**
  `graveyard-recursion` leads the fallen-row family table at every swept value this time (33 / 61 /
  79 / 92 of the fallen protected rows, zero (deck, card) overlap with `enters`), fully recheckable
  off saved per-BETA snapshots (the earlier sweep's family table was not). Three standing gates
  re-confirmed at the shipped state: 32,182 edges / 40,563 reasons / MESHED 288, panel 88.2% debt 0;
  `build-population.ts`'s 0-moved figure is argued rather than re-diffed (no pre-task snapshot
  exists, but the tree is byte-identical and its call chain provably never reads `magnitude` or
  `impactWeights`). `impact-weights.json` restored and verified (`git status --short packages/`
  clean, file reads back `{glut: 3, beta: 0}`, `roleBlend: 1`).
  **If this is reopened again, the next lever is `roleBlend < 1`, not another `BETA` sweep** — it
  would directly shrink the feeder-role contribution to the headline, which is the one thing that
  could break the bleed path Part 2 keeps failing on.
  → spec `2026-08-18-per-role-score-design.md` §8, `2026-08-18-edge-magnitude-design.md` §4.1,
  method and full logs `.superpowers/sdd/2026-08-18-per-role-score/` and `task-6-report.md`
  (untracked — `.superpowers/` is gitignored).
- **THAT LEVER WAS SWEPT THE SAME DAY AND IS REFUSED TOO — the 2-D grid is done, NOTHING SHIPS, and
  an adversarial review (Fable) rewrote the ARGUMENT while leaving the verdict standing.**
  `roleBlend ∈ {0.75, 0.5, 0.25} × BETA ∈ {0.25, 0.5, 0.75, 1.0}`, each BETA arm judged against a
  fresh `beta: 0` baseline at its OWN `roleBlend` (12 arms, 3 baselines, 15 runs).
  **Part 1 clears at all 12 points; part 2 fails at all 12** — against a criterion of ZERO fallen
  protected payoffs the arms land at 45-225 rows. `magnitude.beta` stays **0**, `roleBlend` stays
  **1**, `impact-weights.json` restored and verified.
- **THE FALL COUNT REALLY DOES DROP, AND THE GATE GETS STRICTER — the first write-up said the
  opposite and was WRONG.** The protected set is exactly NESTED over an IDENTICAL 796-row universe
  (recomputed from the three saved baselines: 760 ⊂ 780 ⊂ 796), because the executable predicate is
  `supply-demand.ts:129` `2 * authority >= score`, i.e. `authority >= roleBlend * feederLift`
  (`analyze.ts:417`) — a lower blend lowers the bar and ADMITS more rows. So 45 falls over a
  superset of the 732 rows protected at `roleBlend: 1` is a genuine reduction, not a denominator
  artifact, and the "different row sets" claim is withdrawn. Total classified falls
  (protected + feeder, immune to reclassification) drop **114 → 89 → 75 → 45** at BETA 0.25.
- **WHAT THE REDUCTION BUYS IS NOTHING — the fall RATE is flat across the whole grid.** Total
  headline falls per moved rating sits in **10.0-12.3% in all 16 cells** while ratings moved spans
  **366 to 2,511 (6.9×)**, and at BETA 0.25 it RISES as `roleBlend` falls (10.1 → 10.5 → 10.9 →
  12.3%). Lowering `roleBlend` does not stop the discount hurting scarce payoffs; it moves fewer
  ratings, and the same ~1 in 9 of the ratings that move is a payoff falling. Read the rate as an
  index, not a probability — falls are `(deck, tag, card)` payoff rows and moved ratings are
  `(deck, card)` over all 6,236 rated cards counting RISES too (`ratings-diff.ts:74`). What IS
  vestigial at `roleBlend: 0.25` is the LABEL, not the gate: 796/796 protected and zero
  majority-feeder rows, so the classification separates nothing there.
- **THE TWO DISCOUNT PATHS ARE NOW DECOMPOSED, AND THE HYPOTHESIS THE RUN WAS BUILT TO TEST IS
  REFUTED.** Two discounts reach a headline: `wFeeder` (supply-glut, feeder-side, scales with
  `roleBlend`) and `mag.payoff` (demand-glut, cuts a payoff's OWN `authority`, `roleBlend`-
  INDEPENDENT) — `analyze.ts:379-380`. Run each alone via a temporary env switch clearing one
  multiplier map, reverted after (tree clean, weights `roleBlend: 1, beta: 0`).
  **THE FEEDER PATH IS THE WHOLE EFFECT**: of the falls, **98 of 100** (r1/BETA 0.25), 247 of 250
  (r1/1.0), **43 of 45** (r0.25/0.25), 104 of 112 (r0.25/1.0). **THE PAYOFF PATH MOVES PART 1 THE
  WRONG WAY** — inversions 1350 → **1352 / 1353** and 1281 → **1281 / 1283**, so part 1 FAILS on it
  alone at all four corners, and feeder-only BEATS both-paths everywhere (1260 < 1264, 1059 < 1066,
  1010 < 1014) because the payoff branch partially CANCELS the feeder gain. **On this evidence the
  demand-glut branch earns removal on its own merits — it costs part 1 and buys 4-9 falls.**
  **REFUTED**: `mag.payoff` is NOT the floor under the residual falls at low `roleBlend` — at
  `roleBlend: 0.25` the feeder path alone still causes 43 of the 45. So the cross-tag feeder bleed
  really does survive shrinking the blend to a quarter, and that sentence is now a MEASUREMENT at
  two corners rather than a carried-forward story. **A fourth attempt has to attack the feeder-side
  bleed itself; the blend cannot dilute it.**
  **The `both` arms reproduced the grid and the spec EXACTLY** (1264 / 100 / 95 and 1066 / 250 / 238
  at `roleBlend: 1`; 1181 / 45 / 40 and 1014 / 112 / 104 at 0.25), so the switch is a no-op when
  unset AND the `roleBlend: 1` anchor row — whose raw snapshots were missing — is recheckable again
  from `decomp/` at BETA 0.25 and 1.0. BETA 0.5/0.75 and every `ratings moved` figure in that row
  remain spec-table-only.
  Two further ceilings: **`roleBlend < 0.25` is unswept** and the count is still strictly decreasing
  at 0.25, so refusing that region is a JUDGEMENT (a headline that barely carries feeder credit is a
  different product; tuning it to clear a gate is Goodharting the gate) and not a measurement; and
  `doubleDuty` flips were not excluded, though every inspected row is consistent with a one-step 0.1
  drop times the 1.15 premium.
  Remaining ceilings: **`roleBlend < 0.25` is unswept** (refusing it is a JUDGEMENT — a headline
  barely carrying feeder credit is a different product, and tuning it to clear a gate is Goodharting
  the gate), and `doubleDuty` flips were not excluded. Baseline inversions are non-monotonic in `roleBlend`
  (1350 / 1365 / 1275 / 1281) with each arm judged against its own baseline, and moving `roleBlend`
  alone is itself a large ungated product change (mean rating 0.978 → 0.882 → 0.781 across the three
  baselines) that this grid licenses nothing about.
  **The supply:demand discount has now been refused three times on the same criterion** (the BETA
  sweep, the per-role re-registration, this grid). The next attempt should DECOMPOSE the two
  discount paths before picking a lever — only one of them is the mechanism the last three
  write-ups blamed.
  → `.superpowers/sdd/2026-08-18-per-role-score/roleblend-sweep/report.md` (grid, logs, script).

