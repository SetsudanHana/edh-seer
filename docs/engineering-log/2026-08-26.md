## State as of 2026-08-26

**STRIPPING HEXPROOF FROM AN OPPONENT IS REMOVAL, NOT PROTECTION — shipped 2026-08-26, free,
`rules.json` 7 -> 8. 3 cards leave `protection` across 8 decks: Shadowspear · Nowhere to Run · The
Fire Nation Drill. Only `protection` moved; population, panel and MESHED byte-identical
(34,235 / 43,254 / 287, panel 95.1% debt 0). Suite 2,558.**
- **A TRUE CATEGORY REACHED BY A FALSE SENTENCE.** *"Permanents your opponents control lose hexproof
  and indestructible"* and *"as though they didn't have hexproof"* exist so your REMOVAL connects.
  Both were counted as Interaction of the PROTECTION kind. **12 corpus cards match the cue.**
- **FOUND BY A PROBE FOR A DIFFERENT ITEM.** These surfaced while sizing I1's grant question, in the
  34 protection cards that derive no grant kind — which is also where the finding that
  `keyword-grant` cannot substitute for a subject came from. **A probe built to answer one question
  is the cheapest place to notice a second.**
- **STRIPPED BY SENTENCE, NOT BY PHRASE, AND THE DIFFERENCE IS LOAD-BEARING**: Shadowspear names TWO
  protection words in one clause, so a phrase-level cut leaves "indestructible" standing and the card
  reads as protection again through the half that never was. **A CARD THAT DOES BOTH KEEPS ITS OWN
  HALF** — Archetype of Endurance grants hexproof to yours and strips it from theirs, and only the
  second sentence goes, which is why this is a strip rather than a refusal.
- **THE GRANT TEST READS THE SAME STRIPPED TEXT**, or "your opponents control DON'T HAVE hexproof"
  matches `GRANTS_PROTECTION` on the word "have" and the two questions disagree.
- **READING THE CANDIDATES ONE BY ONE IS WHAT KEPT TWO REAL CARDS.** My first cue was
  93 corpus cards wide and would have cut **Final Showdown** ("Choose a creature you control. It
  gains indestructible") and **Weathered Sentinels** ("gains indestructible until end of turn") —
  both genuine grants. The narrowed cue is anchored on OPPONENTS.
- **ONE CARD IS REFUSED ON COUNT AND ON ITS OWN COUNTEREXAMPLE.** Oubliette phases a creature out
  *"until this enchantment leaves the battlefield"* — removal — but **The Moment** does exactly that
  to *"target creature YOU control"*, so the cue needs a controller test and the family is 3 corpus
  cards. Recorded rather than built.

**A SPELL THAT CANNOT BE COUNTERED PROTECTS NOTHING BUT ITSELF — I1's largest clean sub-family
shipped 2026-08-26, free. 9 distinct cards leave `protection` across 14 decks; `rules.json` 6 -> 7.
Population, panel and MESHED byte-identical (34,235 / 43,254 / 287, panel 95.1% real 386 / false 20 /
debt 0) — a build category forms no edge, which IS the acceptance test. Suite 2,555.**
- **`protectionIsOwnKeyword` ASKED THE RIGHT QUESTION AGAINST THE WRONG LIST.** It tested every
  protection word against the card's printed KEYWORDS, so it saw "hexproof" on Sylvan Caryatid and
  could not see **"This spell can't be countered"** — the same fact in a sentence rather than a
  keyword. **13 of the 113 cards the rule fires on say only that**: 10 "can't be countered" (Nezahal,
  Niv-Mizzet Parun, Hullbreaker Horror, Toski, Chandra Awakened Inferno …) plus 3 Myojin entering
  with an indestructible counter ON THEMSELVES.
- **STRIPPED, NOT MATCHED**, so a card that says both keeps its other half — what survives the strip
  still has to clear the keyword test, and a GRANT still wins first.
- **9 OF THE 13, AND THE FOUR THAT STAY ARE THE CONSERVATIVE DIRECTION**: Hexing Squelcher and the
  Myojin repeat the word in another sentence ("remove an indestructible counter"), which is a COST
  and not a claim of protection. The strip deliberately does not reach it.
- **NO DECK CROSSES ITS TARGET, WHICH IS WHY MEASURING THE COUNT WAS NOT ENOUGH: 9 decks' Interaction
  count moves and EIGHT were already over 10** (`fight-club` 22 -> 21, `iz-it-izzet` 14 -> 12).
  **Exactly one deck's attainment moves — `magda-mother-of-dragons` 8/10 -> 7/10** — because it was
  already under. The counts get truer and the score barely moves.
- **A GUARD THAT CANNOT FIRE IS LABELLED AS ONE.** The `found(txt).size === 0` branch is UNREACHABLE
  through the shipped rule — the op runs only after the protection pattern matched — so mutating it
  away changes no test. Kept, because the op reads under `not:` where a wrong answer SUPPRESSES the
  rule, and said so in its own test rather than left to be discovered.
- **THE ITEM'S OWN PROPOSED FIX IS A DIFFERENT THING AND STAYS REFUSED**: "a derive-side read of the
  grant's SUBJECT" cannot run, because **145 of 390 derived grants (37%) carry no subject at all** —
  Swiftfoot Boots included. **`keyword-grant` ALONE WAS TESTED AS A SUBSTITUTE AND IS WRONG IN BOTH
  DIRECTIONS**: of the 34 protection cards deriving no grant kind, some are self ("can't be
  countered"), some are real grants the clause layer gives no kind (Cunning Nightbonder, Destiny
  Spinner — "spells you control can't be countered"), and two are outright misclassifications
  (Oubliette and Nowhere to Run phase out an OPPONENT's creature, which is REMOVAL).

**A PLAYER NAMED ONLY INSIDE A COUNT IS NOT THE ACTOR — I2's Soldiers half shipped 2026-08-26, free,
`DERIVE_VERSION` 81 -> 83. Edges 34,233 -> 34,235, reasons 43,252 -> 43,254, MESHED 287 unchanged,
panel 95.1% real 385 -> 386 with FALSE 20 UNCHANGED and debt 0 — a recall win at no precision cost.
Build memberships 0, themes 0 headlines, suite 2,551.**
- **"CREATE A JUNK TOKEN FOR EACH OPPONENT YOU ATTACKED" MAKES THE TOKENS YOURS.** The opponents are
  the multiplier; `parseControl` matched the nearest player and handed them away, so the card
  supplied nothing to its own deck's token payoffs — the opposite of what it does. It now cuts at
  `for each` / `where X is` / `equal to the number of` and reads the HEAD. **An opponent-facing
  subject keeps its cue, because that cue sits BEFORE the count.**
- **THE ITEM SAID ONE CARD; THE FAMILY IS FIVE, AND ALL FIVE WERE READ AGAINST PRINTED TEXT.** Call
  the Coppercoats · Rose, Cutthroat Raider · Hylda's Crown of Winter · Kaito, Bane of Nightmares are
  one mechanism. **Meathook Massacre II is a DIFFERENT one and stays open**: it returns the
  opponent's dying creature *"under your control"* and the emit takes the ANTECEDENT's controller
  over the explicit statement.
- **THE SAME CUE HAD TO GO IN TWICE AND ONLY MEASURING SHOWED IT.** With `parseControl` alone the
  four moved `opp` -> **`any`** — edges form, but `any` is a PERMISSION that satisfies an
  opponent-facing consumer just as readily. `CONTROLLER_DEFAULT` had declined because the sentence
  named a player INSIDE the count. Cutting there too takes three of them to **`you`**, and
  `COUNT_PHRASE` is exported from `subject.ts` so the two readers cannot disagree about where a head
  ends. **THE SECOND CUT REMOVED ONE EDGE THE FIRST ADDED (34,236 -> 34,235)**, which is the
  tightening working rather than a regression.
- **CALL THE COPPERCOATS STAYS `any` AND THE GUARD IS RIGHT TO DECLINE**: its clause says "Choose any
  number of TARGET OPPONENTS" outside the count, so a player really is named. That is the antecedent
  problem, not this cue.
- **I1's RESIDUE IS REFUSED ON A PREMISE CHECK, AND THE PREMISE WAS WORSE THAN FILED.** The item says
  the fix is "a derive-side read of the grant's SUBJECT" — but **Swiftfoot Boots derives
  `static / keyword-grant` with NO SUBJECT AT ALL**, and **145 of 390 derived grants (37%) carry
  none**. So the recipient is not there to read: closing it is a 145-ability derive change with its
  own before/after, not a residue. **Carnage Tyrant, the item's own witness, is not derived at all.**

**THE FIVE CARDS I3 ORPHANED ARE BOUGHT — $0.02, 5 persisted / 0 refused / 0 failed, and the deck
that admits an unread card goes 25 of 71 to ZERO (2026-08-26). Edges 34,119 -> 34,233 (+114), reasons
43,132 -> 43,252 (+120), MESHED 287 UNCHANGED, panel 95.1% real 385 / false 20 / debt 0 BYTE-IDENTICAL
— new claims at no precision cost. Suite 2,547; no file changed, the buy lands in Mongo.**
- **THE COVERAGE PANEL FOUND ITS OWN FIRST ITEM WITHIN THE HOUR.** I3 (2026-08-25) re-pointed five
  colliding names off the wrong doc onto the right one — and the right one was never in the bought
  scope, so **Reanimate (12 decks) · Rampant Growth (10) · Regrowth (3) · Exsanguinate (3) · Seething
  Song (3)** resolved and derived NOTHING. Silent until this afternoon's admission started naming
  them. **A fix that moves a name can orphan the card it moves it to; check derivation after a
  resolution fix.**
- **THE EFFECT WAS PREDICTED BEFORE THE SPEND AND THE PREDICTION IS WHAT MAKES IT CHECKABLE**: edges
  and reasons would RISE, because Reanimate sat in 12 decks forming none. +114 / +120.
- **RATINGS MOVED 291 of 6,236** (mean |Δ| 0.0109), raw scores 828, partner counts 461, **TOP card
  0/71**, 26 facet moves. **THE BASELINE WAS TAKEN BEFORE THE BUY**, which the 2026-08-22 batch is
  recorded as having failed to do — that spend's per-card rating effect is unmeasurable forever.
- **BUILD MOVED EXACTLY 3 MEMBERSHIPS AND ALL THREE ARE THE CLASSIFIER CONFIRMING ITSELF: `ramp`
  gains Seething Song** in `judith-tokens-connoiseur`, `otterly-awesome-spellslinger` and
  `who-is-the-reddest-of-them-all`. It is a ritual; ramp is where it belongs.
- **THEMES: 0 headlines, 4 cohesion moves, and `codie` is NAMED where it declined** — census surplus
  +48 with **1 tag newly closing a loop**.
- **THE PROVIDER TRAP FIRED FOR THE THIRD RECORDED TIME AND THE DRY RUN IS WHY IT COST NOTHING.**
  `packages/tagger/.env` held ONLY `ANTHROPIC_API_KEY`, so the first dry run read
  **`provider: ollama`** and the gate said *"--run would refuse until you source .env"*.
  **FIXED THE SAME DAY at the owner's word: `TAGGER_PROVIDER=anthropic` is in the file now**, verified
  by a dry run with no inline variable reading `provider: anthropic`. The trap is closed on THIS
  machine and not in general — the file is gitignored, so a fresh clone starts without it.

**THE PAIR-JUDGING TOOL IS NOT MOUNTED UNLESS THE OWNER ASKS FOR IT — first exposure's last blocker,
closed 2026-08-26, free. Proven three ways on a live server: on master `GET /api/calibrate/pair`
answers 200; with the gate and no flag BOTH routes answer 404; with `MTG_CALIBRATE=1` it answers 200
again. `POST /api/analyze` is 200 throughout, and the blocked POST left the pairs file and the
fixture untouched. Suite 2,547.**
- **IT IS A WRITE ENDPOINT ON THE PANEL'S OWN INPUTS.** `POST /api/calibrate/verdict` writes
  `pair-calibration`'s pairs file AND its clause fixture — both back a RATCHET the suite enforces
  (`KNOWN_DEFECT_CAP`) — and the panel they feed has been owner-denominated end to end since round 4.
  **Anyone who could reach the server could inject judged pairs into the instrument this project
  measures itself with.**
- **UNMOUNTED, NOT GUARDED.** The controller and its service are left out of the module entirely
  rather than kept with a flag check inside the handler: **there is no handler to reach**, which is
  the difference between an endpoint that cannot run and one that is one bug away from running.
- **EXACTLY `"1"`, NOTHING ELSE.** A gate that accepts any non-empty string opens on
  `MTG_CALIBRATE=false`, which is the classic way one of these fails open. Pinned by a test over
  `"" · "0" · "false" · "no" · "true" · "yes" · "on" · " 1"`.
- **A 404 IS THE TOOL BEING OFF AND THE CLIENT SAYS SO**, rather than printing "pair request failed:
  404" and sending the owner to debug a server behaving exactly as configured.
- **AUDITED RATHER THAN ASSUMED: calibrate is the ONLY write path in the served app.** Grepping
  `writeFileSync|appendFileSync|createWriteStream|unlink|rmSync` across `packages/web/server/src`
  returns those two lines and nothing else; the one other hit in the repo is a client FIXTURE build
  script that is never served.
- **`specs/2026-08-23-first-exposure-plan.md` NOW HAS BOTH BLOCKERS CLOSED** — this and the
  underived-card admission, both today. What is left of it is the cloudflared tunnel and the ruling
  that **92.9% must not go on screen**: it is about the owner's 71 decks and 895 frozen pairs, not a
  friend's deck.

**HALF A PRECON WAS INVISIBLE TO THE SYNERGY ENGINE AND NOTHING SAID SO — the underived-card
admission shipped 2026-08-26, free. `precon-party-time` reads 52 of 100 cards DERIVED, so 48 formed
no edges and carried no theme while every number on the page read as a whole-deck figure. Gates
byte-identical (34,119 / 43,132 / MESHED 287, panel 95.1% debt 0), suite 2,545.**
- **`MissingCards` REPORTS ONLY WHAT FAILED TO RESOLVE, WHICH IS A DIFFERENT FAILURE.** A card that
  resolves with `tags: null` looked identical to a fully analysed one. **The 71 calibration decks run
  ~99% derived — the corpus was bought FOR them — so no instrument here could ever see this**, and it
  was never even an item on the roadmap. Found by measuring the arbitrary-pasted-deck case the week
  plan filed as "a lever with NO item anywhere".
- **THE CLAIM IS NARROW BECAUSE THE LOSS IS NARROW, and over-stating it would be its own wrong
  answer.** `detectBuildRules` matches mostly on ORACLE TEXT and TYPE LINE, and the mana model, land
  count, castability, legality, bracket and combo detection all read PRINTED data — every one still
  works. So the sentence names the half that survives: *"they form no synergy edges and carry no
  theme. Their mana cost, type and text still count everywhere else in this report."*
- **THE SENTENCE SHIPS AS DATA ON THE REPORT, ONE COPY** — no subpath of `@mtg/matcher` is safe to
  value-import from client code, and two copies of a claim is how two surfaces start disagreeing
  (N6's number format, `DeckIdentity`'s stale caveat). Same ruling the commander-tax caveat ships
  under.
- **IT LEADS THE OVERVIEW COLUMN ABOVE LEGALITY and heads the CLI report**, because every number
  below is computed over the part of the deck that could be read. **ABSENT WHEN IT READ EVERYTHING**:
  a report with nothing to admit says nothing, which is why the 71 decks are untouched.
- **THE COUNT IS SLOTS AND THE LIST IS NAMES**, capped at eight as legality caps its lists. A 30-copy
  Dragon's Approach deck has 30 unread SLOTS and one unread NAME, and "1 card" would understate the
  hole on exactly the paste this exists for. Proven by mutation.
- **A SMALL I3 RESIDUE FELL OUT: "Rampant Growth" and "Reanimate" are underived in three separate
  decks.** I3 re-pointed those names off the wrong doc onto the right one, and the right one was
  never in the bought scope. Two cards, ~$0.001 each through `--card`.
- **FIRST EXPOSURE HAS ONE BLOCKER LEFT AND IT IS SECURITY-SHAPED**: `app.module.ts:11` mounts
  `CalibrateController` and `calibrate.deps.ts:163-164` WRITES the pairs file and the fixture, so a
  visitor clicking `#calibrate` can inject rows into the owner-denominated panel cache.

**A THEME COUNTED AS THREE IS A THEME NOBODY IS TOLD ABOUT — subsumption ships into COHESION and is
REFUSED in the RANKING, both on measurement (2026-08-26, free). Two decks the OWNER named
"spellslinger" get a theme where they had NONE: `otterly-awesome-spellslinger` 0.063 -> 0.159,
`kuja-spellslinger` 0.081 -> 0.113. 7 cohesion moves, ALL UP, every one a spell-casting deck;
0 headlines changed. Gates byte-identical (34,119 / 43,132 / MESHED 287, panel 95.1% debt 0, ratings
0 of 6,236, build-population 0 keys). Suite 2,538.**
- **THE RANKING IS THE OBVIOUS PLACE AND IT IS WRONG FROM BOTH ENDS — built, measured, refused, and
  this is the FOURTH ranking redesign this line has killed.** `cast:instant` sits inside
  `cast:-creature` sits inside `cast:spell`, and **31 of 71 decks split their `cast:` themes across
  such a pair**. Extending `subsumedKey` past the literal `:any` sibling eats the family whichever
  way it points. **NARROW ABSORBS WIDE** (the shape `:any` already had): a rarer key carries a higher
  idf and then inherits a commoner one's mass, so **"instants" heads nine decks** and **thirteen MORE
  decks stop being nameable** — `birb-control` "draw" (0.43) -> **"instants" (0.05)**. **WIDE ABSORBS
  NARROW**: `enters:any` absorbs every typed `enters` tag, **"enters" heads a dozen decks**, and
  `marchesa-legends-matter` trades "legendary permanents entering" at **0.98** for "dies" at 0.06.
  An over-SPECIFIC bucket and the universal one. **First time the mechanism is stated rather than the
  outcome.**
- **WHAT SHIPS IS THE SHARE, AND THE DISTINCTION IS ALREADY IN THIS FILE.** `computeCohesion` counts a
  card whose theme sits INSIDE the primary's claim. **Cohesion is a SHARE, the axis is a RANKING** —
  one universal bucket destroys a ranking and a share answers correctly, measured when folding the
  axis was tried 2026-08-18. **A WIDER tag is deliberately NOT counted**: a card that casts creatures
  is not on a "noncreature spells" theme.
- **A LAND IS PLAYED, NOT CAST (CR 305.1), AND WITHOUT THAT THE CHAIN BREAKS AT ITS MIDDLE LINK.**
  `-creature` expands to ALL_CARD_TYPES minus creature, which CONTAINS land and is therefore not a
  subset of `spell` — while "noncreature spell", the phrase the tag came from, plainly is one.
  `themeSubjectKey` ranks a negation ABOVE an umbrella, so the "spell" half is dropped from the key;
  `keyDenotes` restores it by making the `cast:` family's universe the castable types.
- **A SUBTYPE IS REFUSED OUTRIGHT AND THAT IS THE WHOLE SAFETY OF IT.** `wizard` really is inside
  `creature`, and folding it that way IS the family ranking that generalised nine of twelve decks to
  "creatures entering".
- **CRITERION 1 PASSED ON 2 OF ITS 3 DECKS, RECORDED RATHER THAN CHASED.** `orzhov-spellslinger` still
  declines because its ranked head is `gain-life:spell` at deck frequency ONE — not a `cast:` tag, so
  no subset relation can reach it. Declining IS the right answer for a deck whose head is one card.
- Three mutations fire. Verified live: both decks now read *"fueled by noncreature spells"* where
  they printed no theme line at all.

**CHAIN DEPTH IS CLOSED BY ITS OWN GATE: TUTOR DENSITY DOES NOT PREDICT COMBO DEPTH (2026-08-26).
Free, no code kept, nothing shipped.** The item said *"if it fails, the depth frame is wrong and
steps 1-5 still stand alone"*, and it fails.
- **THE CRITERION WAS REGISTERED BEFORE ANY NUMBER EXISTED: Spearman rho >= 0.3 with a 95% bootstrap
  interval excluding zero, over the decks that HAVE a detected combo. MEASURED rho +0.241,
  95% [-0.159, +0.606]** over 27 decks; over all 71 with absent depth as zero, **-0.022**. Median
  depth, distinct pieces and combo count all fail too.
- **THE DECISIVE NUMBER IS THE MEAN, NOT THE CORRELATION: a deck WITH a detected combo runs 0.78
  tutors and a deck WITHOUT one runs 0.77.** The predictor cannot separate having a combo from having
  none. **Five of the 7 combo-TOP decks run ZERO tutors**, and the corpus's biggest tutor package —
  **`gengar` at 8 — has no detected combo and is a reanimator deck**. Tutor density tracks fetching
  ONE card, which is CONSISTENCY, not depth.
- **THE FRAME HAS ALMOST NO POPULATION HERE, and that is the part worth keeping. 44 of 71 decks have
  no detected combo; of the 27 that do, 15 max out at a TWO-card combo** and only 3 exceed depth 3.
  "Multi-link engine" is a shape this corpus barely holds.
- **THE INSTRUMENT WAS CONFIRMED BEFORE THE RESULT WAS BELIEVED** — the L2 lesson. Tutors reading
  median 0 looked like a broken detector; `build.ts:123` already records *"tutor's median share of
  Consistency is 0%, 44 of 71 decks carry none"*, measured 2026-08-21. The zeros are the decks.
- **THE REFUSAL IS CORPUS-SCOPED AND SAYS SO**: one owner's casual decks. A cEDH corpus would carry
  ten to fifteen tutors a deck and could read differently, so the reopening condition is DATA, not
  argument. The probe is not kept as a bin — thirty lines, and a gate that has answered once and
  closed its item is dead weight.

**THE STALE-LINE AUDIT: 11 OF 25 OPEN ROADMAP LINES WERE WRONG — 44% (2026-08-26). Seven were
ALREADY CLOSED and never ticked; four had a premise that had moved. Open items 25 -> 18. No code, no
spend.**
- **IT WAS RUN BECAUSE TWO ITEMS IN A ROW DIED ON A STALE PREMISE.** L4 refused a mana budget on a
  blocker its own comment named — *"the honest version is a goldfish simulator … a project rather
  than a coefficient"* — which had since been BUILT; the Jodah theme item was fixed by A11 five days
  after it was filed. **A roadmap line is a CLAIM ABOUT THE CODE, and an unticked one is a map of
  where the work used to be.** This file already said so twice; the rate is what makes it a rule.
- **THE SEVEN ALREADY CLOSED: the 390px canvas** (`ReportTabs.tsx:112` renders `GraphList` under
  `useIsNarrow`, which IS the review's recommendation) **· the label ceiling** (`GraphView.tsx:845`
  reads *"A FLOOR AND, SINCE 2026-08-14, A CEILING"* — **fixed the DAY AFTER it was filed and open
  for twelve days**) **· the filter-chip camera reset** (`camRef` is a `useRef` and the re-seed is a
  no-op by construction) **· the `img.src` fallback** (`art-cache.ts:57-60`) **· art-cache eviction
  AND version cleanup** (lines 38 and 46 — the item said neither existed; what survives is the
  NARROWER FIFO-not-LRU `ponytail:`) **· Monte Carlo** (shipped as `goldfish.ts`, and both halves of
  its own gate are met — L4a measured the disagreement, A1 and `goldfish-dp.test.ts` are the closed-
  form validation) **· Path to Exile's actor** (refused on its OWN last two sentences: *"worth zero
  edges today"*, *"D already closed both"*).
- **THE FOUR WHOSE PREMISE MOVED: family rollup** (its cut-list half shipped 2026-08-18) **· the CR
  gap-finding instrument** (its Saga finding shipped 2026-08-15 and D1's full sweep returned a null
  result on 2026-08-20 — the METHOD is what is open, not the findings) **· the six §26.3 misses**
  (`historic recursion` is exactly what C2c fixed) **· subsumption** (the defect HOLDS at
  `weights.ts:118`, and its witness moved: `orzhov-spellslinger` splits `cast:instant` 3 /
  `cast:-creature` **4**, not 3 / 7, after I1 and I5).
- **RE-MEASURING A WITNESS FOUND SOMETHING BIGGER THAN THE ITEM: `orzhov-spellslinger`'s ranked head
  is `gain-life:spell` at deck frequency ONE, cohesion 0.016** — a one-card tag leading a 63-card
  deck. A15's floor keeps it off the page (the deck declines to be NAMED), so no false sentence
  reaches a reader, but the ranking underneath is being decided by a tag with a single member. Filed
  beside the subsumption item.
- **THE CHEAPEST CHECK IS THE CODE COMMENT.** Five of the seven were settled by grepping the file the
  line accuses and reading the comment already sitting there — several of which state the fix DATE.

**A11 HAD ALREADY FIXED THE JODAH COMPLAINT FIVE DAYS AFTER IT WAS FILED, AND THE WIDE FIX FOR IT IS
REFUSED ON MEASUREMENT (2026-08-26). What ships is ONE LINE IN THE CENSUS: +6 payoff credits, every
other gate byte-identical (34,119 / 43,132 / MESHED 287, panel 95.1% debt 0, ratings 0 of 6,236,
themes 0 headlines / 0 cohesion). Suite 2,530.**
- **THE ITEM SAID "Jodah, the Unifier reads as a +1/+1-counters deck when it is legends-matter",
  filed 2026-08-14. `enters:legendary` became a real theme key on 2026-08-19 (A11, supertypes got
  their own fold family).** VERIFIED ON MASTER against the item's own witness: a deck led by Jodah
  and built from the corpus's legends reads **"fueled by legendary permanents entering (98% of
  nonlands)"**, unchanged by anything I did. **A legends deck is named by its LEGENDS — 98 implied
  entries — not by one anthem's subject.** Tenth roadmap premise corrected by measurement.
- **THE CARD PREMISE HELD, THE LAYER PREMISE DID NOT.** Jodah does derive `pump` with
  `legendary: true`, and `cardThemeTags` does key a static on `static:<kind>` alone. What is false is
  that this is what names the deck. **CHECK WHETHER A LATER ITEM ALREADY CLOSED THE ONE YOU ARE
  OPENING** — the roadmap is a claim about the code and A11 sits 400 lines above this item.
- **THE WIDE RULE WAS BUILT BECAUSE BUILDING IT IS HOW IT GETS REFUSED.** Crediting the axis with
  EVERY subject-gated static moves 7 headlines and **six of the seven are worse**: `birb-control`
  "draw" (0.43) -> **"islands entering" (0.05)**, `acererak-combo` "lose life" -> "zombies" (0.05),
  `golbez-artifacts` "artifacts entering" (0.57) -> "artifacts" (0.14). **The mechanism is the one
  three theme designs already died on**: a cared-about tag counts in FULL, so ONE cost-reducer naming
  artifacts mints a rare tag that beats a theme the deck actually has on tf-idf. Narrowed to
  SUPERTYPES it moves 0 headlines, 0 cohesion, 0 named decks — correct and worth nothing.
- **THE CENSUS KEY IS THE REAL IF INERT CORRECTION.** `staticPayoffTags` keyed Jodah's anthem
  `enters:creature`, crediting a legends payoff against every creature entering. `legendary` and
  `historic` outrank the card type there now. **Nothing ranks by the census (A2), so moving nothing
  else IS the acceptance test.** `themeSubjectKey` untouched: it keys REASON tags and the panel's
  verdicts are keyed on `producer|consumer|tag`, which cost 22 debt the last time a fact was composed
  into it.
- **THE FAMILY IS 5 DISTINCT CARDS — 7 derived statics carry `legendary: true`, 1 carries
  `historic`** — against 63 subtype-gated and 106 type-only.
- **A MEASUREMENT OF MINE WAS INVALID FOR AN HOUR AND THE A1 NOTE IS WHY I CAUGHT IT: the first arm
  was measured against a STALE `theme-stats.json`**, where every tag the change invents scores the
  MAXIMUM idf. Regenerating first, then decomposing (refresh alone moves **0 of 71**), is what made
  the arm-2 numbers mean anything.

**THE LAND YOU PLAY IS THE ONE YOUR HAND ASKS FOR — coloured-source sequencing shipped 2026-08-26,
free. L4's last item, SO SECTION L IS CLOSED. Mean P(castable) T1 +1.03pp (64 of 71 decks up, 1
down) · T2 +1.07pp (63 up, 0 down) · T3 +0.48pp (51 up, 0 down) · T4 +0.12pp · T5 +0.01pp ·
T6-T8 -0.02pp. Gates byte-identical (34,119 / 43,132 / MESHED 287, panel 95.1% debt 0), clock and
`power@5` move in 0 of 71, suite 2,529.**
- **THE INCUMBENT RULE READ TAPPED-NESS AND THEN HAND ORDER, WHICH IS THE SHUFFLE.** Rule 2 played
  the first land in hand that enters untapped and was blind to colour, so a player holding an Island
  and a Mountain with `{U}{U}` to cast played either one at random. `pickLand` scores candidates by
  the colour pips the HAND asks for that the BOARD cannot yet make; **a tie keeps hand order, so a
  mono-coloured or already-fixed deck is byte-identical.** The information is the player's own — hand
  and board, never a future draw.
- **THE SHAPE OF THE ANSWER IS THE RESULT: SEQUENCING IS WORTH A TURN, AND ONLY A TURN.** By turn
  five the board has five drops and the order stops mattering; from turn six it reads **-0.02pp**,
  because a land's tapped-ness is fixed AT ENTRY, so a different order enters a different conditional
  land against a different board. **An amount tie-break was built to recover that and measured
  +0.006pp, so it was DELETED as decoration** rather than kept as a guard nothing moves.
- **IT SCORES UNMET PIPS AND NOT CARDS, deliberately.** "How many cards does this land make castable"
  means calling `payable` per candidate per card per turn per trial; the colour demand is one pass
  over the same facts and agrees wherever a COLOUR is what binds.
- **BY COLOUR COUNT AT ITS BEST TURN (T2): 3+ colour decks +1.50pp against +1.07pp overall.** Worst
  deck-turn moves `mari-takes-control` T1 +3.78pp · `enchanting-rani` T2 +3.77pp ·
  `vnwxt-blue-aggro-mill` T1 +3.77pp. **P(>=6 mana at T6) moves -0.01pp**, which is why the clock —
  priced off `manaMedian` since the same day — does not move at all.
- **THE BEHAVIOURAL TEST PASSED IN BOTH ARMS AT FIRST, THE FIFTH TIME THIS FILE RECORDS THE SHAPE.**
  Three unit tests on `pickLand` prove the function and nothing about the gate that selects it; the
  simulate-level test asserted 0.4 on a fixture the COLOUR-BLIND rule already read **0.508** at.
  Measured in both arms — **hand order 50.8%, colour-aware 92.5%** on thirty Islands, thirty
  Mountains and forty `{U}{R}` two-drops at turn two — and the threshold IS that measurement.

**THE CLOCK NOW HAS TO PAY FOR ITS OWN BOARD, AND THE DEADLINE IS THE MANA RATHER THAN THE TURN
NUMBER — L4's last live half shipped 2026-08-26, free. 13 of 71 clocks move, 4 EARLIER and 9 LATER;
`power@5` moves in 13, mean +0.68 median -0.80. Gates byte-identical (34,119 / 43,132 / MESHED 287,
panel 95.1% real 385 / false 20 / debt 0), build memberships 0, suite 2,523.**
- **THE REFUSAL NAMED ITS OWN BLOCKER AND THE BLOCKER HAD SINCE BEEN BUILT.** `pressure.ts` refused a
  mana budget in 2026-08-19 because *"the honest version is a goldfish simulator with a stated play
  policy, and that is a project rather than a coefficient"* — and `goldfish.ts` IS that project,
  shipped as I11/L4a. So this is a READ off a model that already runs on every analyze: `manaModel`
  gains `manaMedian`, `analyze.ts` hands it to `computeDeckMath` beside the `castCurves` it already
  shares, and **the product pays nothing extra**. **RE-READ A REFUSAL'S STATED BLOCKER BEFORE
  RE-RANKING THE ITEM** — this one had expired a day earlier.
- **TWO HALVES PULLING OPPOSITE WAYS, WHICH IS WHY IT IS A CORRECTION AND NOT A DISCOUNT.** (a) THE
  BUDGET: creatures deploy cheapest-first against the mana the board could have made by that turn,
  CUMULATIVELY, so a creature-dense deck stops fielding every drawn body at once. (b) THE DEADLINE:
  `manaValue <= turn` is a proxy for a board nothing modelled, and it is the RAMP half — an
  eight-drop is castable the turn the board makes eight. **Half (a) alone moved 6 decks, ALL DOWN;
  both halves move 13, in both directions.** Measuring (a) alone is what showed it was half a fix.
- **THE MOVERS ARE ATTRIBUTABLE, WHICH IS THE CHECK. `izzet-big-mana` read `power@5` = 0.0 and now
  reads 8.4** (clock T10 -> T8) **and `iz-it-izzet` 6.1 -> 20.4** (T7 -> T5), the deck O1 was built
  for and whose commander makes two mana a turn. Down: `marchesa-legends-matter` 18.3 -> 13.2,
  `unify-all-the-universes` 17.7 -> 13.7, `taking-crew-to-the-town` 13.1 -> 9.5 — creature-dense
  decks that cannot pay for their own board.
- **THE CASCADE E3 NAMED IS CLOSED AND MEASURED AT THE PANEL IT FLATTERED.** E3 recorded the ramp gap
  as the ONLY bias in this layer running toward flattering the deck. Mean answer availability moves
  **36.8% -> 36.8% (+0.02pp)** with **13 decks moving and the ramp decks DOWN** — `izzet-big-mana`
  57.3% -> 52.8%, `iz-it-izzet` 36.8% -> 32.5%. **The aggregate is a wash and the flattering was
  never in the aggregate.**
- **STILL A CEILING, STATED IN THE CODE: the budget is every point of mana the board produced**, and
  a real deck spends some on removal, on draw, and on the accelerants that made the rest. It is
  merely FINITE where the incumbent was infinite. Cheapest-first is rule 3's own policy one module
  over, so it maximises BODIES rather than power.
- **A RENDERED CAVEAT WENT FALSE WITH THE FIX AND WAS CORRECTED IN THE SAME CHANGE** — the web clock
  chip said *"there is no mana budget … and no ramp shortens that"*. One copy only; the CLI never
  renders the clock. **`build-population.ts` runs the simulation too**, or the gate would measure a
  clock nothing ships. Six mutations proven to fire.

**A COMMANDER THAT MAKES MANA WAS PRICED AT ZERO, AND THAT — NOT THE FLIP — WAS O1's BLOCKER. Shipped
2026-08-26, free. Exactly the 6 decks with a mana-producing commander move: P(>=6 mana at T6) +2.04pp
mean, `iz-it-izzet` +27.72pp, mean P(castable) +31.47pp on that deck. Gates byte-identical, suite
2,514.**
- **THE ITEM'S PREMISE SHRANK TWICE AND THEN THE BLOCKER MOVED.** (a) **Ashling was ALREADY counted**,
  as a one-mana dork through `producedMana`, so the gap was one mana a turn and never the whole
  engine. (b) **The flip needs no play model at all**: the card is cast in a main phase and flips at
  the NEXT one, which is exactly the dork timing already in the model. (c) The real blocker is that
  **A COMMANDER IS NOT IN THE LIBRARY**, so rule 3 never cast it — **6 of 71 decks have a
  mana-producing commander and every one was worth zero** (Ashling ×2, Yuna ×2, Codie, Fire Lord
  Azula).
- **A COMMANDER THAT IS AN ACCELERANT NOW STARTS IN HAND.** The command zone is not the library: it
  needs no draw and is the most reliable accelerant a deck has. **Still NOT shuffled in (CR 903.6) and
  still priced through `alsoPrice`**, so it never dilutes a deck it is not in. `extras` used to be
  cost-only rows — `accelerant: null`, `output: 1`, `colors: 0` — which is exactly why the mana never
  arrived.
- **A MANA-VALUE RESTRICTION IS SETTLED BY THE DECK RATHER THAN REFUSED, and it is the ONE restriction
  class this module can check** because it knows every card's mana value. The cheapest nonland settles
  it; below the threshold the mana falls back to one. **The standing restricted-mana refusal was
  wider than its own reason** — that rule exists because the module is COLOUR-blind.
- **THE OWNER'S LINE NOW APPEARS IN THE TABLE.** They described `iz-it-izzet` as "T2 Ashling, T3 flip,
  by T4 eight mana, I can cast almost everything". The report now reads **T4 median 7 mana, p75 8,
  82% of the deck payable (p75 95%)** and **72% – 94% for six mana at T6** where it read 30% – 66%.
  **That agreement was not fitted — it fell out of oracle text and a shuffle.**
- **CEILING, STATED: the one-time flip payment is not modelled** (one mana, once) **and the commander
  is cast once and never dies** — the recast tax (CR 903.8, J5) has no model here, so all six figures
  are ceilings.

**O2 IS CLOSED: THE EVENT HALF IS ONE BUCKET BUILT AND FOUR REFUSED, CLASSIFIED BY WHETHER THE
SIMULATOR PRODUCES THE EVENT (2026-08-26). 0.000pp over the 71 decks, 0 moved — no calibration deck
holds the one card family it prices.**
- **THE QUESTION THAT DECIDES IT IS "DOES THIS MODEL PRODUCE THAT EVENT", and asking it corpus-wide
  turned "needs a play model" into a LIST.** A LAND ENTERING is rule 2 — every turn a land drop
  happens — so **landfall mana (Lotus Cobra, Locus Cobra, Nissa Resurgent Animist: 3 corpus cards)
  is built**. Refused: **you CAST a spell** (Birgi — rule 3 casts only ACCELERANTS), **COMBAT** (the
  Azulas, Fire Nation Occupation), **an OPPONENT** (Waste Not), **a BOARD EVENT** (Carnival of Souls,
  Rose). Those five cards are 7 slots in the owner's decks and every one is structurally unreachable.
- **LANDFALL MANA IS A CORRECTION DOWNWARD, NOT AN UPGRADE.** Lotus Cobra was a flat one-mana dork
  paying EVERY turn; it now pays only when a land ENTERS, and a deck misses land drops. **My first
  test asserted the wrong direction and the measurement corrected me** — it reads LOWER, and the test
  now pins exactly that. It pays PER EVENT, so a land-fetch spell putting a second land down is worth
  two, and it misses the drop on the turn it lands (rule 2 runs before rule 3).
- **BIRGI IS THE OWNER'S OWN CARD AND IS REFUSED ON PURPOSE**: pricing "whenever you cast a spell" off
  the accelerant stream would read a fraction of a real deck's spells AND feed back into the casting
  loop it came from. **A wrong number would be worse than the missing one.**
- **A SECOND UNREACHABLE GUARD, RECORDED LIKE THE FIRST: all three landfall cards are UNRESTRICTED.**
  Kept because every other mana reader here checks restriction and landfall being the one that does
  not is how a rule quietly diverges from its neighbours; the test says it has no printed witness.
- **THE PATTERN ACROSS ALL THREE HALVES OF O2: the guards were the work, not the readers.** Tap
  replacement, phase trigger and event trigger each needed one cue and three or four refusals, and in
  every case the refusals decided what the rule was worth.

**A PHASE TRIGGER IS A SOURCE THAT NEVER TAPS — O2's phase half shipped 2026-08-26, free, AND IT
MOVES EXACTLY ZERO IN THE 71 DECKS: 0.000pp on both cells, 0 decks up, 0 down. PREDICTED FROM THE CARD
TEXTS BEFORE IT WAS WRITTEN, then measured.** That is the rule being RIGHT: the only phase triggers
these decks hold are **Muerra (a RATE)** and **Ashling (RESTRICTED, and it needs the flip O1 does not
model)**, both refused by rules that predate this one. Built for the arbitrary paste, like J4's
legality rules and I9's `bfz` row.
- **THE GUARDS ARE MOST OF THE RULE: of 32 corpus cards printing the shape, 13 are priced.** Refused:
  a RATE (Black Market "for each charge counter", Sanctum "where X is"), RESTRICTED mana (Thran
  Turbine, Karn), and **a ONE-SHOT WEARING A TRIGGER — "your NEXT main phase" (Mana Drain) and "first
  main phase OF THE GAME" (Chancellor of the Tangle)**. A one-shot is not a source at any confidence.
- **A PHASE TRIGGER PAYS FROM THE NEXT TURN WHATEVER ITS TYPE LINE SAYS**: you cast it DURING your
  first main phase, so that turn's trigger has already happened. **9 of the 13 are non-creatures**
  (Eladamri's Vineyard, Frontier Siege, Altar of Shadows), where the generic creature branch would
  have paid them a turn early.
- **A CORPUS PROBE FOUND A BUG IN MY OWN GUARD, AND ONLY A PROBE WOULD HAVE.** `PHASE_ADD` stops at the
  symbol run, so "for each Raccoon" sits AFTER the match and `PHASE_RATE.test(phase[0])` could not see
  the words it exists for. It reads the SENTENCE now. **TEST THE GUARD AGAINST THE STRING IT MUST
  JUDGE, not the string the regex happened to capture.**
- **TWO OF THE FIRST FOUR MUTATIONS DID NOT FIRE, and that is the finding worth keeping.** The rate
  test used a ONE-SYMBOL card, where refusing changes nothing; the timing test used a CREATURE, which
  the generic branch already calls a dork. **A test that passes in both arms is measuring nothing** —
  replaced with witnesses that separate, and both fire now.
- **ONE GUARD IS UNREACHABLE ON TODAY'S CORPUS AND SAYS SO IN ITS OWN TEST: no card prints a
  MULTI-SYMBOL rate** ("add {G}{G} for each Elf"), so the rate refusal changes no number today. Kept
  because the shape is printable and the failure direction would be an over-claim; recorded rather
  than shipped as decoration.

**A TAP-REPLACEMENT MAKES AN EXISTING SOURCE PRODUCE MORE, AND ALL OF THEM WERE PRICED AS A FLAT
ONE-MANA ROCK — O2's tap-replacement half shipped 2026-08-26, free. Gates byte-identical
(34,119 / 43,132 / MESHED 287, panel 95.1% debt 0), suite 2,510.**
- **MY OWN FILED PREMISE WAS WRONG AND MEASURING IS WHAT SHOWED IT.** O2 said these cards are
  INVISIBLE; `classifyAccelerant` reads `producedMana`, which Scryfall stamps on every one, so Wild
  Growth and Forsaken Monument were counted all along — **as flat one-mana rocks.** Imprecise, not
  blind. **Check what the OTHER path already does before writing "reads zero".**
- **THREE DEFECTS IN ONE FAMILY.** (1) **A LAND AURA WAITS A TURN**: you tap the Forest for `{G}` to
  cast Wild Growth ON it, so it nets nothing that turn — a DORK's timing, priced as a rock's.
  (2) **THE AMOUNT IS IN A SENTENCE THE TAP READER NEVER LOOKED AT** — Overgrowth adds `{G}{G}`, read
  as 1. (3) **A PER-SOURCE BONUS SCALES WITH THE BOARD**: Forsaken Monument adds `{C}` for EVERY
  permanent tapped for `{C}`, and was worth ONE mana in a deck with twenty-seven colourless lands.
- **THE BONUS COUNTS SOURCES, NOT MANA** — two mana off one tap is still one trigger — **and it is
  checkable only because N11 made `{C}` a real mask bit** hours earlier: before that a colourless
  source and a colourless cost were the same empty mask. It brings no mana of its own, so it goes to
  `bonuses` INSTEAD of the source list; both would pay it once for existing and once per source.
- **BOTH TAILS ARE ATTRIBUTABLE, WHICH IS THE CHECK: P(>=8 at T8) +0.21pp mean, up in
  `colorless-spellslinger` +5.56 · `the-capitoline-triad` +4.21 · `fairdrazi` +3.50 ·
  `colorless-honest-magic` +3.45** — every one a colourless deck holding Forsaken Monument — **and
  down in `bello-enchantress` -1.68pp at T6**, its land auras correctly waiting a turn. **The headline
  cell barely moves because the Monument costs FIVE and is not out by turn six**, which is why the T8
  cell is where it shows and the T6 one is not.
- **STILL OPEN AND DELIBERATELY SO: the PHASE trigger and the EVENT trigger** (O1, Muerra, Birgi),
  which need a play model this module refuses to have. **High Tide stays refused as an Instant: a
  one-shot is not a source, at any confidence.**

**THE OWNER READ THEIR OWN DECK'S REPORT AND NAMED A LINE THE MODEL CANNOT SEE — filed as ROADMAP
SECTION O, MEASURED, NOT BUILT (2026-08-26).** `iz-it-izzet` plays T2 Ashling, T3 pay `{U}` and flip,
then **two mana every first main phase**; the report says 30% – 66% for six mana on turn six.
- **MEASURED AS A BOUND, method stated: +2 mana from turn 3 in every trial** — the owner's line with
  no interaction. **T4 P(>=6 mana) 32% -> 61% · T5 P(>=8) 17% -> 48% · T6 P(>=8) 34% -> 67%.** It
  over-states (assumes the flip lands on T3) and under-states (the extra mana never feeds rule 3, so
  it compounds nothing).
- **THE RESTRICTION COSTS THAT DECK NOTHING, BY CONSTRUCTION: all 62 of its nonlands are mana value 4
  or greater** (mv4:17 · mv5:9 · mv6:15 · mv7:10 · mv8:8 · mv9:1 · mv10:2). *"Spend this mana only to
  cast spells with mana value 4 or greater"* is free in a deck with no cheap spells — a construction
  the report should be able to see and cannot.
- **THE RESTRICTED-MANA REFUSAL IS WIDER THAN ITS OWN REASON.** The rule exists because the module is
  COLOUR-blind; this restriction is by MANA VALUE, which it checks on every card already. **It is the
  one restriction class this module could honour**, and the standing refusal never distinguished them.
- **THE FAMILY, SIZED BEFORE ANY CODE: 16 distinct cards / 30 slots print a recurring mana engine that
  is not `{T}: Add`, so `manaOutput` reads ZERO from them** — **Wild Growth and Utopia Sprawl are pure
  ramp and entirely invisible**, plus Forsaken Monument (4 decks), Birgi, Muerra, Leyline of
  Abundance, High Tide. **Three shapes, not one**: a PHASE trigger (priceable like a rock), a
  TAP-REPLACEMENT that makes another permanent produce more, and an EVENT trigger that needs a play
  model this module refuses to have.
- **WHAT STOPS IT BEING A PATCH: nothing in the simulator transforms anything**, and the flip is a
  per-turn optional payment — a PLAY POLICY, in a module whose whole thesis is that the policy IS the
  model.

**A DOUBLE-FACED CARD'S MANA COST IS ON ITS FRONT FACE, AND `docToCard` DROPPED THE WHOLE `faces`
ARRAY — 58 distinct cards / 116 slots were reading 0% CASTABLE (2026-08-26, found by a live run).
`iz-it-izzet` was told its own commander casts 0% of the time on turn two; it reads 80% now.**
- **A REFUSAL RENDERED AS A MEASUREMENT, at the SOURCE rather than the renderer.** `manaCost` arrived
  `undefined`, `parseCost` returned null, and `simulate` published an all-zero castable curve — so the
  N6 distinction (a refusal prints an em dash, a measured zero prints 0%) was being broken one layer
  below the surfaces N6 fixed. **Ashling, Rekindled costs `{1}{R}`.**
- **FOURTH FIELD THIS JOIN HAS BEEN CAUGHT DROPPING**, after `producedMana`, `allParts` and
  `gameChanger`. The corpus had `faces[0].manaCost` all along. **Check `docToCard` before recording a
  field as unavailable.**
- **IT MOVED THE POPULATION, AND EVERY CLAIM THAT LEFT IS CR 118.7 REACHING CARDS IT COULD NOT SEE:
  edges 34,141 -> 34,119 (-22), reasons 43,156 -> 43,132 (-24), and ALL 24 are `static:cost-reduction`
  on a modal DFC.** **Malakir Rebirth's front face is `{B}` — one pip, NO generic — so a Medallion
  cannot reduce it.** With no cost at all the guard could not refuse; with the real cost it does.
  **MESHED 287 and panel 95.1% real 385 / false 20 / debt 0 UNCHANGED** — a precision win at zero
  recall cost, verified by diffing the reason population rather than assumed.
- **THE CARD-LEVEL COST STILL WINS WHERE THERE IS ONE** (a split card carries its combined cost
  there), and **an EMPTY face cost stays `undefined`**: `""` parses as a FREE spell, which is a worse
  answer than a refusal.

**A CAVEAT OUTLIVED THE DEFECT IT DESCRIBED, AND ONLY A LIVE BROWSER COULD SEE IT (2026-08-26,
verifying the day's work). Fixed; suite 2,506.** `DeckIdentity.tsx` was still printing *"lands and
mana rocks only — land-fetch ramp like Cultivate is not counted, so this reads low"* under the
commander's cast odds. **L4a DELETED that sentence from the CLI** — the figure became a SIMULATION
that models land-fetch ramp, which was the measured defect the line existed to warn about — **and the
web's own copy went on being read for a week.** It now says what the interval IS: *"simulated: the
low end holds up two mana, the high end spends everything on ramp"*.
- **THE N6 SHAPE ONE PANEL OVER: two copies of a sentence, one of them updated.** N6 was two copies of
  a NUMBER FORMAT; this is two copies of a CLAIM, and the claim was false in the direction that tells
  a reader to distrust a figure that is now right.
- **VERIFIED END TO END IN THE BROWSER, and every one of the day's fixes shows on screen**: the
  commander line reads **48% – 59% by turn 5** and the CLI reads the same to the point (N6's shared
  renderer), 61 castability cells with **none over 100%** and a degenerate range collapsing to
  "29% by T9", and **a legal 30-copy Dragon's Approach paste reads 85% – 89% by T3** where N1's defect
  printed 2,805%.
- **A NUMBER I QUOTED MID-SESSION WAS STALE BY ONE FIX AND THE BROWSER CAUGHT IT**: `fairdrazi`'s
  commander was recorded at 59% – 69% after N11, then N12 moved it -10.31pp on castability. Both
  surfaces now agree at 48% – 59%. **Re-measure after the NEXT change, not after the last one.**

**A PHYREXIAN PIP KEEPS ITS COLOUR AND THAT COSTS UP TO 52pp — N14 stated and PRICED 2026-08-26,
free, no behaviour change. SECTION N IS CLOSED: 16 items shipped, 2 refused on their own terms.**
- **THE REASON IS A MISSING MODEL, NOT A READING OF THE CARD.** A Phyrexian pip's alternative is TWO
  LIFE, and **nothing here has a life total** — the goldfish has no opponent, takes no damage and pays
  no life — so pricing the pip as free would be a claim this model cannot support. It demands the
  colour, which is the under-claiming direction, the same one the `{2/W}` ruling takes.
- **THE COST IS MEASURED RATHER THAN ASSERTED, which is the whole of this item.** 6 distinct cards /
  12 slots. At each card's own mana-value turn, against an arm where the life is paid: **+15 to +23pp
  on a single pip** (Phyrexian Metamorph 78% -> 94% across 4 decks, Tezzeret's Gambit 68% -> 88%) and
  **+23 to +52pp where the cost prints two or three** — **Dismember `{1}{B/P}{B/P}` 51% -> 99%**,
  **K'rrik 39% -> 91%**. A far bigger number than `{2/W}`'s, and now written down beside it.
- **ONE PROBE ROW IS AN ARTIFACT AND IS DELIBERATELY NOT QUOTED: Noxious Revival `{G/P}` reads
  "56% -> 0%"**, because stripping its only pip leaves an EMPTY mana cost that `parseCost` correctly
  refuses. **The arm cannot price a zero-cost card, which says nothing about the card** — the same
  shape as every "a probe answered a question nobody asked" entry in this file, caught this time
  before it reached a write-up.

**A FETCH IS NOT FREE, INSTANT AND UNTAPPED — N12 shipped 2026-08-26, free. P(>=6 mana at T6)
-1.04pp mean (35 decks down, 17 up), and mean P(castable) -1.59pp with 50 decks DOWN, the largest
castability correction of the batch. Gates byte-identical (34,141 / 43,156 / MESHED 287, panel 95.1%
debt 0), suite 2,505.**
- **A CRACK THAT COSTS MANA IS NOT A FETCH THIS MODEL CAN PERFORM.** The activation cost is what sits
  before the colon on the fetch's own line; `{T}`, a sacrifice and a life payment are costs it CAN
  pay, a mana symbol is not. **63 land slots** (Myriad Landscape 38 · Demolition Field 19 · Urza's
  Cave 6) **and 25 spell slots** (Wayfarer's Bauble 22 · Burnished Hart 2 · Hithlain Rope 1).
  **Wayfarer's Bauble is cast for {1} and cracked for {2} a turn later**, and was priced as a
  one-mana ramp spell that put a land onto the battlefield immediately.
- **A LAND THAT FAILS THAT TEST IS STILL A LAND** — Myriad Landscape taps for `{C}` and enters tapped;
  it simply neither fixes colours nor thins until someone pays. Under-claiming, which is the
  direction. **Burnished Hart's "fetches TWO" falls out for free**, refused on cost.
- **A FETCH THAT FINDS A TAPPED LAND GIVES A TAPPED LAND**: what is standing there is the FETCHED
  land and the fetch is sacrificed. **38 slots**, and **Fabled Passage (21) is the only card in the
  family printing an untap clause** — modelled as the `slow` shape one board count over, tapped below
  four lands. On a synthetic all-tapped deck that cell reads 0% for four mana on turn four against
  **64%** with the clause read, which is how sharply it separates.
- **THE DECKS THAT GO UP ARE THE COSTED-CRACK DECKS NO LONGER THINNING** — N4's -0.21pp arriving with
  its sign flipped, and the attribution confirming itself. Worst movers `rakdos-landfall` -6.92pp mana
  / -7.79pp castable and `fairdrazi-5-color-less` -10.31pp castable.
- **A GUARD THAT DOES NOT FIRE IS DECORATION, AND ONE OF THESE THREE DID NOT AT FIRST.** The Fabled
  Passage assertion passed with the clause deleted, because a 37-land deck reaches four mana by turn
  five whatever enters tapped. **The separating cell is four mana on turn FOUR** — the land played
  THIS turn is the whole difference — and only that cell proves the clause is read.

**A BOARD OF FORESTS COULD CAST KOZILEK — `{C}` IS A DEMAND AND THE MODEL HAD NO BIT FOR IT.
N11 shipped 2026-08-26, free. 11 distinct cards move, ALL DOWN, all inside 3 decks; gates
byte-identical (34,141 / 43,156 / MESHED 287, panel 95.1% debt 0), suite 2,503.**
- **COLOURLESS IS A MANA TYPE, NOT THE ABSENCE OF ONE (CR 106.1b), AND `{C}` IS PAYABLE ONLY WITH
  COLOURLESS MANA (CR 107.4c).** The empty mask was right for a SOURCE — colourless constrains nothing
  about what it can pay — and exactly backwards for a COST, where it constrains everything.
- **THE SIXTH BIT TOUCHED THREE CALL SITES, NOT EVERY MASK**: `colorMask` sets it for a source that
  produces `{C}`, `parseCost` demands it for a `{C}` pip and ORs it into a colourless hybrid, and
  `payable`'s Hall condition iterates 63 subsets instead of 31 — still bounded by the cost's OWN
  types, so a `{2}{U}` card costs one subset test.
- **BOTH OLD READINGS WERE WRONG IN OPPOSITE DIRECTIONS AND THE BIT MAKES BOTH TRUE.** `{C/W}` read as
  a WUBRG demand priced a colourless deck's own commander at 6%; read as unconstrained it let five
  FORESTS cast Ulalek. It is colourless OR white.
- **MEASURED: 13 deck-card rows move more than 2pp and every one is in `fairdrazi-5-color-less`
  (-3.93pp deck mean), `colorless-spellslinger` or `magar-spellslinger`.** The other 68 decks are
  byte-identical, because nothing in them prints a colourless pip. **Eldritch Immunity and Null
  Elemental Blast both cost `{C}` and fall 50.8pp at TURN ONE** — verified against printed cost, and
  right: a turn-one `{C}` needs a colourless source specifically. `fairdrazi` runs 27 of them among 38
  lands, so its mid-game figures barely move and the commander reads **59% – 69%** where it read
  63% – 80%.
- **`mana-audit.ts`'s JUSTIFYING COMMENT WAS FALSE AS A STATEMENT OF THE RULES**: *"every deck can pay
  generic and colourless costs from any source"* is true of GENERIC and false of COLOURLESS. Corrected
  to the narrower true claim — that table counts COLOURED sources against Karsten's thresholds, so a
  sixth entry there would change what it counts rather than fix it.

**THE RAMP ARM HAS AN EXTERNAL CHECK NOW, AND IT CATCHES N15 — N16 shipped 2026-08-26, test-only,
free. Suite 2,501, no production code changed.**
- **A1 CHECKS LANDS-ONLY AGAINST THE CLOSED FORM; THE RAMP ARM — median +33.8pp, the part doing all
  the work — WAS CHECKED BY NOTHING**, which is where N15 sat undetected through two adversarial
  reviews of this same module. `goldfish-dp.test.ts` is three exact dynamic programs over small
  synthetic decks.
- **THE DP PRICES THE SIMULATOR'S OWN POLICY, NOT AN IDEAL PLAYER'S** — seven cards, one draw per turn
  INCLUDING turn one, one untapped land per turn, greedy cheapest-first, a rock paying the turn it
  lands (CR 302.6) and a dork waiting one. **A DP that modelled a better player would disagree for a
  reason that is not a defect**, and that is the trap an anchor like this has to avoid.
- **STATE IS (lands seen, lands played, ramp seen, ramp online)**: cards seen is `7 + turn` and the
  rest is recoverable — N17's land collapse plus one pair for the ramp category.
- **THE FETCH CASE IS THE ONE THAT WOULD HAVE CAUGHT N15, and it is proven against N15 itself:
  restoring the earliest-land removal fails it by 1.92pp against a 1.5pp tolerance.** Three more
  mutations fire — a rock made to wait a turn, a greedy threshold off by one, a skipped turn-one draw.
- **TOLERANCE IS 1.5pp = THREE STANDARD ERRORS AT 20,000 TRIALS**, so a real disagreement shows and
  sampling noise does not. **The DP is ~1ms against the simulation's ~180ms** and its own guard test
  pins that — the asymmetry that makes ANCHORING affordable where REPLACING is not (6.2s on
  `fairdrazi`, 6.8M colour states).

**A TREASURE-MAKER IS NOT A MANA ROCK, AND "ADD THREE MANA" IS THREE — N9 and N10 shipped
2026-08-26, free. N9 takes P(>=6 mana at T6) DOWN 2.38pp mean over 28 decks (worst
`mari-takes-control` -17.51pp, NONE up); N10 takes it UP 0.32pp over 14 decks (best `draguns`
+2.05pp, NONE down). Gates byte-identical (34,141 / 43,156 / MESHED 287, panel 95.1% debt 0), suite
2,497.**
- **SCRYFALL STAMPS `produced_mana` FROM QUOTED TOKEN TEXT**, so Pitiless Plunderer — which taps for
  nothing and makes Treasures — arrived carrying five colours and was priced as a four-mana dork
  adding one every turn. **Treasure mana is a ONE-SHOT, and `isManaSource` already refuses one-shots
  on exactly that ground: the same rule, one layer over.** 40 distinct cards / 61 slots, against a
  filed 28 / ~45.
- **TWO PRINTED WAYS TO REALLY MAKE MANA: the card says "add" in its OWN text (quotes stripped, since
  a quote holds ANOTHER object's ability), or it GRANTS a mana ability to permanents you control.**
  7 cards are kept by the grant half and every one was read — Enduring Vitality, Springleaf Parade,
  Galazeth Prismari, Resonating Lute, Paradise Mantle, Path of Annihilation, Bigger on the Inside.
  **A GRANT WHOSE QUOTED ABILITY SACRIFICES THE PERMANENT IS THE ONE-SHOT AGAIN IN A GRANT'S
  SENTENCE** — Goldspan Dragon grants to Treasures — so it is refused with the rest.
- **A SUSPENDED ROCK IS NOT A TURN-ONE ROCK (CR 702.62).** Sol Talisman and Mox Tantalite are mana
  value 0 and rule 3 casts by mana value, so both were deployed FREE on turn one; the real sequence is
  a cost plus a three-turn wait. 2 cards / 5 slots. **CEILING, 3 cards / 4 slots, stated not built:
  Chrome Mox (imprints a card), Mox Amber (needs a legendary), Everflowing Chalice (mana value 0
  unkicked = zero counters) still price at one** — conditional on something this model cannot see.
- **N10 REUSED THE `WORD_NUMBER` TABLE ALREADY SITTING THERE for the land gate.** Sceptre of Eternal
  Glory 11 decks **1 -> 3**, Coveted Jewel 1 -> 3, Firemind Vessel 1 -> 2, Relic of Sauron 1 -> 2;
  every other word-form line in the corpus says "one". **SCEPTRE'S GATE FELL OUT FOR FREE**: its
  "three or more lands WITH THE SAME NAME" already matches `LAND_GATE`, so it reads `needsLands: 3` —
  the same-name half is uncheckable and three lands is its floor, so it taps for ZERO below it.
  **`X` IS ABSENT FROM THE TABLE ON PURPOSE: a RATE IS NOT AN AMOUNT.**
- **A SECOND FIXTURE THIS WEEK DID NOT RESEMBLE A REAL CARD.** `commander-ramp-core.test.ts` gave
  "Sol Ring" an EMPTY oracle text with `producedMana: ["C"]`, so N9 correctly stopped classifying it
  and the test failed. Rewritten from corpus text (`{T}: Add {C}{C}.`), like C4's fetchland fixture
  the day before. **A fixture that does not resemble a real card cannot exercise the rule it guards.**

**FOUR STALE CLAIMS IN THE MANA MODEL CORRECTED — N5, N7, N8, N18, 2026-08-26, free, no behaviour
change. RE-MEASURING N8 AFTER N2 FOUND SOMETHING NOBODY ASKED FOR: THE FIVE-COLOUR STRATUM'S MEDIAN
GAP FALLS FROM 23.90pp TO 3.05pp.** The fetch cycle was not matching its own gate, so the ten real
fetchlands fixed no colours — **part of what §T.7 recorded as "the colour model is needed" was the
fetchland defect wearing its clothes.**
- **N8's GROUPING RULE WAS NEVER STATED AND IT IS CR 903.4, THE COMMANDER'S COLOUR IDENTITY** — what
  `legality.ts` already checks the 99 against. The first run grouped by hand and put
  `fairdrazi-5-color-less` in the COLOURLESS bucket on the strength of its NAME; its commander is
  Ulalek, `{C/W}{C/U}{C/B}{C/R}{C/G}`, a five-colour identity, and that one deck is the whole
  difference between the two runs' group counts. **Post-N2 the gradient is 0.00 / 0.20 / 1.15 / 5.15 /
  3.05pp over 3 / 21 / 22 / 18 / 7 decks — monotone to three colours and NOT beyond, because a
  five-colour deck FETCHES.** Falsifier statistic 1.15pp (was 1.45), 29 of 71 decks clear 2pp alone,
  and with the two-colour stratum under the line too **42 of 71 fail the criterion individually**.
- **N5: THE NOISE FIGURE REPRODUCES AND THE COMMENT DID NOT. 10 decks x 20 seeds: SD 0.83-1.22pp
  (median 1.03), worst excursion from the 20,000-trial answer 3.59pp** against a claimed "about
  0.5pp". Conclusion survives — a whole-percent figure inside a 7pp policy band can carry a 1pp SE —
  the sentence did not.
- **N7: A DECOMPOSITION THAT FAILS ITS OWN ADDITION IS THE CHEAPEST CHECK ON IT, AND NOBODY RAN IT.**
  I10b's halves were filed as 1.96 + 0.22 against a stated total of **2.23**; the re-measured land
  half is **+0.28pp over 20 decks** and sums exactly. **"The tron branch moves exactly 6 decks" is
  strictly false** — the seventh moves +0.01pp, since the trio does assemble through the shuffle at
  ~0.2%. **A rounded zero is not a zero.**
- **N18: THE GREEDY TIE-BREAK IS HAND ORDER, i.e. the shuffle**, and it is stated in the rule-3 loop
  rather than left implicit. A rock and a dork at the same mana value give different boards (the rock
  pays the turn it lands, CR 302.6), so it is a POLICY choice — but uniformly random across trials, so
  it AVERAGES rather than biases. Preferring the rock would be a THIRD policy arm, not a fix.

**A MEASURED ZERO IS 0% AND A REFUSAL IS AN EM DASH — N6 shipped 2026-08-26, free, as ONE renderer
rather than three fixes. Suite 2,495; gates unreachable, this is rendering over numbers the engine
already produced.**
- **THE CLI AND `DeckIdentity` FLOORED AT `Math.max(1, ...)` AND `CardList` DID NOT**, so the same
  impossible cast read "1%" in one surface and "0%" in the other. **The floor exists so a real chance
  never reads as "cannot happen"; a model that cannot price a card REFUSES, and a refusal already
  prints an em dash with its reason.** So the floor belongs on that path and never on a measurement —
  20,000 trials of no, on a cost the model can price, is a measurement.
- **THREE CASES, SEPARATED: exactly zero -> `0%`, rounds-to-zero -> `<1%`, otherwise the rounded
  percent.** Flooring a one-in-a-thousand chance to "1%" overstates it two hundred fold.
- **SHIPPED AS `@mtg/engine/percent`, ONE COPY, BECAUSE TWO COPIES IS THE DEFECT.** `band()`'s
  degenerate-range collapse was duplicated three ways beside it. Pure arithmetic in its own subpath,
  so the client never pulls the engine barrel (which `readFileSync`s tag weights at module load) —
  the same rule `@mtg/engine/hypergeometric` already ships under.
- **`DeckIdentity`'s compact "55–62%" IS NOW "55% – 62%"**, the form the CLI and `CardList` already
  used. A surface that renders the same quantity its own way is how they drift apart; its test
  carries the reason rather than just the new string.
- Proven to fire by restoring the old floor: **one test fails in each of the three packages**.

**A PROBABILITY READ 2,805% ON A LEGAL PASTED DECK — N1 shipped 2026-08-26, free. Gates
byte-identical (34,141 / 43,156 / MESHED 287, panel 95.1% real 385 / false 20 / debt 0), suite 2,492,
and THE 71 DECKS COULD NOT HAVE SEEN IT.**
- **`byCardHits` KEYS ON NAME AND WAS INCREMENTED ONCE PER COPY PER TRIAL-TURN.** Thirty Dragon's
  Approach accumulated thirty hits, so the card printed **`castable {low: 28.05, high: 28.61}`** and
  **sorted LAST in a list ordered "hardest cast first"** — the deck's defining card read as its
  easiest. Now **93.5% – 95.4%**, in the right place.
- **COUNTED ONCE PER NAME.** "This card is castable" is a property of the board and the COST, which
  every copy shares. `countsForByCard` is computed once at build time, so there is no per-trial
  allocation. **`affordable` still counts every copy on purpose** — that share really is 30 castable
  cards of 99, and its denominator is the slot count.
- **THE CALIBRATION CORPUS IS BLIND TO IT BY CONSTRUCTION, AND THAT IS CHECKED RATHER THAN ASSUMED:
  the only multi-copy lines in all 71 decks are basic lands** (Swamp 39 · Island 37 · Mountain 33 ·
  Forest 13 · Plains 11 · Wastes 3), and a land is never priced. **An arbitrary paste is a
  first-class input here** — it is what `legality.ts` exists for, and 13 cards say "any number of
  copies".
- **MEASURED THROUGH THE REPORT PATH, not only at the source**: `manaModel` -> `deckCastability`, the
  same call the web `CardList` renders, on a real 30-copy paste. Fixing the source is what makes both
  surfaces right; `pct`'s missing clamp never needed one.

**THE TEN REAL FETCHLANDS ARE FETCHLANDS NOW, AND A TRANSFORM CARD'S LAND BACK IS NOT A LAND —
N2 and N3 shipped 2026-08-26, free, together because both live in the same land-classification line.
Gates byte-identical (34,141 / 43,156 / MESHED 287, panel 95.1% real 385 / false 20 / debt 0). Suite
2,491.**
- **N2: ONE PREDICATE FOR BOTH SIDES — `LAND_FETCH && ONTO_BATTLEFIELD`, and `FETCHLAND` IS DELETED.**
  The land-only cue demanded the literal *"land card"* and **Scalding Tarn prints "an Island or
  Mountain CARD"**; the two sides were always asking the same question. The land side gains the
  onto-the-battlefield half with it, so **a search that puts a land in HAND is not a land drop** —
  which is what tripped Thaumatic Compass.
- **SWEPT AND READ ONE BY ONE: 23 distinct lands / 162 slots newly match, ZERO lost** — the ten real
  fetchlands (110), **Myriad Landscape (38, the widest single card)**, the ten-card Landscape cycle,
  Seething Landscape, Maestros Theater, Riveteers Overlook. Roadmap filed it at ~110; the count is 162.
- **THE COLOUR HALF IS THE PAYOFF AND THE MANA HALF IS SLIGHTLY NEGATIVE: mean P(castable) at T6
  +0.92pp, up in 49 decks / down in 10, `codie` +12.02pp · `unify-all-the-universes` +11.57pp**, while
  the mana headline moves **-0.14pp** because these lands now THIN. Exactly the multicolour decks the
  colour model exists for.
- **N3: `frontTypeLine()`, an ALLOW-LIST of `{transform, flip}` on the `FRONT_FACE_ONLY` precedent**
  (CR 712.4a — a card in hand is its front face). **10 distinct cards / 23 slots; P(>=6 mana at T6)
  -0.85pp mean, 12 decks DOWN and NONE up, worst `rakdos-landfall` -16.86pp.** Phantom land drops
  leaving, and each card is now PRICED as the spell it is — the denominator half of the same defect.
- **A MODAL DFC KEEPS ITS LAND FACE, 28 distinct cards / 186 slots**, which is the half a blanket
  "split on //" rule would have broken. Its own test, proven to fire by adding `modal_dfc` to the
  allow-list: the fixture reads 34.0% against 0.96%.
- **`classifyAccelerant` STILL READS THE JOINED TYPE LINE, DELIBERATELY.** A transform card's
  `producedMana` carries its BACK face's mana, so a front-face read there would make **Ojer Axonil a
  recurring red source** — which it is not until it dies and transforms. The joined-line refusal was
  accidentally protective; it is now stated in the code rather than left as luck.
- **THE C4 FIXTURE REWROTE THE CARD AND IS REWRITTEN FROM THE CORPUS.** *"an Island or Mountain LAND
  card"* is a card this repo invented to pass its own test. **Three mutations proven to fire**: the old
  cue (fails C4 and N2), the joined-line `isLand` (fails N3), and `modal_dfc` in the allow-list.

**THE UNDISCLOSED -6.5pp WAS 90% BUG AND 10% CORRECTION, AND THE DECOMPOSITION IS EXACT — N4
measured 2026-08-26, free, NO CODE.** `e0efd6d` shipped library thinning inside the colour commit and
measured it nowhere. As shipped it moved P(>=6 mana at T6) **-2.11pp mean, DOWN in 59 of 71 decks and
up in NONE, worst `naya-spellslinger` -7.24pp** — reproduced exactly against `90eed3e`'s behaviour.
- **THINNING ALONE IS -0.21pp AND MIXED IN SIGN (40 down, 19 UP, 12 flat); the N15 fetch-removal bias
  is -1.90pp. -0.21 + -1.90 = -2.11, to the decimal.** The correction is nearly neutral and correct:
  you gain the land on board and every later draw is one land poorer. **Two changes rode in one commit
  and the big one was a bug.**
- **THE FILED SUPERLATIVE WAS WRONG: `bant-1-1` at -6.52 is the FOURTH worst**, behind
  `naya-spellslinger` -7.24, `fight-club` -6.95 and `codie` -6.75.
- **§S BIAS ROW 11 CORRECTED ON A COUNT RATHER THAN AN ESTIMATE: the `FETCHLAND` cue catches 7
  distinct lands / 52 slots** (Fabled Passage 21 · Demolition Field 19 · Urza's Cave 6 · Prismatic
  Vista 3 · Evolving Wilds 1 · Terramorphic 1 · Thaumatic Compass 1, and that last one is N3's
  transform card) **against 26 further searcher lands / 187 slots it misses**, the ten REAL fetchlands
  among them at 110. The old row called the whole ~200 slots unmodelled; a quarter is modelled and the
  colour-relevant three quarters is not. **N2 is still open.**
- **"MEASURE THE GATES IN THE SAME COMMIT THAT MOVES THEM", THIRD RECORDED TIME.**
  `population-compare` and `panel-score` cannot see this module — it forms no edge — so
  *"gates byte-identical"* was TRUE and silent about the only table a reader reads.

**A FETCH NOW TAKES A UNIFORMLY RANDOM LAND OUT OF THE LIBRARY, NOT THE EARLIEST ONE IN DRAW ORDER —
N15 shipped 2026-08-26, free, ONE HELPER AT TWO SITES. P(>=6 mana at T6) +1.90pp mean over the 71
decks, up in 57 and down in 2, worst `naya-spellslinger` +6.92pp; P(>=8 at T8) +2.51pp. Gates
byte-identical (34,141 / 43,156 / MESHED 287, panel 95.1% real 385 / false 20 / debt 0), which IS the
acceptance test for a leaf nothing scores. Suite 2,489.**
- **THE MODEL HAD BEEN UNDERSTATING MANA ON EXACTLY THE FETCH DECKS THE FEATURE WAS BUILT FOR.** Every
  fetch prints *"then shuffle"*, so the residual library is exchangeable and a uniformly random land
  leaves; `library.findIndex((c) => c.isLand)` took the earliest, which pushes the survivors later —
  provable on `{L,L,X}`, where removing the first L leaves X ahead of the surviving L two thirds of
  the time instead of half.
- **THE A/B REPRODUCES THE FILED FIGURE EXACTLY** (20k trials, seed 20260822): +1.90pp mean,
  57 up / 2 down / 12 flat. The two falls are noise (-0.18pp, -0.12pp); the 12 flat decks hold no
  fetch, which is the attribution confirming itself.
- **TWO CALL SITES, TWO BRANCHES, AND THE FIRST BEHAVIOURAL TEST COVERED ONLY ONE.** A fetchLAND
  resolves in the land-drop branch and a fetch SPELL in the cast branch; mutating BOTH sites at once
  showed a failing test and hid that the spell arm was untested. **MUTATE ONE SITE AT A TIME** —
  found by doing it, and the second arm (Nature's Lore, verbatim, 60.27% -> 70.72% at T6 on a
  20-fetch fixture) is a test now. Both proven to fire separately.
- **N4 IS UNBLOCKED AND IS NEXT**: its -6.5pp thinning delta was partly this bias, since both arrived
  in `e0efd6d`. Re-measure against the corrected policy before recording it.

